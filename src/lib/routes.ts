import type { PostCategory } from "@/lib/api/posts";
import type { HomeSectionId } from "@/lib/homeSections";

/**
 * 라우트 상수 — 링크 경로 하드코딩 금지 (union-webapp-dev 스킬 §4).
 * 모든 <Link>/<a> 내부 경로는 이 모듈의 상수를 사용한다.
 *
 * 2026-08-17 (§15): 탭 인프라(`TAB_IDS`·`TabId`·`isTabId`·`TAB_QUERY_PARAM`·`homeTab`)를
 * 제거했다. 메인페이지는 섹션 나열이며 활성 탭 개념이 존재하지 않는다.
 * `?tab=` 하위호환 리다이렉트도 만들지 않는다 — 쿼리는 무시되고 콘텐츠는 어차피 보인다(§15.9.2).
 */

/**
 * 외부 링크 상수 — 내부 라우트(ROUTES)와 구분.
 * 외부 링크는 반드시 target="_blank" rel="noopener noreferrer"와 함께 사용한다.
 */
export const EXTERNAL_LINKS = {
  /** 디지털온누리 사용 가이드 (리더 유효성 확인 완료 2026-08-16) */
  onnuriGuide: "https://onnuri.koscomlabor.cloud/",
  /**
   * ★ **주최측 출석체크 페이지**(사용자 제공 2026-08-25).
   *
   * **대상 확인**(응답 실측 2026-08-25): `HTTP 200` · `<title>금융노조 집회참석</title>` ·
   * 본문 *"지정된 시간에만 체크 가능합니다."* — QR 이 가리키는 그 페이지가 맞다.
   * 출석 시간 밖에서는 **입력 폼이 없고 안내 한 줄만** 나온다(그래서 카드 설명이 그 사실을 적는다).
   *
   * ⚠ **호스트가 `mycafe24.com` 이라 한눈에 «공식» 으로 보이지 않는다.** 그래도 도메인을 화면에
   * 노출한다(§14.1 3중 병행) — 감추면 조합원이 **어디로 가는지 모른 채** 누르게 되고,
   * 그것이 이 페이지가 막으려는 바로 그 상황이다.
   * ⚠ 8/28 전에 **주최측에 URL 유효성을 한 번 더 확인**하라(요구 78 과 같은 규율).
   */
  unionAttendance: "https://prpage153.mycafe24.com/bank701/index.php",
  /**
   * 네이버 길찾기 — **도착지 = 국회의사당역 3번 출구**(사용자 지시 2026-08-22).
   *
   * ## ★ KDB산업은행으로 잡았다가 되돌린 것이다 — 같은 실수를 반복하지 마라
   *
   * 한 번 **KDB산업은행 좌표**(`3zfgXq,2AKonz`)로 걸었는데, 사용자가 실기기에서 눌러 보고
   * *"실제 집회위치와 너무 크게 벌어졌어"* 라고 했다. 지도상 거리로는 KDB(122m)가
   * 3번 출구(204m)보다 3구역에 **가까운데도** 그랬다 — 즉 **좌표가 아니라 네이버가 그 이름·좌표로
   * 무엇을 찍느냐**가 문제였다(지점명이 여러 곳에 걸리는 상호는 엉뚱한 지점으로 잡힌다).
   *
   * **지하철 출구는 네이버에서 단일하게 확정되는 지물**이라 이 실패가 없다.
   * 종전에 5번 출구로 잘 동작하던 링크와 **같은 형태**다.
   *
   * ⚠ **안내 문구를 이 링크에 맞춰 되돌리지 마라**(사용자 명시): 집결 안내는
   * `3번 출구 → 여의도공원 방향 KDB산업은행 앞` 그대로다. 링크는 **길찾기 편의상 더 안전한
   * 포인트**를 찍는 것이고, 조합원은 3번 출구에 내린 뒤 안내 문구를 따라 걷는다.
   * 다만 길찾기 카드의 **`도착지는 …입니다` 한 문장은 링크와 반드시 같아야 한다** —
   * 그 문장은 안내가 아니라 **이 링크가 어디로 데려가는지에 대한 설명**이다.
   *
   * ## 좌표부 인코딩
   *
   * **base62(알파벳 `0-9a-zA-Z`) 로 `round(좌표 × 1e7) + 2_000_000_000`.**
   * 5번 출구의 옛 값 `3zf71R,2AKrxU` ↔ `126.9172199, 37.5282738` 로 **양방향 재현**해 얻었다.
   * 여기 값 `3zfaE8,2AKqrw` 는 `126.9186104, 37.5278498`(OSM `railway=subway_entrance` `ref=3`)로 되돌아온다.
   *
   * ⚠ **소수 좌표 형식으로 바꾸지 마라** — HTTP 200 을 주면서 **마포구 성산동**을 보여준다(실측).
   * ⚠ **`index.nhn`·`route.nhn` 레거시와 `nmap://` 앱 스킴 금지** — 앱 미설치 조합원에게 죽은 링크다.
   * ⚠ **HTTP 200 을 동작 근거로 삼지 마라.** SPA 라 없는 경로에도 200 을 준다.
   *   **이 값을 고치는 사람은 실기기에서 도착 핀을 눈으로 확인한다** — 위 사고가 그래서 났다.
   */
  naverDirections:
    "https://map.naver.com/p/directions/-/3zfaE8,2AKqrw,%EA%B5%AD%ED%9A%8C%EC%9D%98%EC%82%AC%EB%8B%B9%EC%97%AD%203%EB%B2%88%20%EC%B6%9C%EA%B5%AC,,/-/transit?c=3zfaE8,2AKqrw,17,0,0,0,dh",
  /**
   * ★ **참석 예비조사 폼**(사용자 제공 2026-08-26 · 검증 §45-1 에서 **직접 재확인**).
   *
   * **도착지 확인**(원본 HTML 실측): 데스크톱·모바일 UA 양쪽 `HTTP/2 200` · **리다이렉트 사슬 0** ·
   * 최종 URL 이 이 값 그대로다. **단축링크가 아니라** «대상이 바뀔 수 있는» 위험이 구조적으로 없다
   * (이 페이지의 QR 사고 — `m.site.naver.com/1tgqt → qrpage.kr/pr/` — 는 단축링크였다).
   * 폼 제목은 `8/28 금융노조 임단투 총력투쟁 결의대회 참석 조사` 이고 설명문이 **우리 사이트
   * `https://koscomlabor.cloud/` 를 링크한다** — 폼이 코스콤지부의 것임을 자료 스스로 말한다.
   *
   * ⚠ **`WebFetch` 의 «설명문 없음» 을 «없다» 로 읽지 마라**(§45-13). 마크다운 변환기가
   * `FB_PUBLIC_LOAD_DATA_` 같은 **스크립트 안 문자를 통째로 버린다** — 폼·위젯은 **원본 HTML** 을 봐야 한다.
   * ⚠ **8/28 직전에 한 번 더 열어 보라**(`unionAttendance` 와 같은 규율) — 폼이 마감되면
   * 배너가 **«닫힌 폼»으로 조합원을 보낸다.** 현재는 응답 수집 중이다(실측 2026-08-26).
   */
  preSurvey:
    "https://docs.google.com/forms/d/e/1FAIpQLScWKY74TnGSHJGLjfdt5k7lkP_EMHaYQAVJnchfW9WdAm4KhA/viewform",
} as const;

/**
 * 온누리 카드에 **표시**하는 도메인 (스펙 §20.12.6).
 *
 * `href` 와 화면 문자열이 갈리면 "클릭 전에 목적지를 알려준다"는 목적을 정면으로 배신하므로
 * 리터럴을 따로 적지 않고 `EXTERNAL_LINKS.onnuriGuide` 에서 파생한다.
 * `host` 는 서브도메인 포함·프로토콜 제외·끝 슬래시 제외 — 채택 표기와 정확히 일치한다.
 * 전체 URL 은 360px 카드 안에서 28px 넘친다(§20.12.1 실측) — 도메인만 쓰는 이유다.
 * 호스트가 더 긴 도메인으로 바뀌면 §20.12.1 의 197px 예산을 **다시 실측**해야 한다.
 */
export const ONNURI_GUIDE_DISPLAY_HOST = new URL(EXTERNAL_LINKS.onnuriGuide).host;

/** 출석체크 카드에 **표시**하는 도메인 — `href` 에서 파생해 링크와 표시가 갈리지 않게 한다 */
export const UNION_ATTENDANCE_DISPLAY_HOST = new URL(EXTERNAL_LINKS.unionAttendance).host;

/**
 * 참석 예비조사 배너에 **표시**하는 도메인 (검증 §45-11 조건 3 · 디자이너 §37.9).
 *
 * ⚠ **리터럴 `docs.google.com` 을 적지 마라** — `href` 에서 파생시키는 것이 조건이다.
 * 배너 이름(`참석 예비조사`)과 도착 페이지 제목(`8/28 금융노조 … 참석 조사`)이 **글자가 달라서**
 * 조합원이 «다른 곳에 왔나» 하지 않도록 **어디로 가는지를 미리 말하는** 장치다.
 */
export const PRE_SURVEY_DISPLAY_HOST = new URL(EXTERNAL_LINKS.preSurvey).host;

/**
 * 오시는 길 — **최근접역 정보. 노선·역·출구·URL 은 한 객체다. 하나만 고치지 마라**(§29.5 · 요구 137).
 *
 * ⚠ **요구 112 — 3구역 좌표가 확정되면 최근접역이 `여의도역` 으로 바뀔 수 있다.** 그때 할 일:
 *   1) `url` 도 함께 바꾼다. **§24.6 확정 URL 이므로 새 URL 은 검증을 다시 받는다**(요구 74·75).
 *      **소수 좌표 형식으로 만들지 마라 — 200 을 주면서 마포구 성산동을 보여준다.**
 *   2) ★ **`급행은 …에 서지 않습니다` 문장을 지워야 한다** —
 *      **여의도역은 급행 정차역이다**(서울시메트로9호선 공식 마크업 `4115 transfer express`).
 *      **역 이름만 치환하면 1급 근거로 확정한 문장이 1급 거짓이 된다.**
 *   3) 블록 2 산문·지도 대체면의 `국회의사당역` 표기도 함께 본다.
 *
 * §12(코스콤지부 위치)에서 **하나의 전제에 매달린 파생 7건이 한꺼번에 무너진** 경험이 이 설계의 근거다 —
 * **한 사실이 바뀌면 그것에 매달린 것이 함께 움직이게 만든다**(요구 88 과 같은 계열).
 */
export const WAYFINDING = {
  line: "9호선",
  station: "국회의사당역",
  exit: "3번 출구",
  /**
   * **급행 미정차역일 때만 `true`.** 최근접역이 여의도역으로 바뀌면 `false` 로 두고 그 문장을 렌더하지 않는다.
   * 플래그로 둔 이유: **주석만 두면 다음 사람이 안 읽는다.** 의존을 코드에 드러낸다.
   *
   * ⚠ **지금은 `true` 이고 그 문장은 필수다**(요구 107·138 — 1급 확인).
   * **플래그가 있다는 것을 이유로 문장을 빼지 마라.**
   */
  expressSkipsStation: true,
  url: EXTERNAL_LINKS.naverDirections,
} as const;

/**
 * 길찾기 링크에 **표시**하는 도메인(§24.5.4 · §14.1).
 * **URL 전체를 화면에 노출하지 마라** — 네이버 내부 인코딩 문자열이라 소음이고 판독 가치가 0이다.
 * `href` 에서 파생해 링크와 표시가 갈리지 않게 한다(온누리 카드와 같은 규칙).
 */
export const NAVER_DIRECTIONS_DISPLAY_HOST = new URL(EXTERNAL_LINKS.naverDirections).host;

/* 상세 경로 빌더 — 개별 상수(ROUTES.notice 등)와 분류 매핑(ROUTES.post)이 같은 함수를 공유한다 */
function noticePath(id: string): string {
  return `/notices/${encodeURIComponent(id)}`;
}

function newsPath(id: string): string {
  return `/news/${encodeURIComponent(id)}`;
}

function educationPath(id: string): string {
  return `/education/${encodeURIComponent(id)}`;
}

/**
 * 분류별 상세 경로 — 분류가 늘어도 이 한 곳만 고친다 (§15.6R-G).
 * 호출부에서 삼항 연산자로 분기하지 말 것 — 분기 누락이 곧 잘못된 링크(404)다.
 * `Record<PostCategory, ...>` 이므로 분류 추가 시 컴파일 에러로 누락이 잡힌다.
 */
const POST_DETAIL_PATHS: Record<PostCategory, (id: string) => string> = {
  notice: noticePath,
  news: newsPath,
  education: educationPath,
};

export const ROUTES = {
  home: "/",
  /**
   * 26년 임단협 투쟁 안내 (§17) — 게시물 분류가 아니라 정적 페이지다.
   * 8페이지 구조 문서이고 투쟁 종료 후 폐기·보존 판단이 별도로 필요해 코드로 관리한다.
   */
  bargaining: "/bargaining-2026",
  /**
   * 8/28 결의대회 참석 안내 (§20.1).
   * `/bargaining-2026` 의 하위 경로로 두지 않는 이유는 상위 페이지가 폐기돼도
   * 공유된 링크가 살아 있어야 하기 때문이다(§20.0-4).
   *
   * ## ⚠⚠ 2026-08-29 — **이 상수는 «참조 0» 이 됐다. 그래도 지우지 마라**(리더 D-9)
   *
   * 8/28 로 가는 길이 이번 라운드에 둘 다 내려갔다 — **홈 배너**(D-6 · `RallyBanner` 삭제)와
   * **`/bargaining-2026` 남은 일정 카드의 링크**(D-9 · `참석 안내 보기` 를 9/4 가 승계). 그래서
   * **사이트 내 도달 경로가 0** 이고, `ROUTES.rally0828` **자체도 더 이상 아무 데서도 읽히지 않는다.**
   *
   * **그것이 «부작용»이 아니라 «판정»이다.** 카톡·문자로 뿌려진 URL 은 **그대로 열려야 하고**,
   * 그러려면 `src/app/rally-2026-08-28/` 라우트가 남아 있어야 한다.
   * ★ **«쓰이지도 않는 상수네» 로 지우지 마라** — 다음 사람이 **그 삭제를 근거로 페이지까지 지운다.**
   *   그러면 조합원이 받은 링크가 404 가 된다.
   * ★ 그 페이지의 **«나오는 길»은 살아 있다**(`SiteHeader` + 상·하단 복귀 링크 2개) —
   *   링크로 들어온 조합원이 갇히지 않는다. 지금 없는 것은 **«들어가는 길»뿐**이다.
   * → 지난 행사 아카이브가 필요해지면 그때 도달 경로를 다시 만든다(`FOLLOWUPS` 등재분).
   */
  rally0828: "/rally-2026-08-28",
  /**
   * 9/4 총파업 참석 안내 (§52.0-1).
   *
   * `rally0828` 과 같은 이유로 최상위 라우트다 — 공유된 링크가 상위 페이지 개편·폐기에
   * 동반 사망하면 안 된다(§20.0-4).
   *
   * ⚠ **`rally0828` 을 지우지 마라**(D-6·D-9) — 이 라운드에 **상수까지 참조 0** 이 됐지만
   * **의도된 상태다.** 근거는 그 상수의 주석에 있다. 지우기 전에 반드시 읽어라.
   */
  strike0904: "/strike-2026-09-04",
  /** 메인페이지의 특정 섹션 앵커 (§15.9.2 — 상세 페이지 "목록으로 돌아가기" 복귀 지점) */
  homeSection: (id: HomeSectionId): string => `/#${id}`,
  /** 공지사항 상세 (DB 전환 — id는 uuid, §15-7) */
  notice: noticePath,
  /** 금융노조 소식 상세 (DB 전환 — id는 uuid, §15-7) */
  news: newsPath,
  /** 노동교육 상세 (§15.6R-G — 링크형은 외부로 직행하므로 상세는 폴백 경로다) */
  education: educationPath,
  /** 분류 → 상세 경로 (§15.6R-G) */
  post: (category: PostCategory, id: string): string => POST_DETAIL_PATHS[category](id),
  /** 관리자 화면 */
  admin: "/admin",
} as const;
