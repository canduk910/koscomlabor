/**
 * 9/4 총파업 안내지도의 **단일 출처** — 좌표 · 기호 · 라벨 · 범례. 고칠 파일은 이 파일 하나이고,
 * 항목을 빼면 **도형·지도 pill·범례 행이 동시에** 사라진다.
 * 정본: `_workspace/00_input/strike-20260904/MAP-PLAN.md` **M-50 ~ M-71** · 검증 **§61 ~ §64**.
 *   ⚠ 종전 표기(**M-50~M-60 · §61·§62**)는 **파일 자신의 내용보다 좁았다** — 이 파일은
 *   `KOSCOM_LOCATION_NOTE`(M-61) · `inFitBounds:false`(M-63) · `CONFIDENCE_VISUAL`(M-70)을 쓰는데
 *   좁은 범위만 읽으면 그것들이 **«근거 없는 코드»로 판단된다.**
 *
 * ★★★ **2026-09-01 — 주최측 신판(v2) 으로 «전면 재작성»했다. «문면 교체»가 아니라 데이터 모델이 바뀌었다.**
 *   구판(v1 `design/IMG_6833.JPG`)에 있던 **「대오 1~4」가 신판에 «개념 자체로» 없다**(검증 §61 A-1).
 *   ⚠⚠ **「v1 은 구판」의 «범위»를 오해하지 마라**(M-71): **v2 자료 4장은 «식순·지도»만 대체했다.**
 *     그 4장에 **「2. 개요」·「3. 집결시간」이 없어서** v1 전사본의 그 부분은 **여전히 살아 있는 출처**다.
 *     **문자 그대로 「v1 인용 금지」를 적용하면 개요·집결시간의 출처가 사라진다.**
 *     ⚠ 다만 v1 전사본 **6행의 「식순 20행 전부 일치」는 «구판 식순»에 대한 확인**이라 **인용하면 거짓**이다.
 *   그래서 `kind: "band"` · `COLUMN_1~4_POLYGON` · 그것에 매달렸던 범례 8행 · pill 4개가 **전부 죽었다.**
 *   ⚠⚠ **되살리지 마라. 게시되는 순간 «조합원이 «내 대오»를 찾는데 화면에 틀린 답이 있는» 상태가 된다.**
 *
 * ★★ **v1 좌표를 «한 점도» 재사용하지 않았다**(검증 §61-6 #3 · 게시 조건 48). 전부 v2 지도에서 새로 냈다.
 *   ⚠⚠ **v1 무대2(37.568206)와 v2 코스콤 구역(37.568220)이 14 m 차이라 «재사용해도 될 것처럼» 보인다 —
 *     독립 도출값이 «우연히» 가까운 것이다.** v1 무대3(37.567482)은 v2 와 어긋난다.
 *     **한 점이 맞는다고 나머지가 맞지 않는다.**
 *
 * ★★★ **v2 는 «등척»이고 v1 은 «이방」이었다 — «v1 이 이방이었다»를 옮기는 것은
 *   «v1 이 등척이라 가정»하는 것과 «똑같은» 오류다**(검증 §62-1):
 *   | 파일 | 아이콘 실측 `sx/sy` | |
 *   |---|---|---|
 *   | v1 `IMG_6833.JPG` | **1.5632 ± 0.0059**(n=12) | 이방 — 등척을 **96.3σ** 로 기각 |
 *   | **v2 `총파업_지부위치.png`** | **1.0027 ± 0.0026**(n=50) | **등척 — 1.0σ** |
 *   v1 은 A4 문서에 붙이며 가로로 늘어난 캡처였고 **v2 는 원본 해상도 그대로**(1300×1179)다.
 *   → 채택 변환은 **등척·무회전 3파라미터**(`s = 0.89897 m/px` · RMS 13.3 m).
 *   ⚠ 축정렬 이방(4파라미터)은 잔차가 **2 m 더 좋았지만 기각**했다 — 요구 `sx/sy 1.0432` 가 아이콘 실측과
 *     **15.6σ** 어긋난다. **잔차가 좋아지는 것은 «모델이 참»이라는 증거가 아니다.**
 *
 * ⚠ **`src/lib/rallyMap.ts` 를 «하나도» import 하지 마라**(M-5) — 그 파일 문면이 8/28 정체성을 지녀
 *   값 하나만 가져와도 9/4 번들이 여의도 안내 문면을 들고 다닌다. 대가로 `LEGEND_KEY` 가 두 벌이다.
 * ⚠ **번호를 매기지 마라** — 9/4 는 `①`~`⑥` 기대 0(§54.16-12). 위치는 범례 문장이 말한다.
 * ⚠ **id 로 스타일을 분기하지 마라** — `confidence`·`tone`·`kind` 로만 분기한다.
 */

/* ── 1. 어휘 — 확신도 · 색 계열 · 배치 ───────────────────────────── */

/** 좌표 확신도 — **렌더 스타일이 이 값에서만 파생된다.**
 *  ⚠⚠ **2026-09-01 — `calculated` 가 «미사용»에서 «사용»으로 바뀌었다**(§63 판정 1): **광화문역**이 그것이다.
 *    종전 주석 *«`calculated` 는 9/4 미사용이지만 남긴다»* 는 **죽었다. 인용하지 마라.**
 *  ★★ **그래서 9/4 는 이제 «세 등급»을 쓰는데 «시각 상태는 둘»이다**(선종 `solid` ↔ `dashed`) —
 *    `calculated` 가 `verified` 와 **똑같이 그려지고 「근사」 줄도 안 붙는다.**
 *    ⚠ **이것을 «임의로» 풀지 마라**(셋째 선종을 만들거나 `calculated` 를 `estimated` 로 낮추는 것 둘 다).
 *      **디자이너·검증 회부 사항**이고, 그때까지는 **§63 이 준 등급 그대로** 둔다. */
export type StrikeGeoConfidence = "verified" | "calculated" | "estimated";

/** 색 계열 — **의미색을 늘리지 않는다**(§54.7 신규 색 0).
 *  ⚠ **`estimated` 를 회색으로 바꾸지 마라**(M-2) — 색은 `tone`, 확신도는 선종·`LEGEND_KEY` 가
 *  전담한다. 두 축을 섞으면 「코스콤지부 구역(근사)」이 「참고 지물」로 읽힌다 */
export type StrikeMapTone =
  /**
   * 파랑 `#093389` — **조합원의 «경로»**: 내리는 역 → 우리 무대 → 우리 구역.
   * ★★ **정의가 넓어졌다**[2026-09-01 · 사용자 확정 · `MAP-EMPHASIS.md` §10] — 종전 **3개**
   *   (코스콤지부 구역 · 역 2)에 **무대 2** 가 더해져 **4개**다.
   *   ⚠ 종전 문면 *«조합원이 갈 곳(코스콤지부 구역·역)»* 은 **죽었다. 인용하지 마라.**
   * ⛔ **«갈 곳처럼 보인다»는 이유로 다른 지물을 `go` 로 옮기지 마라** —
   *   **원문이 «소속»을 말하는 지물만** `go` 다. 임시화장실은 **공용이라 원문이 코스콤의 것이라 말하지 않는다.**
   */
  | "go"
  /** 회색 `#4b5563` — 참고 지물(무대 1·3·4 · 화장실) */
  | "reference";

/**
 * ★★★ **확신도 → 시각 상태 매핑 — 3항을 «전부» 적는다**(M-70).
 *
 * ⚠⚠ **`=== "estimated"` 식의 «우연한 2분기»를 쓰지 마라.** `Record<StrikeGeoConfidence, …>` 라
 *   **등급을 하나 늘리면 «타입이» 빌드를 죽인다** — 그때 «이 항목은 어떻게 그리나»를 반드시 답하게 된다.
 *
 * ★★ **`calculated` 가 `verified` 와 같은 것은 «누락이 아니라 판정»이다**(M-70).
 *   **확신도는 «출처 이력»이고 시각 상태는 «지물 존재»다 — 다른 축이다:**
 *   | 축 | 무엇을 말하나 | 조합원에게 |
 *   |---|---|---|
 *   | 확신도 등급 | **우리가 출처를 몇 개 봤나** | ★ **우리 작업 사정 — §5.3 금지** |
 *   | 점선 · 「근사」 | **지물이 «실제로 거기 있나»** | **행동이 갈린다** |
 *   판정 기준 그대로(*「이 표시가 없으면 조합원이 다르게 행동하는가」*): **아니다.**
 *   조합원은 **우리 핀으로 지하철역을 찾지 않는다**(역 표지로 찾는다). 핀의 역할은 「무대 1 북쪽」이라는
 *   **방향 기준**이고 **수십 m 오차로 방향은 안 바뀐다.**
 *   ★ **임시화장실은 정반대다** — 그 자리에 가서 **못 찾을 수 있다.** 그래서 거기 점선이 «정당»하다.
 *   ★ 범례 키도 안전하다 — **「점선 = 근사」는 «점선만» 정의하고 `solid` 를 정의하지 않는다.**
 * ⚠ **셋째 시각 상태를 만들지 마라. `estimated` 로 강등하지도 마라** — 강등하면 「근사 ±30 m」가 붙는데
 *   그 값은 **픽셀 환산 오차 예산**이고 역 좌표는 **환산 산물이 아니다**(= **없는 오차 예산을 주장**한다).
 */
export interface StrikeConfidenceVisual {
  /** 도형·배지·와드 테두리의 선종 */
  stroke: "solid" | "dashed";
  /** 팝업·거리뷰 시트에 `APPROX_NOTE` 한 줄을 붙이는가 */
  approxNote: boolean;
}

export const CONFIDENCE_VISUAL: Readonly<Record<StrikeGeoConfidence, StrikeConfidenceVisual>> = {
  verified: { stroke: "solid", approxNote: false },
  /* ★ 누락이 아니라 판정(M-70). 확신도는 출처 이력이고 시각 상태는 지물 존재다 — 다른 축이다. */
  calculated: { stroke: "solid", approxNote: false },
  estimated: { stroke: "dashed", approxNote: true },
};

/** 라벨이 앵커에서 뻗는 방향 */
export type StrikeLabelPlacement = "right" | "left" | "top" | "bottom";

export interface StrikeLatLng {
  lat: number;
  lng: number;
}

export interface StrikeBounds {
  south: number;
  west: number;
  north: number;
  east: number;
}

/** 지도 조작 줌 범위(§54.16-1) — **«사용자 조작 상한»이지 «정밀도 주장 방어»가 아니다** */
export const STRIKE_MAP_MIN_ZOOM = 15;
export const STRIKE_MAP_MAX_ZOOM = 19;

/** 확신도 키 — **8/28 `rallyMap.ts` 의 `LEGEND_KEY` 와 한 글자도 같은 문자열**이다(§53-11 (3)).
 *  ⚠ **어휘를 바꾸려면 두 곳을 함께 고쳐라** — 한쪽만 고치면 두 지도가 점선을 다르게 설명한다.
 *  ★ 자리는 **지도 상자 «위»**(§54.16-1) · **화면 출현 1회**(QA-511 — 2회면 복제한 것이다).
 *  ★ **v2 에서도 그대로 유효하다**(M-57 범례표의 「키」 행) — 점선 = 원·사각형의 선종, 옅은 면 = 그 채움 */
export const LEGEND_KEY = "점선 = 근사 · 옅은 면 = 범위 근사";

/**
 * 화장실 안내 — **원본 지도 상자 안 두 줄 중 하나의 축자**(`design/총파업_지부위치.png`).
 *
 * ⚠⚠ **«새 사실»이 아니다 — `MAP-PLAN` M-53 이 이미 축자로 확정했는데 M-57 범례 9행이 안 담아 «유실»됐다.**
 *   렌더 실측 `인근화장실` 0 · `스벅` 0 이었다. → **«승인»이 아니라 «이행»이다.**
 * ★ `화장실` 과 `:` 사이 **공백 1칸은 원문 그대로다**(검증이 원본 3배 확대로 판독 · 다른 줄은 공백 0 이라 조판이 다르다).
 * ⛔ **`스벅` 을 「스타벅스」로 펴지 마라**(M-53 명시 · 뜻이 바뀌지 않는다).
 *
 * ⛔ **지도 마커를 만들지 마라** — 「스벅 등」은 **좌표가 없다.**
 * ⛔ **범례 «행»으로 넣지 마라** — 범례 9행은 **지도 «도형»의 텍스트 등가**다. 도형 없는 행을 넣으면 계약이 깨진다.
 * ⭕ **자리는 범례 «아래» 문장.** ⚠ `<figcaption>` 이 `<figure>` 의 **마지막 직계 자식**이어야 하므로
 *   (HTML 스펙) **`figcaption` «안» · 목록 아래**에 둔다. 밖으로 빼면 그 제약이 깨진다.
 * ⚠ **세로 증분·배치는 QA·디자이너 회부다** — 개발자가 정하지 않았다.
 */
export const TOILET_NOTE = "화장실 : 임시화장실 및 인근화장실(시청역, 스벅 등) 이용";

/** 지도 조작 완화 안내 — `touch-action: none` 의 필연적 귀결(§55-1·§55-2 확정 문면).
 *  ⚠ **`※` 를 붙이지 마라** — 이 페이지의 `※` 는 **종류 1 · 출현 2 고정**이다(§53-12 #15).
 *  ⚠ **좌우를 가리키지 마라** — 좌우 여백이 각 12px 이라 엄지가 안 들어간다(§54.16-4).
 *  ⚠ **`+`/`−` 를 넣지 마라**(§5.3). 대신 **확대 버튼을 조건부로 숨기지도 마라** — 완화가 그것에 기댄다.
 *  ★ **이것도 사실 주장이다** — 빈 곳이 44px 미만이면 문장이 아니라 종횡비를 고친다(게시 조건 19) */
export const MAP_GESTURE_NOTE =
  "지도는 손가락 하나로 움직입니다. 페이지를 내릴 때는 지도 위나 아래 빈 곳을 쓸어 주세요.";

/** 조작 어포던스 — **검증 §62-5 · M-57 확정 문면**. **자리는 조작 안내 문단 «안» 둘째 문장**이다.
 *  ⚠ **새 문단을 만들지 마라** — 지도 «위» 우리 문단이 3덩어리가 되면 «지도에 닿기 전에 읽어야 할 벽»이 된다.
 *  ⚠ **`※` 를 붙이지 마라**(종류 1 · 출현 2 고정) · **부정문을 덧붙이지 마라** — 긍정문이 이미 범위를 닫는다.
 *
 *  ⚠⚠ **이 문장은 «사실 주장»이다** — 지금 참인 것은 `roadview` 가
 *    **코스콤지부 구역 1 · 임시화장실 2 · 역 2 = 다섯 개뿐**이기 때문이다.
 *  ★★ **2026-09-01 — §59-4 가 «늘리면 깨진다»고 적어 둔 «그 경우»가 실제로 발동했다.**
 *    v1 문면은 `지도의 화장실과 역을 누르면 거리뷰가 열립니다.` 였는데 **코스콤지부 구역이 대상에 들어와**
 *    그 문장이 **대상을 좁혀 말하게** 됐다. **문면을 «먼저» 고치고 `ROADVIEW_IDS` 를 갱신했다**(게시 조건 52).
 *  ★ **양방향으로 깨진다:**
 *    · **늘리면** — 무대 4개에 `roadview` 를 켜는 순간 이 문장이 **대상을 좁혀 말한다.**
 *    · **줄이면** — 다섯 중 하나라도 끄면 **«없는 것을 누를 수 있다»** 고 말한다. **같은 무게로 거짓**이다.
 *    · ★ **지도가 «없을» 때(401 · 로드 실패)** — 이 문장은 거짓이 되지만 **피해가 0 이라 그대로 둔다**
 *      (눌러 봐도 아무 일이 안 일어난다. 이 프로젝트가 «거짓»으로 막은 것들은 **전부 «행동 → 피해»가 있었다**).
 *      ⚠⚠ **숨기지 마라** — 같은 문단의 `MAP_GESTURE_NOTE` 둘째 문장(`페이지를 내릴 때는…`)은
 *      **401 에서도 «참»이고 «가장 필요»** 하다: 마운트 노드의 `touch-action:none` 이 `status` 분기 **«밖»** 이라
 *      **지도가 죽어도 상자를 꽉 채우고**, 그 위를 쓸면 갇힌다. **그 문장이 그것을 푸는 «유일한» 문장이다.**
 *      **거짓 문장을 지우려다 그 문장을 함께 지우게 된다.**
 *  ⚠⚠ **`지도의 표식을 누르면…` 으로 뭉뚱그리지 마라**(검증 §62-5) — **무대 4개는 안 열린다.**
 *    원본에 무대 «도형»이 없어 무대 좌표에서 거리뷰를 열면 **빈 도로를 가리킨다.** 뭉뚱그리는 순간 거짓이다.
 *  ★ 아래 `ROADVIEW_IDS` 가드가 «늘리면·줄이면» 두 방향을 **기계적으로** 잡는다(셋째 방향은 못 잡는다 —
 *    데이터가 아니라 런타임 상태다). 그리고 가드는 «알리는» 것까지다 */
export const MAP_AFFORDANCE_NOTE = "지도의 코스콤지부 구역·화장실·역을 누르면 거리뷰가 열립니다.";

/** 지도 `sr-only` 안내문 — **§63 판정 2 확정본**.
 *  ⚠⚠ **8/28 문면을 복사하면 «거짓»이 된다** — 9/4 에는 그 텍스트 블록이 없다.
 *  텍스트 등가는 **지도 아래 범례 9행**이 진다.
 *
 *  ★★★ **답이 «나열»이 아니라 «뭉뚱그리는 낱말»인 이유** — v1 문면은 `무대·대오 위치를` 였는데
 *    「대오」가 죽어 갈아 끼워야 했다. 개발자 잠정안(`무대·코스콤지부 구역 위치를`)은 **화장실 2 · 역 2 가 빠져**
 *    반려됐다. ⚠ **지도가 보여 주는 것은 «9개»** 다(무대 4 · 코스콤 1 · 화장실 2 · 역 2) —
 *    **나열하면 반드시 하나가 빠지고, 이 문장은 «지도를 못 보는 사용자»의 유일한 요약이라 그 손실이 크다.**
 *  | 낱말 | 근거 |
 *  |---|---|
 *  | **`세종대로`** | **원문 축자** + **헤딩 `세종대로 안내지도` 와 같은 낱말** |
 *  | **`집회 장소`** | 9개 항목을 **하나도 빠뜨리지 않고** 덮는다. 무엇이 있는지는 **범례 9행이 전부 말한다** |
 *  ⚠ **둘째 문장을 지우지 마라** — §55-5 가 잡은 함정의 정정본이고 «지도 아래 범례»는 **v2 에서도 참**이다. */
export const MAP_SR_INTRO =
  "아래는 세종대로 집회 장소를 표시한 지도입니다. 같은 내용을 지도 아래 범례에서 텍스트로 제공합니다.";

/** 「근사」 한 줄. **`CONFIDENCE_VISUAL[confidence].approxNote` 가 참인 항목에만** 붙는다
 *  (지금은 `estimated` 7건뿐이고 역 2건은 안 붙는다 — 근거는 그 매핑 주석 M-70).
 *  ⚠⚠ **`id` 로 분기하지 마라**(이 파일 머리 주석) — `confidence` 축으로만 갈라야 **역에 붙을 길이
 *  구조적으로 없다.** `verified` 좌표를 「근사」라 말하는 것은 거짓이다(§58-3).
 *  ★ 자리는 **팝업 «과» 거리뷰 시트 둘 다**다 — 「거리뷰 보기」를 누르면 팝업이 닫히므로(§25.7)
 *  «한 화면에 두 번»이 아니라 **«연속된 두 화면에 각각 한 번»**이다(§5.3 에 안 걸린다).
 *  ⚠ **셋째 자리(지도 캔버스 «위»)를 만들지 마라** — 그 선을 넘으면 §5.3 위반이다.
 *  ⚠ `주변을 살펴 주세요` 류를 덧붙이지 마라 — 우리가 지어낸 행동 지시다(§58-2).
 *
 *  ★★ **2026-09-01 — `±25 m` → `±30 m`. §63 판정 3 «승인 확정».**
 *    ⚠ **금지 목록 회귀 감시 — «계층»을 반드시 병기하라**(안 적으면 프리렌더에 대고 세어 «거짓 실패»가 난다):
 *      `±30 m` = **소스 정본 ≥1 · 프리렌더 «0»(정상 — 팝업·시트 런타임 문면이다) · 런타임 ≥1** ·
 *      `±25 m` = **모든 계층 0**(소스 잔존은 전부 «죽었다»를 기록한 주석).
 *    v1 오차 예산(±25 m)은 v1 축척(0.375 m/px)의 값이고
 *    **v2 는 0.899 m/px 로 2.4배 거칠다**(검증 §62-3 오차 예산표: 모델+기준점 RMS 13.3 · 최대 29.7 ·
 *    대상 픽셀 판독 ±2.7 · 등척 가정 불확실 ±3 → **상한 ±30 m**).
 *    ⚠ **`±25` 를 남겨 두는 것은 «정밀도를 과장»하는 것이다** — 좌표가 나빠졌으면 문장도 따라가야 한다. */
export const APPROX_NOTE = "표시 위치는 약 ±30 m 근사입니다.";

/** 코스콤지부 위치 한 줄 — **사용자 확정 문면**(M-55 ① · **2026-09-01 «천막» 반영 개정**).
 *
 *  ★★★ **사용자 축자: «천막도 코리아나호텔 앞».** 그래서 **한 문장이 «천막과 집회 모두»를 함께 진다.**
 *  **근거 3:**
 *   1. ★ **좌표가 «같다»** — 별도 마커를 만들면 **같은 자리에 두 도형**이 겹치고
 *      조합원이 «둘이 다른 곳인가»로 읽는다. **문장 하나로 족하다.**
 *   2. ★★ **원문 축자에 기댄다** — 지도 범례 상자가 **`천막 및 집회: 코리아나호텔(폴바셋) 앞`** 으로
 *      **한 줄에 함께** 적었다. **우리가 나눈 것이 아니다.**
 *   3. ⛔ **죽었다**[근거 소멸 2026-09-01 · v3] — 종전 *«천막이 «따로» 있나 라는 물음을 닫는다»*.
 *      v3 가 `코스콤 천막 방문 **후** 집회구역 **이동**` 으로 **그 물음을 다시 연다**(우리 문장은 «한 지점»,
 *      v3 는 «두 지점 + 이동»). ★ **문면 자체는 «거짓이 아니다»** — 둘 다 코리아나호텔 앞이다.
 *      **근거 ①(좌표 동일)·②(원문 축자)만으로 유지**하고, 이 ③을 근거로 인용하지 마라.
 *
 *  ⛔ **종전 «천막에서 무엇을 하는지는 말하지 마라 — 9/4 근거 0» 은 죽었다**[근거 소멸 2026-09-01 · v3].
 *    v3 `10:30~11:00` 행이 **정확히 그것을 답한다**(참석명단 작성 · 투쟁용품 수령 · 점심간식 수령 ·
 *    비고 「천막에서 명단 작성 및 물품 수령」) → **지부 자체 자료 1급.** 그 축자는 **일정 블록이 진다.**
 *    기대 개정: **`참석명단` 1 · `물품 수령` 1 · `명단 작성` 2**(부분문자열 — `참석명단 작성` 안 1 + 비고 안 1).
 *  ⚠ **`지부천막` 0 만 «안 죽었다»** — v3 표기는 「코스콤 천막」·「천막」이고 **「지부천막」은 8/28 낱말**이다
 *    (v3 계수 0). ★ **위 셋과 «한 덩어리로» 풀지 마라.**
 *  ★★ **위험은 또 «이동»했다** — 이번엔 «유추가 틀린다»가 아니라 **«유추가 대체로 맞는데 세부 6축이 갈린다»**
 *    (시각 · 배포물 · 간식 · 장소 표기 · QR 괄호 · **반납은 정반대**). **8/28 문장을 옮기는 금지는 그대로다.**
 *  ⚠ **천막용 «새 마커·새 범례 행»을 만들지 마라** — 좌표가 코스콤 구역과 같고, **도형이 없으니
 *    범례에 올릴 것이 없다.** 범례는 **9행 그대로**다.
 *  ★ **자리는 지도 `figure` «안» · 캔버스 «밖»**(§53-15 조건 11). 범례 행 금지 · 지도 밖 본문 금지.
 *  ★★ **M-3 이 미리 정해 둔 대로 «그 자리에서 문면만» 바뀌었다 — 새 자리를 만들지 않았다.**
 *
 *  ⚠⚠ **종전 문면 `코스콤지부가 어느 대오인지는 추후 안내합니다.` 는 «죽었다». 인용하지 마라**(§61 A-4).
 *    그것은 **답이 있는데 «없다»고 말하는** 상태였다.
 *  ★★★ **이 문장은 «확정형»이고 그것이 옳다**(검증 §61-4 · M-51):
 *    **«코스콤지부 = 무대 2 구역»은 좌표가 아니라 «사실»이고 1급으로 확인**됐다
 *    (주최측 자료 **3장이 독립 일치** + 사용자 축자). **좌표가 근사라는 것이 그 사실까지 근사로 만들지 않는다.**
 *    → 화면에서 **두 층이 갈려 있다**: **이 문장은 확정형** · **지도 도형은 `estimated` 표시**.
 *    ⚠ **이 문장에 「근사」를 붙이지 마라** — 그러면 1급 사실을 근사로 격하한다.
 *  ⚠ **「2행」·「3,000명」·「천막 7」을 넣지 마라**(§61-5 (1)(4)) — 배치도를 안 보면 무의미하고
 *    **행동으로 옮길 수 없다.**
 *  ⚠⚠ **키가 없어 지도 섹션이 사라지면 이 문장도 함께 사라진다 — «의도된 상태»다**(위험과 완화가
 *  같은 조건부 안에 있어야 한다). **«사라지는 버그»로 보고 지도 밖으로 빼지 마라.** */
export const KOSCOM_LOCATION_NOTE =
  "코스콤지부는 무대 2 구역입니다. 천막과 집회 모두 코리아나호텔(폴바셋) 앞입니다.";

/* ── 2. 좌표 — 값 · 출처 · 확신도 ────────────────────────────────── */

/*
 * ★★★ **아래 좌표 7건은 전부 검증 §62-3 표의 값이다. 손으로 고치지 마라.**
 *   변환: `E(m) = 0.89897·px − 736.21` · `N(m) = −0.89897·py + 442.24` (lat0=37.5685 · lon0=126.9795)
 *   **전부 `estimated` · 오차 상한 ±30 m.** ★ **승격 근거 0** — 원본에 치수 표기가 없다.
 *
 * ★★ **교차 검산**: 코스콤 구역 `37.568220` ↔ **OSM 코리아나호텔 `37.568231` — 위도 차 1.2 m.**
 *   **환산과 원문 축자(「코리아나호텔(폴바셋) 앞」)가 서로를 확증한다.**
 */

/** 무대 4개 — **원본에 무대 «도형»이 없다.** 노란 라벨 상자가 전부이고 **상자 중심이 무대 지점이라는 것은
 *  «우리 가정»** 이다(검증 §62-3).
 *  ⚠⚠ **도트로 찍지 마라**(게시 조건 51) — `estimated` 좌표의 도트는 없는 정밀도를 주장한다. **원**이다.
 *  ★★ **v1 보다 근거가 «약하다»** — v1 은 라벨 상자 아래 노란 «띠»가 있어 «대오 북단»이라는 **보조 근거**가
 *    있었는데 **v2 에는 띠가 없어 그 보조 근거조차 0** 이다. **v1 의 «원이면 충분하다» 판정을 그대로
 *    옮기지 말고, 이 항목은 «원 + `estimated` 표시»가 «최소선»임을 기억하라.**
 *  ⚠ **경도를 하나로 통일하지 마라** — v1 은 «라벨 상자가 띠보다 넓어 동서를 말하지 않는다»는 이유로
 *    차도 중심 하나로 묶었지만, **v2 에는 그 띠가 없어 그 근거가 소멸**했다. **§62-3 이 준 값 그대로 쓴다.** */
const STAGE_1: StrikeLatLng = { lat: 37.569655, lng: 126.977183 };
const STAGE_2: StrikeLatLng = { lat: 37.568681, lng: 126.977188 };
const STAGE_3: StrikeLatLng = { lat: 37.567606, lng: 126.977188 };
const STAGE_4: StrikeLatLng = { lat: 37.56634, lng: 126.977137 };

/** 무대 원의 반경(m) — **M-56 게시 조건 51 · 리더 확정표가 «반경 30 m」로 못박았다.**
 *  ⚠⚠ **종전 근거(*«오차 예산 상한이 ±25 m 라 25 면 여유가 0 이라 30 이다»*)는 «죽었다». 인용하지 마라** —
 *    v2 오차 상한이 **±30 m** 로 커져 **반경 30 은 이제 «여유 0»** 이다.
 *    **그래도 30 인 것은 확정본이 그 값을 지정했기 때문이지 «여유가 있어서»가 아니다.**
 *    ★ 근거가 바뀌었으니 근거를 다시 적는다 — **다음 사람이 «±30 이니 35 로 올리자»로 뒤집지 않게** 한다.
 *      올리려면 **확정본을 고쳐야 한다.**
 *  ⚠ **도로 횡단선으로 긋지 마라** — 「어느 차로」는 **도출 불가**다(아래 `KOSCOM_AREA` 주석). */
export const STAGE_RADIUS_M = 30;

/**
 * ★★★ **코스콤지부 구역** — 이 지도의 **핵심**이다(§62-4: *«2행이 이 지도의 핵심»* · §5.3 «행동이 갈리는
 * 사실»의 최상위). 주황 사각형 중심 `(586.8, 526.5)` · **40×44 px = 36 × 40 m**(동서 36 · 남북 40).
 *
 * ⚠⚠⚠ **«어느 차로»를 문장으로 «절대» 말하지 마라**(게시 조건 49·53 · 검증 §61-1 (2) · §62-3):
 *   거리뷰 원본에서 폴리곤이 화면 **오른쪽 차로**에 그려져 있지만 **카메라가 어느 차로에 있었는지를 모른다.**
 *   네이버 거리뷰 차량은 차로를 달리므로 **«화면 오른쪽»이 «도로 동측»을 뜻하지 않는다.**
 *   ★ 그리고 **원문 축자가 반대쪽을 가리킨다** — 「코리아나호텔(폴바셋) 앞」·「코리아나 호텔 옆」은 **서측**이다.
 *   → **«그림의 좌우»를 «지상의 좌우»로 읽지 않는다**(§53-4 v1 밴드 사고와 **같은 계보**).
 *   → **도형은 사각형 그대로 그리고, 문장은 「코리아나호텔(폴바셋) 앞」에서 멈춘다.**
 *   ⚠ 기대 개수: **`동측`·`서측`·`오른쪽 차로` 각 0**(§62-8).
 *
 * ★ **폭 36 m 는 차도 대부분을 덮는다 — 그것이 우리가 아는 전부다.** 좁혀 그리면 «차로를 안다»는 주장이 된다.
 */
const KOSCOM_AREA: StrikeLatLng = { lat: 37.56822, lng: 126.977135 };
/** 동서 폭(m) — §62-3 실측 40 px × 0.89897 m/px */
const KOSCOM_AREA_WIDTH_M = 36;
/** 남북 높이(m) — §62-3 실측 44 px × 0.89897 m/px */
const KOSCOM_AREA_HEIGHT_M = 40;

/** 임시화장실 2곳 — 원본 픽토그램 쌍 중심 환산이 **유일한 근거**다. 임시 시설이라 **승격 경로가 없다**.
 *  ⚠⚠ **낱말은 `임시화장실` 이다**(§61-5 (3)) — **v1 의 `간이화장실` 은 죽었다. 되살리지 마라.**
 *  ⚠⚠ **v1 의 「시청역 화장실」 마커도 죽었다** — 신판은 그것을 **「인근화장실(시청역, 스벅 등)」로 «뭉쳤다».**
 *    개별 마커로 되살리면 **원문보다 «정밀한» 주장**이 된다(기대 `시청역 화장실` **0**). */
const TOILET_NORTH: StrikeLatLng = { lat: 37.56913, lng: 126.976362 };
const TOILET_SOUTH: StrikeLatLng = { lat: 37.566844, lng: 126.976623 };

/**
 * 역 좌표 — OSM `railway=station` 노드.
 *
 * ★★ **검증 §62-4 의 실측 검산이 «이 두 값»으로 잰 것이다** — 범례 8행(광화문역 = 무대 1 «북쪽») 은
 *   **218 m**, 9행(시청역 = 무대 4 «남쪽») 은 **96 m** 로 적혀 있고 두 수치가 아래 좌표에서 정확히 나온다.
 *   ⚠ **§62-3 좌표표에는 역이 «없다»** — 역은 §62-2 에서 **기준점(GCP)으로만** 쓰였다.
 *
 * ★★★ **2026-09-01 §63 — «역 노드»로 «확정»됐다. §62-6 #1 의 «판정 못 했다»는 «해소»됐다.**
 *   ⚠⚠ **두 역의 «등급이 다르다». 같게 적지 마라:**
 *   | 역 | 좌표 | 확신도 | 근거 |
 *   |---|---|---|---|
 *   | **시청역(1·2호선)** | `37.565480, 126.977114` | **`verified`** | OSM + **v1 원본 픽셀 환산 1.2 m 일치** |
 *   | **광화문역(5호선)** | `37.571616, 126.976903` | ★ **`calculated`** | **OSM 단독 · v2 원본과 대조 못 했다** |
 *
 *   **기각된 두 안 — 되살리지 마라:**
 *   · ❌ **«출구 두 점»(v1 M-19)** — 그 근거는 **v1 «렌더 특징»** 이었다(*자주색 역명판에서 «줄기» 2개가
 *     ⑥·⑤ 배지에 꽂힌다* · 실측 x 385~394 · 571~583). ★ **v2 는 ⑤ 원형 배지 + 텍스트뿐이고 «줄기가 없다».**
 *     옮기면 **§5.8.2 «구판에 기댄 판정»** 이다.
 *   · ❌ **«출구 한 점»** — v2 배지 `1` 이 OSM 광화문 1번(동남측)과 **어긋나 매칭에 실패**했다.
 *     ⚠⚠ ★ **«가장 가까워 보이는 것»을 고르는 것이 이 프로젝트가 «날조»라 부르는 것이다.**
 *
 *   ⚠ **범례 8행 「무대 1 북쪽입니다」는 «참»이지만 «218 m»를 감춘다**(게시 조건 54) — 그 경고는 **유효하다.**
 *
 * ★ **줌 한 단이 떨어진다** — 역 노드를 담으면 `fitBounds` 가 494 → 682 m 가 되고, **네이버 v3 는 «정수»
 *   줌만 쓰므로** 상자가 좁으면 z16 → z15 로 한 단 떨어진다. **처분은 M-63 ④**(아래 `STRIKE_MAP_FIT_BOUNDS`).
 *
 * ⛔⛔ **회수된 값 — «인용 금지»**(M-66-2): **`13.2 px`** · *«(가)는 24 px 규격 위반»* · *«저울이 뒤집힌다»*.
 *   개발자가 **1차원으로 재고**(세로 성분만) **장식(무대 pill)을 «대상»으로 세어** 만든 **없는 결함**이었다.
 *   ★ **참값(M-66-1 «브라우저 실측»으로 확증)**: 히트 ↔ 히트 최소 간격 **z16 68.0 px**
 *     (`koscom-area` ↔ `toilet-north` · 예보 68.2 와 **오차 0.2 px**) — **24 px 기준 여유 44 px. 통과.**
 *   ⚠ **되풀이 방지로만 남긴다 — «④의 근거»로 쓰지 마라.** ④의 근거는 **판독 축**이고 아래에 따로 있다.
 *   ★ 교훈 두 줄: **«한 축 성분으로 2차원 간격을 판정하지 마라»**(§5.8.4 ③ 계열) ·
 *     **«재기 «전»에 «대상이 무엇인가»를 먼저 갈라라»**(`roadview` 가 없으면 대상이 아니다).
 *
 * ★★★ **M-63 ④ — 광화문역은 `fitBounds` «밖»이다. 마커·pill·범례는 그대로다.**
 *   ⚠⚠ **종전 판정(§63 판정 1-보 «넣는다»)은 «뒤집혔다». 인용하지 마라.**
 *   **근거·기각안은 이 파일 아래 `STRIKE_MAP_FIT_BOUNDS` 주석에 모아 두었다** — 한곳에 모은 것은
 *   «항목 포함»과 «축척»이 **한 판정**이라 두 곳에 적으면 반쪽만 고쳐지기 때문이다.
 *
 *   ★★ **딸려 오는 것 — «초기 화면에서 이 항목이 어디 있나»**(계산 예보 · 실측 아님):
 *   | 뷰포트 | **점**(장식) | **pill**(= 이 항목의 «유일한» 히트) |
 *   |---|---|---|
 *   | 320 px | 위로 47.7 px 밖 | 완전히 밖 |
 *   | 360 px | 위로 22.7 px 밖 | 완전히 밖 |
 *   | 390 px | 위로 4.0 px 밖 | 완전히 밖 |
 *   | 412 px | 안(9.8 px) | ★ **완전히 밖**(4.2 px 모자람) |
 *   | 430 px | 안(21.0 px) | 아래 **7 px 만** 보임 |
 *   | 768 px 이상 | 안(34.8 px) | 24 중 **20.8 px** 보임 |
 *   ★ pill 은 `placement:"top"` · `labelGap` 14 라 **점보다 38 px 더 위**다 — 그래서 점이 화면에
 *     들어와도 pill 은 잘린다.
 *   ★ **배율 축**(QA 실측): `320`+200% 는 **z15**(상자 256×320) — **히트 5개가 전부 상자 안**이라
 *     **그 배율에서 `focusin` panTo 가 «안 도는 것이 정상»이다. 결함으로 보고 가드를 뜯지 마라.**
 *     히트↔히트 32.25 · 히트↔장식 **4.0**(z16 2.0 보다 **넓다** — 확대가 늘 빠듯해지는 것이 아니다).
 *     ⚠ **갈리는 «축»이 바뀐다**: z16 세로 · z15 가로.
 *   ★★ **실측(360×640)**: 히트 rect `y 796.2–820.2` · 상자 top `852.2` → **상자보다 32 px 위 · 통째로 잘린다.**
 *   ⚠⚠⚠ **그래서 «키보드 사용자가 보이지 않는 정거장에 선다»는 WCAG 2.4.7 결함이 있었다**(M-66-5) —
 *     **`StrikeMap.tsx` 의 `focusin` effect 가 그것을 고친다**(포커스 시 지도를 그리로 옮긴다 ·
 *     실측 `gwNowInsideBox: false → true` · 가시 면적 `0 → 2,161 px²`). **그 effect 를 지우지 마라.**
 *     ★ **포인터는 원래 닿았다**(아래로 120 px 드래그 · M-66-4).
 *   ★ **«정보»가 가려진 것은 아니다** — 위치는 **범례 8행**이 그대로 말한다(§0.4 판정은 위 `inFitBounds` 주석).
 *   ★ **`placement` 를 `"bottom"` 으로 바꾸면 pill 이 38 px 안으로 들어온다** — **접근성 결함은 이미 닫혔으므로
 *     이것은 «초기 화면 미관» 문제로 남는다.** 여전히 **디자이너 회부 사항**이다(항목 주석 참조).
 *
 * ⚠ **출구 번호를 안내하지 않는다**(M-11·M-13) — 시청역은 원본에 출구 배지가 없어,
 *   한쪽만 출구를 쓰면 *"시청역은 아무 데나 나와도 되나"* 로 읽힌다.
 */
const GWANGHWAMUN_STATION: StrikeLatLng = { lat: 37.571616, lng: 126.976903 };
/** OSM `railway=station`(`ref 132;201`) ↔ 원본 역 점 픽셀 환산이 **1.2 m 일치**(§54-2).
 *  ★ **§62-2 가 이 교차확인을 «지금도 유효한 것»으로 인용한다** — *"기준점 16개 중 «독립 검증된» 것은
 *  «시청역 역 노드» 1개뿐"*. **v1 픽셀 환산값이 아니라 OSM 노드값**이라 v2 에서도 그대로 선다 */
const CITYHALL_STATION: StrikeLatLng = { lat: 37.56548, lng: 126.977114 };

/* ── 3. 기호 체계 타입 ───────────────────────────────────────────── */

interface StrikeFeatureBase {
  id: string;
  /** 지도 위 이름 pill 본문. **`null` 이면 pill 을 그리지 않는다.**
   *  ★ pill 은 **7개**(무대 4 · 코스콤지부 구역 1 · 역 2) — 9개를 전부 띄우면 반드시 겹친다(§54.5-3).
   *  ⚠ **화장실 2의 pill 생략은 «은폐»가 아니다** — 범례 2행이 그대로 있다 */
  label: string | null;
  /** 범례 행 본문 — **M-57 · 검증 §62-4 «9행» 축자.** 한 글자도 바꾸지 마라 */
  legend: string;
  /** 범례 앞 기호 글리프 — `aria-hidden` 장식 문자. `symbol` 이 있으면 쓰지 않는다 */
  glyph: string;
  /** 종류 픽토그램(화장실) — 있으면 **범례와 지도가 같은 SVG 를 그린다.**
   *  ⚠ **이모지를 쓰지 마라** — 기기마다 다른 그림이 나가고 글꼴이 없으면 두부(□)로 떨어진다 */
  symbol?: "toilet";
  tone: StrikeMapTone;
  placement: StrikeLabelPlacement;
  /** `top`/`bottom` 라벨의 가로 정렬 기준(기본 center) */
  labelAlign?: "west" | "center" | "east";
  /** 앵커에서 라벨까지의 간격(px). 기본 세로 14 / 가로 28. **라벨 충돌을 푸는 자유도** */
  labelGap?: number;
  /** 라벨을 다는 지점을 직접 지정한다. 없으면 `placement` 에서 자동으로 정한다 */
  labelAt?: StrikeLatLng;
  /**
   * ★★ **초기 화면(`fitBounds`)이 이 항목을 «담는가».** 없으면 담는다(기본).
   *
   * ⚠⚠⚠ **`false` 는 «표시하지 않는다»가 «절대» 아니다** — 마커도 pill 도 범례 행도 **그대로 그린다.**
   *   **오직 «초기 화면을 그 항목까지 넓힐 것인가»만** 정한다. 조합원은 **드래그·축소로 닿는다**(M-14).
   * ⚠ **이 필드를 «숨기기» 용도로 쓰지 마라** — 그러면 §0.4(콘텐츠 은폐)에 걸린다.
   *   ★ **§0.4 판정**(M-67-5): §0.4 가 막는 것은 **탭·아코디언 «뒤»에 숨기는 것**이다.
   *     지도는 본래 **움직이는 매체**이고 드래그가 열려 있으며 **조작 안내 문장이 그것을 말한다**
   *     (`지도는 손가락 하나로 움직입니다`). 그리고 **정보 자체는 범례 9행이 그대로 진다** —
   *     화면 밖으로 나가는 것은 «지도 위 표식»이지 «텍스트 등가»가 아니다.
   * ⚠ **지금 `false` 는 광화문역 «하나»뿐이다.** 늘리기 전에 `STRIKE_MAP_FIT_BOUNDS` 주석을 읽어라 —
   *   여러 개를 빼면 **초기 화면이 «무엇을 담는 화면»인지 말할 수 없게 된다.**
   */
  inFitBounds?: false;
  /** 누르면 팝업이 열리고 거기서 거리뷰를 여는 항목인가 — **코스콤지부 구역 1 + 화장실 2 + 역 2**
   *  (검증 §62-5 · M-57). ★ **사용자가 «코스콤 구역에 거리뷰»를 명시 요구**했다.
   *  ⚠⚠ **무대 4개에 붙이지 마라** — **원본에 무대 도형이 없어 «빈 도로»를 가리킨다**(§62-5).
   *    그리고 지도 조작 안내가 «무엇이 눌리는가»를 «주장»하므로 대상이 늘면 그 문장이 거짓이 된다.
   *  ★ 히트 대상은 **«pill 이 있으면 pill · 없으면 기호 배지»** 이고 **점(dot)은 언제나 장식**이다(§54.18-1) */
  roadview?: true;
}

/**
 * ★★★ **축정렬 사각형 — «코스콤지부 구역» 전용.** 중심 + 동서/남북 치수에서 **네 꼭짓점을 파생**한다.
 *
 * ⚠⚠ **이것은 v1 `StrikeBandFeature`(대오 밴드)의 «이름만 바꾼 것»이 아니다:**
 *   · 밴드는 **«도로 위 구간»** 이라 남북으로 길고 **경계가 원본에도 없어** «우리가 만든 선»이었다.
 *   · 이 사각형은 **원본이 «직접 그린» 주황 도형**이고 치수가 **픽셀 실측(40×44 px)에서 나온다.**
 *   → **꼭짓점을 손으로 적지 않는다.** 손으로 적으면 다음 사람이 «중심을 옮겨도 되나»를 알 수 없다.
 * ⚠ **`confidence` 를 `"estimated"` 로 «타입이» 못박는다 — 승격 근거 0**(§62-3).
 */
export interface StrikeRectFeature extends StrikeFeatureBase {
  kind: "rect";
  center: StrikeLatLng;
  /** 동서 폭(m) */
  widthMeters: number;
  /** 남북 높이(m) */
  heightMeters: number;
  confidence: "estimated";
}

/** 무대 원 — 좌표가 없어 **범위로만** 아는 것 */
export interface StrikeCircleFeature extends StrikeFeatureBase {
  kind: "circle";
  center: StrikeLatLng;
  radiusMeters: number;
  confidence: "estimated";
}

/** 점 — 확정 좌표 1개인 지점(역 2 · 화장실 2) */
export interface StrikeDotFeature extends StrikeFeatureBase {
  kind: "dot";
  position: StrikeLatLng;
  confidence: StrikeGeoConfidence;
}

export type StrikeMapFeature = StrikeRectFeature | StrikeCircleFeature | StrikeDotFeature;

/* ── 4. 지도에 그리는 것 — **이 배열이 지도의 전부다** ──────────────── */

/**
 * 표시 항목 **9개** — **배열 순서 = 범례 순서**(M-57 · §62-4). 범례가 **«양 끝이 역으로 닫힌 사슬»** 이라
 * 중간 항목이 **양 끝의 역에 매달린다.** ★ **v2 에서도 이 형태가 성립한다**(§62-4 실측 검산 통과:
 * 코스콤 = 무대2 남 **51 m** · 화장실북 = 무대1 남 58 m / 무대2 북 50 m — **정확히 «사이»** 등).
 *
 * ★★★ **여기 없는 것 — 「대오 1~4」. 다시 넣지 마라.** 신판에 **개념 자체가 없다**(§61 A-1).
 *   조합원이 «내 대오»를 찾는데 **화면에 틀린 답이 있는 것**이 이 프로젝트가 막는 최상위 위험이다.
 *
 * 그 밖에 여기 없는 것 — **다시 넣지 마라:**
 * - **버스 하차 2곳 · 대형버스 주차** — ① v2 자료에 **아예 없다** ② 원본에 앵커가 없어 좌표를 못 구한다
 *   ③ **코스콤지부는 전세버스를 운행하지 않는다**(1급 · M-13). ★ **근거가 셋이다 — 하나가 죽어도 판정이 선다.**
 * - **`서울시의회 본관`·`광화문빌딩`** — **바탕 지도 라벨**이다(§62-4). 우리가 옮겨 적으면 우리 저작이 된다.
 *   ⚠ **`코리아나호텔(폴바셋)` 만 예외** — **원문 축자**(배치도·지도 범례)라 허용된다.
 * - **`코스콤 천막`** — 이 목록에서 **빠졌다.** 위치가 확인됐고(사용자 축자 「천막도 코리아나호텔 앞」 · M-61)
 *   그 한 줄은 **`KOSCOM_LOCATION_NOTE`** 가 진다. **여기에 다시 적지 마라.**
 *   ⚠ **기대는 «0 이 아니라 렌더 1»**(출처 `KOSCOM_LOCATION_NOTE`). **0 으로 되돌리면 «사용자 축자 문면»을 지운다.**
 *   ⛔ **종전 «`참석명단` 0» 은 죽었다**[2026-09-01 · v3] — 기대 **1**(일정 블록 축자).
 *     ⚠ **`지부천막` 0 «만» 유지**다 — **8/28 낱말**이고 v3 계수 0. **`참석명단` 과 한 덩어리로 읽지 마라.**
 *   ⚠ **M-58 · §62-7(«미게시»)은 M-61 로 근거 소멸.** 인용은 남긴다.
 * - **인원 수(`6,000명`·`3,000명`·`4,000명`)** — 알아도 하는 일이 같다(§5.3). 기대 **각 0**.
 * - **`내 위치`** — 우리 좌표가 **±30 m** 라 *«내가 코스콤 구역 안인가»* 에 답할 수 없다(구역 자체가 36×40 m 다).
 * - **`촬영 2026년 04월`** — 원본 스크린샷 «안»의 **네이버 UI 값**이다. 네이버가 갱신하므로 옮겨 적지 마라(§61-4).
 */
export const STRIKE_MAP_FEATURES: readonly StrikeMapFeature[] = [
  {
    kind: "circle",
    id: "stage-1",
    /* ★★ **`메인무대` 가 아니다**(M-55 ③ — **검증 §61-3 판정이 «뒤집혔다». 인용하지 마라**).
       사용자에게 물은 것이 ***«현장 표지판은 어느 쪽입니까?»*** 였고 **`무대 1`** 을 골랐다.
       ★ **현장 표지판이 조합원이 «실제로 보는» 것이다** — 검증자 근거 3개(v1 연속성 · «넷이 대등» 함의 ·
         배치도가 정보가 많다)는 **전부 «우리 화면 안»의 논리**인데 **현장에서 다르게 쓰이면 그것이 이긴다.**
       ⚠ **배치도 헤더는 여전히 「메인무대」다** — **우리는 «지도» 쪽 낱말을 쓴다.** 기대 `메인무대` **0**.
       ⚠ **`/rally-2026-08-28` 의 `메인무대` 는 «정당한 출현»이다**(8/28 여의도 원문). **함께 지우지 마라**(§5.8.3) */
    label: "무대 1",
    legend: "무대 1 — 광화문역 남쪽, 가장 북쪽 무대입니다",
    glyph: "○",
    tone: "reference",
    confidence: "estimated",
    center: STAGE_1,
    radiusMeters: STAGE_RADIUS_M,
    placement: "left",
  },
  {
    kind: "circle",
    id: "stage-2",
    label: "무대 2",
    /* ★★★ **이 한 행이 이 지도의 «핵심»이다**(§62-4) — §5.3 «행동이 갈리는 사실»의 최상위.
       ⚠ **좌표가 근사여도 이 «사실»은 1급이다**(§61-4). **문장을 흐리지 마라.** */
    legend: "무대 2 — 코스콤지부가 있는 무대입니다",
    glyph: "○",
    /* ★★★ **`go`(파랑)다 — 2026-09-01 사용자 확정**(`MAP-EMPHASIS.md` §10). 종전 `reference` 는 **죽었다.**
       ★ **이것은 «강조 목적의 색 차용»이 아니라 «정정»이다** — 무대 2 는 코스콤 조합원에게
         «참고»가 아니라 **«가는 곳»** 이다. 바로 위 `legend` 와 `KOSCOM_LOCATION_NOTE` 가
         *「코스콤지부는 무대 2 구역입니다」* 라고 말한다 — **소속이 아니라 목적지다.**
         → **`reference` 가 «틀린 값»이었고 `go` 가 참값이다. 강조는 그 «정정의 파생»이다.**
       ⚠⚠ **§7-6(«다른 지물의 `tone` 을 강조 목적으로 바꾸지 마라»)과 충돌하지 않는다** —
         그 절이 막는 것은 «강조하려고 성격 값을 빌려 쓰는 것»이고, 여기서 바뀐 것은 **성격 판정 자체**다.
       ★ **원과 pill 이 «함께» 간다**(`drawFeature` 가 `toneColor(feature.tone)` 하나로 둘을 그린다) —
         pill 만 파랑이고 원이 회색이면 **한 지물이 두 성격을 말한다**(리더 판정 · §10).
       ⛔ **`confidence`·`fillOpacity`·선종·`casingWeight` 를 함께 손대지 마라 — 그것은 «확신도»다**(M-70).
         **hue 하나만 옮긴다.**
       ⚠ **pill 문면·상자·`placement`·`kind`·`glyph`·`zIndex` 는 전부 불변이다** — 문면 변경안
         (「무대 2 (코스콤지부)」)은 **기각됐다**(사용자가 직접 고른 낱말 + 여유 2.0 px 자리 재측정) */
    tone: "go",
    confidence: "estimated",
    center: STAGE_2,
    radiusMeters: STAGE_RADIUS_M,
    placement: "left",
  },
  {
    kind: "rect",
    id: "koscom-area",
    label: "코스콤지부 구역",
    /* ⚠ **「코리아나호텔(폴바셋)」에서 «멈춘다»** — 「앞」 뒤에 차로·방위를 덧붙이지 마라(위 `KOSCOM_AREA` 주석) */
    legend: "코스콤지부 구역 — 무대 2 남쪽, 코리아나호텔(폴바셋) 앞입니다",
    glyph: "▨",
    /* ★ **`go`(파랑)다** — `tone` 계약이 *«조합원의 경로»* 를 파랑으로 정했고, v2 에서 그 경로는
       **대오가 아니라 «역 → 무대 2 → 이 구역»** 이다.
       ⚠ 종전 주석은 «갈 곳 = 이 구역» 으로 **단수**였는데 2026-09-01 무대 2 가 `go` 로 오면서
         그 단수형이 죽었다 — **이 구역은 경로의 «끝» 이다**(`StrikeMapTone` 정의 참조).
       ⚠ **`estimated` 를 이유로 회색으로 바꾸지 마라**(M-2) —
       색은 `tone`, 확신도는 **선종·`LEGEND_KEY`** 가 전담한다. 섞으면 «갈 곳»이 «참고 지물»로 읽힌다 */
    tone: "go",
    confidence: "estimated",
    center: KOSCOM_AREA,
    widthMeters: KOSCOM_AREA_WIDTH_M,
    heightMeters: KOSCOM_AREA_HEIGHT_M,
    /* 무대 pill 4개가 전부 `left` 라 **동쪽이 비어 있다** — 그 자리를 쓴다.
       ⚠ 무대 pill 을 `right` 로 옮기지 마라 — 서쪽 열(화장실 배지)과의 간격이 v1 에서 **2.7px** 까지 몰렸던
         자리라 **양쪽을 한꺼번에 흔든다.** 배치를 바꾸려면 QA 재측정이 함께 간다 */
    placement: "right",
    roadview: true,
  },
  {
    kind: "circle",
    id: "stage-3",
    label: "무대 3",
    legend: "무대 3 — 무대 2 남쪽입니다",
    glyph: "○",
    tone: "reference",
    confidence: "estimated",
    center: STAGE_3,
    radiusMeters: STAGE_RADIUS_M,
    placement: "left",
  },
  {
    kind: "circle",
    id: "stage-4",
    label: "무대 4",
    legend: "무대 4 — 시청역 북쪽, 가장 남쪽 무대입니다",
    glyph: "○",
    tone: "reference",
    confidence: "estimated",
    center: STAGE_4,
    radiusMeters: STAGE_RADIUS_M,
    placement: "left",
  },
  {
    kind: "dot",
    id: "toilet-north",
    label: null,
    legend: "임시화장실 — 무대 1 과 무대 2 사이 서쪽입니다",
    glyph: "",
    symbol: "toilet",
    tone: "reference",
    confidence: "estimated",
    position: TOILET_NORTH,
    placement: "left",
    roadview: true,
  },
  {
    kind: "dot",
    id: "toilet-south",
    label: null,
    legend: "임시화장실 — 무대 3 과 무대 4 사이 서쪽입니다",
    glyph: "",
    symbol: "toilet",
    tone: "reference",
    confidence: "estimated",
    position: TOILET_SOUTH,
    placement: "left",
    roadview: true,
  },
  {
    kind: "dot",
    id: "gwanghwamun",
    label: "광화문역(5호선)",
    /* 「(5호선)」은 원본 근거가 있고, **어느 노선으로 갈지가 조합원 행동을 가른다**(§5.3 통과).
       ⚠ **「무대 1 북쪽입니다」는 참이지만 «218 m»를 감춘다**(게시 조건 54) — 위 `GWANGHWAMUN_STATION` 주석 */
    legend: "광화문역(5호선) — 무대 1 북쪽입니다",
    glyph: "◉",
    tone: "go",
    /* ★★ **`calculated` 다 — 시청역(`verified`)과 «등급이 다르다». 같게 적지 마라**(§63 판정 1).
       OSM 단독이고 **v2 원본과 대조하지 못했다.** ⚠ `verified` 로 올리지 마라 — 대조한 적이 없다.
       ⚠ `estimated` 로 내리지도 마라 — 그러면 「근사 ±30 m」가 붙는데 **그것은 픽셀 환산 오차 예산**이고
         이 좌표는 **환산으로 나온 값이 아니다.** 등급을 움직이려면 **검증이 v2 원본을 다시 봐야 한다** */
    confidence: "calculated",
    position: GWANGHWAMUN_STATION,
    /* ⚠⚠ **`placement: "top"` 의 근거가 «두 겹으로» 죽었다. 값은 유지하되 근거를 다시 적는다:**
       ① 종전 근거 *«광화문역 pill 이 «두 점» 위에 서야 한다»*(§54.16-14) — **점이 하나**가 되어 죽었다.
       ② 그 다음 근거 *«지도의 최북단이라 `FIT_PADDING.top` 48 이 자리를 비워 둔다»* — **M-63 ④로
          이 항목이 `fitBounds` 밖이 되어** 초기 화면의 북쪽 극점은 **무대 1 원**이다. 역시 죽었다.
       ★ **지금 남은 근거는 «남쪽이 세종대로 본체 방향이라 pill 을 남쪽으로 뻗으면 무대 1 쪽을 덮는다»** 뿐이고,
         그것은 z16 에서 218 m = 115 px 라 **실제로는 안 덮는다.**
       ★★★ **M-67-4 — 지금 이 값을 지키는 근거는 «옳아서»가 아니라 «바꿀 시간이 없어서»다. 둘은 다르다:**
         **`placement` 를 바꾸면 «충돌 지형»이 통째로 바뀐다.** 지금 히트↔장식 최악이 **2.0 px** 이라
         **여유가 거의 없다.** `"bottom"` 으로 내리면 광화문역 pill 이 **무대 1 pill·원 쪽으로** 내려가고,
         그 결과는 **`320·360·390·412·768·1280` × 기본·200%** 를 **전부 다시 재야** 확정된다.
         **9/4 까지 사흘이라 그 사슬을 지금 흔들지 않는다.**
         ⚠⚠ **«근거가 죽었으니 바꾸자»가 아니다** — 접근성 결함은 `focusin` effect 가 **이미 닫았으므로**
           남은 것은 **«초기 화면 미관»** 이고, 그것은 사흘을 쓸 값이 아니다.
         → **후속 #30 에 합류**(상자 높이 33 px 건과 **같은 사슬**이다). **9/4 이후 «한 번에» 다시 잰다** */
    placement: "top",
    labelAlign: "center",
    roadview: true,
    /* ★★★ **M-63 ④ — 초기 화면이 이 항목까지 넓혀지지 «않는다». 마커·pill·범례 8행은 그대로다.**
       근거는 `STRIKE_MAP_FIT_BOUNDS` 주석에 있다. ⚠ **여기만 보고 «숨긴 것»으로 읽지 마라** */
    inFitBounds: false,
  },
  {
    kind: "dot",
    id: "cityhall",
    label: "시청역(1·2호선)",
    legend: "시청역(1·2호선) — 무대 4 남쪽입니다",
    glyph: "◉",
    tone: "go",
    confidence: "verified",
    position: CITYHALL_STATION,
    /* ⚠⚠ **종전의 `labelGap: 20` 은 «죽었다»** — 그 값의 근거는 *«이 점이 «대오 4 밴드» 한가운데라 pill 을
       밴드 «밖»에 둔다»*(§54.16-11 (3) B) 였는데 **밴드가 없어졌다.** 근거가 소멸했으므로 **파생값도
       회수**하고 기본 간격(28)으로 돌아간다. ⚠ **«전에 20 이었으니 유지»로 되돌리지 마라** — 그것이
       §5.8.2 가 막은 «구판에 기댄 판정»이다. 지금 이 점 주위에는 덮을 도형이 하나도 없다 */
    placement: "right",
    roadview: true,
  },
];

/**
 * `MAP_AFFORDANCE_NOTE` 가 «참인 조건»의 **기계적 방어**. 그 문장은
 * **«코스콤지부 구역·화장실·역»만 눌린다**고 주장하는데, 그 주장은 **양방향으로** 깨진다(늘려도 · 줄여도).
 * 이 집합이 **양쪽을 다** 잡는다.
 *
 * ★★ **2026-09-01 — 이 가드가 «설계대로» 발동했다.** v1 대상(화장실 3 · 역 2)에서 **코스콤지부 구역이
 *   들어오고 「시청역 화장실」이 빠져** 집합이 갈렸고, **문면을 먼저 고친 뒤 이 배열을 갱신**했다
 *   (검증 §62-5 · 게시 조건 52 — §59-4 가 «늘리면 깨진다»고 예고한 그 경우).
 *
 * ⚠⚠ **문면을 이 집합에서 «생성»하지 마라** — 생성된 문면은 **검증을 통과한 적이 없다.**
 *   가드는 **불일치를 «알리는»** 것까지만 한다. 문면은 §62-5 확정본 그대로 손으로 적혀 있어야 한다.
 * ★ **`rally.ts`·`strike.ts` 의 단일 출처 가드와 같은 계보**지만 **거동이 다르다 — 여기는 «죽는다».**
 *   거기는 «옛 날짜를 들고 남는» 것이라 안내 콘텐츠 자체는 유효해 `console.error` 로 족했다.
 *   여기는 **화면에 거짓 문장이 나가는** 것이고, 두 값이 **같은 파일의 정적 데이터**라 불일치는
 *   **`next build` 의 프리렌더에서 반드시 평가된다** → **빌드가 실패하고 조합원에게 닿지 않는다.**
 *   ⚠ 그래서 «페이지가 죽는다»가 아니라 «**배포되지 않는다**»가 이 `throw` 의 실제 효과다.
 */
const ROADVIEW_IDS: readonly string[] = [
  "koscom-area",
  "toilet-north",
  "toilet-south",
  "gwanghwamun",
  "cityhall",
];

{
  const actual = STRIKE_MAP_FEATURES.filter((f) => f.roadview === true)
    .map((f) => f.id)
    .sort()
    .join(" · ");
  const expected = [...ROADVIEW_IDS].sort().join(" · ");
  if (actual !== expected) {
    throw new Error(
      `[strikeMap] roadview 집합이 갈렸습니다 — MAP_AFFORDANCE_NOTE("${MAP_AFFORDANCE_NOTE}") 가 거짓이 됩니다.\n` +
        `  기대: ${expected}\n  실제: ${actual}\n` +
        "  대상을 바꾸려면 검증 §62-5 로 문면을 먼저 고치고 ROADVIEW_IDS 를 함께 갱신하십시오.",
    );
  }
}

/* ── 5. 파생값 — 라벨 앵커 · 초기 범위 (손으로 적지 않는다) ───────────── */

/** 지오데식 상수(위도 1도 ≈ 111,320 m). 반경·치수(m)를 위경도 델타로 환산할 때만 쓴다.
 *  ⚠ 검증이 쓴 `MLAT=111,132` 와 다르다 — 저쪽은 **좌표 «도출»** 상수이고 이것은 **화면 «렌더»** 상수다.
 *  두 값의 차이는 0.16% 로 반경 30 m 에서 **5 cm** 다. **좌표를 이 상수로 다시 계산하지 마라.** */
const LAT_DEGREE_METERS = 111_320;

function lngDegreesPerMeter(lat: number): number {
  return 1 / (LAT_DEGREE_METERS * Math.cos((lat * Math.PI) / 180));
}

function circleBounds(center: StrikeLatLng, radiusMeters: number): StrikeBounds {
  const dLat = radiusMeters / LAT_DEGREE_METERS;
  const dLng = radiusMeters * lngDegreesPerMeter(center.lat);
  return {
    south: center.lat - dLat,
    west: center.lng - dLng,
    north: center.lat + dLat,
    east: center.lng + dLng,
  };
}

/** 사각형의 네 꼭짓점 — **중심 + 치수에서 파생한다. 손으로 적지 않는다**(`StrikeRectFeature` 주석).
 *  ⚠ 반환 순서는 «북서 → 북동 → 남동 → 남서»(시계 방향)다. 폴리곤은 닫힌 고리라 마지막 점을 반복하지 않는다 */
export function rectCorners(feature: StrikeRectFeature): readonly StrikeLatLng[] {
  const dLat = feature.heightMeters / 2 / LAT_DEGREE_METERS;
  const dLng = (feature.widthMeters / 2) * lngDegreesPerMeter(feature.center.lat);
  const { lat, lng } = feature.center;
  return [
    { lat: lat + dLat, lng: lng - dLng },
    { lat: lat + dLat, lng: lng + dLng },
    { lat: lat - dLat, lng: lng + dLng },
    { lat: lat - dLat, lng: lng - dLng },
  ];
}

/** 꼭짓점들의 **극값**(최남·최서·최북·최동).
 *  ⚠ **도형을 사각형으로 근사하는 용도가 아니다** — 그리기는 항상 원본 좌표로 한다 */
function extremesOf(points: readonly StrikeLatLng[]): StrikeBounds {
  const lats = points.map((p) => p.lat);
  const lngs = points.map((p) => p.lng);
  return {
    south: Math.min(...lats),
    west: Math.min(...lngs),
    north: Math.max(...lats),
    east: Math.max(...lngs),
  };
}

/** 항목이 차지하는 범위 — `fitBounds` 가 이것들의 합집합을 담는다 */
export function featureBounds(feature: StrikeMapFeature): StrikeBounds {
  switch (feature.kind) {
    case "dot":
      return {
        south: feature.position.lat,
        west: feature.position.lng,
        north: feature.position.lat,
        east: feature.position.lng,
      };
    case "circle":
      return circleBounds(feature.center, feature.radiusMeters);
    case "rect":
      return extremesOf(rectCorners(feature));
  }
}

/** 라벨 앵커 — **도형의 극점**에서 뽑는다. `top`/`bottom` 은 최북/최남단 바깥이라
 *  **자기 도형을 절대 덮지 않는다.** */
function anchorAtExtreme(
  b: StrikeBounds,
  placement: StrikeLabelPlacement,
  align: "west" | "center" | "east" = "center",
): StrikeLatLng {
  const lng = align === "west" ? b.west : align === "east" ? b.east : (b.west + b.east) / 2;
  switch (placement) {
    case "top":
      return { lat: b.north, lng };
    case "bottom":
      return { lat: b.south, lng };
    case "left":
      return { lat: (b.south + b.north) / 2, lng: b.west };
    case "right":
      return { lat: (b.south + b.north) / 2, lng: b.east };
  }
}

/** 라벨(pill)을 다는 지점. 명시값(`labelAt`)이 있으면 그것이 이긴다 */
export function featureLabelAnchor(feature: StrikeMapFeature): StrikeLatLng {
  if (feature.labelAt !== undefined) return feature.labelAt;
  if (feature.kind === "dot") return feature.position;
  return anchorAtExtreme(featureBounds(feature), feature.placement, feature.labelAlign);
}

/** 점 배지를 찍을 좌표들 — **점 항목만 배지를 갖는다.** 원·사각형은 도형이 자기를 말한다.
 *  ⚠ 사각형 중심에 점을 찍지 마라 — **36×40 m 구역**인데 도트는 «한 지점»을 주장한다 */
export function featurePoints(feature: StrikeMapFeature): readonly StrikeLatLng[] {
  return feature.kind === "dot" ? [feature.position] : [];
}

/** 범례 문면의 «이름 — 설명» 구분자. 9행이 전부 이 형태다 */
const LEGEND_SEPARATOR = " — ";

/** 짧은 이름 — **`legend` 문면의 «— » 앞부분**이다(§58-6). **신규 문자열 0 · 파생값이다.**
 *  나오는 값: `코스콤지부 구역` · `임시화장실` · `광화문역(5호선)` · `시청역(1·2호선)`.
 *  ★ **쓰이는 곳은 «파노라마 와드 라벨» 하나뿐이다.**
 *  ⚠ **별도 이름 상수를 만들지 마라** — 한쪽만 고쳐진다(요구 88). 범례를 고치면 여기가 따라온다.
 *  ⚠ **파노라마 와드에 범례 «전문»을 넣지 마라** — 와드는 `nowrap` 한 줄이라 문장이 화면을 가로지른다.
 *  ★ 화장실 2곳이 같은 이름(`임시화장실`)이 되지만 **와드에서는** 충돌하지 않는다 — **한 번에 하나**만 뜬다.
 *
 *  ⚠⚠ **그래서 이 값을 «접근성 이름»으로 쓰면 안 된다**(M-34) — 접근성 이름은 **다섯 개가 한 목록에 함께
 *  서므로** 같은 이름 둘이 **구별 불가**가 된다. 마커 버튼의 이름은 **`legend` «전문»**이다.
 *  ★ **«구별 불가»는 «못 쓰는 것»이고 «반복»은 «불편»이다 — 무게가 다르다.**
 *  ⚠ 8/28 이 «이름을 짧게»로 푼 것을 근거만 보고 옮기지 마라 — 거기는 **`{번호} {이름}` 의 «번호»가
 *  구별자**였고 **9/4 는 번호가 0**(§54.16-12)이라 **그 처방이 기대던 것이 여기엔 없다.** */
export function featureShortName(feature: StrikeMapFeature): string {
  const cut = feature.legend.indexOf(LEGEND_SEPARATOR);
  return cut < 0 ? feature.legend : feature.legend.slice(0, cut);
}

/** 거리뷰 시작점 — **«그 지물의 지점»**(§58-4). `roadview` 가 아닌 항목은 `null` 이다.
 *  ★ **사각형(코스콤지부 구역)은 «중심»에서 연다** — 36×40 m 구역이라 어느 꼭짓점을 골라도
 *    «그 모서리가 특별하다»는 주장이 되고, 중심이 유일하게 **아무것도 더 주장하지 않는 점**이다.
 *  ⚠ 중심 좌표를 **데이터로 저장하지 마라** — 이 함수가 그때 계산해 넘기는 값이지
 *    지도 도형·범례로 나가는 값이 아니다(§54-8 조건 14 의 정신). */
export function featureRoadviewPoint(feature: StrikeMapFeature): StrikeLatLng | null {
  if (feature.roadview !== true) return null;
  if (feature.kind === "rect") return feature.center;
  return featurePoints(feature)[0] ?? null;
}

/** 라벨 기본 간격(px) — 세로/가로. 항목이 `labelGap` 을 주면 그것이 이긴다 */
const DEFAULT_LABEL_GAP_VERTICAL = 14;
const DEFAULT_LABEL_GAP_HORIZONTAL = 28;

export function labelGapOf(feature: StrikeMapFeature): number {
  if (feature.labelGap !== undefined) return feature.labelGap;
  return feature.placement === "top" || feature.placement === "bottom"
    ? DEFAULT_LABEL_GAP_VERTICAL
    : DEFAULT_LABEL_GAP_HORIZONTAL;
}

/**
 * 초기 화면 범위 — `STRIKE_MAP_FEATURES` 에서 **자동 계산**. ⚠ **하드코딩 중심·줌 금지**(§54.4-3).
 *
 * ★★★ **M-63 ④ — «광화문역만» 담지 않는다**(`inFitBounds: false`). **마커·pill·범례 8행은 그대로다.**
 *   ⚠ **인용 자리 주의**: ④를 채택한 절은 **검증 §64-1**(리더 채택 **M-63**)이고, **§63-2 는 그것이 «개정한»
 *     죽은 절**(*「광화문역을 `fitBounds` 에 넣는다」*)이다. **§63-2 를 ④의 근거로 인용하지 마라.**
 *   ⚠⚠ **그리고 §64-1 이 적은 채택 근거 하나도 죽었다** — *«z15 면 무대2 pill ↔ 화장실북 배지 13.2 px 로
 *     24 px 미달»*. **개발자 오보였고 M-66-2·M-67 이 정정했다**(참값 히트↔히트 **68.0 px** · 위 ⛔ 절).
 *     **④는 아래 «판독» 근거로 선다 — 그것으로 충분하다.**
 *
 * | 담는 것 | 남북 | 초기 줌(320~1280 px) | 코스콤 사각형 |
 * |---|---|---|---|
 * | (가) **+ 광화문역** | 682 m | 320·360·390 은 **z15** | **9.5 × 10.6 px** |
 * | ★ **④ 지금** | **494 m** | **전 뷰포트 z16** | ★ **19.0 × 21.1 px** |
 *
 * ★★★ **채택 근거는 «판독»이다 — «규격»이 아니다**(M-66-2):
 * ```
 * z16 ≈ 1.893 m/px (위도 37.5687)  →  코스콤지부 구역 36 m ≈ 19.0 px
 * z15 ≈ 3.787 m/px                 →                        9.5 px
 * ```
 *   ★★ **그 사각형이 이 지도에서 «유일하게 조합원이 서는 자리»다. 나머지 8행은 전부 참조물이다.**
 *   **맥락 지물 하나를 첫 화면에 담으려고 «행동 대상»을 반으로 줄이지 않는다.**
 *  ⚠⚠ **이것은 회수된 «규격(24 px)» 축과 «다른 축»이다** — 규격 축은 개발자 오보로 지워졌고
 *    (`GWANGHWAMUN_STATION` 주석의 ⛔ 절), **판독 축은 그 정정이 건드리지 않았다.**
 *    **그래서 결론이 살아남았다.** ⚠ 둘을 한 덩어리로 읽고 «④도 같이 죽었다»로 가지 마라.
 *  ★ 함께 서는 근거 하나: ④는 bounds 를 **«좁히는»** 방향이라 **축척이 «같거나 올라간다»** —
 *    위 수치가 계산 예보임에도 **틀려도 (가)보다 나빠질 수 없다.** 그래서 QA 실측을 기다리지 않았다.
 *
 * ⚠⚠⚠ **§63 판정 1-보의 «빼면 범례 8행이 «지도에 없는 것»을 말한다»는 «조건부로만» 유효하다:**
 *   | | 조건 | 상태 |
 *   |---|---|---|
 *   | ① | 범례 8행이 **텍스트로** 위치를 말한다(「무대 1 북쪽입니다」) | **참** |
 *   | ② | 마커가 **닿는다** | ★ **참 — 실측(M-66-4)**: 포인터는 아래로 **120 px 드래그**로 상자 안에 들어온다.
 *     **키보드는 «닿지 못했고»** 그것이 M-66-5 결함이었다 — **포커스 시 지도 이동으로 고쳤다**
 *     (`StrikeMap.tsx` `focusin` effect · 실측 `gwNowInsideBox: true`). **이제 양쪽 다 참이다.** |
 *   **②가 다시 거짓이 되면 ④를 다시 연다.** ⚠⚠ **조건 없이 «은폐 아님»만 적지 마라** —
 *   **그것이 이 프로젝트가 반복한 실패다**(근거가 소멸했는데 판정이 안 고쳐진다).
 *
 * ★ **M-66-6 — «상자를 얼마나 키우면 9개가 다 담기나»**(다음 사람을 위한 실측값 · **이번에 고치지 마라**):
 *   ```
 *   상자 높이 ≥ 453 px 이면 z16 에서 «9개 전부» 초기 화면에 담긴다.
 *     = 남북 682.4 m / 1.8934 m/px = 360.4 px  +  FIT_PADDING(top 48 + bottom 44) = 452.4
 *   현재(360×640) 상자 336 × 420 → 33 px 부족.
 *   ```
 *   ⚠⚠ **종횡비·`FIT_PADDING` 을 건드려 «맞추려» 하지 마라** — **축척 → 서쪽 여유 27 px 사슬**을
 *     사흘 남기고 흔든다. **후속 #30**(M-66-6).
 *   ★ **광화문역 `placement` 건도 같은 후속 #30 이다 — 같은 사슬이라 «한 번에» 다시 잰다**(M-67-4).
 *
 * ★★ **대가 — 브라우저 실측**(360×640 · 스크롤바 0 · dpr 1 · 루트 12px · 상자 336×420 · **z16**):
 *   | 쌍 | 실측 | (예보) |
 *   |---|---|---|
 *   | **전 쌍 최소** | ★ **2.0 px — 겹침 없음** (`toilet-north` [히트] ↔ **무대 2 pill** [장식]) | 2.4 |
 *   | 2~4위 | 7.0 / 10.0 / 20.9 px (전부 히트↔장식) | 6.9 / 9.7 / 21.5 |
 *   | **히트 ↔ 히트 최소** | **68.0 px** (`koscom-area` ↔ `toilet-north`) | 68.2 |
 *   ★ **음수였다면 무대 2 pill 이 가려지고 그것은 «시각»이 아니라 «기능»** 이다(이 지도에서 제일 중요한 라벨).
 *   ⚠⚠ **최근접 상대는 «무대 2 원»이 아니라 «무대 2 pill» 이다**(원으로 재면 3.0 px 로 **더 느슨하게** 나온다).
 *
 *   ★★★ **이 한 쌍에서 «세는 방법이 질문에 안 맞는» 사고가 «두 번» 났다 — 둘 다 «무대 pill 이 없다»로 끝났다:**
 *   | 셈법 | 왜 놓쳤나 |
 *   |---|---|
 *   | `document.querySelectorAll('[data-strike-hit]')` | ★ **히트만** 센다 → 5개. 장식 pill 은 애초에 안 잡힌다 |
 *   | 라벨을 **`span` 으로만** 훑기 | ★★ **무대 pill 은 `<div>` 다**(`pillHtml` 의 장식 분기). 히트 pill 만 `<span role="button">` 이라 **«히트가 아닌 것»이 통째로 빠진다** |
 *   ★ **참값은 «태그 무가정 전수»로만 나온다**: 무대 pill **4개가 실제로 렌더된다**(실측 45~47 × 24 px · 장식).
 *   ⚠ **이 표를 지우지 마라** — 다음 사람이 같은 쿼리로 같은 결론에 또 도달한다.
 *
 * ★ **왜 시청역은 «담고» 광화문역은 «빼나» — 대칭이 목적이 아니다.** **거리가 다르다:**
 *   시청역 **96 m**(368 → 464 m · +26%) · 광화문역 **218 m**(464 → 682 m · **+85%**).
 *   ⚠ **안 적으면 다음 사람이 «일관성»을 이유로 시청역도 빼거나 광화문역을 다시 넣는다.**
 *
 * ⚠ **기각된 세 안 — 되살리지 마라:**
 *   · ❌ **출구 좌표로 교체** — **근거를 세울 수 없다**(`GWANGHWAMUN_STATION` 주석).
 *   · ❌ **종횡비 변경** — §54.16-2 가 **이미 한 번 뒤집은 값**이고 «지도 밖 빈 곳 44 px» 판정선이 매달려 있다.
 *   · ❌ **광화문역 미표시** — **범례 사슬의 북쪽 끝이 끊긴다**(§62-4) · **조합원이 광화문역에서 온다.**
 * ⚠⚠ **`FIT_PADDING` 을 줄여 «맞추려» 하지 마라** — 건드리면 **§54.16-3 의 «서쪽 여유 27 px»가
 *   처음부터 다시 계산돼야 한다.**
 *
 * ⚠ **담기는 항목이 0건이면** `Infinity` 가 나가 지도가 깨지므로 시청역 주변 300 m 로 떨어뜨린다 —
 *   **죽은 분기가 아니다**: 교체 작업 중 항목을 잠시 주석 처리하는 일이 실제로 생긴다.
 *   ★ 판정은 «전체 0건»이 아니라 **«담기는 것 0건»** 이다 — `inFitBounds: false` 를 전부에 붙이면
 *   배열이 비지 않았는데도 범위가 없어진다.
 */
const FITTED_FEATURES = STRIKE_MAP_FEATURES.filter((f) => f.inFitBounds !== false);

export const STRIKE_MAP_FIT_BOUNDS: StrikeBounds =
  FITTED_FEATURES.length === 0
    ? circleBounds(CITYHALL_STATION, 300)
    : FITTED_FEATURES.map(featureBounds).reduce((acc, b) => ({
        south: Math.min(acc.south, b.south),
        west: Math.min(acc.west, b.west),
        north: Math.max(acc.north, b.north),
        east: Math.max(acc.east, b.east),
      }));
