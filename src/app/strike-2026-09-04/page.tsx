import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { SiteHeader } from "@/components/layout/SiteHeader";
import { SiteFooter } from "@/components/layout/SiteFooter";
import { ArrowLeftIcon, ExternalLinkIcon } from "@/components/ui/icons";
import { RallyStatusBadge } from "@/components/rally/RallyStatus";
import { StrikeMap } from "@/components/strike/StrikeMap";
import { strikePhase } from "@/lib/strike";
import { EXTERNAL_LINKS, ROUTES, UNION_ATTENDANCE_DISPLAY_HOST } from "@/lib/routes";

/**
 * 9/4 총파업 참석 안내 (디자인 스펙 §52). **모든 문장은 검증 게이트를 통과한 사실만 담는다.**
 * 근거: 원문 전사 `00_input/strike-20260904/content-strike-20260904.md` · **확정본** 같은 폴더
 * `LEADER-DECISIONS.md`(D-1~D-29) · 검증 `01_verifier_factcheck.md` §51·§52 · 형태 `02_designer_spec.md` §52.
 * ⚠ **확정본 범위를 D-16 까지로 읽지 마라** — 이 페이지가 구현하는 **D-17**·**D-26** 이 그 밖에 있다.
 * ⚠ 최상위 라우트인 이유: 공유된 URL 이 남으므로 `/bargaining-2026` 개편·폐기에
 *   **동반 사망하면 안 된다**(§20.0-4). **하위로 옮기지 마라.**
 *
 * ## ★★ `/rally-2026-08-28` 을 복사해 오지 마라 — 이 페이지는 새로 짠 것이다
 *
 * 복사하면 **8/28 QR 자산이 딸려 온다** — `QrAttendanceCard` **통째로**(주최측 이미지 ·
 * 출석 시각 4개 · `손피켓` · `상품권 5만원` · `참석명단 작성(지부천막)` · `인증샷`·`수기접수`).
 * **그것들은 9/4 근거가 0 이다**(§57-4). 이 페이지는 새로 짠 것이다.
 *
 * ## ⚠⚠ 2026-08-29 — **«URL 도 딸려 온다» 는 죽었다. 나머지는 그대로 살아 있다**(M-26 · 검증 §57)
 *
 * 종전 이 자리는 `EXTERNAL_LINKS.unionAttendance` 를 «딸려 오는 것» 목록에 넣고
 * *«그 URL 은 시각 판정 로직이 살아 있어 9/4 에 누르면 출석에 실패한다»* 라고 적었다.
 * **사용자 확인으로 9/4 QR 이 «같은 주소»를 쓴다**(2급 — 정하는 주체가 본조·주최측이고
 * 사용자는 전달자다 · §57-2) → **그 문장은 거짓이다.** 지금 이 페이지는 **그 URL 을 스스로 쓴다**
 * (아래 QR 출석체크 카드). 함께 죽은 문장: *«"QR 출석체크가 있다"고 적는 것과
 * "8/28 링크를 붙이는 것"은 전혀 다른 일이다»*. **둘 다 인용하지 마라.**
 *
 * ★★ **위험은 «사라진» 것이 아니라 «이동» 했다** — «틀린 링크로 간다» 에서
 *   **«8/28 조건이 함께 온다»** 로(§57-5 #1). 그래서 **주소 하나만** 가져오고 **카드는 안 가져온다.**
 *   ⚠ 이것을 «위험이 사라졌다»로 읽으면 다음 사람이 `손피켓`·`상품권`·`5만원`·`1차·2차`·`지부천막`
 *   **기대 0 까지 함께 풀어 버린다.** 확인된 것은 **«주소 하나»** 뿐이다.
 * ⚠ **게시 조건(§57-2)**: 2급이라 **9/3~9/4 에 도착지를 다시 연다** — 응답 200 ·
 *   `<title>` 이 「금융노조 집회참석」 유지. **«URL 동일성»과 «그 서버가 9/4 에 열린다»는 다르다.**
 *
 * ## ★★★ 코스콤지부 위치 — **2026-09-01 에 «없다»에서 «있다»로 뒤집혔다**
 *
 * ⚠⚠⚠ **종전의 «조건 17(가) — 왜 코스콤지부 위치가 한 줄도 없는가»(D-1)는 «죽었다». 인용하지 마라.**
 *   그 판정의 근거는 ***«사실 자체가 없다»*** 였는데 **v2 자료로 사실이 확인됐다:**
 *   **코스콤지부 = 무대 2 구역**(주최측 자료 **3장이 독립 일치** + 사용자 축자 · **1급** · 검증 §61-4).
 *   ⚠ 함께 죽은 것: 검증 §51-3 의 «블록은 만든다 + C 유형 문면» · 확정 문면이었던
 *     `코스콤지부 대오는 지부 공지로 안내합니다.` · `코스콤지부가 어느 대오인지는 추후 안내합니다.`(§61 A-4).
 *
 * ★★ **지금 그 한 줄은 «지도 `figure` 안 · 캔버스 밖»에 있다**(`strikeMap.ts` `KOSCOM_LOCATION_NOTE`).
 *   **M-3 이 미리 «위치가 정해지면 그 자리에서 문면만 바뀐다»로 정해 뒀고 그대로 이행됐다.**
 *   ⚠ **이 페이지 본문에 «둘째 자리»를 만들지 마라** — 같은 사실이 두 곳이 되면 요구 88(한 출처)·
 *     §5.6(같은 사실을 두 확정도로 말하지 않기) 위반이다. M-3 이 그것을 미리 막았다.
 *
 * ★ **이 페이지의 QR 한 줄과 «같은 상태»로 묶어 읽지 마라.** 그쪽 근거는 **QR `<p>` 바로 위**에
 *   따로 적혀 있다 — **두 근거를 한 덩어리로 합치지 마라**(§52.18-6 · QA 481).
 *   합치면 다음 사람이 «일관성» 을 이유로 **둘 중 하나를 뒤집는다.**
 *
 * ## 의도적으로 **빠져 있는 것** — 자리도 만들지 않았다. **채우지 마라:**
 *
 *  - ⚠⚠ **«코스콤지부 위치»·«천막»은 2026-09-01 에 이 목록에서 «빠졌다»** — 둘 다 지도 `figure` 안
 *    **한 문장**이 진다(위 · `KOSCOM_LOCATION_NOTE`). **여기에 다시 적지 마라.**
 *    **«대오»는 «빠진» 것이 아니라 «없어진» 것이다** — 신판에 개념 자체가 없다(§61 A-1). 기대 `대오` **1**
 *    (**원문 식순 20행**의 `대오정비` 만).
 *    ★★★ **세 처분을 한 덩어리로 읽지 마라 — 셋이 서로 다르다:**
 *    | | 무슨 일이 있었나 |
 *    |---|---|
 *    | **위치** | ★ **생겼다** — 자료 3장 일치 + 사용자 축자(1급) |
 *    | **천막** | ★ **위치가 확인돼 생겼다**(2026-09-01 · 사용자 축자 **「천막도 코리아나호텔 앞」**) |
 *    | **대오** | ★ **사라졌다** — 신판에 개념이 없다 |
 *
 *    ⚠⚠⚠ **`천막` 기대는 «0 이 아니라 «렌더 1»» 이다**(M-69). **0 으로 되돌리지 마라 —
 *      그 한 건이 «사용자가 축자로 준 문장」이라 «기대 0» 을 믿고 지우면 «사용자 지정 문면을 지운다».**
 *      ★ **`추후 안내` 사고와 같은 계보다**(§5.9.3 — 기대 0 으로 적어 둔 문자열을 «나중에 확정된 문면»이
 *      포함해 **정상 문면이 실패로 올라갈 뻔했다**). 이번엔 **더 나쁘다** — 지우는 대상이 사용자 축자다.
 *    ★ **계층을 함께 적는다**(§5.1 — 어느 계층의 기대인가):
 *      **`천막` 렌더 1**(`KOSCOM_LOCATION_NOTE` 안 · 프리렌더 실측 1) · **`참석명단` 0** · **`지부천막` 0**.
 *    ⚠⚠ **`지부천막` 0 은 «유지»다. `천막` 과 한 덩어리로 읽지 마라** — 그것은 **8/28 낱말**이고
 *      **9/4 근거는 여전히 0** 이다(v3 계수 0 · 「코스콤 천막」·「천막」이 v3 표기다).
 *      ⛔ **종전 ««천막에서 무엇을 하는가»는 여전히 근거 0» 은 죽었다**[근거 소멸 2026-09-01 · v3] —
 *        v3 `10:30~11:00` 행이 그것을 답한다. 기대: **`참석명단` 1 · `물품 수령` 1 · `명단 작성` 2.**
 *        ★ **`지부천막` 0 «만» 남는다 — 한 덩어리로 풀지 마라.**
 *      ★★ **위험은 또 «이동»했다** — «유추가 틀린다» → **«유추가 대체로 맞는데 세부 6축이 갈린다».**
 *        **8/28 문장(「참석명단 작성(지부천막)」·「투쟁용품 수령」)을 옮기는 금지는 그대로다.**
 *    ⚠ **M-58 · §62-7(«천막 미게시»)의 근거는 «사용자 확인으로 소멸했다»** — 판정은 뒤집혔다.
 *      **인용을 지우지 않고 남기는 이유**: 지우면 *«왜 천막을 오래 안 썼지»* 가 다음 사람의 일이 된다.
 *  - **「4. 준비사항」 전체** — 조직 준비 분담표라 조합원 대상이 아니다(D-2)
 *  - **「1. 목표 및 요구사항」** — 사이트 `DEMANDS` 와 문면이 갈려 **같은 사실이 두 벌**이 된다.
 *    요구안은 임단협 페이지가 말한다(D-3)
 *  - ★★ **셋의 처지가 «갈렸다». 한 줄로 읽지 마라**(2026-09-01 v3):
 *    · **QR 시각** — ⛔ **근거 0 이 죽었다.** v3 가 시작·종료 두 창을 준다 → **일정 블록이 축자로 게시한다**
 *    · **상품권** — ⭕ **여전히 0.** 근거가 «자료가 없다»에서 **«v3 가 배포물·지급을 «말하는데» 그 목록에
 *      없다»**로 **강해졌다.** ⚠ 그러나 「자료에 없다」를 **「지급이 없다」로 적지 마라**(부재는 신호가 아니다)
 *    · **(8/28) 지급 조건** — ⭕ **금지 유지 · 오히려 더 필요해졌다.** 이 저장소에서 `지급 조건` 은
 *      **8/28 「상품권 5만원 지급 조건」의 고유 지시어**인데 v3 는 **성분이 전부 다르다**
 *      (천막 `코스콤 천막`≠지부천막 · QR `시작/종료`≠1차·2차 · 대가 `조합비를 통한 급여지급`≠상품권).
 *      → **기존 금지는 그대로 두고 «9/4 지급 근거」는 v3 ※ 축자가 «별건»으로 진다**
 *    ⚠ **«링크·방법» 은 2026-08-29 에 이 목록에서 빠졌다**(M-26) — 주소가 확인돼 **아래 카드**가 됐다.
 *    ★ **나머지를 함께 빼지 마라.** «주소가 같다» ≠ «조건이 같다» — 시각·상품권·지급 조건은
 *      **여전히 9/4 근거가 0** 이다(§57-4).
 *  - **`위치서비스를 미리 켜 두세요`** — 9/4 출석이 GPS 기반인지 **근거가 0**(§6.4 는 8/28 자료).
 *    틀리면 조합원이 엉뚱한 준비를 한다
 *  - **주최측 지도 이미지 및 그 파생 사실** — 원본 파일 0건(D-5)
 *  - **정식명칭 줄** — 원문 제목의 「(안)」이 최상단에서 확정도 충돌을 일으킨다.
 *    **결의대회의 «정식명칭 1회 노출» 규칙을 복사하지 마라**(D-7)
 *  - **`past` 상태 문장** — 배지 `완료` 가 상태를 말한다(§52.3)
 *  - **참석 예비조사 배너** — **아직 없다**(검증 조건 14)
 *
 * ## ★★★ 블록 순서 — **「집결시간」이 「개요」 위다. 원문 절 순서와 «반대»이고, 그것이 판정이다**
 *   (리더 D-26 · 디자이너 §52.21)
 *
 * ⚠⚠ **«세로 예산 때문»으로 읽지 마라 — 세로는 «결과»이지 «이유»가 아니다**(그렇게 읽으면 «넉넉해지면
 *   되돌리자»로 간다). 진짜 이유는 §3(정보 위계 = «조합원이 해야 할 행동» 기준)을 **블록 «안»의 행
 *   순서에 이어 블록 순서에도 일관되게 적용한 것**이다(D-14 → D-26). **원문 문자열 변화는 0 이다.**
 * ⚠ **대가**: «어디»가 «몇 시» 뒤로 간다. 완화 3가지 — ① `<h1>` 이 행사를 말한다 ② 홈 배너 부제가
 *   둘을 붙였다 ③ 개요가 바로 다음 블록이다. ★ **셋 중 하나라도 사라지면 순서를 재판정하라.**
 * ⚠ **여백을 깎아서 번 것이 아니다** — 여백·카드 합치기·대형 수치 축소·복귀 링크 제거는
 *   **전부 기각된 처방**이다(§52.21-5).
 *
 * ## ★★ 세로 예산 — 블록을 더하기 전에 읽어라 (D-15 · §52.12 · §52.21). 판정선 **360×640**.
 *
 * **실측값·측정 환경표·삽입 지점 A 증분은 `02_designer_spec.md` §52.21 에 있다** — 값·방법·기준은
 * 한 벌이라 조건 없이 인용하지 마라(`union-qa-testing` §5.7).
 * ⚠⚠ **디자이너 §52 «초판» 예측을 인용하지 마라 — 짧았다**(§52.21-3). ★ **규율: 런타임 주입
 *   프로토타입으로 «세로 예산»을 확정하지 마라** — 가로 넘침과 달리 **세로는 모든 간격 클래스가
 *   살아 있어야 참값이 나온다.**
 * ⚠ **여유가 생겼다고 «비었으니 채우자»로 가지 마라. 얇은 것이 설계값이다**(§52.15-2).
 * ⚠ 예비 수단(개요 `dl` 컨테이너 쿼리 · §52.21-4)을 **지금 도입하지 마라** — 컨테이너 쿼리 전례가
 *   0 이고 QA 측정 규율도 없다.
 *
 * 추후 콘텐츠의 «주소»(플레이스홀더가 아니다):
 *   A. `<h1>` 아래·첫 `<section>` 위 → 참석 예비조사 배너 (**세로 예산 재측정 필수**)
 *   B. **[2026-08-29 채워짐]** 개요 다음·QR 줄 앞 → **`세종대로 안내지도` 섹션**(`StrikeMap`).
 *      ⚠ 순서 교체 전 표현(«집결시간 다음»)은 **죽었다**(§52.21-2) — 코스콤 위치는 «전체 장소»보다
 *        세부라 **일반 → 특수** 순서를 지켜야 한다.
 *   ⚠ **C(«B 섹션 안 → 지도 이미지»)는 죽었다. 인용하지 마라**(M-4·M-17) — 주최측 원본은 바탕이
 *     상용 지도 캡처라 미게시로 확정됐고, 그 자리를 **네이버 지도 API 블록**이 대체했다.
 *
 * ISR revalidate 60초: 상태를 요청 시점에 계산하므로 정적 프리렌더를 쓰지 않는다.
 */
export const revalidate = 60; // strikePhase() 가 렌더 시점 계산이므로 rally 와 같은 값을 승계한다

/*
 * 메타데이터 — **스펙 §52.18-7 확정 문면. 글자 단위로 이대로 쓴다. 개발자가 짓는 자리가 아니다** —
 * 조각별 출처가 전부 승인분이고 **신규 저작 0** 이다(D-8 · 사이트 공통 접미 · 원문 「2. 개요」 축자 · D-10).
 * ⚠ 연결자 `·` 와 마침표만 우리가 넣은 것이고 **그 둘도 검증 대상이다 — 임의로 손대지 마라**(§52.19-4).
 *   ★ `·` 이 없으면 `11시 세종대로` 로 붙어 읽혀 **없는 지명처럼 보인다.**
 * ⚠ `description` 은 **안 보이지만 검색결과·메신저 미리보기로 나간다** — 문안 게이트 대상이다.
 * ⚠ `description` 의 `세종대로 (광화문역, 시청역)` 은 **원문 축자**다.
 *   배너 표기(`세종대로(광화문역·시청역)`)로 맞추지 마라 — **D-20 이 닫았다.**
 */
export const metadata: Metadata = {
  title: "9/4(금) 총파업 참석안내 — 전국금융산업노동조합 코스콤(한국증권전산)지부",
  description:
    "2026년 9월 4일(금) 11시 · 세종대로 (광화문역, 시청역). 전 조합원 집결은 10시 30분입니다.",
};

/*
 * 네이버 지도 Client ID — `NEXT_PUBLIC_*` 은 **빌드타임에 번들로 임베드**된다
 * (`deploy/web/docker-compose.yml` build.args → Dockerfile ARG/ENV).
 *
 * ★★ **미설정이면 `<section>` 을 통째로 렌더하지 않는다**(M-6 · M-16) — 인증 401 이면
 * `window.naver.maps` 내부가 null 이라 정리 경로가 throw 하고 **passive unmount effect 의 예외를
 * React 가 회복하지 못해 트리 전체가 날아간다.** 이 조건부가 **이미 게시된 문자 정보를 지킨다.**
 * ⚠ **죽은 어포던스를 만들지 마라** — 키가 없을 때 «헤딩 + 영구 실패 박스»만 남기지 않는다.
 * ⚠⚠ 이때 **코스콤지부 «위치» 한 줄도 함께 사라진다 — «의도된 상태»다.**
 *   ★ **근거가 2026-09-01 에 바뀌었다**: 종전은 «그 문장이 막는 위험(대오 4개 중 하나를 임의로 고름)이
 *   지도에서만 생긴다» 였는데 **그 위험이 없어졌다.** 지금 한 쌍인 것은
 *   **«지도가 무대 2 를 가리킨다»와 «그 무대 2 가 우리다»** 다 — 지도 없이 문장만 남으면 조합원이
 *   **무대 2 가 어디인지 모른 채 «무대 2」라는 말만** 갖는다. **지도 밖으로 빼지 마라.**
 */
const NAVER_MAP_CLIENT_ID = process.env.NEXT_PUBLIC_NAVER_MAP_CLIENT_ID ?? "";

if (NAVER_MAP_CLIENT_ID === "") {
  console.warn(
    "[strike] NEXT_PUBLIC_NAVER_MAP_CLIENT_ID 미설정 — 9/4 참석 안내 페이지의 지도 섹션을 렌더하지 않습니다.",
  );
}

/** `SectionHeading` 은 **`id` 를 직접 받는다** — 결의대회 페이지의 `<div id="…">` 우회를 승계하지 않는다.
 *  랜드마크 이름 규칙: 화면에 헤딩이 있으므로 `aria-labelledby`(`union-webapp-dev` §8) */
function SectionHeading({ id, children }: { id: string; children: React.ReactNode }) {
  return (
    <h2 id={id} className="text-h2 text-ink md:text-h1">
      {children}
    </h2>
  );
}

/** 시각 문자열의 **구분자 뒤**에 줄바꿈 기회를 준다 — 숫자·콜론·하이픈·물결에는 **줄바꿈 기회가
 *  자체적으로 없다**(`union-design-system` §0.8 셋째 문자 종류).
 *  ★★ **구분자는 «두 종류»다**(2026-09-01 신판) — 대부분 `-` 인데 **`13:00~13:15`·`13:15~13:30`
 *    두 행만 `~`** 다. 이 함수는 **어느 쪽이든 찾아서 그 뒤에 `<wbr>` 을 넣을 뿐**이고 문자 자체는
 *    손대지 않는다. ⚠ **여기서(또는 데이터에서) 구분자를 통일하지 마라** — 원문이 섞어 쓴다
 *    (§51-7 원문 보존 · 검증 §61-5 (2) · 게시 조건 45).
 *  ⚠ **`<wbr>` 은 `textContent` 에는 안 남지만(축자 보존 ✓) 프리렌더 HTML 에서는 갈린다** —
 *  게이트가 HTML 에 대고 `10:30-11:00` 을 grep 하면 **0건**이다. **태그를 제거하고 세라**
 *  (`grep -c` 금지 — `grep -o | wc -l`).
 *  ⚠ **`RallySchedule` 의 같은 헬퍼를 import 하지 마라** — 8/28 전용이라 지역 헬퍼로 다시 쓴다 */
function TimeText({ value }: { value: string }) {
  const i = value.search(/[-~]/);
  // `15:15` 단독 행 — **구분자를 붙이지 마라**(검증 조건 8). 원문은 폐회선언 시각만 말한다
  if (i < 0) return <>{value}</>;
  return (
    <>
      {value.slice(0, i + 1)}
      <wbr />
      {value.slice(i + 1)}
    </>
  );
}

interface ProgramRow {
  /** 원문 표기 그대로. ★★ **구분자를 통일하지 마라** — 20행 중 **`13:00~13:15`·`13:15~13:30`
   *  두 행만 `~`** 이고 나머지는 `-` 다(원문 축자 · §51-7 · 검증 §61-5 (2) · 게시 조건 45) */
  time: string;
  content: string;
}

/** 원문 「5. 총파업 식순」 20행 축자 — **2026-09-01 신판**(`design/총파업_식순.jpg`)으로 전면 교체.
 *  구판 값(폐회 `14:20` · 연대사→투쟁사 순 · 응원이벤트 없음)은 **주최측 신판이 들어온 순간 거짓**이
 *  됐다(검증 §61 A-5 · 게시 조건 45).
 *
 * ★★★ **「비고」를 되살리지 마라.** 신판 원본의 비고란은 **20행 전부 공란**이다(검증자가 원본
 *   직접 판독 · §61-1 · A-6). v1 에 있던 `윤석구 금융노조 위원장`·`김동명 한국노총 위원장 외` 는
 *   **근거가 «두 겹»으로 없다** — 인명도, 소속도 신판에 없다.
 *   ⚠ **«전에 있었으니 유지»가 바로 §5.8.2 가 막은 «구판에 기댄 판정»이다.**
 *   ⚠⚠ **`/rally-2026-08-28` 의 `RallySchedule` 에 같은 두 이름이 남아 있는 것은 «정당한 출현»이다**
 *     — 그쪽 근거는 **8/28 원문의 비고란**이다. **여기서 지웠다고 그쪽까지 지우지 마라**(§5.8.3).
 *
 * ★ **비고 «열»은 이 표에 원래 없다** — D-29 가 이미 3열 → 2열로 정했고, 비고는 내용 셀의
 *   **둘째 줄**이었다. 이번에 사라진 것은 그 둘째 줄이다. **3열로 되돌리지 마라**(200% 에서 내용
 *   열이 한글 1자 폭으로 눌린다 · 디자이너 실측). */
const PROGRAM: readonly ProgramRow[] = [
  { time: "10:30-11:00", content: "대오정비" },
  /* ★★★ **원문은 «두 줄»이다** — `사전집회` 아래에서 **줄바꿈**이고, 그 사이에 ` / ` 가 **없다**
     (원본 `design/총파업_식순.jpg` 직접 판독 · 개발자 재확인). 원문은 `사전집회` 를 **그 시간대의 «이름»**
     으로 세우고 둘째 줄이 내용을 편다 — **한 줄로 이으면 셋이 «대등한 3항 나열»이 되어 위계가 평평해진다.**
     ⚠⚠ **오염 경로를 적어 둔다**: M-53·§61-5 가 *「**두 줄**: `사전집회` / `구호제창 / …`」* 로 적었는데
       그 `/` 는 **«1줄 / 2줄»을 가르는 «우리 표기»** 였다. 세 단계를 거치며 **원문의 일부로 굳었다.**
       ★ **「한 글자도 바꾸지 마라」는 지시도 «원문»이 아니라 «지시 문면»일 수 있다 — 원본을 열어야 갈린다.**
     ⛔ **`상영/경과보고` 의 공백 없음은 «원문 그대로»다. 넣지 마라.**
     ⛔ **3열로 되돌리지 마라 · 「비고」를 되살리지 마라**(신판 비고란은 20행 전부 공란).
     ★ `\n` 은 이 행 «하나»뿐이다 — 나머지 19행은 거동이 바뀌지 않는다 */
  { time: "11:00-11:30", content: "사전집회\n구호제창 / 동영상 상영/경과보고" },
  { time: "11:30-11:35", content: "지도부 및 내외빈 입장" },
  { time: "11:35-11:40", content: "깃발 입장" },
  { time: "11:40-11:45", content: "노동의례" },
  { time: "11:45-11:55", content: "참가조직 소개" },
  { time: "11:55-12:05", content: "총파업 선언 및 대회사" },
  { time: "12:05-12:10", content: "구호제창" },
  { time: "12:10-12:30", content: "격려사" },
  { time: "12:30-12:50", content: "문화공연 1" },
  { time: "12:50-13:00", content: "투쟁사" },
  { time: "13:00~13:15", content: "응원이벤트" }, // ★ 구분자가 `~` — 원문 그대로. `-` 로 고치지 마라
  { time: "13:15~13:30", content: "연대사" }, // ★ 구분자가 `~` — 원문 그대로. `-` 로 고치지 마라
  { time: "13:30-13:50", content: "문화공연 2" },
  { time: "13:50-14:00", content: "투쟁사" },
  { time: "14:00-14:50", content: "문화공연 3" },
  { time: "14:50-15:00", content: "상징의식" },
  { time: "15:00-15:10", content: "결의문 낭독" },
  { time: "15:10-15:15", content: "파업가 제창" },
  { time: "15:15", content: "폐회선언" }, // ★ 종료 시각이 아니라 «폐회선언 시각»이다. 범위로 펴지 마라
];

/** 원문 하단 단서 — **마침표까지 축자다.** 「조정될 수 있습니다」로 문체를 고치지 마라.
 *  **표와 같은 화면에 없으면 게시 불가**다(검증 조건 2 · §51-1) — 표가 길어 표 위(`<caption>`) +
 *  표 아래(`<p>`) 2곳에 둔다(§52.7-4).
 *  ⚠⚠ **`※` 계약이 개정됐다 — 종류 1·출현 2 → «종류 2 · 출현 3»**(게시 조건 27 개정 · 2026-09-01 v3):
 *    ① `※ 세부 프로그램은 조정될 수 있음.` — 원문 식순 단서 · `<caption>` + 표 아래 `<p>` = **2회**
 *    ② `※ 출석부 서명 및 …(조합비를 통한 급여지급의 근거)` — **v3 원문 단서 · 1회**(`ATTENDANCE_NOTE`)
 *  ⛔ **네 번째 `※` 를 만들지 마라.** ⚠⚠ **둘 다 «원문 축자»다 — 우리가 짓는 `※` 는 여전히 0 이다.**
 *  ★ 종전 «하나뿐» 계약이 성립한 유일한 이유는 **원문 `※` 가 하나뿐이었기 때문**이고 **그 전제가 깨졌다** */
const CHANGE_NOTE = "※ 세부 프로그램은 조정될 수 있음.";

/**
 * 「3. 1차 총파업 참가 조합원 상세일정」 — **지부 자체 자료(v3) 표 6행 축자.**
 * 정본: `_workspace/00_input/strike-20260904/V3-PLAN.md` · 전사 `…/content-strike-20260904-v3-member-schedule.md`
 *
 * ★★★ **출처가 식순 표와 «다르다». 두 표를 같은 규칙으로 다루지 마라:**
 *   · **식순(`PROGRAM`)** = **주최측(금융노조) 전사** — 한 글자도 못 고친다.
 *   · **상세일정(여기)** = **지부 자체 자료** — 사용자 지시가 오면 고친다.
 * ⚠⚠ **구분자를 통일하지 마라** — 식순은 `-` 17 · `~` 2 · 무구분자 1 이고 **여기는 6행 전부 `~`(U+007E)** 다.
 *   **출처가 다르다**(§51-7 · 게시 조건 45). ★ 전사본에서 **바이트 그대로 추출**했다.
 * ⚠ 셀 안 `\n` 은 **원문의 줄바꿈**이다(`whitespace-pre-line` 이 살린다). `- ` 머리표도 **원문 표기**다.
 * ⛔ **`금노` 를 `금융노조` 로 고치지 마라 — 원문 훼손이다.** 우리가 «짓는» 문장에서만 `금융노조` 를 쓴다.
 * ⛔ **`코스콤지역` 을 `코스콤지부` 로 고치지 마라** — 원문 표기이고 8/28 원문도 같다(지부 관용 · 오타 아님).
 * ⚠ **원본 이미지 축자 대조를 «못 했다»**(**게시 조건 V3-63** — v3 원본이 `design/` 에 없다). «통과»가 아니다.
 *   ⚠⚠ **«조건 63» 으로 쓰지 마라 — 그 번호가 «두 계약»을 가리켰다:**
 *     ① **식순 묶음**(`11:00-11:30` 두 줄) — **해소됨** · ② **v3 원본 축자 대조** — **열려 있음(= 이것)**.
 *   → 열린 쪽에 **`V3-63`** 이라는 «이름»이 붙었다. ⛔ **번호를 재배정하지 마라** — 세 문서가 각각의 63 을 인용한다.
 */
interface MemberScheduleRow {
  time: string;
  detail: string;
  /** 원문 「비고」 열. ★ **6행 전부 차 있다** — v2 식순의 비고(20행 전부 공란)와 **정반대**다.
   *  ⚠ **열은 접어도 문자열은 한 글자도 지우지 마라**(§5-1 «순서·열·묶음» 판정) */
  note: string;
}

const MEMBER_SCHEDULE: readonly MemberScheduleRow[] = [
  {
    time: "08:40~10:30",
    /* ★ `여의도 출근없이` — **1급 사실**(사용자 확인 축자: *「여의도 출근없이는 회사와 이야기된 사항이야.」*).
       ⛔ **「근무 주장이라 위험하다」로 다시 회부하지 마라 — 그 회부는 죽었다.**
       ⚠ 이 페이지에서 `여의도` 는 **근무지**를 뜻한다. `/rally-2026-08-28` 에서는 **집회장소**(렌더 8)라
         **뜻이 다르다** — 금지·기대 목록을 **«페이지별»로** 적어라(게시 조건 68).
       ⚠ **10:30 은 「전 조합원 집결 10시 30분」과 정확히 맞물린다** — 집결시간 블록·metadata·홈 배너
         세 곳은 **전부 유효하다. 08:40 을 보고 그것들을 고치지 마라.** */
    detail: "여의도 출근없이 오전 집결장소로 이동",
    /* ⛔ 이 출구 번호를 **개요 「장소」 줄·metadata·지도 pill·범례에 넣지 마라** — 출처가 다르고
       (개요는 v2 「2. 개요」 축자) 우리 지도 핀은 **OSM 역 «노드»이지 출구가 아니다.**
       핀에 「6번 출구」를 붙이면 **그 핀이 6번 출구라는 주장**이 되어 거짓이다 */
    note: "세종대로 코리아나호텔 앞\n(광화문역 6번 출구, 시청역 3번 출구 인근)",
  },
  {
    time: "10:30~11:00",
    detail:
      "코스콤 천막 방문 후 집회구역 이동\n- 참석명단 작성\n- 투쟁용품 수령 (투쟁조끼, 등산방석, 손피켓 등)\n- 점심간식 수령 (서브웨이 샌드위치, 음료)",
    /* ⛔⛔ **8/28 `RallySchedule` 의 같은 자리 문장을 옮기지 마라 — 6축이 갈린다:**
       시각 18:30~18:50↔10:30~11:00 · 배포물 `총파업머리띠,손피켓,우의`↔`투쟁조끼,등산방석,손피켓` ·
       간식 저녁↔점심 · 장소 `(3구역)`↔`(천막에서 …)` · QR 괄호 8/28 에만 있음 · **반납은 정반대**
       (8/28 은 사용자 지시로 «반납 없음», v3 는 «반납 후 귀가»).
       ★ 특히 8/28 은 «투쟁조끼»를 «총파업머리띠»로 **바꾼** 줄인데 v3 는 **«투쟁조끼»로 돌아온다.** */
    note: "- 집회장소 내 코스콤지역\n(천막에서 명단 작성 및 물품 수령)\n- 금노 QR출석코드 인증\n(수령한 손피켓 또는 조합사이트 링크)",
  },
  { time: "11:00~11:30", detail: "시작 출석체크", note: "금융노조 QR코드" },
  {
    time: "11:30~15:15",
    detail: "총파업 행사",
    /* ⚠ `행진이 생략됩니다` 는 «원래 있었다»를 전제하는 부정문이고 **우리 페이지는 그 기대를 세운 적이 없다**
       (사이트 전체 `행진` 0). **원문 칸 안에 그대로 두고 풀어 쓰거나 강조하지 마라.** */
    note: "금노진행\n- 이번 총파업에는 행진이 생략됩니다.",
  },
  /* ★★ **종료 출석체크가 식순 「15:15 폐회선언」 «전»에 닫힌다** — 폐회선언을 기다렸다 하면 늦는다.
     그래서 시작·종료 두 행은 **함께** 게시한다. ⚠ `11:30~15:15` 와 겹치는 것은 **원문 그대로**다. 고치지 마라 */
  { time: "15:00~15:15", detail: "종료 출석체크", note: "금융노조 QR코드" },
  /* ⚠ 「자유투쟁」이 **무엇을 하는 것인지 원문이 말하지 않는다 — 풀어 쓰지 마라.**
     ⚠ **반납 «장소·방법»도 원문에 없다** — 8/28 의 「집회장소에서 반납」을 옮기지 마라 */
  { time: "15:15~17:40", detail: "자유투쟁 실시", note: "투쟁용품 반납 후 귀가" },
];

/**
 * v3 표 아래 단서 — **원문 축자(노란 형광 강조).** `※` 계약의 **② 종류**다(위 `CHANGE_NOTE` 주석).
 *
 * ⛔⛔ **괄호 축자 그대로가 아니면 게시 불가**(게시 조건 64). 원문은 「급여지급의 **근거**」라고만 하고
 *   **금액·시점·대상·방법을 말하지 않는다.** 「출석체크를 하면 급여가 지급됩니다」로 풀어 쓰면 **없는 확정을 만든다.**
 * ⛔ **`급여` 단독으로 줄이지 마라** — 「**조합비를 통한** 급여지급」은 **노조가 조합비로** 지급하는 것이라
 *   줄이면 **회사 임금으로 읽힌다.**
 * ★ 게시하면 얻는 것: 비어 있던 «9/4 지급 근거» 자리가 8/28 의 `식대·교통비(상품권 5만원)` 유추를 부르는데
 *   이 축자가 그 자리를 **채워서 막는다.** ⛔ 그렇다고 8/28 지급 조건 문장을 옮기지는 마라 — 성분이 전부 다르다.
 */
const ATTENDANCE_NOTE =
  "※ 출석부 서명 및 금융노조 QR코드를 통한 출석체크를 반드시 부탁드립니다. (조합비를 통한 급여지급의 근거)";

export default function StrikePage() {
  // 하드코딩 0 — 렌더 시점 계산이다
  const phase = strikePhase();

  return (
    <>
      <SiteHeader asHeading={false} />
      <main className="flex-1">
        <div className="mx-auto mt-8 w-full max-w-page px-4 md:mt-14 md:px-8">
          {/* 상단 복귀 링크 — 하단과 같은 문자열 2회는 허용 패턴이다(§20.3.7).
              ⚠ `aria-label`·`title` 을 붙이지 마라 — 내부 텍스트가 접근성 이름을 진다(§8) */}
          <p>
            <Link
              href={ROUTES.bargaining}
              className="inline-flex min-h-touch items-center text-caption font-semibold text-primary hover:underline focus-visible:outline-3 focus-visible:outline-primary focus-visible:outline-offset-2"
            >
              <span aria-hidden="true">←&nbsp;</span>
              26년 임단협 투쟁 안내로 돌아가기
            </Link>
          </p>

          {/* 상태 배지 — today/past 에서만. upcoming 은 아무 상태도 말하지 않는 것이 정상 상태다 */}
          {phase !== "upcoming" ? (
            <p className="mt-4">
              <RallyStatusBadge phase={phase} />
            </p>
          ) : null}

          {/* 리더 확정 문면(D-8) — **한 글자도 고치지 않는다**. 취소선 금지.
              `break-words` 는 필수다 — `9/4(금)` 이 숫자·괄호·기호라 끊을 자리가 없다(§0.8) */}
          <h1
            className={`text-title break-keep break-words text-ink md:text-display ${phase === "upcoming" ? "mt-4" : "mt-3"}`}
          >
            9/4(금) 총파업 참석안내
          </h1>

          {/* ── 블록 1 — 집결시간 (원문 「3. 집결시간」 축자) · **페이지 유일 대형 수치** ── */}
          <section aria-labelledby="gather-heading" className="mt-section md:mt-section-lg">
            <SectionHeading id="gather-heading">집결시간</SectionHeading>
            <div className="rounded-panel shadow-card mt-6 bg-bg p-5 md:p-8">
              {/*
                ★ **행 순서가 원문과 반대다 — 승인된 역전이다**(리더 D-14). 두 사실이 둘 다 온전히
                게시되고 대상 명칭이 각 행에 축자로 붙으며, 정보 위계는 «조합원이 해야 할 행동» 기준이다(§3).
                ⚠ **간부 행을 빼지 마라** — 순서만 바뀌는 것이지 내용이 주는 것이 아니다.
                ⚠⚠ **D-14 의 조건 문면 «대상 명칭이 시각과 «같은 줄»에서 갈리게 하라» 는 죽었다**
                  (D-17 · §52.20-0). **인용하지 마라 — 조판 수단이 없다.** 그 목적(간부가 `10시 30분` 을
                  자기 시각으로 오독하는 것 방지)은 **대상 캡션이 대형 수치 «위»에 오는 것**으로 달성된다.
                ★ **개요 블록과 합치지 마라** — 한 카드에서 `11시` 와 `10시 30분` 이 나란히 서면
                  *"11시까지 가면 되나"* 오독이 페이지 안에서 재현된다(D-10).
              */}
              <p className="text-caption font-semibold text-ink-muted">전 조합원</p>
              {/*
                **`10시 30분` 이 이 페이지의 유일한 대형 수치다.**
                ⚠ **`10:30` 으로 바꾸지 마라** — 원문 「3. 집결시간」 축자다(배너의 `10:30` 은 사용자 지정 문면).
                ⚠ **`sr-only` 장형(`오전 10시 30분`)을 만들지 마라** — `10시 30분` 은 이미 한국어라
                  병기는 **신규 문자열 창작**이 된다.
              */}
              <p className="font-display mt-1 text-hero leading-none break-keep break-words text-ink md:text-hero-lg">
                <time dateTime="2026-09-04T10:30:00+09:00">10시 30분</time>
              </p>

              <dl className="mt-6 grid gap-y-1 md:mt-8 md:grid-cols-[auto_1fr] md:gap-x-4 md:gap-y-2">
                <dt className="break-keep break-words text-caption font-semibold text-ink-muted">
                  지부 위원장 및 전체상임간부
                </dt>
                <dd className="break-keep break-words text-body text-ink">10시</dd>
              </dl>
            </div>
          </section>

          {/* ── 블록 2 — 개요 (원문 「2. 개요」 축자) ── */}
          <section aria-labelledby="overview-heading" className="mt-section md:mt-section-lg">
            <SectionHeading id="overview-heading">개요</SectionHeading>
            <div className="rounded-panel shadow-card mt-6 bg-bg p-5 md:p-8">
              {/*
                ★★ **`grid-cols-[auto_1fr]` 을 모바일에도 주지 마라**(디자이너 실측 · §52.2) —
                `break-words` 는 **min-content 기여값을 줄이지 않아** `1fr` 트랙이 원래 폭을 그대로 잡고,
                200% 에서 카드가 넘쳐 **문서가 가로로 밀린다.** → 모바일 1열 스택, 2열은 `md:` 에서만.
                ⚠ **`break-all`·`overflow-wrap:anywhere` 로 우회하지 마라** — 레이아웃을 고치는 쪽이 회귀가 작다.
                ⚠ **`md:` 는 텍스트 확대와 무관하다** — 미디어 쿼리의 `rem` 은 루트 `font-size` 를 따르지 않고
                  초기 16px 고정이다. 데스크톱 2열의 200% 안전은 **따로 쟀다**(§52.2).
                간격 비율 참값 — **항목 간 15px : 라벨↔값 3px = 5:1**(`row-gap 3 + dt mt-4 12 = 15px`).
                ⚠ **클래스 이름의 숫자(`mt-4`·`gap-y-1`)를 간격 비율로 적지 마라 — «4:1» 은 오답이다**
                  (§52.21-3 · QA 491). «클래스 비율»은 화면 값이 아니고,
                  **`row-gap` 은 «6행 사이 5곳 전부»에 붙는다.**
                라벨 3개(`일시`·`장소`·`참석대상`)는 **원문 축자**다. 우리가 지은 이름이 아니다.
              */}
              <dl className="grid gap-y-1 md:grid-cols-[auto_1fr] md:gap-x-4 md:gap-y-2">
                <dt className="break-keep break-words text-caption font-semibold text-ink-muted">
                  일시
                </dt>
                <dd className="break-keep break-words text-body text-ink">
                  <time dateTime="2026-09-04T11:00:00+09:00">2026년 9월 4일(금) 11시</time>
                </dd>

                {/* ⚠ **배너 표기(`세종대로(광화문역·시청역)`)로 맞추지 마라 — 게시 조건이다**(§52-6).
                    배너는 사용자 확정 요약 표기, 여기는 **원문 축자**이고 두 표기는 한 화면에 없다
                    (배너=홈 · 개요=이 페이지). 공백·쉼표까지 원문 그대로다. */}
                <dt className="mt-4 break-keep break-words text-caption font-semibold text-ink-muted md:mt-0">
                  장소
                </dt>
                <dd className="break-keep break-words text-body text-ink">
                  세종대로 (광화문역, 시청역)
                </dd>

                <dt className="mt-4 break-keep break-words text-caption font-semibold text-ink-muted md:mt-0">
                  참석대상
                </dt>
                <dd className="break-keep break-words text-body text-ink">금융노조 전 조합원</dd>
              </dl>
            </div>
          </section>

          {/*
            ── 삽입 지점 B — 세종대로 안내지도 (2026-08-29 · M-1~M-21 · 디자인 §54·§54.16) ──

            ★ **자리 근거**: 개요의 `장소` 행이 세운 「세종대로」를 이 지도가 **세부로 받는다**
              (일반 → 특수). **첫 화면 판정선(`360×640`)에는 영향이 0 이다** — 이 자리가 판정선 아래다.
              ⚠ 그래도 **QA 가 삽입 «후»에 다시 재라**(`today` 를 반드시 함께).
            ⚠ **헤딩 문면 `세종대로 안내지도` 를 좁히지 마라**(M-10) — 이 지도는 **무대 4 · 코스콤지부 구역 ·
              임시화장실 2 · 역 2** 를 함께 그린다. «집결 위치»·«코스콤 위치» 류로 바꾸면 **범위를 좁혀 거짓이 된다.**
              ⚠⚠ **코스콤 구역이 들어왔다고 «코스콤지부 위치» 로 바꾸지 마라** — 나머지 8개가 이름에서 사라진다.
            ⚠ **이 섹션 «밖»에 지도 관련 문장을 만들지 마라** — 완화 안내·확신도 키·코스콤 한 줄·범례가
              전부 `figure` 안에 있고 그 자리는 `StrikeMap` 이 정본이다.
          */}
          {NAVER_MAP_CLIENT_ID !== "" ? (
            <section aria-labelledby="map-heading" className="mt-section md:mt-section-lg">
              <SectionHeading id="map-heading">세종대로 안내지도</SectionHeading>
              <StrikeMap clientId={NAVER_MAP_CLIENT_ID} />
            </section>
          ) : null}


          {/* ── 블록 3 — 총파업 일정 (원문 「5. 총파업 식순」) ──
              ★ **섹션 이름이 「총파업 식순」 → 「총파업 일정」으로 개명됐다**(사용자 지시 2026-09-01).
                괄호 안은 **원문 절 제목**이라 보존한다. `id="program-heading"` 는 **영문이라 개명이 안 닿는다** — 유지.
              ⛔ **`/rally-2026-08-28` 의 `식순` 5곳(렌더 3건)을 함께 바꾸지 마라** — 그쪽은 **원문 축자 + 게시 조건**이다.
                `sed -i 's/식순/일정/g' src/` 를 돌리면 **21곳이 바뀌고 그중 3건이 8/28 렌더**다.
              ★★ **한 섹션 · 두 패널**이다(§5-1). 두 표는 **출처가 다르다** — 그것을 화면에서 갈라야 해서 `<h3>` 를 둘 둔다.
              ⛔ **두 `<section>` 으로 쪼개지 마라** — 「총파업 일정」과 「총파업 식순」이 나란히 서서 **개명의 뜻이 사라진다.** */}
          <section aria-labelledby="program-heading" className="mt-section md:mt-section-lg">
            <SectionHeading id="program-heading">총파업 일정</SectionHeading>

            {/* ── 패널 A — 조합원 상세일정 (지부 자체 자료 v3) ──
                ★★ **1열 스택(`<dl>`)이다. 표가 아니다.** 판정선 실측(360 · 루트 32px = 200% · 카드 슬롯 201px):
                  스택 **2,640px / 7.75자·줄** ↔ 2열 표 3,418px/4.74자 ↔ **3열 표 5,170px/2.37자**.
                ★ 차이를 만드는 것은 «열 수»가 아니라 **`w-[78px]` 시간 열**이다 — px 고정이라 확대를 안 따라가고
                  200% 에서 슬롯의 **38.8%**(320 기기 48.4%)를 먹는다. 식순은 셀이 짧아 버텼지만
                  **상세일정은 최장 62자**라 같은 손실이 곧바로 3배 줄 수가 된다.
                  → **처방은 «열을 줄이는 것»이 아니라 «시간 열을 열에서 빼는 것»이다.**
                ⛔ **3열 표로 되돌리지 마라** — **기본 배율에서는 «완벽히 통과»한다**(457.5px · 넘침 0). 그것이 함정이다.
                ⛔ `overflow-x-auto` 금지(스택은 넘침 0) · **접기·탭·아코디언 금지**(§0.4 — 접히는 것이 **행동 지시**다).
                ⚠ **비고 «열»은 사라져도 «문자열»은 한 글자도 안 지웠다** — 6행 전부 차 있다(v2 식순은 20행 전부 공란이라 정반대).
                  ★ **원문 구조 변경은 «열» 1건뿐**이다(순서·묶음 변경 0). `grep` 은 이것을 세지 않으니 판정 기록에 남긴다. */}
            <div className="rounded-panel shadow-card mt-6 bg-bg p-5 md:p-8">
              {/* 문면 확정: 「1차」 제외 · **「참가」 한정은 반드시 유지**(사용자 답 Q1).
                  ⚠ 한정을 떼면 **전 조합원 대상 근무 지시**가 되어 불참 조합원·필수유지업무 지정자까지 포함된다. */}
              <h3 className="break-keep break-words text-lead text-ink">총파업 참가 조합원 상세일정</h3>
              <dl className="mt-4 flex flex-col">
                {MEMBER_SCHEDULE.map((row) => (
                  <div key={row.time} className="border-t border-border-soft py-3 first:border-t-0 first:pt-0">
                    {/* `<wbr>` 은 **이 페이지 지역 헬퍼**를 쓴다. ⛔ `RallySchedule` 의 같은 헬퍼를 import 하지 마라 */}
                    <dt className="break-keep break-words text-caption font-semibold text-ink">
                      <TimeText value={row.time} />
                    </dt>
                    {/* `whitespace-pre-line` — **셀 안 원문 줄바꿈**을 지킨다.
                        ⛔ `pre`·`pre-wrap` 금지(JSX 들여쓰기가 화면에 샌다) */}
                    <dd className="mt-1 whitespace-pre-line break-keep break-words text-caption text-ink">
                      {row.detail}
                    </dd>
                    {/* 원문 「비고」. `ink-muted` 는 **위계 표시**이지 «덜 중요하다»가 아니다 —
                        집결 장소·출구 · QR 경로 · 반납이 전부 여기 있다. ⛔ 흐리다고 지우지 마라 */}
                    <dd className="mt-1 whitespace-pre-line break-keep break-words text-caption text-ink-muted">
                      {row.note}
                    </dd>
                  </div>
                ))}
              </dl>
              <p className="mt-4 break-keep break-words text-caption text-ink">{ATTENDANCE_NOTE}</p>
              {/*
                ★★★ **검증 §72 확정 문면(V-a·V-b) — 축자다.** 사용자 답 Q3·Q4 가 밝힌 «관계»를 옮긴다.
                  자리는 **표 «밖» · 표 «아래»** 이고 **원문 묶음(`<dl>` → ※) 뒤**다 — 우리 문장을 원문 사이에 끼우지 않는다.
                ★ **왜 필요한가**: 조합원이 `10:30~11:00` 비고의 `금노 QR출석코드 인증` 을 읽고 **10:40 에 QR 을 누르면
                  시간창 밖이라 실패한다** → 자기가 잘못한 줄 알거나 출석을 포기한다. ※(급여지급 근거)가 걸려 있다.
                  또는 **«두 번 해야 하는 줄»** 알고 11:00 에 한 번 더 찾는다.
                ★★ **핵심은 「한 번만」이 아니라 「같은 것」이다** — 「한 번만」은 **10:40 실패를 못 막는다**
                  (그 한 번을 10:40 에 할 수 있다). ⛔ **두 문장의 순서를 바꾸지 마라.**
                ⛔ **한 문장으로 합치지 마라** — 네 낱말이 한 문장에 들어가고 **하나가 틀리면 둘 다 무너진다.**
                ⛔ **「위 두 곳」 류 지시어 금지** — 자리 의존이고 스크린리더에서 「위」가 불명확하다.
                ⛔ **시각을 쓰지 마라.** 요구 88 말고 **이유가 하나 더 있다**: 원문은 두 시간대에 걸쳐 적었고
                  사용자는 **「같은 것」까지만** 말했다. 「11:00~11:30 에」라고 쓰면 **원문보다 정밀한 주장**이다.
                ⛔ **`지부천막` 을 쓰지 마라**(8/28 낱말 · 기대 0 유지). **「천막에서」는 사용자 축자**다.
              */}
              <p className="mt-4 break-keep break-words text-caption text-ink">
                금노 QR출석코드 인증과 시작 출석체크는 같은 것입니다. 한 번만 하면 됩니다. 참석명단
                작성과 출석부 서명은 같은 것입니다. 천막에서 한 번만 하면 됩니다.
              </p>
            </div>

            {/* ── 패널 B — 금융노조 공식 순서 (주최측 전사) ──
                ⛔⛔ **아래 표를 한 글자도 건드리지 마라.** 회귀 기준값: 200% 높이 **4,755px** · 열 78/123 ·
                  최대 셀 5줄 · 넘침 0. ★ 열 머리 문면은 **8/28 이 쓴 「금융노조 공식 순서」 축자**다. */}
            <div className="rounded-panel shadow-card mt-6 bg-bg p-5 md:p-8">
              <h3 className="mb-4 break-keep break-words text-lead text-ink">금융노조 공식 순서</h3>
              {/*
                ★ **표는 2열이다(시간 / 내용). 비고는 내용 셀 둘째 줄.**
                ⚠ **3열로 만들지 마라** — 200% 에서 내용 열이 **한글 1자 폭**으로 눌린다(디자이너 실측).
                  **`RallySchedule` 의 3열을 근거로 밀지도 마라**: 그쪽 셋째 열은 «전 행에 걸친 다른
                  출처»였고 이번 비고는 **같은 출처의 부속 정보 2건**이다.
                ⚠ **`overflow-x-auto` 스크롤 상자를 만들지 마라** — 2열은 담긴다(실측 넘침 0).
                  필요 없는 스크롤 상자는 **«옆에 더 있다»는 거짓 단서**를 준다.
                ⚠ **20행을 접지 마라**(«더 보기»·아코디언 금지 · §0.4 콘텐츠 은폐 금지).
              */}
              <table className="w-full table-fixed">
                <caption className="mb-3 break-keep break-words text-left text-caption text-ink">
                  {CHANGE_NOTE}
                </caption>
                <thead>
                  <tr>
                    <th
                      scope="col"
                      className="w-[78px] pb-2 text-left text-caption font-semibold text-ink-muted md:w-[128px]"
                    >
                      시간
                    </th>
                    <th
                      scope="col"
                      className="pb-2 text-left text-caption font-semibold text-ink-muted"
                    >
                      내용
                    </th>
                  </tr>
                </thead>
                <tbody>
                  {PROGRAM.map((row) => (
                    <tr key={row.time}>
                      {/*
                        ★★ **시간 셀의 `break-words` 를 빼지 마라**(디자이너 실측 · §52.7-3) — 200% 에서
                        `<wbr>` 이 만든 최장 덩어리가 셀 폭을 넘겨 **내용 열 위로 겹쳐 찍힌다.**
                        ⚠ **`w-[78px]` 을 키워서 고치려 하지 마라** — px 고정이라 확대를 따라가지 않고,
                          200% 를 덮을 만큼 키우면 내용 열이 사라진다.
                          **처방은 «셀 안에서 접히게 하는 것» 하나뿐이다.**
                      */}
                      <td className="border-t border-border-soft py-3 pr-1.5 align-top break-keep break-words text-caption text-ink">
                        <TimeText value={row.time} />
                      </td>
                      {/* ⚠ **둘째 줄(구 「비고」)을 되살리지 마라** — 신판 비고란은 20행 전부
                          공란이다(`PROGRAM` 주석 · 검증 §61 A-6).
                          ★★ **`whitespace-pre-line` 은 «원문 줄바꿈»을 지키기 위한 것이다**(`11:00-11:30` 한 행) —
                            `PROGRAM` 의 `\n` 이 여기서 줄로 나온다. ⚠ **빼면 그 행이 한 줄로 이어져
                            «대등한 3항 나열»이 되고 원문 위계가 무너진다**(`PROGRAM` 주석).
                          ★ `pre-line` 은 **연속 공백을 접고 줄바꿈만 살린다** — JSX 들여쓰기가 새 나가지 않는다.
                            ⚠ `pre`·`pre-wrap` 으로 바꾸지 마라(그것들은 공백을 보존해 들여쓰기가 화면에 나온다).
                          ★ `break-keep break-words` 와 함께 선다 — 200% 판정선은 **셀 최장 덩어리**가 정하는데
                            이 변경은 그 덩어리를 **짧게** 만드는 방향이라 재측정이 필요 없다 */}
                      <td className="border-t border-border-soft py-3 align-top whitespace-pre-line break-keep break-words text-caption text-ink">
                        {row.content}
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
              <p className="mt-4 break-keep break-words text-caption text-ink">{CHANGE_NOTE}</p>
            </div>
          </section>

          {/*
            ── 블록 — 원본 자료 (사진 3장 · 사용자 지시 2026-09-01) ──
            사용자 축자: *「첨부한 원문 중 총파업_식순 제외하고는 원사진을 모두 페이지에 올려줘.」*

            ★★★ **자리 근거 — 왜 «지도 섹션 밖»인가**(개발자 발견 · 리더 확정):
              바로 위 지도 섹션은 **`NAVER_MAP_CLIENT_ID` 조건부**라 **키가 없으면 통째로 사라진다.**
              그런데 이 사진들의 가치 하나가 **«네이버 API 가 죽어도 남는다»** 이다.
              → **섹션 «안»에 두면 그 가치가 0 이 된다.** 밖에 두었기에 **지도가 죽었을 때의 위치 안내**가 된다.
              ⛔ **위 조건부 «안»으로 옮기지 마라 — 그 순간 이 근거가 통째로 죽는다.**

            ⚠ **헤딩 문면 `원본 자료` 는 «사용자 확정 축자»다.** ⛔ 「주최측 자료」 류로 바꾸지 마라 —
              **배치도는 금융노조 것이고 지도·로드뷰는 «지부가 표시를 얹은» 것**이라 거짓이 된다.
            ⚠ 캡션 3개도 **사용자 확정 축자**다. ⛔ **출처 배지·꼬리표·`※` 로 출처를 되살리지 마라**
              — 8/28 에서 사용자가 걷어낸 바로 그것이다.
            ★★★ **자리 = 「총파업 일정」 «뒤»**(검증 §74 확정 · 2026-09-01). 개선폭 실측:
              **일정 섹션 시작 3,498px → 1,862px** — 행동 정보가 **1.9 화면** 당겨진다(4.1 → 2.2 화면).
              ⚠ 첫 화면 판정선(`360×640`)에는 **영향 0**.

            ⛔⛔ **«암묵 근거»를 미리 죽여 둔다** — 다음 사람이 스스로 만들 수 있는 논리다:
              *「지도와 사진이 «같은 대상»이니 붙어 있어야 대조가 된다」*.
              **대조는 «붙어 있어야» 성립하지 않는다.** 조합원이 지도를 보고 사진으로 내려가 대조하는 행동은
              **«가정»이지 «관측»이 아니고**, 한다 해도 **행동을 가르지 않는다**(§5.3).
              반면 **«일정을 못 읽는 것»은 행동을 가른다.** → **이 순서로 되돌리지 마라.**

            ★★ **세 장을 «묶음»으로 두는 근거 — 감각이 아니라 «판별기»다.**
              8/28 이 「출처 증거」 판정의 **숨은 전제**를 명시로 적어 뒀다: *「같은 정보를 지는 «다른 도구»가
              페이지에 있다」*. 셋 다 그 전제가 선다 →
                배치도 ↔ `KOSCOM_LOCATION_NOTE` + 범례 2·3행 / 지도 ↔ 네이버 지도 + 범례 9행 /
                로드뷰 ↔ 거리뷰 «기능» + 범례 3행.
              **성격이 안 갈리므로 가르지 않는다.**

            ⚠⚠ **로드뷰만 조건이 하나 붙는다 — 두 문장을 «한 벌»로 읽어라:**
              ① 거리뷰 «기능»은 **`NAVER_MAP_CLIENT_ID` 조건부**라 키가 없으면 그 전제가 깨지고
                 **로드뷰 사진이 «유일한 시각 경로»가 된다.**
              ② **그래도 자리를 가르지 않는다** — 키가 없어도 사진은 남고 **스크롤로 닿는다.**
                 **§0.4 는 «숨김»을 막지 «아래»를 막지 않는다.**
              ⛔ 한 문장만 읽고 *「401 이면 유일 경로인데?」* 로 되돌리지 마라.

            ⚠ 헤딩 문면 **`원본 자료`** 는 §5.3 «우리 작업 사정 금지»에 **안 걸린다**(그건 「검증 중」 류다).
              ⛔ **8/28 캡션 「주최측 배치도」를 옮기지 마라** — **로드뷰는 «지부»가 네이버 캡처에 그린 것**이라
                «주최측»으로 묶으면 **거짓**이 된다.
            ⛔ **`TOILET_NOTE`(화장실 문장)를 여기로 함께 옮기지 마라** — 그 자리 근거는
              **«범례 6·7행을 읽은 뒤»** 이지 «사진 옆»이 아니다. **지도 섹션에 남는다.**

            ⚠ **압축 판정이 3장 각각 다르다. 통일하지 마라**(8/28 이 «일부러» 갈라 놓은 것과 같은 계보).
              `quality` 는 **`[75, 90]` 만 허용**된다(`next.config.ts`) — **목록 밖 값(92 등)은 400 → 조용히 75** 다.
              `sizes` **두 항 다 «실측»이다. 어느 쪽도 관용 기본값이 아니다:**
                · **데스크톱 624px** = 패널 672 − `p-8` 24×2. ⛔ 8/28 의 832px 복사 금지(33% 과대 요청).
                · ★★ **모바일 `calc(100vw - 54px)`** — ⛔ **`100vw` 로 되돌리지 마라.**
                  **54 는 임의 숫자가 아니라 «패딩 사슬»이다:**
                    루트 **12px**(`html{font-size:75%}` — 이 사이트의 특수 조건)
                      컨테이너 `px-4` = 1rem    → 16px 이 «아니라» **12px**
                      패널     `p-5`  = 1.25rem → 20px 이 «아니라» **15px**
                      사슬 = (12 + 15) × 2 = **54px**   ← 16px 를 가정하면 **72 로 틀리게** 나온다
                  실측 검산: 320→266 · 360→306 · **390→336** — **세 폭 전부 일치.**
                  ⚠⚠ **`100vw` 였을 때의 대가**: 390·dpr3 에서 390×3=1170 → **`w=1200`** 을 받아
                    첫 방문 **391.4 → 451.1 KB(+61.1 · +15.7%)**. **집회 현장 모바일 데이터다**(§0.6).
                  ⛔⛔ **`4.5rem` 으로 바꾸지 마라 — 한 번 그렇게 했다가 «회귀»로 되돌렸다**(QA §75 실측).
                    ★★★ **기전**: **`sizes` 의 폰트 상대 단위는 «초기» `font-size`(16px)로 푼다** —
                      **프리로드 스캐너가 «스타일 적용 전»에 평가**하므로 이 페이지의 `html{font-size:75%}` 를
                      **못 본다.** → **`4.5rem` 은 54px 이 아니라 «72px»** 로 계산된다.
                    **관측(두 모델 다 6조건과 일치 — 즉 «rem 이 72px 로 평가된다»가 참이다):**
                      · `320`/dpr3 — 슬롯 266 · `54px` **828** ⭕ ↔ `4.5rem` 750 ❌ **48px 부족(6.0%)**
                      · `390`/dpr2 — 슬롯 336 · `54px` **750** ⭕ ↔ `4.5rem` 640 ❌ **32px 부족(4.8%)**
                      · 나머지 4조건은 동일.
                    ⚠⚠ **«전송량이 더 줄어 좋다»가 아니다** — 이 두 장은 **판독 대상이 걸린 이미지**이고
                      (`화장실 : … 스벅 등) 이용` · `PAUL BASSETT` 간판) **판독 생존 확인이 `w=1080` 기준**이었다.
                      **과소 요청은 그 확인을 무효로 만든다.** 최악 조건 전송량은 **두 판본이 391.4KB 로 같다.**
                    ★★ **«rem 이면 글자 크기 슬라이더를 따라간다»는 전제 자체가 성립하지 않는다** —
                      `sizes` 는 어차피 **초기값으로 얼어 있어 둘 다 슬라이더를 못 따라간다.**
                      `rem` 은 **따라가지도 않으면서 «기본 배율에서» 틀린** 쪽이다.
                    ⚠ 슬라이더로 루트를 키우면 **레이아웃 슬롯은 «실제로» 준다**(실측 루트 18px → 306 → 279).
                      **그러나 `sizes` 에는 그것을 표현할 수단이 없다.**
                      ★ **레이아웃 관측을 `sizes` 판정에 옮기지 마라 — 두 값이 «다른 시점»에 평가된다.**
          */}
          <section aria-labelledby="source-heading" className="mt-section md:mt-section-lg">
            <SectionHeading id="source-heading">원본 자료</SectionHeading>
            <div className="rounded-panel shadow-card mt-6 bg-bg p-5 md:p-8">
              {/* ① 지부배치도 — **문자 표**라 성질이 QR 쪽이다.
                  ⚠⚠ **`unoptimized` 는 «의도»이고 아래 둘과 «반대 판정»이다** — 재인코딩에 경계선이 무너진다.
                    **PNG 를 JPEG 로 바꾸지 마라.** `quality`·`sizes` 를 일부러 비웠다(`unoptimized` 가 둘 다 무시한다).
                    실측: 무손실 PNG **46KB** < WebP **98KB** — **작고 무손실이라 두 축 다 이긴다.**
                  ⚠ 재압축은 `png({compressionLevel:9, palette:false})` 만 무손실이다 — **`effort:10` 은 팔레트
                    경로를 켜 «조용히 손실»이 된다.** 이 파일은 raw 버퍼 비교로 **픽셀 동일**을 확인했다.
                  ⚠ **`unoptimized` 를 빼면 «확대용 링크·버튼·안내 문장 0» 판정이 함께 무너진다** — 핀치 확대가
                    원본 733px 에 닿는 것이 그 근거다. **둘은 함께 움직인다.**
                  ★★★ **`alt` 는 검증 §73 확정본이다. 한 글자도 바꾸지 마라.**
                    ⛔ 이 이미지 안에는 **`메인무대`(기대 0) · `6,000명`·`천막 8`(M-53 미게시)** 가 있다.
                      **`alt` 에 옮기지 마라 — 금지어가 «속성 계층»으로 렌더된다.**
                    ★★ **북단 무대를 «이름으로 부르지 않는» 것이 판정이다 — «불완전해 보인다»고 채우지 마라:**
                      · `메인무대` → 기대 0 위반 + ★ **비시각 사용자«만» «현장 표지판과 다른 이름»을 얻는다**
                        (시각 사용자는 범례·pill·본문이 전부 `무대 1` 이라 정정되는데 **`alt` 만 듣는 사용자는
                        정정 경로가 없다**)
                      · `무대 1` → **이 이미지에 «없는 글자»**
                      → **셋째 길**: 그 대상의 «이름»은 **행동 정보가 아니다**(행동 정보는 «코스콤이 어느 구역인가»).
                    ⚠⚠ **아래 지도 이미지는 처분이 «갈린다»** — 거기는 **원본 라벨이 `무대 1`** 이라 **쓴다.**
                      **두 `alt` 를 통일하지 마라.**
                    ⚠ **좌측 세로열(천막 목록)은 말하지 않았다** — 약칭뿐이고 위치 정보가 없다.
                      ★ **정직하게 적는다: 시각 사용자는 그 열을 본다 — «완전 등가»가 아니다.** */}
              <figure className="mb-6">
                <Image
                  src="/images/strike-2026-09-04/branch-layout.png"
                  width={733}
                  height={2083}
                  unoptimized
                  alt="지부를 무대 구역별로 나눈 표입니다. 위에서부터 네 구역이 이어지고, 코스콤(한국증권전산)지부는 위에서 둘째인 무대 2 구역에 있습니다."
                  className="rounded-badge border border-border-soft block h-auto w-full"
                />
                <figcaption className="mt-3 break-keep break-words text-caption text-ink">
                  지부배치도
                </figcaption>
              </figure>

              {/* ② 집회 장소 — 바탕이 지도 타일이라 **최적화가 이긴다**(실측 PNG 898KB → WebP **178KB**).
                  ★ **우리 인터랙티브 지도 좌표의 «원본»이다.** ⚠ **폭 상한을 두지 마라** — 안에 읽을 문자가 있다.
                  ★★★ **`alt` 는 검증 §73 확정본이다.**
                    ⛔ **`오른쪽`·`왼쪽` 을 넣지 마라** — ★ **비시각 사용자에게 «화면의 오른쪽»이라는 개념이 없어**
                      낭독되는 순간 **«지상 방위»로 읽힌다** → `오른쪽 차로`·`동측`·`서측` **기대 0 위반**
                      (게시 조건 49·53 — *「카메라가 어느 차로에 있었는지 모른다」*).
                    ⚠ **「남쪽」은 «쓴다»** — 지도에서 방위는 확정적이다(실측 코스콤 = 무대 2 남 **51 m**).
                      ⛔ **「아래」로 바꾸지 마라 — 그건 «화면 참조»다.**
                    ⚠⚠ **상자 두 줄(천막·화장실)을 `alt` 에 넣지 않았다. 근거가 «두 벌 금지»가 «아니다»** —
                      그러면 «문장을 빼고 `alt` 에 넣자»가 성립한다. **참 근거: 그 경로가 «이미지»가 아니라
                      «문장»이라 비시각 사용자에게 «직접» 닿는다.**
                      ⚠⚠⚠ **조건부다** — **`TOILET_NOTE`(범례 아래 문장)가 실제로 게시되어야 이 판정이 선다.**
                        **빼면 `alt` 가 유일 경로가 되어 이 판정이 다시 열린다.**
                    ⛔ **`코스콤지역` 을 이 이미지의 출처로 적지 마라** — 그 낱말은 **이 지도 상자에 없다**
                      (v3 상세일정 «비고»와 8/28 `RallySchedule` 에 있다 · 리더 오기 정정). */}
              <figure className="mb-6">
                <Image
                  src="/images/strike-2026-09-04/location-map.png"
                  width={1300}
                  height={1179}
                  quality={90}
                  sizes="(min-width: 768px) 624px, calc(100vw - 54px)"
                  alt="세종대로를 따라 무대 1부터 무대 4까지와 임시화장실 두 곳이 표시된 지도입니다. 코스콤 로고에서 나온 화살표가 무대 2 남쪽 사각형을 가리킵니다."
                  className="rounded-badge border border-border-soft block h-auto w-full"
                />
                <figcaption className="mt-3 break-keep break-words text-caption text-ink">
                  집회 장소
                </figcaption>
              </figure>

              {/* ③ 코스콤지부 구역 — **사진이라 손실 압축이 적합**하다(3.8MB → 1600폭 q90 4:4:4 **456KB** → WebP 166KB).
                  ★★ **우리 인터랙티브 거리뷰가 «못 하는 것»을 한다** — **차도 위에 구역이 그려져 있다.**
                    우리 거리뷰는 파노라마만 띄우고 도형을 못 얹는다. **중복이 아니라 보완이다.**
                  ⚠ **개인정보**: 인도에 사람·차량이 있으나 **네이버 로드뷰가 이미 처리한 공개 자료**이고 우리가
                    흐림을 «해제»한 것이 없다. ⛔ **크롭·확대로 개인을 «키우지» 마라.**
                  ★★★ **`alt` 는 검증 §73 확정본이다.**
                    ⛔⛔ **세로 라벨을 «축자로 인용»하지 마라** — 원본이 **세로쓰기**(`코스콤 / 지부구 / 역`)라
                      **공백을 결정할 수 없다.** 그래서 이 `alt` 는 라벨을 **축자 인용하지 «않는다».**
                      **누가 그것을 축자로 인용하려 하면 막아라.**
                    ⛔ `오른쪽`·`왼쪽` 금지는 위 지도와 같다. ⛔ `2026년 04월` 도 넣지 마라(기대 0 · 네이버 UI 값). */}
              <figure>
                <Image
                  src="/images/strike-2026-09-04/location-roadview.jpg"
                  width={1600}
                  height={926}
                  quality={90}
                  sizes="(min-width: 768px) 624px, calc(100vw - 54px)"
                  alt="세종대로를 북쪽으로 바라본 거리 사진입니다. 차도 위에 코스콤지부 구역이 사각형으로 표시돼 있고, 건물 1층에 폴바셋 간판이 보입니다."
                  className="rounded-badge border border-border-soft block h-auto w-full"
                />
                <figcaption className="mt-3 break-keep break-words text-caption text-ink">
                  코스콤지부 구역
                </figcaption>
              </figure>
            </div>
          </section>

          {/*
            QR 출석체크 — **사실 한 줄**(리더 D-4 개정 · 검증 §52-4 확정 문면).

            ## ★★★ 조건 17(나) — 왜 **이 한 줄만** 있는가

            **사실이 «있다».** 지부 당사자 진술이라 **«있다»가 1급 확정**이고, 사용자 답변도
            *"9/4 당일에도 있다"* 였다 — **알려 주라는 답**이다.
            ⚠⚠ **종전 «없는 것은 세부(방법·시각·URL)뿐이다» 는 죽었다 — 셋 다 채워졌다.**
              **«무엇이 언제»가 갈린다**: **방법·URL 은 2026-08-29**(M-26 — 아래 카드가 그것이다) ·
              **시각은 2026-09-01**(v3 상세일정). ★ 이 줄이 «M-26 이후 미수정»으로 남아 있던 자리다.
            ⚠ **이 절을 지우지 마라** — 지우면 *«왜 QR 을 오래 한 줄로만 뒀지»* 가 다음 사람의 일이 된다.
              절의 나머지(위험 이동 · 형태 조건 4건 · 자리 근거)는 **전부 유효**하다.

            침묵했을 때 조합원이 하는 일이 문제다: **출석체크를 안 찾거나, 8/28 방식을 유추한다.**

            ⚠⚠ **2026-08-29 개정(M-26 · 검증 §57-5 #1)** — 종전 이 자리는
            *«유추가 틀리면 **출석에 실패한다** — 그래서 침묵이 새 위험을 만든다»* 였다.
            **틀렸다. 유추는 «맞았다»** — 9/4 QR 이 같은 주소를 쓴다(사용자 확인 · 2급).
            ★ **그런데도 침묵은 여전히 새 위험을 만든다. 이유가 «바뀐» 것이다:**
            조합원이 8/28 페이지에서 주소를 찾아낼 때 **`1차·2차`(8/28 시각) · `상품권 5만원`(8/28 조건) ·
            `참석명단 작성(지부천막)`(8/28 동선) · `손피켓`(8/28 배포물)이 «함께» 딸려 온다.**
            ★★ **위험은 «소멸»이 아니라 «이동» 했다.** 그래서 처분이 «침묵»이 아니라
            **«주소만 우리가 준다»**(아래 카드)로 정해졌다.
            ⚠ 이것을 «위험이 사라졌다»로 읽지 마라 — 그러면 위 네 항목의 **기대 0 까지 함께 풀린다**(§57-4).

            ★ **파일 머리의 «코스콤지부 위치는 왜 없는가»(조건 17(가))와 «같은 상태»로 묶어 읽지 마라.**
              그쪽은 **사실 자체가 없고**, 여기는 **사실은 있고 세부가 없다.** 처분이 다른 것이 옳다.
              ⚠ **두 근거를 한 덩어리로 합치지 마라**(§52.18-6 · QA 481) — 합치면 다음 사람이
              «일관성» 을 이유로 **둘 중 하나를 뒤집는다.**

            ★ 형태 조건 4건(D-10) — **전부 여기서 이행된다:**
             1. **`※` 를 붙이지 마라** — 사실 진술이지 단서가 아니다. 이 페이지의 `※` 는 원문 단서 1종뿐이다
             2. ⚠ **개정됐다(M-26 · 검증 §57-3 (3))**. 종전: *«링크·필 버튼·카드로 만들지 마라 —
                `<p>` 하나이고 면·테두리·그림자·`href` 가 0 이다»*. **그 근거(«누를 곳이 없다»)가
                소멸했다** — 주소가 확인돼 **바로 아래 카드가 누를 곳이다.**
                ★ **살아남은 것 3가지**: ① **필 버튼으로 만들지 마라**(필 버튼은 «페이지 «안» 조작»의
                형태다 · §0.7 — 외부 이동은 **카드**) ② **`※` 로 달지 마라**
                ③ **본문 한 줄과 카드를 갈라 놓지 마라 — 한 벌이다**
             3. **원문 축자 블록 «안»에 넣지 마라** — `<section>` 3개 어디에도 안 들어간다(컨테이너 직속).
                위·아래 여백이 대칭이라 **어느 블록에도 소속되지 않는다**
             4. **두 문장을 분리하지 마라** — 단일 텍스트 노드 1개다. `<br>`·`<span>` 으로 쪼개면 grep 이 깨진다

            ⚠⚠ **§52-2 는 «절반»만 살아 있다**(M-26 · §57-5 #2). 문면이
              `방법과 시각은 추후 안내합니다` → **`인증 시각은 추후 안내합니다`** 로 바뀌었다.
               - **«시각은 추후»** — ⛔ **죽었다**[근거 소멸 2026-09-01 · v3]. 종전 *«9/4 출석 시각은
                 여전히 미정이다(§57-6 #2)»* 는 **그 자체로 거짓 사실 주장**이 됐다 — v3 가
                 `11:00~11:30 시작 출석체크` · `15:00~15:15 종료 출석체크` 를 준다.
                 → 문면은 **게시 조건 28** 대로 **«그 자리에서»** 바뀌었다(둘째 문장 삭제).
                 ⚠ **줄을 지우거나 새 자리를 만들지 마라** — **앞 문장은 여전히 참이고 필수다.**
               - **«방법은 추후»** — **죽었다.** 방법(=주소)을 안다. **아래 카드가 그 방법이다.**
              ★ 종전 근거(*«8/28 참석자가 그때 방식을 유추하고, 유추가 틀리면 출석에 실패한다»*)는
                **틀렸다 — 유추는 맞았다**(§57-5 #1). 막을 것은 유추가 아니라
                **«유추와 함께 딸려 오는 8/28 조건»** 이다.
              ⚠ **`인증 시각은` 을 `시각은` 으로 줄이지 마라** — 카드가 붙어 있어 «카드를 누르는 시각»
                으로도 읽힌다(§57-3 (1)).
            ⚠ 금지어(요구 157 A 유형으로 미끄러진다): `확인 중` · `확인되는 대로` · `반영하겠습니다` ·
              `준비 중` · `업데이트 예정`. 그리고 **`지부 공지로` 를 쓰지 마라** — 사용자 축자가
              *"추후 추가할게"* 라 **이 페이지에 추가될 가능성이 높은데** 그 표현은 조합원을 다른 채널로 보낸다.

            자리 근거: 읽는 순서가 집결시간(내가 몇 시) → 개요(무슨 행사) → **여기**(그날 출석체크가 있다)
            → 일정 이다. 최상단에 두지 않는 이유는 **이 줄에 지금 할 수 있는 행동이 없어서**이고,
            일정 뒤에 두지 않는 이유는 **표가 길어 그 아래가 안 읽혀서**다.
            ⚠ 종전 수치 `806px` 은 **낡은 파생값**이다(현행 2열 실측 783.1px) — **판정 방향은 유지되고
              상세일정 스택이 붙어 «더 강해졌다».** 수치는 재측정 대상이라 문장에서 뺐다.
            ★ 자리 근거도 다시 쓴다: 종전 «이 줄에 지금 할 수 있는 행동이 없어서» 는 **죽었다**
              (시각이 확정돼 카드가 «당일 도구»가 됐다). 남는 근거는 **«표 뒤에 두면 안 읽힌다»** 다.
            ★ **카드가 붙어도 이 자리 판정은 그대로다** — 카드가 준 것은 «누를 곳»이지 «지금 할 수 있는
              행동»이 아니다. 도착지는 **당일 지정 시각에만** 체크를 받는다.
          */}
          {/* ★★ **게시 조건 28 «발동»**(2026-09-01 v3) — *「9/4 시각이 안내되면 이 한 줄이 «그 자리에서»
              바뀐다 — 새 자리를 만들지 마라」*. 둘째 문장 `인증 시각은 추후 안내합니다.` 를 **삭제**했다.
              시각이 확정됐으므로 그 문장은 **답이 있는데 «없다»고 말하는** 상태가 됐다.
              ★ **첫 문장은 참이고 오히려 강해진다 — 2회(시작·종료)다. 지우지 마라.**
              ⛔ **시각을 여기에 다시 쓰지 마라** — 아래 「총파업 일정」이 축자로 진다(요구 88 · 한 출처).
              ⛔ `<br>`·`<span>` 분할 금지(단일 텍스트 노드 계약) · 새 자리 금지 · 필 버튼 금지 · `※` 금지.
              ⚠ **아래 카드의 `당일 지정된 시각에만 체크됩니다.` 는 그대로 «참»이다 — 함께 고치지 마라.**
                표가 그 «지정된 시각»의 «값»을 준다. **«규칙 + 그 값»이지 «같은 사실의 두 확정도»가 아니다.** */}
          <p className="mt-section break-keep break-words text-body text-ink md:mt-section-lg">
            당일 QR 출석체크가 있습니다.
          </p>

          {/*
            ★★ **QR 출석체크 카드**(리더 M-26 · 검증 §57-3 (2) — **한 글자도 고치지 마라**).

            ## 왜 «넣는다» 인가 — 근거는 «편익»이 아니라 **«대안이 더 나쁘다»** 다

            M-24 로 `/rally-2026-08-28` 이 `/bargaining-2026` 에서 **한 번의 클릭 거리**가 됐다.
            그래서 **«링크를 안 넣는다»가 링크를 «막는» 것이 아니다** — 조합원은 8/28 카드에서
            같은 주소를 찾아내고, 그때 **`1차·2차`(8/28 시각) · `상품권 5만원`(8/28 조건) ·
            `참석명단 작성(지부천막)`(8/28 동선) · `손피켓`(8/28 배포물)** 을 **한 덩어리로** 가져간다.
            → **«안 넣는다»는 링크에 «8/28 조건을 묶어서» 주는 것이다**(§57-1).

            ## ⚠ 옮겨 오면 안 되는 것 — **확인된 것은 «주소 하나»뿐이다**

            **8/28 카드의 설명 3줄을 옮기지 마라** — *"손피켓이 없어도 이 링크로 인증할 수 있습니다."* ·
            *"손피켓의 QR을 찍으면 나오는 출석체크 페이지와 같습니다. 지정된 출석 시간에만 체크됩니다."*
            **전부 손피켓 전제**이고 **9/4 배포물은 미확인**이다(§57-6 #3).
            ⚠ **`손피켓` 금지** · ⚠ **`바로하기` 금지** — 8/28 카드 제목이 `손피켓 QR인증 바로하기` 다.
              **두 페이지의 라벨이 갈려야** «한 문자열 두 목적지» 충돌이 없다(§20.0-3 계열).
            ⚠ **`지정된 출석 시간` 을 쓰지 마라** — 9/4 는 시각이 미정이라 **그 확정 명사를 못 쓴다.**
              그래서 `당일 지정된 시각에만` 이다.

            ## 문면 근거

            | 줄 | 근거 |
            |---|---|
            | `금융노조 QR 출석체크` | 도착지 `<title>` 「**금융노조** 집회참석」과 낱말이 겹쳐 **«맞게 왔다»가 확인된다**. 뒤 낱말은 위 `<p>` 와 같다 |
            | `당일 지정된 시각에만 체크됩니다.` | ★ 도착지 실측 문면(「지정된 시간에만 체크 가능합니다」)의 **요지**다 — 우리 창작이 아니다. **`당일` 이 «미리 눌러 출석했다고 믿는» 경로를 막는다**(성공 피드백 유무는 확인 불가 · §57-6 #1) |

            ## 형태 — **8/28 카드와 «일부러» 같게 뒀다**(§57-3 (3) 조건 5 · §0.7)

            **카드 = 외부 이동**이다. ⚠ **필 버튼으로 바꾸지 마라** — 필 버튼은 «페이지 «안» 조작»의 형태다.
            남색 면(`bg-primary` ↔ 흰 글자 대비 **12.6** AAA)까지 `QrAttendanceCard` 와 같은 값을 쓴다 —
            **두 페이지가 같은 것을 같은 형태로** 말한다.
            ⚠ **클래스 문자열이 두 벌이다**(여기 · `src/components/rally/QrAttendanceCard.tsx`).
              **공통 컴포넌트로 묶지 않았다** — 그쪽 카드는 제목·설명 구성이 다르고 주석이 8/28 판정을
              통째로 지고 있다. **한쪽 형태를 고치면 다른 쪽도 열어 보라.**

            ## 접근성 — **`aria-label` 을 붙이지 마라**(`union-webapp-dev §8`)

            **카드 전체가 단일 `<a>`** 라 **내부 텍스트(제목 + 설명 + 도메인 줄)가 접근성 이름을 진다.**
            `aria-label` 을 붙이는 순간 **설명 줄(«당일 지정된 시각에만»)이 링크 낭독에서 사라진다.**
            외부 이동 **3중 병행**(§14.1): ↗ 아이콘 + 도메인 표기 + 내부 텍스트가 지는 이름.
            ★ **도메인은 `href` 에서 파생한다**(`UNION_ATTENDANCE_DISPLAY_HOST`) — 리터럴을 적으면
              링크와 표시가 갈린다. **`break-all` 은 도메인 줄에만**(공백 없는 라틴 덩어리 · §0.8).
          */}
          <a
            href={EXTERNAL_LINKS.unionAttendance}
            target="_blank"
            rel="noopener noreferrer"
            className="rounded-card shadow-card ease-out-soft group mt-4 block bg-primary p-4 transition-opacity duration-150 hover:opacity-95 hover:outline-2 hover:-outline-offset-4 hover:outline-white focus-visible:outline-3 focus-visible:outline-primary focus-visible:outline-offset-2"
          >
            <span className="block break-keep break-words text-lead font-bold text-white group-hover:underline">
              금융노조 QR 출석체크
              <ExternalLinkIcon className="ml-1 inline size-5 align-[-3px]" />
            </span>
            <span className="mt-1 block break-keep break-words text-caption text-white">
              당일 지정된 시각에만 체크됩니다.
            </span>
            <span className="mt-1.5 block break-all text-caption text-white/80">
              외부 링크(새 창) · {UNION_ATTENDANCE_DISPLAY_HOST}
            </span>
          </a>

          {/* ── 돌아가기 ── */}
          <p className="mt-section md:mt-section-lg">
            <Link
              href={ROUTES.bargaining}
              className="ease-out-soft inline-flex min-h-touch items-center gap-2 text-body font-semibold text-primary transition-colors duration-150 hover:underline focus-visible:outline-3 focus-visible:outline-primary focus-visible:outline-offset-2"
            >
              <ArrowLeftIcon className="size-5" />
              26년 임단협 투쟁 안내로 돌아가기
            </Link>
          </p>
        </div>
      </main>
      <SiteFooter />
    </>
  );
}
