import Image from "next/image";

import { ExternalLinkIcon } from "@/components/ui/icons";
import { EXTERNAL_LINKS, UNION_ATTENDANCE_DISPLAY_HOST } from "@/lib/routes";

/**
 * QR 출석체크 안내 — **주최측 원본 이미지 + 이미지에 없는 것만 문자로**.
 *
 * ★★ **중복 제거**(사용자 지시 2026-08-22 — *"그림과 제목/컨텐츠가 중복돼. 불필요한 중복컨텐츠는 삭제하자"*).
 *
 * 종전에는 이미지가 말하는 것을 **네 블록 전부 문자로 다시 적었다**:
 *   `출발 전 확인`(위치서비스) · `출석체크는 총 2회`(+시간표) · `인증 제한` · `등록하지 못한 경우`
 * 넷 다 **이미지 안에 그대로 있다.** 조합원은 같은 안내를 두 번 읽어야 했다.
 *
 * ⚠ **되살리기 전에 읽어라 — 지운 것은 «그림이 이미 말하는 것» 뿐이다.**
 * 아래 세 줄은 **이미지에 없어서 남겼다.** 지우면 정보가 사라진다:
 *   1. 손피켓 배포 — 주최측 캡션 원문(이미지 본문에는 없다)
 *   2. 위치 접근 요청 동의 — 실제로 눌러야 하는 행동
 *   3. **폐회 후 2차 출석** — 식순 대조로만 나오는 사실이고 **행동이 갈린다**
 *      (폐회하면 끝난 줄 알고 귀가하면 2차 출석을 놓친다)
 *
 * ⚠ **`alt` 가 이제 이미지의 **완전한 텍스트 등가**다.** 종전 `alt` 는
 * *"같은 내용을 이 카드에 텍스트로 적었습니다"* 라고 **카드를 가리켰는데, 그 카드가 사라졌다** —
 * 그대로 뒀으면 스크린리더 사용자를 없는 곳으로 보내고 시간표를 통째로 잃는다.
 * **`alt` 를 짧게 줄이지 마라.**
 */
/**
 * ★ **이미지가 교체됐다**(사용자 제공 2026-08-25). 종전 `qr-guide.png` 는 지웠다.
 *
 * ⚠⚠ **출석 시각이 바뀌었다 — 이 값을 쓰는 곳이 이 파일만이 아니다:**
 *   1차 `18:30~19:30` → **`18:30~19:20`**   ·   2차 `20:00~21:00` → **`20:10~21:00`**
 * `rally-2026-08-28/page.tsx` 의 *"※ 2차 출석은 …"* 문장도 **함께 고쳤다.**
 * 시각이 또 바뀌면 **두 곳을 같이 본다.**
 *
 * ★ **이 이미지의 QR 은 우리 «손피켓 QR인증 바로하기» 링크와 같은 곳으로 간다**(실측 2026-08-25):
 *   `https://m.site.naver.com/2eTDe` → `https://prpage153.mycafe24.com/bank701/index.php`
 *   (`<title>금융노조 집회참석</title>`)
 * ⚠ **종전 이미지에서는 그렇지 않았다** — 그 QR 은 `qrpage.kr/pr/`(웹 제작 업체 회사소개)로 갔다.
 * 그래서 아래 카드 문안이 *"손피켓 QR로"* 에서 *"QR을 찍으면"* 으로 돌아올 수 있었다.
 * **이미지를 다시 교체하면 QR 을 반드시 다시 디코딩해 확인하라.**
 *
 * ★ **`.jpg` 인 이유**: 원본은 2794×1566 PNG 로 **3.9MB** 였다. 배경이 사진성 그러데이션이라
 * PNG 로는 1920 폭에서도 2.1MB 밖에 안 줄어든다. 1600 폭 JPEG q92 = **361KB** 이고
 * **QR 은 그 상태에서도 디코딩된다**(실측). 집회 당일 현장 데이터로 받는 이미지다 — 무게가 중요하다.
 * ⚠ 더 줄이려고 품질을 낮추지 마라. **QR 이 읽히는지부터 확인**하고 바꿔라.
 */
const QR_IMAGE = {
  src: "/images/rally-2026-08-28/qr-guide-2.jpg",
  width: 1600,
  height: 896,
  alt:
    "주최측 배포 “출석체크 QR코드 안내”. " +
    "참석확인은 총 2회 진행합니다 — 1차 출석 오후 6시 30분부터 7시 20분까지, " +
    "2차 출석 오후 8시 10분부터 9시까지. " +
    "한 개의 휴대폰으로 한 명만 인증할 수 있습니다. " +
    "아이폰 SAFARI, 갤럭시 인터넷에서 위치 서비스 이용 동의 설정이 필요합니다. " +
    "휴대폰 GPS 설정 오류 등으로 등록하지 못한 경우 각 지부로 현장 인증샷을 제출하거나 " +
    "수기접수를 요청해 주세요. " +
    "왼쪽에는 “실질임금 인상! 지방이전 저지! 주4.5일제 도입!” 손피켓 그림과 출석체크 QR코드가 있습니다 — " +
    "같은 곳으로 가는 링크가 이 이미지 아래에 버튼으로 있습니다.",
} as const;

export function QrAttendanceCard() {
  return (
    <figure className="mt-6">
      <Image
        src={QR_IMAGE.src}
        width={QR_IMAGE.width}
        height={QR_IMAGE.height}
        alt={QR_IMAGE.alt}
        unoptimized
        className="rounded-badge border border-border-soft block h-auto w-full max-w-[480px]"
      />
      <figcaption className="mt-3 text-caption text-ink">주최측 배포 안내자료</figcaption>

      {/* 이미지에 **없는** 것만. 순서는 조합원이 하는 순서다 — 받는다 → 켠다 → 끝나도 남는다 */}
      <p className="mt-3 break-keep text-caption text-ink">
        ※ 출석 QR은 손피켓에 넣어 배포합니다. (주최측 안내자료 기준)
      </p>
      <p className="mt-2 break-keep text-caption text-ink">
        ※ 위치 접근 요청이 표시되면 동의해 주세요.
      </p>

      {/*
        ★★ **지급 조건**(사용자 지시 2026-08-26 · 검증 §48-1 축자 · **한 글자도 고치지 마라**).

        ## 자리 — `※` 2줄 «다음», 아래 면 블록 «앞» (§48-3)

        순서가 **조건 → 결과 → 행동**이 된다:
        `이 문장(둘 다 해야 한다)` → `아래 면 블록(폐회 후라도 21:00 전에)` → `링크 카드(손피켓 QR인증 바로하기)`.
        ★ **뒤로 옮기지 마라** — 이 문장이 «둘 다 해야 한다»를 **먼저 세워야** 아래 블록의
        *"2차 출석을 아직 하지 않았다면"* 이 **왜 중요한지**가 성립한다. 뒤에 두면 경고를 읽고 나서 이유를 듣는다.
        ⚠ **링크 카드 뒤에 두지 마라** — 누르고 나가면 안 읽는다.

        ## ⚠⚠ 어순 — `지부천막에서` 를 «앞»으로 빼지 마라 (§46 · 절대 규율)

        ```
        ✗  지부천막에서 참석명단 작성과 금융노조 QR인증을 모두 마쳐야 …
             └ 「지부천막에서」가 두 항목을 «모두» 수식 → «QR도 천막에서» → **확인된 거짓**이고,
               조합원이 QR 을 안 하고 상품권을 잃는다
        ✓  참석명단 작성(지부천막)과 …
             └ 괄호가 장소를 「참석명단 작성」에만 묶는다
        ```

        ## 괄호가 둘인 이유 — **형태가 «두 개의 별개 조건»을 말한다**

        1. **괄호 대칭**: `참석명단 작성(지부천막)` 과 `금융노조 QR인증(1차·2차)` 이 **각자 자기 괄호**를 진다.
           **«A 와 B 는 별개다»가 이 문장에서 가장 지켜야 하는 사실**이다. 풀어 쓰면 그 대칭이 사라진다.
        2. ★ **`QR인증 1차` 라는 새 이름을 만들지 않는다** — 괄호가 있으면 **횟수 한정어**로 읽히고,
           없으면 복합명사(=새 이름)로 기운다. **회차의 이름은 여전히 `1차 출석`·`2차 출석`**(위 `alt` 표기)이다.
        ⚠ **`1·2차` 로 축약하지 마라** — 위 이미지·`alt` 와 **글자가 어긋난다.**
        ⚠ **`모두` 를 괄호 안에 또 넣지 마라** — 한 문장에 `모두` 가 둘이면 어느 쪽을 받는지 흐려진다.
          **바깥 `모두` 하나**가 A ∧ B ∧ (1차·2차)를 전부 받는다.
        ⚠ **`식대·교통비 (상품권 5만원)` 은 사용자 지정 문면**이다 — 가운뎃점 `·`(`및` 아님) ·
          **괄호 앞 공백 하나** · `상당` 금지. **조사만** 조정 가능하다.

        ## ★ `(1차·2차)` 는 «금지가 뒤집힌 것»이 아니라 «근거가 소멸한 것»이다

        종전 검증(§46-7 조건 3)은 **`1차`·`2차` 표기를 금지**했는데, 그 이유는 **조건 B 의 범위 미확인**이었다.
        사용자 확인(I-10)으로 **«1차·2차 모두»** 임이 확정된 지금은 **반대로 명시해야 한다** —
        안 적으면 조합원이 **1차만 하고 귀가해 상품권을 잃는다.** ⚠ **옛 금지를 근거로 괄호를 지우지 마라.**

        ## ⚠ 시각을 적지 마라 · 기존 2차 출석 문장 2건 무수정

        `18:30~19:20`·`20:10~21:00` 을 **여기에 다시 쓰지 마라**(§5.6) — 위 이미지와 블록 1 의 `※` 가 이미 진다.
        이 문장이 지는 축은 **횟수 + 지급 조건**이라 **새 중복이 0** 이다. 세 문장이 서로 다른 축을 진다:
        블록 1 `※` = **시각** · 아래 면 블록 = **폐회와의 관계(30분 공백)** · 이 문장 = **횟수 + 지급 조건**.
        ★ **셋이 서로를 강화한다**: *2차 창은 21:00 까지인데 참석 시간은 20:30 에 끝난다* →
        *그 30분 공백에 자리를 뜨면 놓친다* → ***놓치면 상품권까지 잃는다.***
        ⚠ **하나라도 지우면 나머지 둘의 «왜»가 약해진다.**

        ## 형태 — `※` 아님 · 면 없음 (무게는 위 `※` 둘과 아래 면 블록 «사이»)

        `※` 는 단서의 형태인데 이것은 **본문 사실**이라 `※` 로 달지 않는다(그리고 이 페이지는 `※` 개수를 센다).
        동시에 **아래 면 블록보다 무겁게 만들지 않는다**(§48-3 — *이유가 결과보다 무거우면 위계가 뒤집힌다*):
        **면·테두리를 주지 않고 `text-body` 로만 올린다.** `※`(caption·면 없음) < **이 문장**(body·면 없음)
        < 면 블록(body·면 있음) 순이고, 간격도 `mt-2` → `mt-3` → `mt-4` 로 함께 벌어진다.
        ⚠ **`rounded-card p-4` 로 감싸지 마라** — 면이 생기는 순간 아래 블록과 같은 급이 된다.
          (디자이너 §38.1 이 이 문장을 «카드 안» 예로 든 것은 **슬롯 폭 분류**이지 형태 확정이 아니다.)

        ## 200% — `break-keep break-words` 는 «넘침 대응»이 아니라 «비용 0 인 보험»이다

        ★★ **이 문장은 지금 어느 슬롯에서도 넘치지 않는다. 그 사실을 먼저 적는다** —
        *"넘치던 줄"* 로 읽고 자리를 옮기거나 클래스를 정리하면 **틀린 판단을 한다.**

        **QA 실측 정정본**(`_workspace/04_qa_report.md` 부록 D). **이 페이지에는 200% 슬롯이 두 종류다:**

        | 자리 | 360·sb0 | 360·sb15 | 320·sb15 |
        |---|---|---|---|
        | **카드 «안»**(`rounded-panel … p-5` — 블록 1·2) | 216 | **201** | **161** |
        | **카드 «밖»**(컨테이너 `px-4` 만) | 296 | **281** | **241** |

        ★ **이 QR 출석체크 섹션은 «카드 밖» 이다** — `page.tsx` 의 `rounded-panel` 은 블록 1·블록 2 **둘뿐**이고
        이 섹션엔 없다(실측: 이 `figure` 폭 **281**, 위 `<p>` 3개도 전부 281).
        이 문장의 `width:0` `scrollWidth` = **165**(`keep-all`) / 31(`+break-words`) 이라
        **281·241 을 포함한 전 구간에서 초과 0** 이다.

        ⚠ **자리를 «카드 안»(블록 1·2 같은 `rounded-panel p-5`)으로 옮기면 슬롯이 161 까지 좁아져 320급에서 4px 넘친다** —
        **그때는 `break-words` 가 실제로 필요해진다.** 지금 붙여 두는 이유는 그 대비 + 형태 일관성이고,
        **높이 비용은 0 이다**(612 → 612 실측).

        ⚠ **범인이 `식대·교통비` 만이 아니다**: `지급됩니다.` 도 **정확히 같은 164.7px** 이라
        좁은 자리로 옮겨 넘치기 시작하면 **문면을 손봐도 해결되지 않는다. 클래스로 푼다.**
        (§38.3 — **괄호에는 줄바꿈 기회가 있고 가운뎃점에는 없다.** 눈으로 세지 마라.
         측정은 **`width:0` 의 `scrollWidth`** 로 한다 — `width:min-content` 의 rect 는 `break-words` 를
         반영하지 않아 **거짓 실패**가 난다.)
      */}
      {/* ⚠ **이 문장을 여러 줄로 쪼개지 마라** — JSX 는 줄바꿈을 공백으로 접어 렌더는 같지만,
          **소스 `grep` 으로 축자 대조가 안 된다.** 축자 문면은 한 줄에 통째로 둔다. */}
      <p className="mt-3 break-keep break-words text-body text-ink">
        참석명단 작성(지부천막)과 금융노조 QR인증(1차·2차)을 모두 마쳐야 식대·교통비 (상품권 5만원)가 지급됩니다.
      </p>

      {/*
        ★ **이것만 `※` 가 아니라 면을 가진 블록이다.** 나머지 둘과 무게가 다르다 —
        폐회하면 끝난 줄 알고 귀가하면 **2차 출석을 놓친다.** 행동이 갈리는 유일한 줄이라
        `※` 더미에 섞지 않는다(§5.3 — 단서는 개수를 세고, 무게가 다른 것은 형태로 가른다).
      */}
      <p className="rounded-card bg-primary-soft mt-4 break-keep p-4 text-body text-ink">
        2차 출석 시간은 식순상 폐회(20:20~)보다 늦게까지 열려 있습니다. 폐회 후라도 2차 출석을 아직
        하지 않았다면 21:00 전에 완료해 주세요.
      </p>

      {/*
        ★ **출석체크 페이지 바로 가기**(사용자 지시 2026-08-25 · URL 도 사용자가 줬다).
        QR 을 찍지 못하는 상황 — 손피켓을 못 받았거나, 카메라가 QR 을 못 읽거나 —
        에서 **같은 곳으로 가는 두 번째 경로**다. 대상은 QR 이 가리키는 그 페이지가 맞다
        (응답 실측: `<title>금융노조 집회참석</title>`).

        ⚠ **«아웃라인 필 버튼» 모양으로 만들지 마라**(§24.5 · `WayfindingBlock` 과 같은 규칙).
        이 프로젝트에서 **필 버튼은 «우리 페이지 안에서 일어나는 조작»의 형태**이고
        **외부로 나가는 것은 다르게 생겨야 한다.** 그래서 **카드**다 — 누르는 면은 오히려 더 넓고,
        어디로 가는지도 함께 말한다.

        ★ **면을 남색으로 채웠다**(사용자 지시 2026-08-25 — *"조금 더 강조해줘. 남색바탕에 흰색글씨"*).
        **형태는 그대로 카드**이므로 위 규칙과 충돌하지 않는다 — 바뀐 것은 **무게**뿐이다.
        길찾기 카드(흰 면 + 테두리)와 **일부러 다르게** 둔다: 길찾기는 *가는 길*이고
        이것은 **당일 반드시 해야 하는 행동**이라 페이지 안에서 가장 무거운 항목이다.
        대비: 흰 글자 ↔ `#093389` = **12.6**(AAA).

        ⚠ **hover 테두리를 남색으로 두지 마라** — 남색 면 위에서는 보이지 않는다. 안쪽 흰 선이다.
        ⚠ 포커스 링은 **바깥**(`outline-offset-2`)이라 흰 배경 위에 그려진다 — 남색 그대로 둔다.

        외부 이동 **3중 병행**(§14.1): ↗ 아이콘 + 도메인 표기 + 접근성 이름
        (카드 전체가 단일 `<a>` 라 내부 텍스트가 접근성 이름에 자동 포함된다).

        ⚠ **둘째 줄을 지우지 마라.** 8/28 출석 시간 밖에서 누르면 대상 페이지에 **입력 폼이 없고**
        *"지정된 시간에만 체크 가능합니다."* 한 줄만 나온다(실측). 이 문장이 없으면
        조합원이 **«링크가 고장났다»** 로 읽고 다른 경로를 찾아 헤맨다 — 행동이 갈린다(§5.3).
        시간표 자체는 위 이미지가 이미 말하므로 **여기에 다시 적지 않는다.**

        ★★ **«QR 을 찍으면 나오는 페이지와 같다» 는 실측된 사실이다** — 짐작이 아니다.
        위 이미지의 QR 을 디코딩해 최종 도착지까지 따라간 결과가 이 링크와 같다(→ `QR_IMAGE` 주석).

        ⚠ **이 문장은 이미지에 매달려 있다.** 2026-08-25 이전 이미지의 QR 은
        **업체 회사소개 홈페이지로 갔고**, 그때는 이 문장을 쓸 수 없어 *"손피켓 QR로"* 라고 특정해야 했다.

        ★★ **2026-08-26 에 `손피켓의` 가 다시 붙었다(사용자 지시). 그러나 «되돌림»이 아니다**(검증 §50-2):
          종전 `손피켓 QR로`  = **배제적 특정** — 이미지 QR 이 엉뚱한 데로 가서 **거짓을 피하려고** 특정했다
          이번 `손피켓의 QR을` = **명명** — 현장에 QR 이 여럿일 때 **어느 QR 인지 이름을 붙이는 것**이다
        **종전 근거는 이미 소멸했다**(교체분 이미지 QR 도 같은 곳으로 간다). 그러므로 이 문장은 **거짓이 되지 않는다.**
        ⚠ **부작용 하나**: `손피켓의` 를 붙이면 **위 이미지 안 QR 은 이 문장이 안 다루게 된다.**
          정보가 줄지만 거짓은 아니다. 되돌리려면 **이 표를 먼저 읽어라.**
        **이미지를 교체하면 QR 을 다시 디코딩하고, 다른 곳으로 가면 이 문장부터 고쳐라.**
      */}
      <a
        href={EXTERNAL_LINKS.unionAttendance}
        target="_blank"
        rel="noopener noreferrer"
        className="rounded-card shadow-card ease-out-soft group mt-4 block bg-primary p-4 transition-opacity duration-150 hover:opacity-95 hover:outline-2 hover:-outline-offset-4 hover:outline-white focus-visible:outline-3 focus-visible:outline-primary focus-visible:outline-offset-2"
      >
        <span className="block break-keep break-words text-lead font-bold text-white group-hover:underline">
          손피켓 QR인증 바로하기
          <ExternalLinkIcon className="ml-1 inline size-5 align-[-3px]" />
        </span>
        {/*
          ★★ **안심 문장**(사용자가 직접 고른 문면 2026-08-26). **제목 «바로 아래»가 자리 판정이다.**

          제목이 `손피켓 …` 으로 시작하면서 **위험이 하나 생겼다**(검증 §50-3 — 실재 · 크기 «중간»):
          **이 카드의 존재 이유가 «손피켓이 없는 사람»** 인데(아래 링크 주석 참조),
          **그 사람이 제목을 보고 «해당 없음»으로 읽고 안 누를 수 있다. 그가 정확히 이 링크가 필요한 사람이다.**
          ★ **대안 경로가 없다** — 페이지에 QR 이미지가 있어도 **자기 폰 화면의 QR 을 자기 폰으로 찍을 수 없다.**

          → 위험이 **«제목만 스캔하고 건너뛰는 경로»** 이므로 **제목에서 가장 가까운 자리**여야 닿는다.
            설명 줄 뒤·카드 밖은 그 경로를 못 막는다. **카드 «안»이라 접근성 이름에도 포함**된다(단일 `<a>`).
          ★ **비대칭**: 스크린리더 사용자는 카드 전체가 단일 `<a>` 라 설명의 «같습니다»를 **반드시 듣는다.**
            **위험은 시각 스캐너 쪽에만 있다.**

          ⚠ **이 문장이 말하지 않는 것 — 확대하지 마라:**
            **«손피켓 없이도 출석이 됩니다»가 아니다.** 출석은 **1·2차 QR 인증**이고 이 링크는 그 인증의 **경로**다.
            **경로와 결과를 섞으면** 조합원이 «링크만 누르면 끝»으로 읽는다.
            참석명단 작성(지부천막)·상품권 조건과도 섞지 마라 — 위쪽 문장이 이미 진다.

          ★ 사실 근거: 도착지 URL 에 **개인 토큰이 없다**(이미지 QR 의 최종 도착지와 같은 일반 URL · 실측).
            **누구나 같은 페이지로 간다.**
        */}
        <span className="mt-1 block break-keep break-words text-caption text-white">
          손피켓이 없어도 이 링크로 인증할 수 있습니다.
        </span>
        <span className="mt-1 block break-keep break-words text-caption text-white">
          손피켓의 QR을 찍으면 나오는 출석체크 페이지와 같습니다. 지정된 출석 시간에만 체크됩니다.
        </span>
        {/* 도메인 줄만 한 단계 낮춘다 — **흐리게가 아니라 «덜 굵게»** 다.
            `white/80` 대비는 남색 위에서 여전히 8 이상이라 읽는 데 지장이 없다.

            ★ **`break-all` 은 텍스트 확대에서 카드 밖으로 나가지 않게 하는 보험이다**(2026-08-25 추가).
            도메인은 공백이 없어 **통째로 끊기지 않는 덩어리**다 — 200% 실측에서 이 줄 하나가
            **118px** 넘쳤다(온누리 카드가 같은 이유로 이미 쓰고 있던 처방이다).
            ⚠ **`truncate`·말줄임으로 바꾸지 마라** — 주소가 잘리면 표시하는 의미가 사라진다. */}
        <span className="mt-1.5 block break-all text-caption text-white/80">
          외부 링크(새 창) · {UNION_ATTENDANCE_DISPLAY_HOST}
        </span>
      </a>
    </figure>
  );
}
