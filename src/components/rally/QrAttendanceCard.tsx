import Image from "next/image";

import { ExternalLinkIcon } from "@/components/ui/icons";
import { EXTERNAL_LINKS, UNION_ATTENDANCE_DISPLAY_HOST } from "@/lib/routes";

/**
 * QR 출석체크 안내 — **주최측 원본 이미지 + 이미지에 없는 것만 문자로**.
 * ⚠ **이미지가 말하는 것을 다시 적지 마라**(사용자 지시 2026-08-22): `출발 전 확인` · `출석체크는 총 2회` · `인증 제한` · `등록하지 못한 경우` 는 넷 다 이미지 안에 그대로 있어 삭제했다.
 * ⚠ **아래 세 줄은 이미지에 «없어서» 남긴 것이다 — 지우면 정보가 사라진다:** 손피켓 배포 · 위치 접근 요청 동의 · **폐회 후 2차 출석**(폐회하면 끝난 줄 알고 귀가하면 놓친다 — 행동이 갈린다).
 * ⚠ **`alt` 를 짧게 줄이지 마라** — 그 카드가 사라진 지금 `alt` 가 이미지의 **완전한 텍스트 등가**다.
 */
/**
 * ⚠⚠ **출석 시각을 쓰는 곳이 이 파일만이 아니다** — `rally-2026-08-28/page.tsx` 의 `※ 2차 출석은 …` 문장이 같은 값을 쓴다.
 * 시각이 또 바뀌면 **두 곳을 같이 본다**(2026-08-25 교체분: 1차 18:30~19:20 · 2차 20:10~21:00).
 * ★ **이 이미지의 QR 은 아래 «손피켓 QR인증 바로하기» 링크와 같은 곳으로 간다**(디코딩 실측 — `m.site.naver.com/2eTDe` → `prpage153.mycafe24.com/bank701/index.php`). 종전 이미지의 QR 은 달랐다.
 * ⚠ **이미지를 다시 교체하면 QR 을 반드시 다시 디코딩해 확인하라** — 아래 카드 문안이 여기에 매달려 있다.
 * ★ **`.jpg` 인 이유**: 원본 2794×1566 PNG 가 **3.9MB** 였고 배경이 사진성 그러데이션이라 PNG 로는 1920 폭에서도 2.1MB 다.
 * 1600 폭 JPEG q92 = **361KB** 이고 **QR 은 그 상태에서도 디코딩된다**(실측 · 현장 모바일 데이터로 받는 이미지다).
 * ⚠ 더 줄이려고 품질을 낮추지 마라 — **QR 이 읽히는지부터 확인**하고 바꿔라.
 */
const QR_IMAGE = {
  src: "/images/rally-2026-08-28/qr-guide-2.jpg",
  width: 1600,
  height: 896,
  alt:
    "주최측 배포 “출석체크 QR코드 안내”. " +
    "참석확인은 총 2회 진행합니다 — 1차 출석 오후 6시 30분부터 7시 20분까지, " +
    "2차 출석 오후 8시 10분부터 9시까지. " +
    "한 개의 휴대폰으로 한 명만 인증할 수 있습니다. " +
    "아이폰 SAFARI, 갤럭시 인터넷에서 위치 서비스 이용 동의 설정이 필요합니다. " +
    "휴대폰 GPS 설정 오류 등으로 등록하지 못한 경우 각 지부로 현장 인증샷을 제출하거나 " +
    "수기접수를 요청해 주세요. " +
    "왼쪽에는 “실질임금 인상! 지방이전 저지! 주4.5일제 도입!” 손피켓 그림과 출석체크 QR코드가 있습니다 — " +
    "같은 곳으로 가는 링크가 이 이미지 아래에 버튼으로 있습니다.",
} as const;

export function QrAttendanceCard() {
  return (
    <figure className="mt-6">
      <Image
        src={QR_IMAGE.src}
        width={QR_IMAGE.width}
        height={QR_IMAGE.height}
        alt={QR_IMAGE.alt}
        unoptimized
        className="rounded-badge border border-border-soft block h-auto w-full max-w-[480px]"
      />
      <figcaption className="mt-3 text-caption text-ink">주최측 배포 안내자료</figcaption>

      {/* 이미지에 **없는** 것만. 순서는 조합원이 하는 순서다 — 받는다 → 켠다 → 끝나도 남는다 */}
      <p className="mt-3 break-keep text-caption text-ink">
        ※ 출석 QR은 손피켓에 넣어 배포합니다. (주최측 안내자료 기준)
      </p>
      <p className="mt-2 break-keep text-caption text-ink">
        ※ 위치 접근 요청이 표시되면 동의해 주세요.
      </p>

      {/* ★★ **지급 조건**(사용자 지시 2026-08-26 · 검증 §48-1 축자 — **한 글자도 고치지 마라**).
          ⚠ **자리**(§48-3): `※` 2줄 «다음» · 아래 면 블록 «앞». 이 문장이 «둘 다 해야 한다»를 먼저 세워야 아래 블록의 «아직 하지 않았다면»이 성립한다. **뒤로·링크 카드 뒤로 옮기지 마라**(누르고 나가면 안 읽는다).
          ⚠⚠ **어순 — `지부천막에서` 를 «앞»으로 빼지 마라**(§46 절대 규율): 장소가 두 항목을 다 수식하면 **«QR도 천막에서»라는 확인된 거짓**이 되고 조합원이 QR 을 안 해 상품권을 잃는다. **괄호가 장소를 「참석명단 작성」에만 묶는다.**
          ⚠ **괄호 둘을 풀어 쓰지 마라** — 대칭이 «A 와 B 는 별개 조건»을 말하고, 괄호 없는 `QR인증 1차` 는 복합명사(=새 이름)로 기운다(회차의 이름은 `1차 출석`·`2차 출석` · 위 `alt` 표기).
          **`1·2차` 로 축약하지 마라**(이미지·`alt` 와 글자가 어긋난다) · **`모두` 를 괄호 안에 또 넣지 마라**(바깥 `모두` 하나가 전부를 받는다).
          ⚠ **`(1차·2차)` 를 옛 금지(§46-7 조건 3)를 근거로 지우지 마라** — 그 금지의 근거는 «범위 미확인»이었고 사용자 확인(I-10)으로 «1차·2차 모두»가 확정됐다. 안 적으면 **1차만 하고 귀가해 상품권을 잃는다.**
          ⚠ **시각(`18:30~19:20`·`20:10~21:00`)을 여기에 다시 쓰지 마라**(§5.6). 세 문장이 다른 축을 진다 — 블록 1 `※` = 시각 · 아래 면 블록 = 폐회와의 관계 · 이 문장 = 횟수 + 지급 조건. **하나라도 지우지 마라.**
          ⚠ **`rounded-card p-4` 로 감싸지 마라** — 면이 생기면 아래 블록과 같은 급이 되어 위계가 뒤집힌다(§48-3): `※`(caption·면 없음) < 이 문장(body·면 없음) < 면 블록(body·면 있음), 간격도 `mt-2`→`mt-3`→`mt-4`.
          ⚠ **`식대·교통비 (상품권 5만원)` 은 사용자 지정 문면** — 가운뎃점 `·` · 괄호 앞 공백 하나 · `상당` 금지(조사만 조정 가능).
          `break-keep break-words` 는 지금 어느 슬롯에서도 안 넘친다 — «넘치던 줄»로 읽고 옮기거나 정리하지 마라. 카드 «안»(`rounded-panel p-5`)으로 옮기면 실제로 필요해진다(QA 부록 D). */}
      {/* ⚠ **이 문장을 여러 줄로 쪼개지 마라** — JSX 는 줄바꿈을 공백으로 접어 렌더는 같지만 **소스 `grep` 으로 축자 대조가 안 된다.** */}
      <p className="mt-3 break-keep break-words text-body text-ink">
        참석명단 작성(지부천막)과 금융노조 QR인증(1차·2차)을 모두 마쳐야 식대·교통비 (상품권 5만원)가 지급됩니다.
      </p>

      {/* ★ **이것만 `※` 가 아니라 면을 가진 블록이다** — 폐회하면 끝난 줄 알고 귀가하면 2차 출석을 놓친다. **행동이 갈리는 유일한 줄이라 `※` 더미에 섞지 않는다**(§5.3). */}
      <p className="rounded-card bg-primary-soft mt-4 break-keep p-4 text-body text-ink">
        2차 출석 시간은 식순상 폐회(20:20~)보다 늦게까지 열려 있습니다. 폐회 후라도 2차 출석을 아직
        하지 않았다면 21:00 전에 완료해 주세요.
      </p>

      {/* ★ **출석체크 페이지 바로 가기**(사용자 지시 2026-08-25 · URL 도 사용자가 줬다) — QR 을 못 찍는 상황에서 **같은 곳으로 가는 두 번째 경로**다(응답 실측 `<title>금융노조 집회참석</title>`).
          ⚠ **«아웃라인 필 버튼» 모양으로 만들지 마라**(§24.5) — 필 버튼은 «페이지 안 조작»의 형태이고 **외부로 나가는 것은 카드**다. 면을 남색으로 채운 것은 사용자 지시이고 **바뀐 것은 무게뿐**이다(흰 글자 ↔ `#093389` = 12.6 AAA).
          ⚠ **hover 테두리를 남색으로 두지 마라**(남색 면 위에서는 보이지 않는다 — 안쪽 흰 선이다). 포커스 링은 바깥(`outline-offset-2`)이라 흰 배경 위에 그려지므로 남색 그대로 둔다.
          외부 이동 **3중 병행**(§14.1): ↗ 아이콘 + 도메인 표기 + 접근성 이름(카드 전체가 단일 `<a>`).
          ⚠ **둘째 줄(`지정된 출석 시간에만 …`)을 지우지 마라** — 출석 시간 밖에서 누르면 대상 페이지에 입력 폼이 없어 조합원이 «링크가 고장났다»로 읽고 헤맨다(§5.3). 시간표 자체는 위 이미지가 진다.
          ⚠ **`손피켓의` 는 «되돌림»이 아니라 «명명»이다**(§50-2) — 종전 `손피켓 QR로` 는 이미지 QR 이 엉뚱한 데로 가서 거짓을 피하려던 **배제적 특정**이었고 그 근거는 소멸했다(부작용: 이미지 안 QR 은 이 문장이 안 다룬다).
          **이미지를 교체하면 QR 을 다시 디코딩하고, 다른 곳으로 가면 이 문장부터 고쳐라.** */}
      <a
        href={EXTERNAL_LINKS.unionAttendance}
        target="_blank"
        rel="noopener noreferrer"
        className="rounded-card shadow-card ease-out-soft group mt-4 block bg-primary p-4 transition-opacity duration-150 hover:opacity-95 hover:outline-2 hover:-outline-offset-4 hover:outline-white focus-visible:outline-3 focus-visible:outline-primary focus-visible:outline-offset-2"
      >
        <span className="block break-keep break-words text-lead font-bold text-white group-hover:underline">
          손피켓 QR인증 바로하기
          <ExternalLinkIcon className="ml-1 inline size-5 align-[-3px]" />
        </span>
        {/* ★★ **안심 문장**(사용자가 직접 고른 문면 2026-08-26). **제목 «바로 아래»가 자리 판정이다**(§50-3): 이 카드의 존재 이유가 «손피켓이 없는 사람»인데
            제목이 `손피켓 …` 으로 시작해 **그가 «해당 없음»으로 읽고 안 누를 수 있다.** 대안 경로가 없다 — 자기 폰 화면의 QR 을 자기 폰으로 찍을 수 없다.
            위험이 «제목만 스캔하고 건너뛰는 경로»라 **제목에서 가장 가까운 자리**여야 닿는다(설명 줄 뒤·카드 밖은 못 막는다). 스크린리더 사용자는 단일 `<a>` 라 설명을 반드시 듣는다.
            ⚠ **확대하지 마라**: «손피켓 없이도 출석이 됩니다»가 아니다 — 출석은 1·2차 QR 인증이고 이 링크는 그 **경로**다. 참석명단 작성(지부천막)·상품권 조건과도 섞지 마라(위쪽 문장이 진다).
            ★ 근거: 도착지 URL 에 개인 토큰이 없다(실측) — **누구나 같은 페이지로 간다.** */}
        <span className="mt-1 block break-keep break-words text-caption text-white">
          손피켓이 없어도 이 링크로 인증할 수 있습니다.
        </span>
        <span className="mt-1 block break-keep break-words text-caption text-white">
          손피켓의 QR을 찍으면 나오는 출석체크 페이지와 같습니다. 지정된 출석 시간에만 체크됩니다.
        </span>
        {/* 도메인 줄만 한 단계 낮춘다 — **흐리게가 아니라 «덜 굵게»** 다(`white/80` 대비는 남색 위에서 8 이상).
            ★ **`break-all` 은 텍스트 확대 보험이다** — 도메인은 공백이 없어 통째로 끊기지 않는 덩어리이고, 200% 실측에서 이 줄 하나가 **118px** 넘쳤다.
            ⚠ **`truncate`·말줄임으로 바꾸지 마라** — 주소가 잘리면 표시하는 의미가 사라진다. */}
        <span className="mt-1.5 block break-all text-caption text-white/80">
          외부 링크(새 창) · {UNION_ATTENDANCE_DISPLAY_HOST}
        </span>
      </a>
    </figure>
  );
}
