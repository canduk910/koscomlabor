# 05. 최종 게이트 (2차 검증) — 노동교육 콘텐츠 게시 대조

작성: 2026-08-17 | 수행: 리더 (오케스트레이터) | 대상: 프로덕션 `https://koscomlabor.cloud`
기준: `_workspace/00_input/decision-education-content.md`
(= `01_verifier_factcheck.md` §10 의 **개정된** 기준값 — 원본 표는 3곳이 낡았다)

## 판정: **통과** — 승인 원문과 실제 렌더가 전건 일치

1차 게이트(`01_verifier_factcheck.md`)에서 승인·조건부 승인된 5건이, 사용자 판정("5건 전부 게시")과
검증자가 요구한 완화 조치를 **모두 반영한 상태로** 프로덕션에 게시되었음을 확인했다.

## 1. 순서·제목·출처·URL 대조 (공개 API 실측)

`GET https://union-api.koscomlabor.cloud/posts?category=education` 응답 순서 그대로:

| 표시순 | title | source | url | 판정 |
|--------|-------|--------|-----|------|
| 1 | 노동조합이란 | 금융노조 교육문화본부 | `…watch?v=ATbGKR-Agmk` | ✓ |
| 2 | 산별노동조합이란 | 금융노조 교육문화본부 | `…watch?v=-WrzgLtvuPU` | ✓ |
| 3 | 산별중앙교섭과 쟁의절차 | 금융노조 | `…watch?v=jeK7W_SADUs` | ✓ |
| 4 | 오바마 대통령이 말하는 노조 | 하종강의 노동과 꿈 | `…watch?v=Vj3lQ7Y71PU` | ✓ |
| 5 | 노조가 필요한 이유 | 마이크임팩트 | `…watch?v=OFfbgB5dOIA` | ✓ |

**순서·제목·출처·URL 전건 일치.** 의도한 학습 순서(개념 → 산별노조 → 중앙교섭·쟁의절차 →
외부 채널 2건)대로 렌더된다. 역순 등록(⑤→①)으로 `published_at DESC` 정렬을 맞췄고,
게시일 충돌을 막기 위해 건당 1.1초 간격을 두었다.

## 2. 금지 사항 검사 (전건 통과)

| 항목 | 근거 | 결과 |
|------|------|------|
| "교수" 직함 표기 | §5-d 현재 직함 미확인 | 없음 ✓ |
| 강연 연도 표기 | §5-c 업로드 2016 vs 설명문 2019 로 시점 확정 불가 | 없음 ✓ |
| `?si=` 추적 파라미터 | 리더 메모 | 없음 ✓ |
| `youtu.be` 단축 URL 잔존 | §15.6R-D 판정 1 (정규형 통일) | 없음 ✓ |
| **"금속노조" 오기** | §2 경고 — WebFetch 가 실제로 이 오류를 냈다 | 없음 ✓ |
| 사용자 축약 라벨 잔존 (`오바마`·`진중권` 등) | §8 제목 교체 요구 | 없음 ✓ |
| 창작된 설명 문구 (`body`) | 5건 모두 영상 미시청 메타데이터 검증 | 전건 `null` ✓ |

`body` 가 전건 `null` 이고 카드에 설명 줄이 없으므로(§15.6R-C), **문안을 창작할 표면이
구조적으로 0** 이다 — 검증 범위를 넘는 서술이 화면에 들어갈 경로가 없다.

## 3. 완화 조치 이행 확인 — 채널명 구분 표기

검증자가 조건부 승인 2건에 건 **필수 조건**: "카드에 채널명을 노출해 금융노조 제작물이
아님을 구분할 것." 프로덕션 렌더 마크업 실측:

```
오바마 대통령이 말하는 노조 ↗
  2026.08.17 · 하종강의 노동과 꿈
  외부 링크(새 창) · www.youtube.com

노조가 필요한 이유 ↗
  2026.08.17 · 마이크임팩트
  외부 링크(새 창) · www.youtube.com
```

금융노조 공식 3건은 `금융노조 교육문화본부`/`금융노조` 로 찍혀 **외부 채널 2건과 즉시 구별**된다.
이 표시가 곧 게이트 조건의 이행이다.

**이 조건은 구현 단계에서 한 번 무력화될 뻔했다** — `PostList.tsx:98` 이 `source` 를
`!isExternal` 일 때만 렌더하고 있었고, 노동교육 5건은 전부 링크형이라 채널명이 어디에도
표시되지 않는 상태였다. 리더 실측으로 발견 → §15.6R-D 로 2행 메타 재설계 → 반영.
**검증 게이트를 통과한 조건이 구현 단계에서 조용히 사라질 수 있다**는 사례로 기록한다.

## 4. 게시 맥락 검증 (프로덕션 실측)

| 항목 | 결과 |
|------|------|
| 헤딩 아웃라인 | `h1 지부명 → h2 공지사항 / h2 금융노조 소식 / h2 노동교육 / h2 방명록` ✓ |
| 섹션별 렌더 건수 | 공지 0 · 소식 1 · 노동교육 5 — 0건 섹션이 다른 섹션을 가리지 않음 ✓ |
| 탭 인프라 잔존 | `role="tab"`·`role="tabpanel"`·`aria-selected` 전부 **0건** ✓ |
| `hidden` 속성 | 1건 (Next 셸 빈 서스펜스 경계, 콘텐츠 0 — QA 12회차 독립 확인) ✓ |
| 구분점 이상 | `· ·`·행 선두 `·`·행 말미 `·` 전부 **0건** ✓ |
| 상세 라우트 | `/education/<id>` 200, 교차 접근 `/notices/<eduId>` 404 ✓ |
| 구버전 URL | `/?tab=news` 200 (리다이렉트 없이 콘텐츠 노출) ✓ |
| 회귀 | `/admin` 200, `/guestbook` 200 ✓ |

## 5. 승인 이력 (감사 추적)

| 시점 | 주체 | 판정 |
|------|------|------|
| 2026-08-17 | 사용자 | 링크 5건 제공 (축약 라벨 형태) |
| 2026-08-17 | fact-verifier | 도달성 5/5, 승인 3 · 조건부 2 · 반려 0. #4 는 **사용자 확인 필수** |
| 2026-08-17 | **사용자** | **5건 전부 게시 선택** (검증자 권고는 공식 3건만이었음 — 정파성·상업성 리스크를 그대로 전달한 뒤 받은 결정) |
| 2026-08-17 | 리더 | 완화 조치 전건 적용 확정 (`decision-education-content.md`) |
| 2026-08-17 | 리더 | 프로덕션 게시 + 본 최종 게이트 통과 |

## 6. 미검증 / 한계 (게시 후에도 유효)

- **영상 5건을 시청하지 않았다.** 이번 게시는 메타데이터 검증에 근거한다.
  #3·#4·#5 는 설명문에 내용 서술이 없어 제목 외 근거가 없다
- **#4(진중권) 의 정파성·상업성 리스크는 해소된 것이 아니라 사용자가 감수를 선택한 것**이다.
  업로더가 직접 단 태그가 정치 논객 콘텐츠로 포지셔닝돼 있고 설명문이 유료 강연 티켓 판매
  퍼널이다. 조합원 반응에 따라 재검토가 필요할 수 있다
- **#3(오바마) 은 원출처 미표기 재업로드**로 예고 없이 삭제될 수 있다 → 링크 정기 점검 대상
- 5건의 **자막(캡션) 제공 여부 미확인** (접근성)
- **표시 순서는 취약하다**: 사용자가 admin 에서 글을 추가하면 맨 위로 올라가 학습 순서가
  깨진다. 정렬 수단(`sort_order`)은 후속 과제 (§15.6R-D3)

---

# 최종 게이트 (2차): 8/28 결의대회 참석 안내 — 2026-08-18

- **대상**: `/rally-2026-08-28` · `/` 참석 안내 진입 블록 · `/bargaining-2026` 추가분 · 온누리 카드 도메인 표시
- **기준**: 검증 4회차 §9 · 5회차 §5-10 · §5-12-9 게시 문안 / `content-rally-20260828.md`(§1~§6.7) / `02_designer_spec.md` §20.10 / 수정 요구 1~35
- **방법**: **렌더 결과 대조.** 실행 중인 dev 서버(포트 3000)에 **읽기 전용 GET** 3회.
  서버 재시작·빌드·파일 변경 없음 → QA 와 충돌하지 않는다. HTTP 200 × 3
- **검증자**: fact-verifier (verifier-rally)

## 판정: **게시 승인**

**반려 0건. 조건부 0건.** 수정 요구 1~35 전건 이행, 금지 문자열 전건 0, 원문 대조 불일치 0.

---

## 1. 조합원 행동에 직결되는 값 — 전건 일치

| 확인 항목 | 렌더 결과 | 판정 |
|---|---|---|
| 집결 시각 | `집결 / 2026년 8월 28일(금) / 18:30` — 페이지 유일 대형 수치 | ✅ |
| `18:00` 이 집결 시각으로 쓰였는가 | **아니오.** 페이지 전체에서 `18:00` 은 **식순표 1행(`18:00~18:30 장내 정리 및 조합원 안내`)에만** 등장 | ✅ |
| 본대회 | `본대회 / 19:00 개회` | ✅ |
| 출석 2회 | `출석체크는 총 2회 진행됩니다` + `1차 출석 18:30 ~ 19:30` · `2차 출석 20:00 ~ 21:00` | ✅ |
| **폐회 단서(생략 불가)** | `※ 2차 출석 시간은 식순상 폐회(20:20~)보다 늦게까지 열려 있습니다. 폐회 후라도 2차 출석을 아직 하지 않았다면 21:00 전에 완료해 주세요.` — **존재** | ✅ |
| `※ 두 시간대 모두 확인해 주세요.` | 존재 | ✅ |
| 식순 16행 | 18:00~18:30 ~ `20:20~ 폐회선언` **16행 전부** 렌더 | ✅ |
| `※ 상황에 따라 식순 변경 가능` | **표 위(`<caption>`) + 표 아래 2곳** 동시 노출 | ✅ |
| 화장실 표기 | `여의도공원 화장실 2호(개나리) 이용` + `※ 정확한 위치는 지도 앱에서 “여의도공원 화장실”로 검색해 주세요.` + `안내자료 표기: “여의도공원2호 개나리 개방화장실”` | ✅ |
| 인명 | `윤석구 금융노조 위원장` · `김동명 한국노총 위원장` — **소속 없는 단독 표기 0건**(전수 grep) | ✅ |
| 정식명칭 1회 | `2026년 산별중앙교섭 투쟁 승리를 위한 총파업 총력투쟁 결의대회` | ✅ |
| 배너 문구 | `8/28(금) 저녁 결의대회 참석 안내` — 사용자 지정 문구 **한 글자도 변형 없음** | ✅ |

**D-day 독립 검산** (오늘 2026-08-18 기준): 8/28 → **D-10**, 9/4 → **D-17**. 렌더값과 일치.

## 2. 창작 금지 — 전건 0

렌더 텍스트 3종 전수 grep.

| 금지 대상 | 검출 |
|---|---|
| OS·기기별 위치서비스 설정 경로 (`설정 >`, `개인정보 보호`, `위치 서비스`) | **0** |
| `크롬` (가부 양방향) | **0** |
| `무효` (2회 미이행 단정) | **0** |
| `20:00까지` · `폐회 후 출석` | **0** |
| `QR이 배포됩니다` (확정형) | **0** |
| 위치정보 수집 범위 안심 문구 | **0** |

원문에 없는 사실 추가: **없음.** 다만 아래 2개 문장은 우리가 붙인 것이며, 성격을 명시해 둔다.

| 문장 | 성격 | 판정 |
|---|---|---|
| `※ 현장에서 설정하려면 늦습니다. 출발 전에 확인해 주세요.` | 사실 주장이 아닌 **행동 권고**. `※` 로 구분됨 | 5회차 §5-10 에서 이미 허용 판정 |
| `※ 2차 출석 시간은 식순상 폐회(20:20~)보다…` | 원문 두 사실(2차 20:00~21:00 · 폐회 20:20~)의 **병치**. 새 사실 0 | 요구 15 이행 |

`한개`→`한 개`, `한명`→`한 명` 맞춤법 교정 반영 — 5회차 §5-10 에서 명시 허용한 범위다.

## 3. 확정도 구분 — 전건 유지

| 항목 | 렌더 문자열 | 판정 |
|---|---|---|
| 무대 | `메인무대는 국회의사당역 5번 출구 앞에 설치될 예정입니다.` | ✅ 예정 유지 |
| 지도 라벨 | `메인무대(설치 예정)` — **문자로** 표기 | ✅ 요구 27 |
| QR 배포 | `※ 출석 QR은 손피켓에 넣어 배포할 예정입니다. (주최측 안내자료 기준)` | ✅ 예정 유지 |
| **운영=확정 / 배포=예정 분리** | 출석 절차는 카드 본문(확정형), 배포 문장만 **카드 밖 이미지 캡션 자리** | ✅ 요구 20 |
| 대오 문안 | `더샵아일랜드파크 앞 의사당대로 · [결의대회대오 2]` | ✅ |
| `우측 도로` | **0건** | ✅ 요구 28 |
| 거리 | `약 220~340 m (도보 약 4분)` — 단일 수치 `320` 계열 **0건** | ✅ 요구 29 |

## 4. 금지 문자열 — 전건 0

| 대상 | 렌더 검출 |
|---|---|
| `528세대` | **0** |
| `열렸` / `개최` / `성황` | **0** |
| 확정형(`QR이 배포됩니다` 등) | **0** |
| 본문 곧은따옴표 `"` | **0** (곡선따옴표 5건으로 통일) |

- 소스에 남은 `528세대`·`우측 도로`·`열렸습니다` 는 **전부 "쓰지 마라"는 코드 주석**이다
  (`page.tsx:30-31`, `RallyStatus.tsx:34`). 렌더되지 않으며, 오히려 재발 방지 장치다

## 5. 지도 표기

| 항목 | 렌더 결과 | 판정 |
|---|---|---|
| 범례 ⑤ | `여의도더샵아일랜드파크 — 위치 기준 단지 부지` | ✅ `건물` **0건**(전 페이지) |
| 확신도 선언 | `실선 = 확인된 위치 · 점선 = 근사 · 옅은 면 = 범위 근사` — **문자로** 선언 | ✅ 요구 24·26 |
| ① 5번 출구 | `국회의사당역 5번 출구 — 확인된 위치` | ✅ |
| ② 메인무대 | `주최측 설치 예정. 정확한 지점이 확정되지 않아 대략 범위만 원으로 표시했으며, 당일 변경될 수 있습니다` | ✅ 요구 27 |
| ③ 대오 1 | `범위는 근사이며 실제와 다를 수 있습니다` | ✅ 요구 26 |
| ④ 대오 2 | `안내자료를 지도로 옮긴 근사 구간이라 **구간 전후로 이어질 수 있습니다**` | ✅ 요구 25 |
| **LED무대 마커·좌표** | **0건** | ✅ 요구 27 |
| 화장실 지도 핀 | **0건** | ✅ |
| 도로명 라벨·도로 하이라이트 | **0건** | ✅ |

**좌표 대조 (내가 낸 값과 소스 1:1)**

| 대상 | 소스 | 판정 |
|---|---|---|
| 5번 출구 | `37.5282738, 126.9172199` | ✅ §5-1 값과 동일 |
| 대오 2 폴리곤 | 20점 — §5-12-5 와 **전 점 일치** | ✅ |
| 대오 1 폴리곤 | 18점 — §5-12-7 와 **전 점 일치**, `confidence: "estimated"` | ✅ |
| 부지 폴리곤 | **13점** — §5-13-4 의 14점에서 **닫는 점(첫 점과 동일)만 제외.** 네이버 `Polygon` 이 링을 자동으로 닫는다 | ✅ **추가 단순화 없음** |
| bbox 사각형 | **소스에서 제거됨** | ✅ 요구 31 |

> ⚠ **`LED무대` 문자열 1건은 위반이 아니다.** 범례 각주 `LED무대는 … 지도에 표시하지 않았습니다`
> 이며 **부재를 밝히는 문장**이다. 지우면 오히려 §0.4(은폐 금지) 위반이 된다.
> 리더 체크리스트 "LED무대 0건"의 실질 의도(마커·좌표 0건)는 충족했다.

## 6. 상태 전이 문안

- `RALLY_PAST_NOTE = "2026년 8월 28일 일정이 지났습니다. 아래 안내는 기록으로 남겨 둡니다."`
  — 리더 확정본과 **문자 단위 일치**. **개최 단정 없음** ✅
- `오늘` / `완료` 배지는 `RallyStatus` 단일 출처. `완료` 는 §17.3 기존 배포 문자열 재사용 ✅

> **한계 명시**: 오늘은 2026-08-18 이라 `phase === "upcoming"` 이다.
> **`today`·`past` 분기는 렌더로 확인하지 못했고 정적 검증(상수 대조)으로 판정했다.**
> 두 문자열 모두 **보간 없는 리터럴 상수**이고 렌더 시 가공이 없으므로 이 방식으로 충분하다.
> 검증 안 한 것을 통과로 표시하지 않기 위해 방법을 명기한다.

## 7. 진입점 — 같은 문자열 → 같은 목적지

| 페이지 | 라벨 | 목적지 |
|---|---|---|
| `/` | `참석 안내 보기` ×1 | `/rally-2026-08-28` |
| `/` | `자세히 보기` ×2 | `/bargaining-2026` |
| `/bargaining-2026` | `참석 안내 보기` ×1 | `/rally-2026-08-28` |

**규칙 위반 0.** 진입 블록은 조건부 렌더가 아니며 `upcoming` 에서 배지 없이 정상 렌더된다.

## 8. 온누리 카드 도메인 표시 (별건)

- 표시: `onnuri.koscomlabor.cloud` / `href`: `https://onnuri.koscomlabor.cloud/`
- **리터럴이 아니라 `new URL(EXTERNAL_LINKS.onnuriGuide).host` 파생**이다 →
  링크가 바뀌면 표시도 함께 바뀐다. **표시 주소와 실제 목적지가 갈릴 구조적 여지가 없다** ✅
- 기존 2줄(`디지털온누리 사용 가이드` / `코스콤 조합원 대상 안내 · 외부 페이지가 새 창에서 열립니다`)
  **문구 변경 0** — 추가만 됐다 ✅

## 9. 개발 중 문자열 변경 2건 — **둘 다 원문 훼손 아님. 승인.**

### 9-1. 범례 ⑤ `위치 기준 건물` → `위치 기준 단지 부지`

**승인.** 원문 훼손이 아니라 **정확도 정정**이다.

- 이 문자열은 **우리가 쓴 범례 문구**이지 주최측 원문이 아니다 → 원문 대조 대상이 아니다
- OSM `way 682330255` 의 태그는 `landuse=commercial` 이며 `building` 이 아니다.
  대지 경계이고 주차장·조경을 포함하며, 부지 안에 101·102동이 따로 있다(검증 §5-13-2)
- 즉 **이전 문자열 `건물` 이 부정확했고 지금 것이 맞다.** 요구 35 의 정확한 이행이다
- 렌더 전 페이지에서 `건물` **0건** 확인

### 9-2. 화장실 2줄 인용부호 곧은(`"`) → 곡선(`“ ”`)

**승인. 문안 내용 불변 확인 — 따옴표 문자만 바뀌었다.**

| 줄 | 따옴표 안 문자열 | 변경 |
|---|---|---|
| 검색 안내 | `여의도공원 화장실` | 없음 |
| 병기 줄 | `여의도공원2호 개나리 개방화장실` | 없음 |

- **핵심**: 병기 줄의 따옴표는 **우리가 붙인 인용 부호**다.
  사용자 원문(§1)은 `❍ 화장실 : 여의도공원 화장실 이용 (여의도공원2호 개나리 개방화장실)` 로
  **괄호**였고 따옴표가 아니었다. 따라서 따옴표 종류는 **원문 문자열의 일부가 아니다** —
  어느 형태를 쓰든 원문 훼손이 성립하지 않는다. 인용 안 문자열은 원문과 **한 글자도 다르지 않다**
- 검색 안내 줄의 따옴표도 **검색어 구분 기호**다. 안내 형식은 `…로 검색해 주세요` 이고
  조합원은 따옴표 안쪽을 입력한다 — 검색 실패 위험은 실질적으로 없다
- 실측: 렌더 본문 **곧은따옴표 0건 / 곡선따옴표 5건** → 페이지 전건 통일 확인.
  섞여 있을 때가 오히려 복사·검색 결과를 가르므로 **통일이 옳다**
- 소스에 남은 곧은따옴표는 **전부 코드 주석**이며 렌더되지 않는다

## 10. 원본 이미지 재대조

- `design/QR.png` → `public/images/rally-2026-08-28/qr-guide.png` **이동**(원본은 이제 없음)
- 배포본 규격 `1920×1080` · `283,445 bytes` · mtime `2025-09-16` — **원본과 동일**
- **배포본 이미지를 직접 재판독**해 5회차 §5-0 대조표와 다시 맞췄다 → **불일치 0.**
  제목·체크 3항·형광펜 시각 2개·경고 2줄·손피켓 시안·빨간 원 표시 전부 동일
- `alt`: `주최측 배포 “QR 출석체크 안내” 원본 이미지 — 같은 내용이 위 텍스트에 있습니다` ✅ (5회차 §5-10 제시 문자열)
- `figcaption`: `주최측 배포 안내자료` ✅ (출처 표기, 요구 12)
- **스캔 유도 문구 0건 / 스캔 금지 문구 0건** ✅ 요구 19
- 게시 근거: `content-rally-20260828.md` **§6.7 기재 완료** → 5회차 §5-6 잠정 단서 해제 상태 유지

## 11. 관찰 사항 (게시 저해 아님 — 기록용)

1. **대오 유보 절 제거는 사용자 결정이다.** 4회차 §2-2 가 요구한
   `※ 지부별 대오 배치는 … "각 지부별 대오 논의" … 당일 변경될 수 있으니` 는 페이지에 없다.
   근거는 §6.7 결정 경위의 **"대오도 대강 정해졌고"**(사용자 = 지부 담당자)이며 §20.10 에
   해제가 명문화돼 있다. 배치는 지부가 아는 사실이므로 **사용자 판단이 우선한다.**
   안전장치로 `※ 현장에서 지부 깃발을 확인해 주세요.` 가 남았고, 이 문장은 **조건부 렌더가 아니다**
2. **지도 섹션은 `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID` 미설정 시 통째로 렌더되지 않는다.**
   그 경우 근사 단서 3종(범례 ③④ + 확신도 선언)이 함께 사라진다.
   다만 위치 정보 본체와 깃발 확인 문장은 블록 2 에 **항상** 있으므로 은폐가 아니다.
   → **운영 확인 사항**: 프로덕션 배포 시 Client ID 설정 여부를 확인할 것
3. **`src/lib/struggleSchedule.ts` 8/28 항목은 갱신되지 않았다**(`서울 여의도 · 저녁` / `총파업 D-7 집회입니다.`).
   4회차 §4 에서 이는 **충돌이 아니라 권고**로 판정했고 `D-7` 은 정확하다 → **게시 저해 없음**
4. 블록 2 첫 줄에 `주최측 안내자료의` 라는 출처 귀속어가 없다(지도 범례 ④ 에는 있다).
   `[결의대회대오 2]` 대괄호 표기가 안내자료 라벨의 인용임을 드러내고, 확정형 금지가 해제된
   상태이므로 **사실 오류가 아니다.** 향후 문안 손질 시 고려할 여지로만 남긴다

## 12. 이번 게이트에서 검증하지 않은 것

- `today` · `past` 상태의 **렌더** (상수 대조로 대체 — §6 에 방법 명기)
- 지도 도형의 **화면상 실제 좌표 정확도** — 좌표 데이터가 내 산출값과 1:1 임은 확인했으나,
  네이버 지도 API 가 그린 결과의 픽셀 검증은 QA 소관
- 이미지 3·4(주최측 지도 도면) **실파일** — 여전히 미확보.
  대오 2 구간의 **±50 m 는 보증되지 않는다**(5회차 §5-12-6). 파일 확보 시 재검증 대상
- 접근성·대비·빌드·린트 — QA 소관(실패 0 통과 보고 받음)

---

**결론: 게시 승인. 커밋해도 된다.**

---

# 최종 게이트 (2차): 지도 조작·로드뷰 모달·종료 시각 20:30 — 2026-08-20

**대상**: `4656df8` 이후 변경분(사용자 요청 5건). 검증 요구 **1~66** 전건 + `content-rally-20260828.md` §6.8.
**방법**: **실제 렌더 대조**. `next dev` 기동 후 ① SSR HTML 텍스트 추출 ② 브라우저(Playwright)로 지도 로드·줌 조작·로드뷰 모달 개폐까지 실행해 **화면에 실제로 나온 문자열**을 기준값과 대조했다. 정적 grep 은 주석에 걸려 신뢰할 수 없어 보조로만 썼다.

## 판정: **게시 승인**

승인 원문과 실제 렌더가 **전건 일치**한다. 반려 0건, 조건부 0건.
아래 §6 에 **보존 요건 1건(요구 67)** 과 기록 사항 2건이 있으나, 셋 다 **현재 구현이 이미 충족**하고 있어 게시를 막지 않는다.

| 묶음 | 대상 요구 | 결과 |
|---|---|---|
| 종료 시각 20:30 | 9회차 57~66 | **전건 이행** |
| 범례 각주 이관 | 8회차 52~56 | **전건 이행** — 미검증 문구 `현장 안내판` 제거 확인 |
| 라벨 문구 이관 | 7회차 45~51 | **전건 이행** — 범례 무수정 **바이트 단위 입증** |
| ⑥ 여의도공원 입구 | 6회차 36~44 | **전건 이행** |
| 그 이전 | 4·5회차 1~35 | **전건 유지** |
| 로드뷰 모달 신규 문안 | (미게이트 8건) | **전건 승인** — §5 |

---

## 1. 종료 시각 `20:30` — 9회차 §9-9 기준값과 **문자 단위 일치**

`grep -Fx`(전행 완전일치)로 대조했다. **5건 전부 1건씩 정확히 검출**.

| 기준값 (9회차 §9-9) | 렌더 | 결과 |
|---|---|---|
| `<dt>` `코스콤지부` | 있음 | ✅ |
| `<dd>` `20:30까지 참가 계획` | 있음 | ✅ |
| 각주 1 `※ 20:30 은 코스콤지부의 참가 계획입니다. 주최측 식순상 폐회선언은 20:20~ 이며, 종료 시각은 안내되지 않았습니다.` | **완전일치** | ✅ |
| 각주 2 `※ 2차 출석은 20:00~21:00 입니다. 현장에서 위치가 확인돼야 하니 자리를 뜨기 전에 완료해 주세요.` | **완전일치** | ✅ |
| QR 카드 요구 15 문장 | **무수정** | ✅ |

### 렌더된 `<dl>` — 행 순서 요구 62 이행

```
집결        2026년 8월 28일(금) / 18:30
본대회      19:00 개회
코스콤지부   20:30까지 참가 계획      ← 신규
장소        국회의사당역 5번 출구 메인무대 앞
```

**`<dt>` 열만 훑으면 `집결 / 본대회 / 코스콤지부 / 장소`** 다. 9회차 §9-2 가 반려한 `종료` 라벨이 이 열에 없다 —
주어 상속으로 "주최측 행사가 20:30 에 끝난다"로 읽힐 경로가 **구조적으로 차단**됐다.

### 금지 표현 전수 — **0건**

| 검사 | 렌더 검출 |
|---|---|
| `종료 20:30` · `20:30 종료` | **0** |
| `해산` | **0** |
| `끝납니다` | **0** |
| `자리를 뜨면` (미검증 지오펜스 단정) | **0** |
| `20:00까지 오시면 됩니다`(요구 16) | **0** |

### 요구 63(블록 1 밖 전파 금지) — **완전 이행**

`20:30`·`20시 30` 사이트 전수:

| 경로 | 검출 |
|---|---|
| `/rally-2026-08-28` | **2건 — 둘 다 블록 1 안**(`<dd>` · 각주 1) |
| `/` (홈·`RallyEntryCard`) | **0** |
| `/bargaining-2026` | **0** |
| `/notices` · `/news` | **0** |
| `<meta name="description">` | **0** — `…18:30 국회의사당역 5번 출구 집결…` 무수정 |
| `struggleSchedule.ts` 8/28 | **0** — `서울 여의도 · 저녁` 무수정 |

홈 진입 카드도 `집결 18:30 · 국회의사당역 5번 출구` 그대로다(9회차 §9-9 기준값).

### 요구 61(위계) — `18:30` 이 **유일한 대형 수치**

렌더 HTML 에서 `text-hero` 클래스 **1개소**뿐이고 그 내용이 `18:30` 이다.
`20:30` 은 `text-body text-ink` 로 `19:00 개회` 와 **완전히 같은 급**이며, 각주 2 의 `20:00~21:00` 에
**색 강조가 붙지 않았다**(요구 13 유지 — 색 강조는 QR 카드 전용).

> **참고**: 각주 1 의 `종료 시각은 안내되지 않았습니다` 에 `종료` 가 들어 있으나 이는 **승인 문안 본체**다.
> 9회차가 금지한 것은 **라벨·주어 없는 종료 표기**이지 이 단어의 전면 금지가 아니다(§9-3-1 — 이 절을 빼면 `20:20` 이 종료 시각으로 굳는다).

---

## 2. 범례 각주 이관 (8회차 요구 52~56) — **전건 이행**

9회차 §9-4 에서 **미이행**으로 지적했던 건이다. 이번 묶음에서 반영됐다.

| 요구 | 렌더 확인 |
|---|---|
| 52 `현장 안내판` 삭제 | **`현장 안내판` 사이트 전수 0건** ✅ |
| 53 ⑥ 범례를 §8-4 확정안으로 교체 | **문자 단위 일치** ✅ |
| 54 각주에서 화장실 문장 제거, LED무대만 | ✅ |
| 55 범례 ②③④·키 줄 무수정 | ✅ §3 에서 입증 |
| 56 지도 라벨 `여의도공원 입구` 유지 | ✅ §4 |

**렌더된 ⑥ 범례 행** — 8회차 §8-4 확정안과 대조:

```
⑥ 여의도공원 입구 — 확인된 위치입니다. 화장실은 공원 안에 있으며, 개별 위치는
   확인되지 않아 지도에 표시하지 않았습니다 — 위 화장실 안내를 참고해 주세요.
```
→ **일치.** `확인된 위치입니다`(① 과 표기 정렬) · `개별 위치는`(무엇을 표시 안 했는지 특정) 둘 다 있다.

**렌더된 각주** — LED무대 한 문장만 남았다:

```
LED무대는 주최측 안내자료의 “기타 토의” 항목에만 나오고 배치가 논의 중이라 지도에 표시하지 않았습니다.
```
→ 종전 각주의 `화장실은 좌표가 확인되지 않아…` **0건**(⑥ 행으로 이관됨). 정보 손실 0, §0.4 유지.

### 범례는 지도 실패 시에도 렌더된다 — 확인

`figcaption`(범례)이 `status === "ready"` **바깥**에 있다(`RallyMap.tsx:1186`). 지도가 실패해도 대체면 아래에
범례 전체가 그대로 나온다. **⑥ 의 뜻과 화장실 안내가 어떤 상태에서도 사라지지 않는다.**

---

## 3. 라벨 문구 이관 (7회차 요구 45~51) — **범례 무수정을 바이트 단위로 입증**

요구 48·49 는 이관 승인의 **전제**이므로 눈으로 훑지 않고 **기계 대조**했다.
`4656df8` 판과 현재 판의 `legend` 문자열 전체를 추출·정렬해 `diff` 한 결과:

```
1a2
> "여의도공원 입구 — 확인된 위치입니다. 화장실은 공원 안에 있으며, …"
```

**추가 1행뿐이고 수정·삭제 0행.** `LEGEND_KEY` 도 `git diff` 상 변경 0.
→ **요구 48·49 충족. 7회차 45~47 승인이 유효하다.**

| 항목 | 렌더된 지도 라벨 | 요구 | 결과 |
|---|---|---|---|
| ② | **`메인무대(예정)`** | 47 — `(예정)` 필수, 완전 삭제 반려 | ✅ **`(예정)` 살아 있다** |
| ③ | **`대오 1`** | 45 | ✅ `labelSuffix` 전부 `""` |
| ④ | **`코스콤지부`** | 46 | ✅ |

② 는 렌더 지도에서 z16·z17·z18 **전 줌에서 `메인무대(예정)`** 로 확인됐다.
`메인무대(설치 예정)` 0건 / `메인무대` 단독 0건 — **7회차 조건부 승인선을 정확히 지켰다.**

---

## 4. ⑥ 도트 · 화장실 (6·8회차) — 이행. **줌 임계는 단축이 아니다**

| 요구 | 결과 |
|---|---|
| 37 좌표 `37.525530, 126.919669` | ✅ `PARK_ENTRANCE = { lat: 37.52553, lng: 126.919669 }` |
| 38 라벨에 `입구` 포함 · **`화장실` 단독 금지** | ✅ 전 줌에서 `화장실` 단독 표기 **0건** |
| 50 `여의도공원 입구` **단축 금지** | ✅ **문자열 온전** — §4-1 |
| 41 화장실 카드 거리 1줄 | ✅ `코스콤지부 집결 위치에서 여의도공원까지 약 80 m (도보 약 1분)` |
| 42 거리는 **대오 2 기준** | ✅ **`390` 사이트 전수 0건** |

화장실 카드 기존 3줄(4·5회차 승인 문안)도 한 글자도 안 바뀌었다.

### 4-1. ⑥ 라벨이 기본 화면(z16)에 안 보인다 — **검사한 뒤 승인한다**

렌더 지도에서 z16 캔버스 텍스트는 `① 5번 출구 / ② 메인무대(예정) / ③ 대오 1 / ④ 코스콤지부 / ⑤ / ⑥` 이다.
**⑤·⑥ 은 번호 배지만 나온다.** 요구 50(단축 금지) 위반인지 실제로 확대해 확인했다.

| 줌 | ⑥ 라벨 |
|---|---|
| z16 (기본) | 배지 `⑥` 만 |
| **z17** | **`여의도공원 입구`** (완전형) |
| **z18** | **`여의도공원 입구`** (완전형) |

→ **문자열이 잘린 것이 아니라 노출 줌 임계(`labelPriority: "tertiary"`)다.** 요구 50 은 *라벨 문구를 줄이지 마라*는 규정이고, 문구는 온전하다. **위반 아님.**

근거도 확인했다: 공원 입구는 대오 2 밴드 남동단에서 약 20 m 라 **z16 에서 화면상 약 10px** — ④ `코스콤지부` pill 과 사실상 같은 자리다. z16 에서 텍스트를 켜면 **④ 를 덮는다.** 겹침을 피한 선택이며,
**기본 화면에서도 ⑥ 의 뜻은 바로 아래 범례가 완전한 문장으로 말한다**(§2 — 범례는 접히지 않고 항상 렌더된다).
§0.4 은폐가 아니다. 7회차 §7-1 이 확정도 메타데이터의 범례 이관을 승인한 것과 **같은 구조**다.

> 덧붙여 **요구 38 의 취지(그 점에 화장실이 있다고 읽히는 것)는 오히려 더 안전해졌다** — 라벨이 없으면 화장실로 오독할 문자 자체가 없다.

---

## 5. 로드뷰 모달 — **문안 게이트 미통과 신규 문자열 8건, 전건 승인**

모달 전환으로 생긴 문자열을 전수 식별하고 **각각 사실 주장이 있는지** 판정했다.

| # | 신규 문자열 | 사실 주장 | 판정 |
|---|---|---|---|
| 1 | `축소` / `확대` | 없음 (조작어) | **승인** |
| 2 | `처음 위치로` | 기능 서술 — 동작 검증은 QA 소관 | **승인** |
| 3 | `닫기` | 없음 | **승인** |
| 4 | `로드뷰를 불러오는 중입니다.` | 상태 | **승인** |
| 5 | `이 위치의 로드뷰를 불러오지 못했습니다.` | 상태 | **승인** |
| 6 | `국회의사당역 5번 출구 로드뷰` (`aria-label`) | ★ **로드뷰 지점이 5번 출구다** | **확인** — §5-1 |
| 7 | `국회의사당역 5번 출구 주변을 볼 수 있습니다.` | ★ 동일 | **확인** — §5-1 |
| 8 | `로드뷰는 시각 자료입니다. 위치 안내는 페이지 본문 텍스트를 참고해 주세요.` | 없음 | **승인** — §0.4 이행 문장 |

**새 사실을 만든 문자열이 0건이다.** 6·7 만이 사실을 주장하고, 둘 다 코드로 확인된다.

### 5-1. 6·7 검증 — 파노라마 좌표가 실제로 5번 출구다

```
new Panorama(node, { position: new maps.LatLng(EXIT5.lat, EXIT5.lng), pov: { pan: 130, … } })
EXIT5 = { lat: 37.5282738, lng: 126.9172199 }   ← 4회차 승인 좌표
```
→ **문구와 좌표가 일치한다. 확인 등급.**

**부수 확인 — 초기 시야 방향이 맞다.** `pan: 130°` 는 "의사당대로 남동쪽(대오 방향)"이라는 주석의 값이다.
5번 출구에서 대오 1 밴드 남동단까지 실제 방위각을 계산하니 **131.5°** 였다(대권 방위각).
**1.5° 차이 — 초기 화면이 조합원이 걸어갈 방향을 정확히 향한다.**

### 5-2. §0.4 검사 — 모달이 지도를 숨기지 않는가

7회차·8회차가 반복해 확인한 은폐 위험이라 **실제로 열고 닫아 봤다.**

| 검사 | 결과 |
|---|---|
| SSR HTML 의 `<dialog>` 에 `open` 속성 | **없음** — 닫힌 상태로 시작. 스크린리더에 `닫기`·sr-only 문구가 새지 않는다 |
| 모달 열림 | `dialog.open === true`, `aria-label` 정확, **포커스가 `닫기` 로 이동** |
| 모달 안 표시 문자 | `로드뷰는 시각 자료입니다…` + `닫기` — **로드뷰에만 있는 정보 0** |
| 모달 닫은 뒤 지도 | **그대로 렌더돼 있다**(`코스콤지부` 라벨 확인) |
| 포커스 복귀 | **`로드뷰 보기` 버튼으로 복귀** |

→ **로드뷰가 지도를 대체하지 않는다.** 탭 패턴(§0.4 은폐)과의 유사성이 실제로 해소됐음을 렌더로 확인했다.
컨트롤 행은 `축소 / 확대 / 처음 위치로 / 내 위치 표시 / 로드뷰 보기` **5개**로, 상태와 무관하게 동일하다.

### 5-3. 기록 — 로드뷰는 과거 촬영본이다 (게시 저해 아님)

페이지는 로드뷰 촬영 시점을 밝히지 않는다. 그러나 ① 문구가 **무엇이 보인다고 주장하지 않고**
② 무대는 페이지 전체에서 `메인무대(예정)` · `설치될 예정입니다` 로 일관되게 미확정으로 표기된다.
조합원이 "로드뷰에 무대가 없으니 장소가 틀렸다"로 읽을 위험은 낮다. **반려하지 않는다. 기록만 남긴다.**

---

## 6. `[대오 2]` 잔존이 **1곳으로 줄었다** — 승인. 단 보존 요건을 명문화한다

이번 묶음에서 대오 식별자가 두 곳에서 제거됐다(사용자 결정 §21.6).

| 자리 | 4656df8 | 현재 |
|---|---|---|
| 본문 블록 2 | `더샵아일랜드파크 앞 의사당대로 · [결의대회대오 2]` | `더샵아일랜드파크 앞 의사당대로` |
| 지도 대체면 | `코스콤지부 — … 의사당대로 [대오 2]` | `코스콤지부 — … 의사당대로` |
| 지도 라벨 ④ | `코스콤지부 [대오 2]` | `코스콤지부` (7회차 승인) |
| **범례 ④ 행** | `코스콤지부 [대오 2] — …` | **그대로** |

**7회차 §7-3 이 ④ 라벨 단축을 승인한 근거는 "`[대오 2]` 가 같은 화면 안에 3곳 남는다"였다. 이제 1곳이다.**
전제가 바뀌었으므로 다시 판단했다.

- **판정: 승인 유지.** 렌더 전수 검사 결과 `대오 2` 는 **범례 ④ 행 1곳**에 있고, 그 범례는
  §2 에서 확인한 대로 **지도 성공·실패·스크립트 차단 어느 상태에서도 렌더된다.** 접히지도 않는다
- 현장 안내방송이 "대오 2"라 부를 때 조합원이 연결할 고리가 **화면에 항상 존재**한다 → 정보 손실 0
- 본문·대체면에서 빠진 것은 **위치 정보가 아니라 주최측 내부 식별자**이고, 도로·방향·거리·행동 지시는 전부 남아 있다

다만 **여유가 사라졌다.** 3중 중복이던 것이 단일점이 됐다. 요구 48(범례 무수정)이 이제 라벨 이관의 전제일 뿐 아니라 **정보 보존 요건까지 겸한다.** 명문화한다.

> **요구 67 [보존·신규]** 범례 ④ 행 `코스콤지부 [대오 2] — …` 은 **사이트 전체에서 `대오 2` 가 존재하는 유일한 자리**다.
> 이 행에서 `[대오 2]` 를 빼면 식별자가 **0곳**이 되어 현장 방송과 연결할 고리가 사라진다.
> 요구 48(범례 ②③④ 무수정)은 계속 유효하며, **이 행에 한해 사유가 하나 더 늘었다.**
> 산문(본문·대체면)에 되살리는 것은 사용자 결정(§21.6)에 반하므로 하지 않는다 — **범례에서 지우지 않는 것**으로 지킨다.

---

## 7. 금지 문자열 전수 — **전건 0**

렌더 텍스트 기준이다(주석 오검출 없음).

| 검사 | 검출 |
|---|---|
| `우측 도로` (요구 9) | **0** |
| `528세대` (요구 10) | **0** |
| `열렸` · `개최` · `성황` (`RALLY_PAST_NOTE` 원칙) | **0** |
| `결의대회대오` | **0** |
| `현장 안내판` (요구 52) | **0** |
| `화장실은 좌표가` (구 각주) | **0** |
| `(범위는 근사)` · `(설치 예정)` (요구 45·47) | **0** |
| `390` (요구 42) | **0** |
| **직선따옴표 `"` · `'`** | **0** — 곡선따옴표 `“ ”` 4곳만 |
| `18:00` 을 집결 시각으로 (요구 6) | **0** — 식순표 `18:00~18:30 장내 정리` 행에만 존재 |

### 확정도·인명 (요구 1~4) 유지 확인

| 항목 | 렌더 |
|---|---|
| 대회사 | **`윤석구 금융노조 위원장`** — 소속 병기 ✅ |
| 격려사 | **`김동명 한국노총 위원장`** — 소속 병기 ✅ |
| QR 배포 | `※ 출석 QR은 손피켓에 넣어 배포할 **예정**입니다. (주최측 안내자료 기준)` ✅ |
| 무대 | `메인무대는 국회의사당역 5번 출구 앞에 설치될 **예정**입니다.` ✅ |
| 식순 단서 | `※ 상황에 따라 식순 변경 가능` — 표 **위·아래 2회** 노출 ✅ |
| QR 이미지 `alt` | `주최측 배포 “QR 출석체크 안내” 원본 이미지 — 같은 내용이 위 텍스트에 있습니다` ✅ (요구 12) |

### 기존 데이터 정합 (4회차 §4)

`/bargaining-2026` 의 8/28 항목은 `서울 여의도 · 저녁` · `D-8` 로, 참석 안내의 `18:30`·`19:00 개회` 와 **충돌하지 않는다.**

---

## 8. 이번 게이트에서 검증하지 않은 것

| 항목 | 사유 |
|---|---|
| 핀치 확대·축소, 한 손가락 스크롤 계약, 포커스 트랩, 대비, 라벨 겹침 픽셀 | **QA 소관.** 19회차 회귀 통과 |
| `처음 위치로` 의 복귀 정확도 | 기능 검증 — QA 소관 |
| 로드뷰 파노라마 촬영 시점 | 네이버 제공 데이터. 우리가 주장하지 않는다(§5-3) |
| 여의도공원 화장실 2호(개나리) 개별 좌표 | **검증 불가 유지**(6회차 8경로 실패). 지도 개별 핀 계속 금지 |
| 주최측 행사의 실제 종료 시각 | **검증 불가.** 식순표 `20:20~` 열린 표기 — 게시하지 않는다 |
| 출석 지오펜스 반경 | **검증 불가.** `자리를 뜨면 출석 불가` 류 단정 계속 금지 |

## 9. 게시 후에도 유효한 제약 (배포 담당자 확인용)

1. **`20:30` 을 블록 1 밖으로 내보내지 마라**(요구 63) — 히어로·홈 카드·메타·달력
2. **8/28 경과 후 `참가 계획입니다` 를 과거형으로 바꾸지 마라**(요구 64)
3. **범례 ②③④·키 줄 무수정**(요구 48·49·**67**)
4. **`현장 안내판` 을 되살리지 마라**(요구 52) — 안내판의 존재도 화장실 표기도 검증된 바 없다
5. **각주에 화장실 문장을 다시 넣지 마라**(요구 54) — ⑥ 행과 이중이 되어 서로를 부정하는 것처럼 읽힌다
6. **지도 개별 화장실 핀 금지**(요구 36) · **여의도공원 전체 면 표시 금지**(요구 40)

