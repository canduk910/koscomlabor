# 최종 게이트 (2차 검증) — 8/28 결의대회 참석 안내 — 2026-08-21

**검증자**: verifier-zone3 · **대상**: 동결 빌드 `pTrFeUvhVlIo6KtN05Hgd`
**방법**: **소스가 아니라 렌더 산출물을 검증했다** — `.next/server/app/rally-2026-08-28.html`(SSR 본문 58,921 B)
+ RSC 페이로드 + 클라이언트 번들 `.next/static/chunks/*.js`(지도 라벨은 네이버 API 가 클라이언트에서 그리므로 SSR 에 없다).

> ⚠ **React 텍스트 노드 분할 처리**: SSR HTML 은 보간 지점에 `<!-- -->` 를 넣어 문장을 쪼갠다.
> 이 주석을 제거해 문장을 재결합한 뒤 대조했다. **이 처리를 하지 않으면
> `※ 급행은 국회의사당역에 서지 않습니다` 같은 정상 문장이 "없음"으로 잘못 잡힌다**(실제로 1차 시도에서 그랬다).

---

## 판정: **조건부 승인**

**조합원에게 나가는 콘텐츠는 전부 정확하다. 사실 오류·금지 표현·누락 0건.**
**조건은 1건이고 렌더 산출물을 바꾸지 않는 주석 추가다**(§F-3).

---

## A. 동결 검증

| 항목 | 기대 | 실측 | |
|---|---|---|---|
| `BUILD_ID` | `pTrFeUvhVlIo6KtN05Hgd` | 동일 | ✅ |
| `src/lib/rallyMap.ts` | `988038e3809e8ca1` | 동일 | ✅ |
| `src/components/rally/RallyMap.tsx` | `12e380a10ceb5575` | 동일 | ✅ |
| `src/app/rally-2026-08-28/page.tsx` | `0d89991a6e99e2ba` | 동일 | ✅ |

**검증 중 소스를 수정하지 않았다.**

## B. 항목 1·2 — 폐기·금지 문자열 **렌더 0건**

SSR 본문 + RSC 페이로드 + 클라이언트 번들 **전 범위** 대조.

| 문자열 | 결과 | 문자열 | 결과 |
|---|---|---|---|
| `확인 중` | ✅ 0 | `여의도공원 쪽`·`여의도공원 방향` | ✅ 0 |
| `논의 중` | ✅ 0 | `더샵` | ✅ 0 |
| `메인무대(예정)` | ✅ 0 | `배정되었습니다` | ✅ 0 |
| `메인무대는 이 앞에` (옛 ① 행) | ✅ 0 | `약 327` | ✅ 0 |
| `여의도공원 입구` | ✅ 0 | `약 30~100` · `30~170` | ✅ 0 |
| `LED무대는 주최측…`(옛 각주) | ✅ 0 | 치수 리터럴 `34.7`·`139.3`·`33.2`·`149.6`·`36×141` | ✅ 0 |
| `±5 m`·`±5m`·`±13 m`·`±13m` | ✅ 0 | 좌표 노출 | ✅ 0 |

### 오탐 2건 — **위반이 아니다. 근거를 남긴다**

| 검출 | 실제 | 판정 |
|---|---|---|
| 번들에 `확인 중` 1건 | **관리자 로그인 버튼**(`disabled ? "확인 중…" : "로그인"`, `2qh3tue2ic6v-.js`). **이 페이지와 무관한 컴포넌트** | ✅ 무관 |
| 본문에 `확정` 1건 | **② 범례의 `정확한 지점이 확정되지 않아`** — 금지 대상은 *확정 단정*이고 이것은 **정반대(불확실 선언)** 다 | ✅ 정상 |
| 본문에 `2호(개나리)` 1건 | **화장실 카드 1곳뿐**(`여의도공원 화장실 2호(개나리) 이용`). **지도 라벨·범례에는 0건** | ✅ **요구 153 준수** |

> ⚠ 리더 지시대로 **⑤ 범례의 `여의도공원 안 화장실입니다` 는 검사 대상에서 제외**했다.

## C. 항목 3 — 유지 문자열 **전건 존재**

| 문자열 | 렌더 |
|---|---|
| `코스콤지부는 집회 3구역에 배정될 예정입니다` | ✅ ×1 |
| `코스콤지부 — 집회 3구역(배정 예정)`(지도 대체면) | ✅ ×1 |
| `※ 지도의 구역 표시는 주최측 배치도를 옮긴 근사 위치입니다` | ✅ ×1 |
| `※ 현장에서 지부 깃발을 확인해 주세요` | ✅ ×1 |
| `※ 급행은 국회의사당역에 서지 않습니다. 일반열차를 이용해 주세요.` | ✅ ×2 (블록 1 · 지도 섹션) |
| `국회의사당역 5번 출구 메인무대 앞`(`<dl>` 장소, 원문 §1 축자) | ✅ ×1 **무수정** |
| 지도 라벨 `(예정)` 3종 — `무대 1(예정)` · `무대3(예정)` · `코스콤지부 3구역(예정)` | ✅ 클라이언트 번들 확인 |

**범례 5행 렌더 전문 확인** — 번호·기호·문안 전부 확정안과 일치:
`● ① … 무대 1은 이 앞에 설 예정입니다` / `○ ②` / `○ ③` / `▨ ④` / `◌ ⑤`
→ **요구 181(① 행 `메인무대` → `무대 1`) 반영 확인.**

## D. 항목 4 — 같은 문장 2회 출력 **0건**

| 문장 | 출현 |
|---|---|
| `코스콤지부는 집회 3구역에 배정될 예정입니다` | **1회** ✅ |
| `근사 위치입니다` | 1회 ✅ |
| `현장에서 지부 깃발을 확인해 주세요` | 1회 ✅ |
| **빈 `<p>`** | **0개** ✅ |

→ **요구 163-1(`pending`·`pendingOnMap` 상수째 제거 + DOM 제외) 이행 확인.**

> **무대3 문장 2회는 의도된 것이다** — 블록 2 `3구역 앞에는 무대3(LED)이 설치될 예정입니다.` ·
> 무대 카드 `코스콤지부 3구역 앞에는 …`(`STAGE3_SENTENCE_CARD`). **서로 다른 문자열**이고
> 6회차 원칙(*두 블록을 이어 읽지 않는다*)에 따른 요구 182 이행이다. **중복 출력이 아니다.**

## E. 항목 5·9 — 방위 단서 · 요구 191

| 항목 | 결과 |
|---|---|
| `3구역은 나란한 두 구역 중 북동쪽 — KDB산업은행 쪽입니다.` | ✅ 블록 2, **경로 문장 다음·무대 문장 앞** — 지정 위치 그대로 |
| `국회의사당역 5번 출구에서 **여의도** 의사당대로를 따라 남동쪽으로 약 250~400 m` | ✅ **요구 191 반영** |
| `<dl>` 장소 행 | ✅ **무수정**(원문 §1 축자 인용 유지) |

> 지도 대체면은 `여의도` 없이 `국회의사당역 5번 출구에서 남동쪽으로 약 250~400 m` 다.
> **결함 아님** — 요구 191 은 *"페이지에서 `여의도` 가 사라진 것"* 을 고치는 것이고 블록 2 가 그것을 진다.

## F. 항목 6·7·8 — 좌표 · 수치 · 파생 근거 대조

### F-1. 좌표 혼용 **0건**(요구 179) ✅

| 상수 | 값 | 대조 |
|---|---|---|
| `ZONE3` 4꼭짓점 | `37.526976,126.919658` / `37.525951,126.920623` / `37.525750,126.920284` / `37.526775,126.919318` | **§23-1 완전 일치** |
| `STAGE3` | `37.527039, 126.919331` | **§23-1 완전 일치** (옛 아핀값 `…007,…237` 아님) |
| `PARK_TOILET` | `37.525898, 126.920944` | OSM `node 12642249218`(`37.5258979,126.9209438`)의 6자리 반올림. **편차 < 0.05 m** |
| `EXIT5` | `37.5282738, 126.9172199` | 무변경 ✅ |

**A·B 두 변환의 값이 섞이지 않았다.** `kind:"band"` · `confidence:"estimated"` · `tone:"go"` · `textMode:"always"` 전부 확정안대로.

### F-2. 렌더 수치 **전수 재검산**(§25-4 표) ✅

**렌더 본문에서 `m` 단위 수치는 정확히 4건이다.** 채택 좌표로 다시 계산했다.

| 렌더 값 | 위치 | 채택 좌표 실측 | |
|---|---|---|---|
| `약 250~400 m` | 블록 2 | 폴리곤 최근접 **249 m** ~ 최원 꼭짓점 **396 m** | ✅ |
| `약 250~400 m` | 지도 대체면 | 동일 | ✅ |
| `약 30~60 m` | 화장실 카드 | 뒤쪽 변 **28.9 ~ 60.5 m** | ✅ |
| `약 170 m` | 화장실 카드 | 앞쪽 변 **165.0 ~ 173.4 m** | ✅ |

**그 밖에 좌표 파생 수치는 렌더되지 않는다.** 단일 수치 0건(요구 151 준수).

### F-3. ★ 요구 188② 첫 실행 — **주석 ↔ 렌더 값 대조**

**화장실 거리 (page.tsx:400 주석)** — **완전 일치** ✅

```
주석: 최단 28.9 ~ 최장 173.4 m / 뒤쪽 변 28.9~60.5 → `약 30~60 m`
      앞쪽 변 165.0~173.4 → `약 170 m` / 무대3 ↔ 앞쪽 22.9 · 뒤쪽 165.1
렌더: 구역 뒤쪽 끝에서 약 30~60 m · 무대3 쪽 끝에서는 약 170 m
```
내 독립 계산값(28.9 / 60.5 / 165.0 / 173.4 / 22.9 / 165.1)과도 **소수점까지 일치**한다.

**`약 250~400 m` — ⚠ 파생 근거 주석이 없다.** ← **이 게이트의 유일한 조건**

`page.tsx:266` · `RallyMap.tsx:2630` 두 곳 모두 **요구 188① 미이행**이다.
**값 자체는 정확하다**(F-2 에서 249~396 확인). 문제는 **근거가 기록돼 있지 않아 다음 좌표 변경 때 대조할 대상이 없다**는 것이다.
**`약 30~100 m` 오류가 살아남은 원인이 정확히 이것**이었다 — 요구 188 을 만든 이유다.

## G. 판정과 조건

### 조건 (1건) — **렌더 산출물을 바꾸지 않는다**

> **F-1. [요구 188① 이행]** `src/app/rally-2026-08-28/page.tsx:266` 과
> `src/components/rally/RallyMap.tsx:2630` 의 `약 250~400 m` 에 **파생 근거 주석**을 붙여라:
> ```
> 파생 근거(요구 188) — 채택 좌표 §23-1 기준
>   5번 출구 ↔ 3구역 : 폴리곤 최근접 249 m ~ 최원 꼭짓점 396 m
>   (앞쪽 변 중점 253 m · 중심 322 m · 뒤쪽 변 중점 392 m)
> ```
> **문자열은 한 글자도 바꾸지 마라.** 주석만 추가하므로 **렌더 HTML 이 불변**이고
> **QA 30회차 재실행이 불필요**하다.

### 승인 (조건 이행 시 즉시 게시 가능)

**콘텐츠 자체는 지금 상태로 정확하다.** 조건은 감사 추적 보강이며 조합원 노출 문구를 바꾸지 않는다.
**리더가 "먼저 배포하고 주석을 뒤에 붙인다"로 판단해도 사실관계상 문제가 없다** —
다만 그 경우 **요구 188 을 미결로 추적**해야 한다. 판단은 리더 몫이다.

## H. 수용된 스펙 이탈 1건 — **결함 아님**

| 항목 | 내 §20-6 판정 | 구현 | 판정 |
|---|---|---|---|
| ④ 3구역 `labelPriority` | `primary` | **`secondary`**(§30.16.2-2·§30.17.1 확정) | **수용** |

**근거**: `secondary` 는 `z ≥ 16` 에서 텍스트 pill 노출이고 **초기 화면이 z16 이므로 기본 화면에서 이름이 보인다.**
접힘은 **사용자가 z15 로 축소했을 때만** 일어나고 그때도 **배지가 남고 범례 ④ 행이 뜻을 설명**한다(정보 손실 0 · §0.4 저촉 아님).
② 메인무대가 같은 근거로 `secondary` 를 유지한 선례가 있다.
**결정적인 것은 `textMode: "always"` 이고 그것이 지켜졌다** — 내 §20-6 의 실제 요지가 그것이다.

## I. 검증 불가 / 이번 게이트의 한계

| 항목 | 상태 |
|---|---|
| **(b) 도면 자체의 정확도** | **검증 불가.** `근사 위치입니다`·`estimated`·`±20~30 m` 가 이 한계를 진다 |
| `3구역 배정` 확정 여부 | **`예정` 유지** — 배치도 원문 |
| 화장실 노드의 실재·명칭·운영시간 | **미확인.** 29 m 라 현장 즉시 판별. `2호(개나리)` 동일시는 문면에서 분리됨 |
| **브라우저 실렌더 확인** | **하지 않았다** — SSR HTML·번들 문자열 대조까지다. **화면 계측은 QA 30회차 소관**(통과) |
| 지도 라벨의 실제 표시 | **번들 존재까지만 확인.** 네이버 API 가 그리는 결과는 QA 육안 항목 |

## J. 이 게이트에서 새로 발견한 것

1. **`약 250~400 m` 파생 근거 주석 누락**(§F-3) — 조건 1건
2. **`확인 중`·`확정` 오탐 2건**(§B) — 다음 게이트가 같은 것을 결함으로 올리지 않도록 근거를 남긴다
3. **문안 대조 방법론**: SSR 은 보간 지점에 `<!-- -->` 를 넣어 문장을 쪼갠다.
   **주석 제거 후 대조하지 않으면 정상 문장이 "없음"으로 잡힌다.**
   `rallyMap.ts` 가 *"거리 문구는 완성된 문자열로"* 를 세운 이유가 바로 이것인데,
   **`WAYFINDING.station` 보간 문장(`※ 급행은 {station}에 서지 않습니다`)은 그 규칙 밖에 있다.**
   현재는 요구 155 로 무수정이라 문제되지 않으나, **최근접역이 바뀌는 날 이 문장은 grep 대조가 안 된다** →
   **다음 게이트는 반드시 주석 제거 후 대조하라.**

---

# 최종 게이트 — 라운드 2: 3단계-B 전체 화면 지도 복원 — 2026-08-21

**대상**: 동결 빌드 `hP2osMZPzOdaYEUP7HtUc` · **변경 파일 1개**(`RallyMap.tsx`)

## 판정: **통과** — 수정 요구 0건

---

## K. 방법 — 모달 내용을 어떻게 확인했는가 (리더 요청으로 기록)

**모달은 SSR 에 들어 있다.** `<dialog>` 는 닫힌 상태여도 마크업이 렌더되므로
`.next/server/app/rally-2026-08-28.html` 에서 **본문·`sr-only`·하단 바를 그대로 읽을 수 있었다**(`<dialog>` 2개 확인 — 로드뷰 + 전체 화면).
→ **클라이언트 DOM 열람이 불필요했다.**

**단 팝업 본문은 SSR 에 없다** — 네이버 지도가 클라이언트에서 그린다.
그래서 **팝업은 문자열 대조가 아니라 파생 경로 검증**으로 판정했다(§N).

⚠ **§J-3 함정 회피 적용** — `<!-- -->` 제거 후 대조했다.

### ★ 핵심 검증: **직전 통과 빌드와 렌더 텍스트 차분**

두 빌드의 렌더 본문을 정규화해 `difflib` 로 대조했다.

```
삭제 0줄 · 변경 0줄 · 추가 5줄
  + 지도는 시각 자료입니다. 기호의 뜻은 아래 줄에 있고, 지점 설명은 번호를 눌러 확인해 주세요.
  + 처음 위치로
  + 닫기
  + 실선 = 확인된 위치 · 점선 = 근사 · 옅은 면 = 범위 근사
  + ※ 지도의 번호를 누르면 각 지점 설명이 나옵니다.
```

**삭제 0 · 변경 0** → **직전 게이트 통과분이 한 글자도 바뀌지 않았음이 렌더 수준에서 증명된다.**
리더가 *"`page.tsx`·`rallyMap.ts` 변경 0이라 다시 볼 필요 없다"* 고 한 전제가 **해시가 아니라 산출물로 확인됐다.**
추가된 5줄이 이번 검증 대상의 전부다.

## L. 항목 1 — `LEGEND_KEY` 전문 (문안 게이트 74)

| | |
|---|---|
| 렌더 | `실선 = 확인된 위치 · 점선 = 근사 · 옅은 면 = 범위 근사` **×2**(페이지 범례 · 모달 하단 바) |
| 소스 | `RallyMap.tsx:2712` 이 **상수 `LEGEND_KEY` 를 그대로 참조**한다. 리터럴 복사·축약판 **0** |

✅ **요구 49·156 준수.** 축약판 없음.

## M. 항목 2 — 모달 `sr-only` 개정본 (문안 게이트 56)

| | |
|---|---|
| 개정본 | `지도는 시각 자료입니다. 기호의 뜻은 아래 줄에 있고, 지점 설명은 번호를 눌러 확인해 주세요.` **×1** ✅ |
| 폐기본 조각 `전체 안내는 페이지 본문의 범례에 있습니다` | **0건** ✅ |

✅ **거짓 진술 없음** — 키 줄이 실제로 모달 안에 있고(§L), 문장이 그것을 *"아래 줄"* 로 지시한다.

## N. 항목 3 — 팝업 본문 ↔ 페이지 범례 행 **문자 일치**

**이번 판정의 핵심 근거**다. 문자열 비교가 아니라 **파생 경로**로 판정했다 —
문자열이 두 벌 존재하지 않으므로 어긋날 수 있는 지점 자체가 없다.

| 검증 | 결과 |
|---|---|
| 팝업 컴포넌트 | **`MapPopupPanel` 단 하나**. 페이지 지도(`:1993`)와 전체 화면 모달(`:2677`)이 **같은 컴포넌트를 호출**한다 |
| 팝업 본문 출처 | `{feature.legend}`(`:973`) — **범례 행과 같은 필드** |
| 페이지 범례 행 출처 | `{circledNumber(index)} {feature.legend}`(`:2152`) — **같은 필드** |
| 배열 | 두 지도 모두 **`MAP_FEATURES` 를 같은 순서로 순회**(`:1271` · `:2448`), 팝업에 넘기는 index 도 **배열 index**(`:1480` · `:2522`) |
| 별도 문자열 상수 | **0개** (`grep "{feature.legend}"` → 1곳) |

✅ **번호·라벨·본문 3요소 전부 구조적으로 일치가 보장된다.**
→ **"모달에 범례 5행 불필요" 판정의 전제가 성립한다.**

> ⚠ **깨지는 조건을 남긴다**: 전체 화면 지도가 `MAP_FEATURES` 를 **필터링하거나 재정렬**하는 순간
> `circledNumber(index)` 가 갈리고 **모달 배지 ↔ 페이지 범례 번호가 어긋난다.**
> 현재는 두 지도가 같은 배열을 그대로 쓴다. **바뀌면 이 항목을 재검증하라.**

## O. 신규 렌더 문자열 3건 검토 — 전부 문제 없음

| 문자열 | 판정 |
|---|---|
| `※ 지도의 번호를 누르면 각 지점 설명이 나옵니다.` | ✅ **요구 87 승인 문구 그대로**(페이지와 동일). **모달에 반드시 있어야 하는 문장이다** — 범례 5행에 도달하는 경로를 지시하므로 §0.4 은폐가 성립하지 않게 만드는 주체다 |
| `처음 위치로` · `닫기` | ✅ UI 조작 레이블. **사실 주장 아님.** 오도 요소 없음 |

## P. 직전 게이트 조건 — **이행 확인. 종결**

라운드 1 의 유일한 조건(요구 188① 파생 근거 주석)이 **두 곳 모두 적용됐다**:
`page.tsx:267` · `RallyMap.tsx:2743` — 내가 제시한 문구 그대로이며
`★ 좌표가 바뀌면 이 값을 다시 재고 렌더 문자열과 대조하라` 한 줄이 **추가로 보강**됐다.
→ **요구 188 미결 항목 없음.**

## Q. 판정 사유가 아닌 것 (리더 지정 · 확인함)

| | |
|---|---|
| 모달에 범례 5행 없음 | **의도.** §N 으로 전제 성립 확인 |
| 모달 축척 z16 유지 | 설계 판정. 문안 검증 대상 아님 |

## R. 이번 라운드 검증 불가 / 범위 밖

| 항목 | 상태 |
|---|---|
| 모달의 **실제 화면 계측**(축척·라벨 겹침·포커스 트랩) | **QA 30-B 소관.** 이 게이트는 문안만 본다 |
| 팝업의 **클라이언트 렌더 결과** | 파생 경로까지 검증(§N). 실제 그려진 배지 대조는 QA 육안 항목 |
| `전체 화면` 버튼 레이블 | **SSR 본문에 없다**(클라이언트 조건부 또는 `aria-label`). 문안 3건에 포함되지 않아 판정하지 않았다 |

---

## S. ★ 라운드 2 보정 — **차분이 놓친 것 1건. 원인은 내 추출 방법이다**

리더 지적으로 재대조했다. **판정은 통과 유지**지만 **방법 결함 2건을 기록하고 고친다.**

### S-1. 세 갈래 중 어느 쪽인가 — **1과 2가 겹쳤다**

| 리더 선택지 | 답 |
|---|---|
| **2. 의도적으로 뺐는데 이름을 잘못 찾았다** | **맞다.** 나는 `전체 화면` 이라는 **내가 지어낸 이름**으로 검색했다. 실제 레이블은 `지도 크게 보기` 다. **결론(`SSR 본문에 없다`)은 우연히 맞았고 경로가 틀렸다.** **없을 것으로 예상한 문자열을 내 짐작 이름으로 찾는 것은 검증이 아니다** |
| **1. 차분에서 놓쳤다** | **맞다. 그리고 더 근본적이다** — 차분이 2번을 막아 줬어야 하는데 못 막았다. 원인 2건은 §S-2 |
| **3. 실제로 SSR 에 없다** | **버튼에 한해 사실.** 다만 **결함 아니다** — 지도가 준비된 뒤에만 나오는 컨트롤이고 **지도가 없으면 크게 볼 것도 없다**(그 자리는 대체면이 진다) |

### S-2. ★ 방법 결함 2건 — 다음 게이트가 반드시 반영할 것

| # | 결함 | 무엇을 못 보게 되나 |
|---|---|---|
| **1** | **태그 제거 정규식 `<[^>]+>` 가 속성 텍스트를 통째로 버린다** | `aria-label`·`title`·`alt` — **스크린리더 사용자에게는 이것이 본문이다.** 실제로 `<dialog aria-label="결의대회 위치 지도 크게 보기">` 가 내 차분에 **나올 수 없었다** |
| **2** | **SSR 차분은 클라이언트 전용 렌더를 못 본다** | 지도 준비 후에만 나오는 컨트롤 전부(`지도 크게 보기` · `지도 조작` 등). 플래그·상태 분기가 서버에서 `false` 면 문자열이 아예 없다 |

**고친 방법 — 이번 라운드에 실제로 적용했다:**

```
① 속성 텍스트 전수 추출:  aria-label · title · alt · aria-description · placeholder
② 변경 파일의 소스 문자열 전수 차분:  git show <배포커밋>:<file> vs 현재
   → 주석 제거 후 "  " · `  ` · >…< 에서 한글 포함 문자열을 모두 뽑아 집합 차분
   렌더 시점(서버/클라이언트)과 무관하게 **저작된 문자열의 변화**를 남김없이 잡는다
```

### S-3. 재대조 결과 — **놓친 것은 1건이고, 신규 저작이 아니다**

**① 신 빌드 SSR 의 속성 텍스트 전수 (5건)**

| 속성 | 값 | 판정 |
|---|---|---|
| `aria-label` | `결의대회 위치 지도 크게 보기` | **이번에 렌더되기 시작한 1건.** 다이얼로그 접근성 이름 — **정확하고 유용하다** ✅ |
| `aria-label` | `국회의사당역 5번 출구 로드뷰` | 기존 ✅ |
| `alt` | `주최측 배포 “QR 출석체크 안내” 원본 이미지 — 같은 내용이 위 텍스트에 있습니다` | 기존 승인분 ✅ |
| `alt` | `전국금융산업노동조합` · `코스콤` | 기존 ✅ |

**② 변경 파일(`RallyMap.tsx`) 소스 문자열 전수 차분 — `d37c749` → 현재**

```
추가 1 · 삭제 1
  + 지도는 시각 자료입니다. 기호의 뜻은 아래 줄에 있고, 지점 설명은 번호를 눌러 확인해 주세요.
  - 지도는 시각 자료입니다. 지점 설명은 번호를 눌러 확인하고, 전체 안내는 페이지 본문의 범례에 있습니다.
```

> ★ **이번 변경이 새로 저작한 사용자 문자열은 정확히 1건**이고 **그것이 §M 에서 검증한 문안 게이트 56 개정본**이다.
> `지도 크게 보기` · `결의대회 위치 지도 크게 보기` · `지도 조작` · `처음 위치로` · `LEGEND_KEY` · `※ 지도의 번호를…` 은
> **전부 `d37c749` 소스에 이미 있었고 플래그(`STAGE3B_FULLSCREEN_MAP: false`) 뒤에 있어 렌더되지 않던 것**이다.
> **플래그가 문자열을 만든 것이 아니라 드러낸 것이다.**
> (`git diff --stat`: 211 삽입 / 100 삭제 — 코드는 크게 바뀌었으나 **문안은 1건**이다.)

### S-4. `지도 크게 보기` 레이블 판정 — **오도하지 않는다. 무수정**

리더 지적: **§31.6 이 *"크게 보기가 축척을 올리지 않는다"* 고 명시**했다. 늘어나는 것은 도형이 아니라 **면적 1.86배의 맥락·조작 여유**다.

| 검토 | 판정 |
|---|---|
| 레이블이 **무엇을 약속하는가** | `크게 **보기**` — **보는 틀이 커진다**는 진술이다. `확대`·`자세히 보기` 가 아니다 |
| 실제로 일어나는 일 | 지도 영역이 박스에서 **뷰포트 전체**로(면적 1.86배). **약속과 일치한다** |
| 도형이 안 커지는 것이 거짓인가 | **아니다.** 레이블은 **지도**를 목적어로 삼고 **도형**을 목적어로 삼지 않는다 |
| 오도했다면 어떤 말이었을까 | `지도 확대` · `자세히 보기` · `크게 확대` — **셋 다 아니다** |

**잔여 위험과 그 완화**: *"크게 = 확대"* 로 읽는 조합원은 밴드가 그대로인 것에 잠시 갸웃할 수 있다.
그러나 **모달 안에 `+/−` 컨트롤이 있어 z16→z19 로 실제 확대가 가능**하고, 어포던스 문구가 함께 있다.
**레이블을 고치면 오히려 `확대` 를 약속하게 되어 §31.6 과 어긋난다.** → **무수정.**

### S-5. 낡은 주석 2건 — **리더 관측으로 기록**(내 발견 아니다)

플래그를 켜면서 그 옆 주석이 거짓이 됐다. **렌더되지 않으므로 이 게이트의 판정 대상은 아니다.**

| 위치 | 거짓이 된 내용 |
|---|---|
| `RallyMap.tsx:2045~2046` | *"QA-260 판정 대기 · `true` 로 되돌리면 복구된다"* — **판정은 끝났고(§31) 플래그는 이미 `true`** |
| `RallyMap.tsx:2040~2043` | *"4개 기준 폭 검산(280.0 / 264.4)은 무효다 · 되살아나면 다시 기준이 된다"* — **되살아났고 QA 실측이 정확히 그 값** |

**이 프로젝트가 반복해서 값을 치른 실패 방식이다**(요구 181 에서 *"소스 주석도 함께 갱신하라 — 안 고치면 다음 사람이 원복한다"* 로 이미 한 번 지적했다). 개발자 지시 대상.

### S-6. 판정

**라운드 2 판정은 통과 그대로다.** 놓친 1건(`aria-label`)을 검토한 결과 **정확하고 문제 없다**.
**바뀐 것은 판정이 아니라 방법이다** — §S-2 의 2단계를 **다음 게이트부터 표준으로 쓴다.**

---

## T. 계수 불일치 2건 — **원인 하나. `grep -c` 는 줄 수를 센다**

리더 관측(`지도 크게 보기` 1 · `실선 = 확인된 위치` 1)과 내 보고(`LEGEND_KEY` ×2)가 **같은 파일에서 갈렸다.**
동결 빌드 `hP2osMZPzOdaYEUP7HtUc` 의 프리렌더 HTML 로 실측했다.

```
파일 줄 수: 개행 없음 — 전체가 한 줄이다  →  grep -c 는 무엇을 세든 항상 1
```

| 문자열 | `grep -c`(줄) | **실제 출현(`grep -o | wc -l`)** |
|---|---|---|
| `실선 = 확인된 위치 · 점선 = 근사 · 옅은 면 = 범위 근사` | 1 | **2** |
| `닫기` | 1 | **2** |
| `지도 크게 보기` | 1 | **1** |
| `지도는 시각 자료입니다…` · `처음 위치로` | 1 | 1 |

### T-1. `LEGEND_KEY` — **내 ×2 가 맞다. "페이지 + 모달" 서술도 정확하다**

두 출현이 **같은 프리렌더 HTML 안**에 있고, 각각의 부모가 다르다:

```
① <figcaption class="mt-4"><p …>실선 = 확인된 위치 · 점선 = 근사 · 옅은 면 = 범위 근사</p>   ← 페이지 범례
② …safe-area-inset-right))"><p …>실선 = 확인된 위치 · 점선 = 근사 · 옅은 면 = 범위 근사</p>   ← 모달 하단 바
```

→ **리더 선택지 3번(모달 하단 바가 SSR 에 없다)은 아니다.** **SSR 근거로 성립한다.**
→ 선택지 1(서로 다른 산출물에서 왔다)도 아니다 — **둘 다 SSR 마크업**이다.

### T-2. `지도 크게 보기` — **리더 관측을 정정한다. 버튼은 SSR 에 없다**

**1건뿐이고 그것은 버튼이 아니라 속성이다:**

```
<dialog aria-label="결의대회 위치 지도 크게 보기" class="m-0 h-[100dvh]…
```

**버튼의 텍스트 노드(`children:"지도 크게 보기"`)는 프리렌더 HTML에 없고 클라이언트 번들에만 있다**
(`1i33zhytm5aqy.js`). 지도 준비 상태에 묶여 클라이언트에서 그려진다.

→ **§S-1 판정 유지**: **2번(내가 `전체 화면` 이라는 짐작 이름으로 찾았다)** + **1번(속성 텍스트를 버려 `aria-label` 을 못 봤다)**,
그리고 **3번도 버튼에 한해 사실**이다. **다만 결함은 아니다** — 지도가 없으면 크게 볼 것도 없고 그 자리는 대체면이 진다.

## U. 리더 질문 — **`렌더 DOM innerText 로만 판정` 을 적용했는가: 아니다**

**정직하게 답한다.** §J-3 에서 내가 세운 규율은 *"`<!-- -->` 제거 후 대조하라"* 였고 **innerText 가 아니다.**
이번 라운드에 쓴 경로는 **프리렌더 HTML 텍스트 추출**이며, **그것이 §S-2 의 두 구멍을 낳았다.**

### U-1. 프리렌더 HTML 을 계속 쓸 것인가 — **쓴다. 단 3계층으로 쓴다**

리더 지적대로 `.next/server/app/*.html` 은 **서버 없이 동결 산출물을 직접 보는 가장 가까운 대조 대상**이고
`<!-- -->` 도 그대로 있다. **버린다면 그 이점을 잃는다.** 구멍은 계층을 더해 메운다.

| 계층 | 무엇을 보나 | 도구 |
|---|---|---|
| ① 프리렌더 본문 텍스트 | 서버가 확정한 문안 | `<!-- -->` 제거 후 태그 제거 |
| ② **속성 텍스트** | `aria-label`·`title`·`alt` — **스크린리더 사용자의 본문** | 속성 정규식 전수 |
| ③ **소스 문자열 집합 차분** | **클라이언트 전용까지 전부** | `git show <배포커밋>:<file>` vs 현재 |

> **계수는 `grep -o | wc -l` 로 한다. `grep -c` 를 쓰지 마라** — 이 HTML 은 한 줄이라 **모든 값이 1로 보인다.**

### U-2. ★ innerText 를 안 쓰는 이유 — **문자열 게이트에는 ③이 더 강하다**

`innerText` 는 **그 순간 실행된 분기만** 보여준다. 플래그·상태·뷰포트에 따라 안 그려진 문자열은 **없는 것과 구분되지 않는다.**
③ 집합 차분은 **저작된 문자열 전부**를 보므로 **실행 여부와 무관**하다 — 이번에 `지도 크게 보기` 가 신규 저작이 아님을
증명한 것이 정확히 ③이다.

**innerText 가 필요한 판정은 따로 있다** — **배치·가림·순서·실제 표시 여부**. 그것은 **QA 소관**이고 30-B 가 한다.
**두 게이트가 다른 것을 보는 것이 맞고, 겹치면 둘 다 약해진다.**

---

## V. 라운드 2 마무리 — 두 가지를 남긴다

### V-1. 속성 텍스트 미검증의 소급 노출 — **없다. 확인했다**

리더 지적: *"문안 게이트가 시각 텍스트만 보고 있었다."* **맞다.** §S-2 이전의 모든 게이트는
`aria-label`·`title`·`alt` 를 **구조적으로 볼 수 없었다.**

**그러나 남은 노출은 없다** — §S-3 에서 **현행 동결 빌드의 속성 텍스트를 전수(5건) 열거하고 전부 판정했다.**
과거 게이트가 못 본 문자열들이 **지금 전부 검토된 상태**다. 소급 재검증 대상 0건.

| 속성 | 값 | 판정 |
|---|---|---|
| `aria-label` | `결의대회 위치 지도 크게 보기` | ✅ 정확 |
| `aria-label` | `국회의사당역 5번 출구 로드뷰` | ✅ 정확 |
| `alt` | `주최측 배포 “QR 출석체크 안내” 원본 이미지 — 같은 내용이 위 텍스트에 있습니다` | ✅ **§0.4 이행 문구까지 포함** |
| `alt` | `전국금융산업노동조합` · `코스콤` | ✅ 로고 대체텍스트 |

### V-2. ★ QA-30B 실패 건이 **§N 전제와 맞물린다** — 설계 판정 전에 남긴다

QA 30-B: *`내 위치` pill 이 ⑤ 화장실 배지를 59.8% 덮어 중심 탭을 가로채 **팝업이 안 열린다**.*

**이것은 조작 편의 문제로만 다루면 안 된다.** §N 에서 *"모달에 범례 5행을 넣지 않아도 되는 근거"* 가
**"팝업이 같은 문자열을 그대로 전달한다"** 였기 때문이다.

| 화면 | ⑤ 콘텐츠 도달 경로 | 팝업이 막히면 |
|---|---|---|
| **페이지 지도** | **범례 ⑤ 행이 지도 아래 상시 노출** + 팝업 | 범례가 남는다 → **§0.4 문제 없음** |
| **전체 화면 모달** | **팝업 하나뿐**(범례 5행 없음 — 의도) | **모달 안에서 ⑤ 콘텐츠가 도달 불가** |

→ **모달에서는 이 결함이 조작 실패가 아니라 §0.4 콘텐츠 은폐가 된다.**
**조건부다** — `내 위치` 를 **표시했을 때만** 발생하고 기본 상태에서는 나타나지 않는다.
그래도 **`내 위치` 는 조합원이 현장에서 자기 자리를 확인하려고 누르는 기능**이라 **실제 현장에서 켜져 있을 상황**이다.

**판정 요청이 아니라 근거 제공이다.** §32 순위 규칙을 정할 때
*"pill 이 배지를 덮어도 되는가"* 가 **모달에서는 다른 무게를 갖는다**는 사실을 넣어 달라.
(§N 의 깨짐 조건으로 이미 *"`MAP_FEATURES` 필터링·재정렬"* 을 적어 뒀는데, **이것은 두 번째 깨짐 경로**다 —
배열이 아니라 **탭 도달성**이 전제를 깬다.)

---

# 최종 게이트 — 라운드 3: 모달 z-order 정정 — 2026-08-21

**대상**: 동결 빌드 `WhtioYcl7R5I0Bf_JMFZj` · 변경 파일 1개(`RallyMap.tsx` `69ea00b3dd77e1b8`)
**동결 확인**: 3종 해시·`BUILD_ID` 전부 일치. 검증 중 소스 미수정.

## 판정: **통과** — 수정 요구 0건. **문자열 변화 0 확인**

---

## W. §S-2 3계층 적용 결과

### ⚠ 먼저 — **기준선을 우회했다. 근거를 남긴다**

리더 지시는 *"직전 게이트 통과분(`e40da6fa7d8cad48`)과 비교하라"* 였다.
**그 상태는 어떤 커밋에도 없다** — 라운드 2 동결은 **미커밋 작업 상태**였고 지금 작업본에 덮였다.
`git show` 로 꺼낼 수 없다.

**대신 전이(transitive)로 증명했다:**

```
라운드2:  strings(freeze2) − strings(d37c749) = { +개정본, −폐기본 }
라운드3:  strings(freeze3) − strings(d37c749) = { +개정본, −폐기본 }   ← 동일
  ⇒  strings(freeze3) = strings(freeze2)  ⇒  직전 통과분 대비 추가 0 · 삭제 0
```

**커밋된 공통 조상(`d37c749`)을 경유하면 사라진 기준선 없이 같은 결론에 도달한다.**

### 계층별 결과

| 계층 | 기준 | 결과 |
|---|---|---|
| **① 프리렌더 본문 텍스트** | 라운드 2 통과 빌드 추출본(`r2.txt`) | **변화 0줄** ✅ |
| **② 속성 텍스트 전수** | §S-3 의 5건 | **5건 동일** ✅ (`aria-label` 2 · `alt` 3) |
| **③ 소스 문자열 집합 차분** | `d37c749` | **추가 1 · 삭제 1 — 라운드 2와 완전 동일** ✅ |

> **리더가 지정한 기대값(`추가 0 · 삭제 0`)이 충족된다.** 범위 이탈 문자열 없음.
> 계수는 전부 `grep -o | wc -l`(§U-1). **`grep -c` 를 쓰지 않았다.**

**부수 관측(참고)**: `d37c749` 대비 `237 삽입 / 101 삭제`(라운드 2 는 `211 / 100`).
→ **이번 라운드 순증은 삭제 1 · 추가 26 줄**이고 **문자열은 0** 이다 —
*"z-order 한 줄 + 근거 주석"* 이라는 지시와 정합한다.

## X. QA-389 §0.4 판단 — **QA 등급이 맞다. 은폐 아니다**

> ④ 팝업이 열린 상태에서 그 패널이 ② 마커 히트를 94.1% 가져간다.

### X-1. §V-2 와 무엇이 다른가 — **판정선을 세운다**

| | **§V-2 (이전, 은폐)** | **QA-389 (이번, 조작 결함)** |
|---|---|---|
| 원인 상태 | `내 위치` 표시 — **덮는다는 인과를 조합원이 알 수 없다** | **조합원이 방금 스스로 연 팝업** |
| 모달 안 도달 경로 | **0** — ⑤ 팝업이 아예 안 열린다 | **있다** — 팝업을 닫으면 ② 가 눌린다 |
| 회피 수단이 화면 안에 있나 | **없다**(겹침의 원인을 특정해야 한다) | **3개 전부 모달 안**: `닫기` 버튼(`:1001`) · `Esc`(`:1486`) · **지도 빈 곳 클릭**(`:1458`) |

```
★ 모달 안 §0.4 판정선
   도달 경로가 모달 안에서 0 이 되면        → 은폐(§0.4)
   사용자가 되돌릴 수 있는 자기 행동의 결과이고
   되돌리는 수단이 화면 안에 있으면        → 조작 결함
```

**QA-389 는 아래에 해당한다.** → **§0.4 아님. 배포 후 처리에 동의한다.**

### X-2. 내가 쓰지 않은 논거 — 일관성을 위해 밝힌다

**"모달을 닫고 페이지 범례 ② 행을 보면 된다"를 근거로 쓰지 않았다.**
그 논거를 허용하면 **모달 안 어떤 은폐도 정당화**되고, §V-2 에서 내가 쓰지 않은 논거다.
**판정은 모달 안에서만 따졌다.**

### X-3. 기록해 둘 잔여 사실

**팝업이 열려 있는 동안 조합원은 ② 가 거기 있다는 것 자체를 보지 못한다**(94.1% 가림).
실제 위험은 낮다 — ④ 팝업을 연 목적은 **자기 구역 정보**이고, 다른 지점을 찾는 것은 **다음 행동**이라
그때 팝업을 닫는 것이 자연스러운 동선이다.
**다만 후속에서 볼 때의 승격 조건을 남긴다:**

> **닫기 수단(3개)이 하나라도 줄거나, 팝업이 다른 배지를 덮은 채 자동으로 안 닫히는 경로가 생기면
> 이 판정을 다시 하라.** X-1 판정선의 오른쪽 칸이 무너진다.

### X-4. §V-2 후속 — **전제 성립 확인 접수**

QA 388 이 **5점 × ①~⑤ = 25건 전부 열림**을 확인했고, 직전에 막혔던 3구역 북서단 ⑤ 도 열린다.
→ **§N 의 전제(모달에서 팝업 도달성이 §0.4 이행의 유일한 수단)가 실측으로 성립한다.**
§35 가 그 전제를 §31.2 에 명문화하고 2-C 를 *"지도 위에 놓이는 어떤 요소도"* 로 넓힌 것까지 확인했다.
**§N 의 두 깨짐 경로(배열 재정렬 · 탭 도달성)는 그대로 유효하다.**
