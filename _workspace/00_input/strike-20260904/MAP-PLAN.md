# 9/4 총파업 안내지도 — 사용자 계획 (2026-08-29)

## 사용자 지시 (축자)

> 이미지를 바탕으로 결의대회처럼 네이버지도 API기반의 안내지도만들어보자.

> 미리 구역 4개를 네이버지도에 넣어놓고 나중에 우리 구역이 정해지면
> 우리 구역은 "이 중 x구역이다."를 추가로 반영할 계획이야.

## 이 지시가 확정하는 것

### ① 지도의 목적이 **2단계**다
| 단계 | 지도가 답하는 질문 | 시점 |
|---|---|---|
| **지금** | *"세종대로 어디에 무엇이 있나"* — 무대 4 · 대오 4 · 화장실 3 · 버스 하차 2 | 이번 라운드 |
| **나중** | *"그중 우리는 어디인가"* — **«이 중 x구역이다»** | 코스콤 위치 확정 후 |

→ ★ **1단계 지도가 2단계를 «수용할 수 있는 구조»여야 한다.**
  나중에 «우리 구역» 이 들어올 때 **지도를 다시 짜지 않아도 되게** 한다.
  ⚠ 그렇다고 **지금 빈 자리를 만들어 두라는 뜻이 아니다**(D-1 과 같은 정신) —
  «어디에 무엇을 얹을지» 를 **주석에 남기고**, 화면에는 아무것도 없다.

### ② 대오 4개를 **전부** 넣는다
8/28 지도처럼 «우리 자리 하나»만 가리키는 것이 아니다. **네 개를 대등하게** 그린다.
→ 그래서 **«강조 하나» 가 없다.** 이것이 8/28 지도와 다른 시각 과제다.
→ 2단계에서 **그중 하나가 강조로 올라간다** — 그 전환이 자연스러워야 한다.

---

## ⚠⚠ 리더가 지목하는 위험 — 검증·설계가 반드시 판정할 것

### 위험 1 — **«구역» 과 «대오» 가 다른 말이다**
- **9/4 원문**: `대오 1` · `대오 2` · `대오 3` · `대오 4` (+ `메인무대`·`무대 2`·`무대 3`·`무대 4`)
- **사용자 지시**: *"미리 **구역** 4개"*, *"이 중 **x구역**이다"*
- **8/28 페이지**: `집회 **1·2·3구역**` 이라는 말을 **이미 썼다** (지금도 살아 있다)

→ ★★ **조합원이 8/28 의 «3구역» 과 9/4 의 «대오 3» 을 같은 체계로 읽을 위험**이 있다.
  **두 집회는 장소도(여의도 ↔ 세종대로) 구획도 다르다.**
→ **판정 사항**: 화면 문면을 «대오»(원문 축자)로 갈 것인가, 사용자 낱말인 «구역»으로 갈 것인가.
  ⚠ 사용자 지정 문면은 바꾸지 않는 것이 이 프로젝트 규율이지만,
  **위 지시는 «계획» 을 말한 것이지 «화면 문면» 을 지정한 것이 아닐 수 있다.**
  → **리더가 사용자에게 물어야 한다.**

### 위험 2 — **좌표는 «우리 추정» 이다**
원본(`design/IMG_6833.JPG`)은 **네이버 지도 스크린샷 위에 도형을 얹은 이미지**다. 좌표 데이터가 아니다.
→ 무대·대오를 실지도에 올리려면 **랜드마크 기준으로 픽셀→좌표 환산**을 해야 하고, 그것은 **주최측 값이 아니다.**
→ 이 프로젝트에 **좌표 확신도(confidence) 체계**가 이미 있다(`src/lib/rallyMap.ts`).
   **어느 확신도가 되는지, 그 확신도로 게시가 가능한지**를 검증이 판정한다.
⚠ **축척 왕복 3회 사고** 이력이 있다.

### 위험 3 — **대오의 «경계» 는 원본에도 없다**
원본은 노란 띠 위에 `대오 1`~`대오 4` 라벨을 얹은 것이다.
**띠의 시작·끝이 어디인지 도로 위 지점으로 확정되어 있지 않다.**
→ 우리가 그리는 «구역 4개» 의 **경계선은 우리가 만든 것**이 된다.
→ ★ 조합원이 그 경계를 **정확한 것으로 읽으면** 현장에서 «여기가 3구역인가 4구역인가» 를 우리 지도로 판단한다.
   **판정 사항**: 경계를 **선으로 그릴 것인가, 순서만 보일 것인가.**

### 위험 4 — 2단계에서 **«이 중 x구역이다» 가 지도와 본문 두 곳에 생긴다**
→ §5.6(같은 사실을 두 확정도로 말하지 않기) · 요구 88(한 출처) 대상이다.
→ **지금 구조를 잡을 때 «그 문장이 어디에 사는가» 를 정해 둔다.**

---

# 리더 판정 M-1 ~ M-8 (2026-08-29 · 사용자 답변 4건 반영)

> **이 절이 지도 작업의 확정본이다.** `LEADER-DECISIONS.md` D-1~D-31 과 함께 읽되,
> **충돌하면 아래 M-3 이 지목하는 대로** 처리한다.

## M-1. 낱말 — **원문 축자 「대오」** (사용자 선택)
지도 라벨·본문·`alt` 전부 **`메인무대` · `대오 1` · `무대 2` · `대오 2` · `무대 3` · `대오 3` · `무대 4` · `대오 4`**.
⚠ **「구역」으로 바꾸지 마라.** 8/28 페이지가 **`집회 1·2·3구역`(여의도)** 을 **지금도 쓰고 있어**
같은 낱말이면 **장소도 구획도 다른 두 집회가 섞인다.**
⚠ 사용자가 계획을 말할 때 쓴 낱말이 「구역」이었으나 **화면 문면으로는 「대오」를 지정했다.**

## M-2. 경계 — **띠로 그리되 「근사」로 표시** (사용자 선택)
기존 확신도 체계를 그대로 쓴다: **`estimated`** → 짧은 점선 · 옅은 면 · 범례 `점선 = 근사 · 옅은 면 = 범위 근사`.
⚠⚠ **`estimated` 를 승격하지 마라.** 8/28 은 **주최측 규격 박스의 복제**라는 승격 근거가 있었는데
**9/4 원본에는 치수 표기가 0** 이다 — **근거가 하나 더 약하다.**
⚠ **색은 확신도가 아니라 `tone` 이 전담한다.** `estimated` 를 회색으로 바꾸지 마라.

## M-3. ★ **D-1 을 개정한다** — 「코스콤지부 대오」 한 줄을 «넣는다» (사용자 선택)

**D-1 의 «코스콤지부 위치·대오·천막을 페이지에 한 줄도 넣지 않는다» 는 죽었다. 인용하지 마라.**

**왜 뒤집혔는가 — 상황이 바뀌었다:**
| | D-1 시점 (문자 페이지) | 지금 (지도) |
|---|---|---|
| 화면에 대오가 | **안 보였다** | **4개가 보인다** |
| 침묵하면 | 조합원이 지부 깃발을 찾는다(원래 행동) | **조합원이 «내 대오» 를 임의로 고른다** |

→ **확정 문면(검증자가 최종 확정한다 · 초안):** `코스콤지부 대오는 추후 안내합니다.`
→ ★ **QR 한 줄(D-4·D-10)과 같은 계보다** — «사실은 있고 세부가 없다».
   §52-3 이 «코스콤 위치는 사실 자체가 없어 침묵» 이라고 갈랐는데, **지도가 대오 4개를 보여 주는 순간
   «넷 중 하나» 라는 사실이 화면에 생긴다.** 그래서 계보가 바뀌었다.
⚠ **A 유형 금지어를 쓰지 마라**: `확인 중` · `확인되는 대로` · `반영하겠습니다` · `준비 중` · `업데이트 예정`.
⚠ **`지부 공지로` 를 쓰지 마라**(D-11) — 사용자가 *"추후 **추가**할게"* 라 **이 페이지에 추가된다.**

**2단계 전환 지점**: 위치가 정해지면 **이 한 줄이 그 자리에서 바뀐다**(새 자리를 만들지 않는다).
초안: `코스콤지부는 대오 N 입니다.` — **문면은 그때 사용자에게 받는다.**

## M-4. 원본 이미지 — **게시하지 않는다** (사용자 선택)
`design/IMG_6833.JPG` 는 **대조용**이다.
⚠ 근거가 둘이다: ① **바탕이 상용 지도 서비스 화면 캡처**라 그대로 올리면 저작권 문제
② 네이버 지도와 **같은 사실을 두 번** 말하게 된다.

## M-5. ★★ `RallyMap` 을 재사용하지 마라 — **치명적 거짓 게시 경로**
`RallyMap.tsx:4335·4344·4348` 의 **대체면(fallback) 3줄이 통짜 문자열**이다:
`국회의사당역 3번 출구 KDB산업은행 앞` · `코스콤지부 — 집회 3구역`.
→ **키 미설정·401·로드 실패 시 9/4 페이지가 «여의도»를 안내한다.**
→ 4,353줄이고 `FIT_PADDING`·`FIT_MAX_ZOOM`·`aspect-[4/5]` 가 **전부 여의도 기하에서 유도**됐다.
→ **새 경량 컴포넌트를 짠다.** 재사용할 것은 **파일이 아니라 «패턴»**:
  로더 4중 실패 경로(`onError` / `navermap_authFailure` / 8초 타임아웃 / 이미 로드된 채 마운트) ·
  생성·파괴 동일 effect · `figure`+`figcaption` 골격 · `aria-owns` 로빙 그룹.
★ **선 규칙이 이미 서 있다**(`strike.ts:12-18`): **행사 중립이면 공유, 행사 정체성을 지니면 복제.**

## M-6. 네이버 키 조건부 — **필수**
`NEXT_PUBLIC_NAVER_MAP_CLIENT_ID` 미설정이면 **지도 블록을 렌더하지 않는다**(8/28 과 같은 처리).
⚠ 근거: 401 결과에 대해 개발자 기록(«대체면 정상»)과 QA 기록(«페이지 통째 백지»)이 **충돌**하고
**어느 쪽도 재현되지 않았다(미확인).** 최악이 «백지» 라면 이 조건부는
**이미 게시된 문자 정보(개요·집결시간·식순)를 지키는 방화벽**이다.
⚠ **죽은 어포던스 금지** — 키가 없을 때 «헤딩 + 영구 실패 박스» 만 남기지 마라.

## M-7. 세로 예산 — **삽입 «전»에 재측정한다**
현재 여유: `upcoming` 334.52 / **`today` 361.52**(판정선 640 · 여유 **278.48**).
**지도 블록이 이 여유를 먹는다.** ⚠ **런타임 주입 프로토타입으로 세로를 확정하지 마라**(`union-qa-testing §5.9.3`).
⚠ **삽입 지점 A(참석 예비조사)가 아직 남아 있다** — 지도가 여유를 다 먹으면 그것이 못 들어온다.

## M-8. 이미지 안에만 있던 정보 — **판정 사항**
원본 이미지에는 **머리글**(`- 세부 무대 및 간이화장실 위치`)과 **캡션**
(`※ 대형버스 주차장은 남산 주차장과 청파치안센터 인근 도로 주차`)이 **이미지 안에** 있었다.
→ **이미지를 안 쓰기로 했으므로(M-4) 이 정보는 어디에도 없다.**
→ ★ **대형버스 주차**는 전세버스·자가용 이동 판단을 가른다 — **§5.3 «행동이 갈리는 사실»** 후보다.
→ **검증자가 게시 여부와 문면을 판정한다.**

---

# 리더 판정 M-9 ~ M-12 (2026-08-29 · 검증 §53 수령 후)

## M-9. ★★ §53-4 대오 띠 동서 위치 — **C 안 채택** (폭 유지 + 차도 중심 정렬)

**A(원본 충실) / B(차도 전폭) / C(폭 26 m 유지 · 차도 중심 정렬)** 중 **C.**

**A 를 버리는 근거 — «틀린 것을 알면서 게시하게 된다»:**
원본 띠는 **38% 만 차도 위**이고 나머지는 서쪽 보도·건물이다. 편의는 **전 구간 19~23 m 로 일정**해
**국소 오차가 아니라 계통**이고, **같은 모델이 같은 자리의 횡단보도를 0.5 m 로 맞춘다**(§53-3).
→ **우리 환산 오차가 아니라 원본 작성자의 작도 오차임이 확정됐다.**
→ 그대로 그리면 조합원이 **«여기 서라» 로 읽고 인도에 선다.** 「장소: 세종대로」(원문 1급)와도 어긋난다.

**B 를 버리는 근거**: 원본에 **폭 치수가 0** 이다. 26 → 35 m 로 넓히면 **없는 주장을 만든다.**

**C 의 성격 — 반드시 주석에 남긴다(§53-15 조건 5):**
> ★ **C 는 «원본을 옮긴 것» 이 아니라 «우리가 옮긴 것» 이다.**
> **원본 픽셀값(415.5~497.6) · 환산 경도(126.976712–126.977061) · 조정 후 경도** 를
> **셋 다** 소스 주석에 적는다. 안 적으면 다음 사람이 **원본과 대조하다 «틀렸다» 고 되돌린다.**

⚠ **«우리가 옮겼다» 를 화면 문장으로 만들지 마라.** `estimated` 표현(점선·옅은 면·범례 «근사»)이
이미 그 역할을 한다 — 문장을 더하면 §5.3 위반이고 **우리 작업 사정 서술**이 된다.

## M-10. 문면 — 검증 §53 확정본을 **그대로** 채택

| 자리 | 확정 문면 |
|---|---|
| **코스콤 대오 한 줄** | **`코스콤지부가 어느 대오인지는 추후 안내합니다.`** |
| 지도 `sr-only` | `아래는 무대·대오 위치를 표시한 지도입니다. 같은 내용을 지도 아래 범례에서 텍스트로 제공합니다.` |
| 섹션 헤딩 | **`세종대로 안내지도`** (검증 후보 중 채택 — 범위를 안 좁힌다) |
| 범례 11행 | §53-11 (3) 표 **축자** |

★ **초안 `코스콤지부 대오는 추후 안내합니다.` 는 죽었다.** 지도 라벨이 `대오 1`~`대오 4` 인데
`코스콤지부 대오` 가 **다섯 번째 것**으로 읽힌다. 확정 문면은 **번호 붙은 넷 중 하나**임을 문법으로 못 박는다.

★ **자리: 지도 `figure` «안» · 캔버스 «밖»**(§53-9 (5)). 범례 행 금지 · 지도 밖 본문 금지.
⚠ **키가 없어 지도가 사라지면 이 문장도 함께 사라지는 것이 «의도된 상태» 임을 주석에 적어라** —
안 적으면 다음 사람이 «사라지는 버그» 로 보고 지도 밖으로 뺀다.

⚠ **`무대 4 — 대오 3 남쪽입니다` 에 «끝» 을 넣지 마라** — 대오 3 남단↔무대 4 가 **75 m**(다른 무대는 20~30 m)라
«바로 남쪽» 이라고 말할 수 없다. **한 글자가 사실 주장이다.**

## M-11. 사용자 질문 2건 — **기본값으로 가고 보고한다**(게시를 막지 않는다)

1. **전세버스 운행 여부** → 미확인. **원문 캡션은 미게시 유지**(§53-10 (2)).
   확인되면 «주차» 가 아니라 **승·하차 지점과 시각**을 지부 전용 안내로 새로 받는다.
2. **시청역 출구** → 원본에 **출구 배지가 없다.** → **출구 번호를 쓰지 않고 «시청역» 으로만** 안내한다.
   ⚠ 광화문역은 원본에 **⑤⑥ 배지가 있어** 안내 가능하다 — **두 역을 대칭으로 쓰려 하지 마라.**

## M-12. `doc-commit` 대상 2건 — 이번 라운드가 만든 규율

1. ★★ **«북업 웹메르카토르는 등척이 참값» 을 파일에 무조건 인용하지 마라.**
   8/28 은 아핀의 이방성을 **13.1σ 로 기각**하고 등척을 채택했는데, **9/4 원본은 등척이 96.3σ 로 기각**된다
   (A4 문서에 붙이며 **가로로 약 1.56배 늘어났다**). **지도 서비스가 등척이어도 «그 파일» 은 아닐 수 있다.**
   → 인용했으면 **잔차 RMS 70 m · 최대 102 m** 짜리 좌표를 냈다.
   ★ **판별법**: 렌더된 **원형 POI 아이콘의 종횡비**를 재라(2차 모멘트) — 8/28 과 **같은 도구, 반대 결론**.
2. **아핀의 여분 자유도는 두 라운드 연속으로 노이즈를 먹었다** — 잔차는 좋아지는데(10.7→8.7)
   **실재하지 않는 8.07° 전단**을 만든다. **아이콘 주축각(−0.202°±0.074°)이 112σ 로 잘랐다.**
   → **잔차만 보고 모델을 고르지 마라. 독립 관측량으로 잘라라.**

## M-13. ★ 사용자 확인 — **지부 전세버스 없음** (2026-08-29)

**사용자 축자:**
> 지부에서는 별도 버스운행은 없어. 알아서들 오는거야. 주차장 안내는 없어.

### 이것이 «닫는» 것 — 근거가 바뀐다

**⚠ §53-8 #1 의 «버스 하차를 안 그리는 근거» 를 다시 쓴다.**

| | 종전 근거 | **지금 근거** |
|---|---|---|
| 버스 하차 2곳 미표시 | «원본 태그에 앵커가 없어 **점 좌표를 못 구한다**» (기술적) | ★ **«코스콤지부는 전세버스를 운행하지 않는다. 조합원이 그 지점을 쓸 일이 없다»** (사실 · 1급) |
| 대형버스 주차 미게시 | §5.3 «대상이 조합원이 아니다» (추론) | ★ **«지부가 버스를 운행하지 않는다»** (사실 · 1급) |

★ **두 근거가 다 살아 있지만 강도가 다르다.** 종전 근거는 *"좌표를 구하면 그릴 수 있다"* 로 읽히는데,
새 근거는 **좌표를 구해도 안 그린다**를 말한다. **근거 하나가 죽어도 판정이 유지되도록 둘 다 적는다.**
⚠ 다음 사람이 *"앵커를 찾았으니 이제 그리자"* 로 가지 않게 **이 절을 지목하라.**

### 이것이 «여는» 것 — 조합원은 **각자 대중교통으로 온다**

→ ★ **역 위치의 중요도가 올라간다.** 이제 지도의 실질 기능이 하나 더 늘었다:
  *"세종대로 어느 역에서 내려 어느 쪽으로 걸을 것인가."*

**판정:**
- **광화문역 · 시청역의 «역 위치» 를 지도에 표시한다.** 조합원이 어느 역이 어느 끝인지 알아야 한다.
- ⚠ **출구 번호는 안내하지 않는다**(M-11 유지 · 근거 강화):
  - 광화문역은 원본에 **⑤⑥ 배지**가 있으나 **시청역은 노선 배지(①②)뿐이고 출구 배지가 없다.**
  - **두 역을 대칭으로 쓰려고 시청역 출구를 우리가 고르지 마라** — 원문에 없다.
  - ★ 한쪽만 출구를 쓰면 조합원이 *"시청역은 아무 데나 나와도 되나"* 로 읽는다.
    **둘 다 «역» 까지만 말하는 것이 일관된다.**
- ⚠ **`RallyMap` 의 `국회의사당역 3번 출구` 류를 복제하지 마라**(M-5) — 8/28 은 출구가 확정돼 있었다.

### 미게시 확정 (근거가 «판정 대기» 에서 «사실» 로 바뀌었다)
`남산` · `청파` · `대형버스` · `주차장` · `버스 하차` — **전부 기대 0**(§53-12 #7·#10 유지).
→ **FOLLOWUPS #27 의 «대형버스 주차는 판정 안 됨» 도 이 절로 닫힌다.**

---

# 리더 판정 M-14 ~ M-17 (2026-08-29 · 디자인 §54 수령 후)

## M-14. ★★ 지도 조작 — **드래그·확대 허용** (사용자 선택 · 디자이너 §54.15-10 권고를 뒤집는다)

**사용자 선택: «8/28처럼 드래그·확대 허용».**
→ ⚠⚠ **§54.0-3 «고정 뷰 지도다. 드래그·휠·핀치·키보드 이동·확대 버튼 전부 없음» 은 죽었다. 인용하지 마라.**
→ **§54.3(«움직이지 않는 지도») 전체가 재작성 대상이다.**

**디자이너 권고 근거는 타당했다** — *"±25 m 좌표를 확대하면 없는 정밀도를 주장한다."*
**사용자가 그 위험을 고지받고 «결의대회처럼» 을 선택했다.** 사용자 결정이므로 따른다.

### 그래서 «딸려 오는 것» — 디자이너가 재판정할 것

1. ★ **없는 정밀도 주장을 무엇이 막는가.** 고정 뷰가 하던 일을 **다른 수단이 져야** 한다.
   - **점선·옅은 면(`estimated`)이 모든 줌 배율에서 유지되는가** — 이것이 1차 방어다.
   - **범례 `점선 = 근사 · 옅은 면 = 범위 근사` 가 확대 상태에서도 화면에 있는가.**
   - ⚠ **`FIT_MAX_ZOOM`(확대 상한)으로 막으려 하지 마라.**
     8/28 에서 이 값이 **3왕복**했고, QA 권고를 그대로 따랐으면 **조합원이 확대를 못 하게 됐다.**
     **확대 상한은 «정밀도 주장» 이 아니라 «사용자 조작 상한» 이다 — 다른 계약이다.**
2. **`touch-action`** — 좁은 화면에서 **지도 위를 쓸었을 때 페이지가 스크롤되는가.**
   고정 뷰에서는 기본값으로 해결됐는데 **드래그를 열면 지도가 그 제스처를 먹는다.**
   → **8/28 이 이 문제를 어떻게 풀었는지 확인하고 같은 해법을 쓰라**(승계 대상).
3. **8/28 의 `※` 안내 문구가 딸려 오는가** — `※ 지도는 손가락 하나로…` 류.
   ⚠⚠ **`※` 는 종류 1 · 출현 2 고정이다**(§53-12 #15). **새 `※` 를 만들면 그 값이 깨진다.**
   → **`※` 없이 안내할 수 있는지**, 아니면 **안내 자체가 필요 없는지** 판정하라.
     (8/28 은 확대 버튼이 있었다 — 9/4 에 버튼을 둘지도 함께.)
4. **점유율 규율** — 8/28 의 «지도 점유율» 측정(81.5% vs 85.4% 사고)이 이 지도에도 걸리는가.
5. ⚠ **§54.13-6·-7 의 «쓰지 마라» 두 줄이 재판정 대상이다**
   (`※ 지도의 번호를 누르면…` = 팝업 없음 → **유지** / `※ 지도는 손가락 하나로…` = 드래그 없음 → **근거 소멸**).
6. **§54.14 QA-498**(«드래그·휠·핀치로 지도가 움직이지 «않는»가»)이 **거꾸로 됐다.** 항목을 다시 써라.

## M-15. §54.15 판정 9 — **밴드 테두리 유지. 승인**
`BAND_STYLE` 주석이 *"밴드가 2개 이상이 되면 이 판정으로 돌아와라"* 라고 했고 **돌아와서 판정했다** —
9/4 네 밴드는 **22~75 m 떨어져 맞닿지 않아** «두 밴드 사이 경계선» 위험이 발생하지 않는다.
⚠ **테두리를 빼면 WCAG 1.4.11(3:1)을 못 만든다.**
★ 이것이 «조건부 판정에 조건을 함께 적는다» 규율이 **실제로 작동한 사례**다 — 기록해 둔다.

## M-16. §54.15 판정 11 — **M-6 의 근거를 다시 쓴다**
> **M-6 의 종전 근거**: *"401 결과에 대해 개발자 기록과 QA 기록이 **충돌**하고 **어느 쪽도 재현되지 않았다(미확인)**.
> 최악이 «백지» 라면 이 조건부는 방화벽이다."*
> ⚠ **이 «미확인» 표현은 죽었다.**

**§54.10 이 재현했다. QA 기록이 맞았다.**
- `oapi.map.naver.com/v3/auth` **401** → `window.naver.maps` 는 **있지만 내부가 null**
- 정리 경로의 `setMap(null)`·`destroy()` 가 네이버 코드 안에서 throw
- **passive unmount effect 의 예외는 React 가 회복하지 못해 트리 전체가 날아간다**
- `main` **0개** · `h2` **0개** · `body.innerText` = *"This page couldn't load"*

→ **M-6 의 조건부는 «미확인 최악에 대한 보험» 이 아니라 «확인된 실패 모드에 대한 방화벽» 이다.**
→ ★ **정리 경로 방어가 «필수 구현 항목» 으로 승격된다**(§54.10 제약 1).
⚠ **한계**: `next dev`(StrictMode 이중 마운트)에서 재현했다. **프로덕션 미확인** → **QA-495 가 판정한다.**
⚠ **이 결함은 `/rally-2026-08-28` 에서 재현됐다 — 그 페이지에도 있다.** FOLLOWUPS 대상.

## M-17. §54.15 판정 12 — **§52.8 삽입 지점 C 폐기 확인**
M-4(원본 이미지 미게시)가 «지도 이미지» 자리를 없앴고 **§54 가 그 자리를 API 블록으로 대체**했다.
→ **§52.8 표의 C 행을 인용하지 마라.**

## M-18. ★ 거리뷰(로드뷰) 추가 — 사용자 지시 (2026-08-29)

**사용자 축자:** *"거리뷰도 유사하게 구성해서 구현해줘"*

### ⚠⚠ 먼저 — 8/28 로드뷰에는 **미해결 위험이 걸려 있다** (`FOLLOWUPS #12`)

두 기능이 **네이버 공식 문서에 없는 API** 에 기댄다:
| 기능 | 기대는 API |
|---|---|
| 바닥 탭 이동 | `getProjection().fromOffsetToCoord` |
| 로드뷰 안 라벨 | `Marker({ map: panorama })` |

→ **`union-webapp-dev §7` 이 이것을 위해 만들어졌다.** 금지가 아니라 **조건**이 규칙이다:
  ***«없어졌을 때 무엇이 남는가».*** 기능 감지 + 조용한 비활성으로 감싸고 **실패해도 기존 경로가 남게** 한다.
⚠ **9/4 에 복제하면 그 위험도 «두 벌» 이 된다.** 무엇을 가져오고 무엇을 안 가져올지 **명시 판정**하라.

### ★★ 8/28 과 다른 근본 문제 — **어디를 비출 것인가**

| | 8/28 | **9/4** |
|---|---|---|
| 로드뷰 목적 | **우리 자리가 실제로 어떻게 생겼는지 미리 본다** | ❌ **우리 자리를 모른다** |

→ **초기 시점(파노라마 위치·방위)을 무엇으로 정할 것인가**가 이번의 핵심 판정이다.
**후보와 각각의 «조합원 행동»:**

| 후보 | 답하는 질문 | 판정 |
|---|---|---|
| **광화문역 출구 앞** | *"역에서 나오면 어느 쪽인가"* | ★ **M-13 과 정합** — 조합원은 **각자 대중교통으로 온다** |
| **시청역 부근** | 〃 (남쪽에서 오는 조합원) | 〃 |
| 대오 1~4 중 하나 | *"내 자리가 어떻게 생겼나"* | ❌ **우리 대오를 모른다.** 하나를 고르면 **그것이 우리 자리로 읽힌다** |
| 세종대로 중앙 | *"현장이 어떻게 생겼나"* | 약함 — 행동이 갈리지 않는다 |

⚠⚠ **대오 하나를 초기 시점으로 고르지 마라.** 지도에서 넷을 «대등하게» 그려 놓고
로드뷰가 하나를 비추면 **그 대오가 우리 자리로 읽힌다** — M-3 확정 문면과 정면 충돌한다.

⚠ **로드뷰 안에 대오 라벨을 띄우지 마라**(8/28 의 `Marker({map: panorama})` 기능).
같은 이유다 — **넷 중 하나를 화면 가운데 두는 순간 그것이 «우리 것» 이 된다.**
→ 가져온다면 **역·화장실처럼 «대오가 아닌 것»에만.** 판정하라.

### 지켜야 할 것
- **좌표 확신도**: 로드뷰 시작점이 **역 출구**라면 `verified`(OSM 직접 도달 · §53-5 (1))다.
  ★ **`estimated` 좌표(대오·무대)를 로드뷰 시작점으로 쓰지 마라** — ±25 m 면 **엉뚱한 곳을 비춘다.**
- **§54.10 의 401 방어**가 로드뷰에도 걸린다(같은 스크립트·같은 정리 경로).
- **`FOLLOWUPS #12`** 를 이 라운드에서 **닫을지 이월할지** 판정하라.
- **세로 예산** — 로드뷰 블록이 추가로 세로를 먹는다. **§54.9 예측(지도 약 1,015px)에 더해 재측정.**

## M-19. 검증 §54(역 좌표·범례) — **전부 채택**

| 항목 | 확정 |
|---|---|
| **시청역** | `37.565480, 126.977114` · **`verified`** — OSM 과 원본 픽셀 환산이 **1.2 m 일치** |
| **광화문역** | ⚠ **OSM 역 노드를 쓰지 않는다.** **⑥ `37.569796,126.976615` · ⑤ `37.569823,126.977422` 두 점** · `verified` |
| 범례 | **13행 + 확신도 키** (§54-4 (3) 축자) |

### ★ 광화문역이 «두 점» 인 이유 — 비대칭은 우리가 만든 것이 아니다
- OSM 역 노드는 **지도 프레임 밖 165 m**(메인무대 북쪽 233 m). 넣으려면 지도를 넓혀야 하고 **대오·무대가 작아진다.**
- **⑥·⑤ 중점을 만들지 마라**(조건 14) — 두 출구가 도로 **양쪽 보도**에 있어 중점은 **세종대로 노면 한가운데**다.
  거기 찍으면 **«역이 도로 위에 있다»** 가 되고, 진짜 승강장은 233 m 북쪽이라 **두 겹으로 틀린다.**
- ★ 원칙은 대칭이다 — **«원본이 그 역을 어디로 가리켰는가».** 시청역은 역 점을 그렸고(1.2 m 일치),
  광화문역은 **역명판에서 자주색 줄기 2개가 ⑥·⑤ 배지에 정확히 꽂힌다.** **개수 차이는 원본에서 온다.**
- **구현 조건(15)**: 광화문역 두 점을 **한 항목으로 묶어라** — 범례 **1행** · 로빙 그룹 **1개** · 라벨 pill **1개**.
  쪼개면 **출구 번호를 안 쓰기로 한 M-13 이 화면에서 깨진다**(같은 이름 둘이 구별되지 않는다).

### ★★ 검증자가 자기 오류를 잡았다 — 범례 3행 개정 (조건 16)
| 행 | 종전 | **개정** | 사유 |
|---|---|---|---|
| 11 | `시청역 화장실 — 대오 4 남쪽 **끝**, 시청역입니다` | **`시청역 화장실 — 시청역 안 화장실입니다`** | ★ **거짓이었다.** 실측하니 대오 4 **북단 78 m · 남단 89 m — 거의 한가운데**다 |
| 1 | `메인무대 — 세종대로 북쪽 끝, **광화문역 방향**입니다` | **`메인무대 — 대오 1 북쪽 끝입니다`** | 신규 12행과 **같은 사실 두 번**. 「세종대로 북쪽 끝」도 위험 — **세종대로는 훨씬 북쪽까지 간다** |
| 8 | `대오 4 — 무대 4 남쪽, **시청역 방향** 구간입니다` | **`대오 4 — 무대 4 남쪽 구간입니다`** | 신규 13행과 중복 |

★ **결과가 «낱말은 줄었는데 얻는 것은 늘었다»** — 범례가 **양 끝이 역으로 닫힌 사슬**이 됐다:
`광화문역 → 메인무대 → 대오 1 → … → 대오 4 → 시청역`.
⚠ **7행만 「끝」이 없다**(대오 3 남단↔무대 4 = **75 m**). 그 단서는 그대로 살아 있다.
⚠ **`시청역 — 대오 4 남쪽 «끝»입니다` 로 쓰지 마라** — 시청역은 **대오 4 «안»** 이다(북단 99 m · 남단 67 m).

### ★★ M-9(C 안)의 파생 결과 — **시청역 점이 대오 4 밴드 «안»에 들어간다** (조건 17)
원본에서는 역 점이 밴드 **동쪽 바로 밖**(픽셀 510 vs 밴드 425~498)이었는데,
**C 안이 밴드를 동쪽으로 옮기면서 삼킨다**(밴드 479~548).
⚠ **사실 오류가 아니다** — 시청역은 실제로 세종대로 **아래**에 있고 대오 4 는 그 **위**를 덮는다. **기하는 참이다.**
⚠ **그러나 시각 문제다** — 밴드 안의 점이 *"여기 서라"* 로 읽히고 **역 점과 밴드가 서로를 가린다.**
→ **디자이너 판정.** ⚠⚠ **역 점을 밖으로 «옮겨» 풀지 마라 — 그 순간 `verified` 가 `estimated` 가 된다.**
  **라벨 배치·z 순서·기호로 푼다.**

### ★ 방법론 하나 — 이방비가 «판별기» 가 됐다
§53-1 이 세운 **이방비 1.55** 로 **«바탕 지도» 와 «저자 주석»을 갈랐다**:
바탕 지도 요소는 늘어나 있고(⑤ 원 **1.606** · 시청역 흰 타원 **1.42**),
문서에서 얹은 주석은 안 늘어나 있다(검은 판의 ①② **1.051·1.105**).
→ **«저자가 의도적으로 표시한 역 정보는 「① ② 시청」 하나»** 임이 실측으로 갈렸다.
★ **같은 측정값이 «좌표 변환» 과 «출처 판별» 두 곳에 쓰였다.** `doc-commit` 대상.

## M-20. 디자인 §54.16 판정 — 리더 확정 (2026-08-29)

### ★ 판정 15 — **초기 시점 «임의 탭» 채택. 내 권고(«역 출구 앞»)를 버린다**

**디자이너 근거가 내 것보다 낫다:**
> 원문 「장소: **세종대로 (광화문역, 시청역)**」이 **두 역을 대등하게** 말한다.
> 하나를 초기 시점으로 두면 ***«그쪽으로 오라»*** 로 읽힌다.
> ★ **내가 «대오 하나를 고르면 그것이 우리 자리로 읽힌다» 고 막은 것과 «정확히 같은 기전»이다.**

→ **내 권고는 그 기전을 대오에만 적용하고 역에는 적용하지 않았다. 일관되지 않았다.**
→ ⭕ **내 권고의 «행동» 은 그대로 달성된다** — 파란 길이 **역 출구부터 세종대로 전 구간**에 깔려
  조합원이 **자기가 내릴 역에서** *«나오면 어느 쪽인가»* 를 확인한다.
→ ★ **«조합원이 고른다» 가 문제를 원천 소거한다 — 우리가 아무 곳도 주장하지 않는다.**

### ★★ 판정 18 — 로드뷰가 **문서에 없는 API 를 «하나도» 안 쓴다**
`getProjection().fromOffsetToCoord`(바닥 탭) · `Marker({map: panorama})`(로드뷰 안 라벨) **둘 다 안 가져온다.**
→ `union-webapp-dev §7` 의 조건(*«없어졌을 때 무엇이 남는가»*)을 **«아예 안 기댄다» 로 푼다 —
  가장 강한 형태의 만족이고 위험이 두 벌이 되지 않는다.**
→ 대신 **파노라마 이동은 네이버 기본 화살표**가, **«어디인지» 는 시트 제목 + 지도 위 마커·부채꼴**이 진다.

### 나머지 판정
| # | 확정 |
|---|---|
| **16** 종횡비 `3/5`→**`4/5`** | ⭕ **채택.** `3/5` 면 «지도 밖 빈 곳» 이 **12px** 이라 **완화 문구가 거짓이 된다.** 대가(라벨 여유 39→27px)는 **확대가 진다** |
| **17** 키보드 지도 이동 **안 연다** | ⭕ **채택 + «알려진 제약» 으로 문서화.** 열면 **방향키로 페이지를 못 내린다** — 그쪽이 더 나쁘다. 확대는 버튼으로 되고 그 사용자의 경로는 **범례**다 |
| **19** 밴드 «안» 항목이 **둘** | ⭕ 접수. **시청역 화장실도 동쪽 테두리에서 0.35 m 안**이다. 같은 처방을 둘 다에. ⚠ **범례 11행 문면은 그대로 참**(`시청역 안 화장실입니다`) — **조건 17 의 «대상 개수» 만 정정된다** |
| **20** `kind: "dots"` 신설 | ⭕ **승인.** 조건 15(범례 1행 · pill 1개)를 쪼개지 않고 만족하는 **유일한 형태**다. `strikeMap.ts` 에 정의 |
| **21** 남는 위험(점이 밴드 테두리 18px 가림) | ⭕ **QA 판정에 맡긴다. 게시 조건으로 올리지 않는다.** 근거: **확대 1단이면 해소**되고(밴드 31px > 점 18px) **사용자가 확대를 열었다**(M-14). 게시 조건으로 올리면 QA 가 **해소 가능한 것을 실패로** 올린다 |
| **22** `FIT_PADDING.top` 44→48 | ⭕ 확인. 축척 0.607 → 약 0.60 px/m |

### ⚠ 내 우려 하나가 성립하지 않았다
*«로드뷰 블록이 추가로 세로를 먹는다»* → **시트가 `position: fixed` 오버레이라 문서 세로를 «0» 먹는다.**
토글 버튼도 지도 «안»이라 0. **삽입 지점 A(참석 예비조사)는 그대로 들어온다.**

## M-21. 검증 §55(거리뷰·조작 문면) — **전부 채택**

### ★ 낱말 — **「거리뷰」로 통일한다**
**「로드뷰」를 9/4 렌더에 쓰지 마라.** 네이버 **공식 명칭이 「거리뷰」** 다.
⚠ 사용자 지시도 *"**거리뷰**도 유사하게 구성해서"* 였다. **8/28 의 「로드뷰」 혼용은 이 라운드 범위 밖**(FOLLOWUPS 후보).

### 확정 문면

| 자리 | 문면 |
|---|---|
| **조작 안내** | `지도는 손가락 하나로 움직입니다. 페이지를 내릴 때는 지도 위나 아래 빈 곳을 쓸어 주세요.` |
| **시트 제목** | `거리뷰` (그것만) |
| 시트 `sr-only` | `거리뷰는 시각 자료입니다. 위치 안내는 지도 아래 범례를 참고해 주세요.` |
| 로딩 | `거리뷰를 불러오는 중입니다.` |
| **없음** | `이 지점 주변에는 거리뷰가 없습니다. 지도에 표시된 길 위의 다른 지점을 눌러 주세요.` |
| 실패 | `거리뷰를 열지 못했습니다. 잠시 후 다시 눌러 주세요.` |

★ **조작 안내는 8/28 문면 그대로(`※` 제외)다.** 검증자가 **문장을 주장 단위로 쪼개 9/4 조작 계약에 각각 대조**했고
**세 주장이 다 참**이었다 — 그래서 새 문장을 짓지 않는다(§5.3 «우리가 만든 문장을 최소로»).
★ **「없음」 문면은 8/28 을 «고쳤다»** — 8/28 은 *«지도의 **파란 길**을 눌러…»* 로 **색 단독 전달**이라
**색각 이상 사용자에게 안 닿는다.** **「지도에 표시된 길」** 로 바꿨다.
★ **시트 `sr-only` 도 8/28 을 그대로 옮기면 «또» 거짓이 된다** — 8/28 은 *«페이지 본문 텍스트를 참고»* 인데
**9/4 에는 그 본문이 없다.** 지도 `sr-only` 에서 잡은 **같은 함정의 두 번째 사례**다.

### 넣지 않는 것
- ⚠ **`+`/`−` 확대를 조작 안내 문면에 넣지 마라**(§5.3 — 버튼이 보이는 어포던스다).
  ★ **다만 «겹치면 확대하면 된다» 완화가 «버튼이 보인다» 에 의존한다 — 확대 버튼을 조건부로 숨기지 마라.**
- ⚠ **«키보드로는 움직일 수 없습니다» 를 쓰지 마라** — 우리 작업 사정이고 범례가 텍스트 등가를 진다.
- ⚠ **`거리뷰 — 세종대로` 류를 만들지 마라** — **누른 점이 세종대로 위라는 보장이 없다.**

### ★ 보고 — 키보드 단독 사용자가 거리뷰에 도달하지 못할 수 있다 (§55-7)
8/28 은 진입 경로가 **둘**(지도 클릭 · 팝업 버튼)인데 **9/4 는 «지도 클릭» 하나뿐**이다(팝업 없음 · 키보드 지도 이동 없음).
→ **게시 차단은 아니다** — 거리뷰는 **텍스트 등가가 없는 순수 시각 보조**이고 **정보는 지도와 범례 13행이 전부 진다.**
→ **QA 회부.** ⚠ **«팝업을 되살려 푼다» 로 가지 마라**(§54.13-6 이 별도 근거로 막았다).
  **토글 버튼이 키보드로 눌리는지, 눌린 뒤 무엇이 열리는지부터 실측**하는 것이 순서다.

### ⚠ QA 가 «참값을 정해야» 하는 것 3건 (설계 예측이지 실측이 아니다)
1. **완화 문구의 «빈 곳» 106px** — `max-w-[420px]` 가 컴파일 CSS 에 없어 못 쟀다(§5.9.3).
   ★ **문면이 «위나 아래 빈 곳» 이라고 «주장» 하므로 QA 실측이 그 문장의 참·거짓을 정한다.**
2. **「지도에 표시된 길」이 실제로 어떻게 보이는가** — `StreetLayer` 렌더를 9/4 산출물에서 못 봤다.
3. **실기기 툴바가 빈 곳을 얼마나 먹는가** — `svh`/`lvh` 대조로 «못 잼» 을 판별하라(§5.8-③).

## M-22. QA §54 수령 — 판정 (2026-08-29)

**판정: 통과 34 · 실패 5 · 미검증 3 · 게시 차단 0.**

### ★ 「참값을 정하라」 3건 — 전부 답이 나왔다

1. ⭕ **완화 문구 「지도 위나 아래 빈 곳」 — 문면이 참이다.**
   1px 격자 스윕 실측: **빈 곳 합 152px 고정 · 최악 지점 큰 쪽 76.17px**(판정선 44 의 **1.73배**).
   `touch-action` 히트 체인으로도 확인(위·아래 `auto` / 지도 위 `none`).
   ★ 설계 예측(106)이 작았던 이유가 갈렸다 — **«캡션이 늘 화면에 있다» 고 놓았는데
   최악 지점에서는 캡션이 화면 위로 빠진다.**

2. ★★ **「파란 길」이 9/4 에서 재현되지 않는다 — 실제는 «연보라»**
   흰 도로 `rgb(253,253,252)` → **`rgb(205,203,239)`**. OFF/ON 픽셀 차분으로 도로망 전체 변화 확인
   (변화 3,246점 · ΔRGB 중앙값 112 로 육안 명확).
   → ★ **§55-6 의 «색 이름을 쓰지 마라» 판정이 실측으로 뒷받침됐다.**
     8/28 문면을 그대로 옮겼으면 **「파란 길」이라는 거짓을 게시**했다.
     (그 판정의 원래 근거는 «색 단독 전달이라 색각 이상에 안 닿는다» 였는데,
      **근거가 하나 더 생겼다 — 색 이름 자체가 틀렸다.**)

3. ⚠ **실기기 툴바 — 못 쟀다**(`100svh == 100lvh == 640`). **«통과» 로 적지 않았다.**
   ★ **대신 식을 남겼다**: 빈 곳 합 = 가시높이 − 488 · 최악 한쪽 = (x−488)/2
   → **44px 보장에 `x ≥ 576` 이 필요하다. 툴바가 64px 이상 먹으면 최악 지점에서 44px 이 깨진다.**
   → **그때 조작 안내 문구가 «거짓» 이 된다.** FOLLOWUPS 대상.

### 실패 처분

| # | 항목 | 처분 |
|---|---|---|
| **F1** | ★ **지도 컨트롤의 `:focus-visible` 링이 «그려지지 않는다»**(WCAG 2.4.7) | **게시 전 수정. 디자이너 판정 회부** |
| **F2** | 시청역 화장실 배지 ↔ 시청역 점 겹침(중심거리 13.04 · 깊이 7.96px) | **디자이너 회부**(리더 lean: M-20 판정 21 과 일관되게 «확대가 해소») |
| **F3** | 밴드 테두리 가림 **32.6%**(예측 18% 의 1.6배) · **명시 처방이 못 고친다** | **처방 문면 정정 필요.** 게시 차단 아님 |
| **F4** | 기대 개수 표가 «어느 계층» 을 세는지 없다 | **리더가 문서 정정**(아래) |
| **F5** | 세로 예산 예측 900 → 실측 **910.26px** | **리더가 문서 정정**(아래) |

### ★★ F1 이 중요한 이유
`keyboardShortcuts: false` 라(M-14 확정) **이 버튼 4개가 키보드 사용자의 «유일한» 확대 경로**다.
그런데 `MapControlStack` 의 `overflow-hidden` 이 **바깥 2~5px 링을 통째로 클리핑**한다
(버튼 box 와 스택 box 가 **정확히 같은 rect**).
★ 그리고 **§54.16-3 의 «겹치면 확대하면 된다» 완화가 이 버튼에 의존한다**(M-21) —
  **F1 은 F2·F3 의 완화 근거까지 무너뜨린다.** 그래서 셋을 한 덩어리로 판정해야 한다.

### F4·F5 문서 정정 — 리더가 한다
- **F4**: `5호선`·`1·2호선` 이 **프리렌더 각 1 / 라이브 DOM 각 2**(초과분은 지도 pill).
  → **기대 개수 표에 «어느 계층에서 세는가» 를 명시한다.** 문면 결함이 아니다.
  ★ 이것은 이 프로젝트의 **«세는 방법이 질문에 안 맞는» 계보의 새 사례**다 —
    이번엔 «렌더 vs 소스» 가 아니라 **«프리렌더 vs 라이브 DOM»** 이다.
- **F5**: 원인 2건이 갈렸다 — ① **완화 안내 줄이 1줄이 아니라 2줄**(23 → 39.75) ② 범례 예측에
  **실재하지 않는 `mt-2` 6px**. **첫 화면 판정선 영향은 여전히 0.**

### 관찰 O1 — 디자이너 확인
**`3,3`(원)과 `1,6`(밴드)이 둘 다 `shortdot` 인데 점선 밀도가 눈에 띄게 다르다**
(네이버 `_getDashStyle` 원본으로 확정 — `shortdash` 면 9,3 또는 6,6).
→ **확신도 단일(§54.5-1)은 지켜졌으나 «보이는 것» 이 다르다.** 의도인지 판정하라.

### 미검증 3 — 통과로 적지 않았다
U1 실기기 가용 높이 · **U2 실제 터치/핀치**(`maxTouchPoints 0` — 합성 이벤트로만 확인) · U3 `today`/`past` 서버 렌더 실물.

## M-23. 디자인 §54.17(F1~F3 처방) — **전부 채택. 내 lean 을 기각한다**

### ★★ F1 — **ⓑ(안쪽 링) 채택. 내가 낸 ⓐ 를 버린다**

**내가 ⓐ 를 lean 한 이유(«기존 판정을 지킨다»)는 틀렸고, 내가 걱정한 위험도 없었다:**
디자이너 실측 — 버튼 사이 틈 **0·0**, `rounded-card` **12px**(루트 75%), ⓐ 적용 시 **모서리 어긋남 위험 0.**
→ **«ⓐ 는 모서리가 자식으로 흩어져 위험» 은 성립하지 않았다.**

**★ ⓐ 를 기각하는 «진짜» 이유 — 링이 «이웃 버튼» 위에 놓인다:**
이웃 버튼 배경 `rgb(9,51,137)` = 링 색 `#093389` → **대비 1 : 1.**
링 면적 612 px² 중 **가로 두 변 288 px²(47%)가 이웃 위**이고, **링을 링으로 읽게 하는 것이 가로 두 변**이다.
**중간 버튼은 세로 두 줄만 남는다** — WCAG 2.4.7 을 간신히 면해도 **판독 가능한 포커스 표시가 아니다.**

★★ **그리고 결정적인 것**: 남은 좌·우변도 **«지도 타일 위»라 대비를 보장할 수 없다.**
`union-design-system §0.7` 이 이미 그것을 판정했다 — *«배경이 매 프레임 바뀌는 곳에서
테두리로 만든 경계는 배경에 따라 약해진다»*.
→ **«바깥 링» 은 «클리핑되기 전»에도 이 페이지에서 근거가 없었다. F1 은 그 사실을 «드러낸» 것이다.**

**채택 처방:**
```
focus-visible:outline-3 focus-visible:-outline-offset-3
  OFF(남색 면): focus-visible:outline-white      대비 11.37
  ON (흰 면)  : focus-visible:outline-primary    대비 11.37
⚠ MapControlStack 의 overflow-hidden 은 «그대로 둔다» — 구조 변경 0
```
★ **종전 판정을 «뒤집는» 것이 아니라 «완성하는» 것이다.**
종전 이유(*«남색 면 위에 남색 링을 그리면 안 보인다»*)는 **지금도 참**이고,
그 판정이 빠뜨린 것은 **«링 색을 바꾼다»는 선택지**였다.
★ **배경이 «우리 면»이라 대비표를 만들 수 있다** — 바깥 링은 «지도 타일»이 배경이라 그 표가 애초에 불가능하다.

⚠ **구현 함정**: `-outline-offset-3` 이 **지금 컴파일 CSS 에 없다**(프로토타입에서 `outlineOffset: 2px` 로 나왔다).
→ **소스에 쓰면 Tailwind 가 생성한다. 그러나 런타임 주입으로는 검증 불가**(§5.9.3).
→ ★ **구현 후 `getComputedStyle(btn).outlineOffset === "-3px"` 를 «값으로» 확인하라**(QA-531).

### F2 — **«확대가 해소한다» 로 닫는다 + z 순서를 규칙으로 고정**
크기로는 못 고친다(값으로 확정): 두 지점 **25.5 m** · 초기 축척 **0.54 px/m** · 필요 중심거리 21px vs 실제 13.04
→ 해소하려면 `r7+r6` 인데 **픽토그램이 판독 불가**가 된다. **z17 에서 겹침 0.**

★ **다만 «무엇이 위인가» 는 지금 규칙으로 정한다:**
> **지도 마커의 `zIndex` 는 «범례 행 순서» 를 그대로 따른다. 뒤 행이 위다.**
> (11행 시청역 화장실 < 13행 시청역 → **역 점이 위**)

근거 3: ① **위계가 맞다**(역은 계약 ③ «어느 역에서 내리나» · 화장실은 «도착 후») ②
**확신도 손실이 작다**(역 점이 위면 화장실은 면이 남고 테두리 일부만 가린다 — 반대면 **r9 점이 통째로 사라진다**)
③ **규칙이 하나다**(8/28 도 `zIndex: 200 + index` 로 배열 순서가 z 를 정했다).
⚠ **범례 행 순서를 바꾸면 z 순서가 함께 바뀐다. 둘은 한 쌍이다 — 주석에 적어라.**

### F3 — **명시했던 처방을 «지운다». 대체 처방은 «없다»**
★★ **원인이 «계산 오류» 가 아니라 «셀 대상 누락» 이다** — §54.16-11 **(1)에서 «밴드 안 항목이 둘» 이라고
직접 지목해 놓고 (2)의 가림 계산에는 점 하나(18px)만 넣었다.** 화장실 배지(**24px**)가 빠졌다.
→ ★ **이 프로젝트의 «세는 방법이 질문에 안 맞는» 계보와 «다르다» — 세는 방법이 아니라 «셀 대상» 이 빠졌다.
  같은 절 안에서 앞 문단이 «둘» 이라 적고 뒤 문단이 «하나» 로 셌다.** `doc-commit` 대상.

**«점 12 → 10px» 문장을 지워라**(적용해도 32.6% → 30.6%).
대체 후보 전부 기각: 화장실 배지 축소(판독 불가) · 좌표 이동(**M-19 금지**) · 밴드 폭 확대(**M-9 B 안 기각**).
→ **확대에 위임하고 그것을 명시한다.** 게시 차단 아님(범례 13행이 「구간」을 문자로 확정한다).

### 처분 «순서» 가 정해졌다
**F1 → F2·F3.** F2·F3 의 «확대가 해소한다» 완화가 **F1 이 고치는 버튼에 의존**한다.

---

# 리더 판정 M-24 (2026-08-29 · 결의대회 페이지 열람 복원)

## M-24. ★★ **D-9 를 뒤집는다** — 8/28 페이지 도달 경로를 복원한다

**사용자 지시(축자):**
> 결의대회 참석 안내도 일정부분에서 페이지를 열람할 수 있게 하자.
> 대신 단서를 달아서 이미 완료된 일정이라는걸 명확히 알 수 있게 하고. 그냥 지우기엔 아쉬워.

### ⚠ **D-9 의 «고아 라우트는 부작용이 아니라 판정이다» 는 죽었다. 인용하지 마라.**

D-9 의 근거 3 중 **①(새 문자열을 만들지 않는다)만 남고 ②③ 은 사용자 결정으로 대체됐다:**
- ② *«`참석 안내 보기` 는 «지금 참석할 것» 의 라벨이다»* → **여전히 참이다.** 그래서 **새 라벨을 쓴다.**
- ③ *«지난 행사의 참석 안내는 행동을 바꾸지 않는다»* → **사용자가 «자료 가치»를 근거로 뒤집었다.**
  그 페이지에는 **지도·금융노조 배치도·식순·출석 안내**가 있다.
- ★ **①의 «새 문자열 금지» 는 이제 «비용» 이 됐다** — 사용자가 그 비용을 지불하기로 했다.

### 확정

| 항목 | 확정 |
|---|---|
| **자리** | `/bargaining-2026` 「남은 일정」의 **8/28 카드**(이미 `종료` 배지 + `DoneBadge` 가 붙는다) |
| **라벨** | **`지난 안내 보기`** (사용자 선택) |
| 페이지 안 단서 | ★ **이미 있다** — `RALLY_PAST_NOTE`: `2026년 8월 28일 일정이 지났습니다. 아래 안내는 기록으로 남겨 둡니다.` |
| **QR 출석체크 카드** | **그대로 둔다**(사용자 선택 — 숨기지 않는다) |

★ **사용자가 요구한 «단서» 는 새로 만들 필요가 없다.** `RALLY_PAST_NOTE` 가 `phase === "past"` 에서
`<h1>` 바로 아래 이미 렌더된다(`rally-2026-08-28/page.tsx:124-126`). **문안을 새로 짓지 마라.**

### ⚠⚠ 라벨 충돌 — 이것이 D-9 를 만든 원인이었다
`bargaining-2026/page.tsx:141-147` 주석이 규율을 못박는다:
> *«같은 문자열이 다른 곳으로 가면 그 자체가 QA 실패다(§20.0-3)»*

→ **`참석 안내 보기` 는 9/4 전용으로 «유지»하고, 8/28 은 `지난 안내 보기` 를 쓴다.**
→ ⚠ **그 주석을 «또» 고쳐야 한다.** 이번 라운드에 이미 한 번 고쳤다
  («항상 `/rally-2026-08-28`» → «항상 `/strike-2026-09-04`»).
  **이제 «라벨이 둘이고 각각 1:1 이다» 로 다시 쓴다.**

## ★ 검증 회부 — **내가 사용자에게 말하지 않은 위험이 하나 있다**

사용자에게 QR 카드 위험을 물을 때 내가 든 근거는 *«남색 강조 면이라 «지금 눌러» 로 읽힌다»* 였다.
**그런데 더 큰 위험을 말하지 않았다:**

> ★★ **9/4 가 «다가온다».** 조합원이 8/28 페이지를 열어 QR 카드를 보고
> ***«9/4 에도 이걸 쓰면 되나»*** 로 읽을 수 있다.
> ⚠ **하필 9/4 QR 방법·시각이 «아직 미정»**(D-4 · «추후 안내합니다»)이라 **채울 것이 없는 빈자리에
> 8/28 자산이 답으로 들어간다.**
> ⚠ 그 카드에는 `손피켓 QR인증 바로하기` 링크(**8/28 시각 판정이 걸려 9/4 에 누르면 출석 실패**) ·
> `1차·2차` 시각 · `상품권 5만원` 지급 조건이 **전부 살아 있다.**

★ 이 프로젝트가 **이미 같은 계보의 사고를 지목했다** — `PreSurveyBanner` 주석:
*«지난 대회의 «참석 예비조사» 배너는 조합원에게 «아직 참석 신청을 받는다» 로 읽혀 거짓이 된다»*.
**그 판정은 컴포넌트 «안»에 두었다 — «어디에 갖다 붙여도 거짓이 될 수 없게» 하려는 것이었다.**
**QR 카드는 그 판정을 받은 적이 없다.**

→ **검증자가 판정한다.** 사용자 결정(그대로 둔다)을 따르되,
  **위험이 실재하면 사용자에게 되묻는다.** 새 사실을 제시받으면 판단이 달라질 수 있다.

## M-25. ★★ 사용자 확인 — **9/4 QR 출석은 8/28 과 «같은 주소»를 쓴다** (2026-08-29)

**질문**: *"9월 4일 총파업의 QR 출석도 8/28 과 같은 주소를 씁니까?"*
**답**: **«같은 주소를 쓴다»** — 지부 당사자 진술이므로 **1급 출처**다.

### ① QR 카드 처방이 «불필요»해졌다 — §56-3 의 (가)·(나) 둘 다 죽는다

검증 §56-2 가 세운 위험은 ***«도착지가 날짜를 말하지 않아 «9/4 에도 쓰면 되겠다»로 읽힌다.
그것이 맞는지 우리가 모른다»*** 였다.
→ ★ **«맞다» 가 확인됐다. 위험의 전제가 사라졌다.**
→ **§56-3 의 한 줄(`이 출석 안내와 링크는 8월 28일 집회의 것입니다.`)을 «넣지 않는다».**
  ⚠ **넣으면 오히려 거짓에 가깝다** — 링크는 9/4 것이기도 하다.
→ **`QrAttendanceCard` 는 손대지 않는다.** `phase` 를 받게 만들 필요도 없다.

⚠ **다만 §56-2 의 «도착지 실측» 은 죽지 않는다** — `HTTP 200` · 제목 `금융노조 집회참석` ·
본문 `지정된 시간에만 체크 가능합니다` · **«8월 28일» 표시 0**. 그 값들은 그대로 유효하고,
**«날짜 없는 이름이 재사용의 신호»라는 검증자 추론이 사용자 확인으로 «맞았다»** 로 판명됐다.

### ② ★ 새 사실이 생겼다 — **9/4 페이지의 «추후 안내» 가 부분적으로 낡았다**

**D-4·D-10 확정 문면**: `당일 QR 출석체크가 있습니다. 방법과 시각은 추후 안내합니다.`
→ ★ **«방법»(=이 링크)은 이제 안다. «시각»은 여전히 모른다.**
→ **문면이 절반만 낡았다.** 이 프로젝트의 «근거가 바뀌면 근거를 다시 쓴다» 대상이다.

**리더가 판정할 수 없는 것 — 검증 회부:**
1. **9/4 페이지에 그 링크를 넣을 것인가.**
   ⚠ 넣으면 «시각을 모르는 채 링크만» 주게 된다. 도착지가 *«지정된 시간에만 체크 가능합니다»* 라고 하는데
   **그 시간이 언제인지 우리도 조합원도 모른다.** → **누르고 실패하는 경로**가 열린다.
   ★ 반대로 안 넣으면 조합원이 **8/28 페이지를 뒤진다**(§52-1 이 이미 지목한 유추 경로).
2. **넣는다면 문면을 어떻게 고치는가** — `방법과 시각은 추후 안내합니다` 를 어떻게 가르는가.
3. ⚠⚠ **8/28 QR 카드의 «나머지» 를 옮기지 마라** — `1차·2차`(8/28 시각) · `상품권 5만원`(8/28 조건) ·
   `참석명단 작성(지부천막)`(8/28 동선). **확인된 것은 «주소 하나» 뿐이다.**
   ★ **§5.8.2 역방향 검증 대상이다** — «우리가 «주소가 다르다»에 기대어 만든 문장» 을 먼저 열거하라.

### ③ 8/28 열람 복원은 **그대로 진행한다**
`/bargaining-2026` 8/28 카드에 **`지난 안내 보기`** 링크 복원(M-24). **QR 카드 손대지 않음.**

## M-26. 검증 §57 — **9/4 에 QR 링크를 넣는다. 회수 9건이 따라온다**

### ★★ 판정 1 — 내가 세운 «넣는다 / 안 넣는다» 표가 **틀렸다**

내 표의 «안 넣으면» 칸은 *«조합원이 8/28 페이지를 뒤진다»* 였는데,
**M-24 로 그 페이지가 «한 번의 클릭» 거리가 됐다.** 그러므로:

> ★★ **«안 넣는다» 는 링크를 «막는» 것이 아니라, 링크에 «8/28 조건을 묶어서» 주는 것이다.**

| | 조합원이 실제로 손에 넣는 것 |
|---|---|
| **넣으면** | **링크 하나** + 우리가 통제하는 한 줄 |
| **안 넣으면** | **같은 링크 + 8/28 조건 전부** — `1차·2차` · `상품권 5만원` · `참석명단 작성(지부천막)` · `손피켓` · `2차 출석은 21:00 전에` |

→ **넣는다.** 근거는 «편익» 이 아니라 **«대안이 더 나쁘다»** 다.

★ **«시각 미정» 우려도 작다** — 도착지 본문이 **«지정된 시간에만 체크 가능합니다»** 뿐이라
**미리 눌러도 «아직이구나» 로 읽고 돌아온다.** 그리고 **실패 비용이 비대칭**이다:
미리 눌러 실패 → 다시 오면 된다 / 8/28 카드를 뒤져 `상품권 5만원`·`지부천막` 을 9/4 에 기대 →
**현장에서 헛걸음하고, 우리가 말한 적 없는 것을 우리 책임으로 안다.**

### ⚠ 판정 — 출처 등급이 **1급이 아니라 2급**이다
내가 M-25 에 «1급» 이라 적었다. **틀렸다.**
| | 코스콤 대오 배정 | **QR 주소 동일** |
|---|---|---|
| 정하는 주체 | **지부 자신** | ★ **본조·주최측** |
| 사용자의 지위 | **당사자** | **전달자** |
→ **2급.** 단독 게시는 가능하나 **«주최측이 바꾸면 우리 링크가 틀린 곳으로 간다»** 가 남는다.
→ ★★ **게시 조건: 9/3~9/4 에 도착지를 다시 연다**(§5.4). 확인 항목 = **200 응답 + `<title>` 유지.**
⚠ **우리가 확인한 것은 «URL 동일성» 이지 «그 서버가 9/4 에 열린다» 가 아니다.**

### 확정 문면
```
본문   당일 QR 출석체크가 있습니다. 인증 시각은 추후 안내합니다.
카드   금융노조 QR 출석체크
       당일 지정된 시각에만 체크됩니다.
       외부 링크(새 창) · prpage153.mycafe24.com
```
⚠ **`손피켓` 금지**(9/4 배포물 미확인) · ⚠ **`바로하기` 금지**(8/28 카드 제목이 `손피켓 QR인증 바로하기` —
**두 페이지의 라벨이 갈려야** 한 문자열 두 목적지 충돌이 없다).

### ★★★ 회수 9건 — **안 고치면 개발자가 정상 구현을 되돌린다**
| 죽는 것 | 처분 |
|---|---|
| **§52-4 조건 2**(«링크·필 버튼·카드로 만들지 마라 — 누를 곳이 없다») | ❌ **근거 소멸.** 이제 누를 곳이 있다 |
| **D-11**(«조건 13 위험도가 올라갔다 — «있다»를 적으면 «그럼 링크도»로 갈 유인») | ❌ **회수.** 안 하면 **정상 구현이 «조건 13 위반»으로 되돌려진다** |
| `page.tsx` 주석 **4곳**(`:27`·`:30`·`:57`·**`:436-437`**) | ❌ 개정. ★ **`:57` 은 «링크»만 빼고 «시각·상품권» 은 그대로** |
| §52-4 `prpage153` 기대 **0** | ❌ **뒤집힌다 → 1** |
| §56-3 (가)·(나) | ❌ 사망(이미 죽였다) |
| §52-2(«방법과 시각은 추후»는 조건) | **절반 사망** — «시각은 추후» 유효 / «방법은 추후» 죽음 |
| §53-9 (6)(«추후 안내합니다»가 2개) | ⭕ **유지** — 개정 문면도 그 말로 끝난다 |
| §56-8(8/28 페이지 렌더에 `9월 4일`·`9/4` 기대 0) | ⭕ **유지** — 주소가 같아도 그 페이지가 9/4 를 말할 이유는 없다 |
| §52-1 ⚠(«유추가 틀리면 출석에 실패한다») | ❌ **틀렸다.** ★ **위험이 «소멸» 이 아니라 «이동» 했다** — «틀린 링크» → **«8/28 조건이 함께 온다»** |

★★ **마지막 행이 이 표의 핵심이다.** «사라졌다» 로 적으면 다음 사람이
**§57-4 의 기대 0 들(`상품권`·`1차·2차`·`지부천막`·`손피켓`)을 «함께» 풀어 버린다.**

### 금지 목록 개정
`prpage153` **0 → 1** · `QR 출석체크` **1 → 2** · `지정된` **0 → 1** · `추후 안내` **2 (불변)**

---

# 리더 판정 M-27 (2026-08-29 · 화장실 로드뷰 팝업 + 파노라마 마커)

## 사용자 지시 (축자)
> 결의대회 페이지처럼 화장실 위치들에 대해서 로드뷰 보기 팝업과 함께
> 로드뷰 화면에서 와드(길사진 내 위치표시) 추가해줘.

## ★★ 이 지시가 **§54.16-6 의 판정 두 개를 뒤집는다**

**§54.16-6 (1) 표의 두 행이 죽는다. 인용하지 마라:**
| 죽는 판정 | 기대던 API |
|---|---|
| *«팝업에서 로드뷰 열기 — ❌ 팝업이 없다»* | — |
| *«로드뷰 «안» 라벨 — ❌ 안 가져온다»* | ★ **`Marker({ map: panorama })`** |

→ ⚠⚠ **그 절이 세운 «자랑»도 함께 죽는다:**
> *«그 결과 9/4 는 `FOLLOWUPS #12` 의 두 API 를 «하나도» 쓰지 않는다.
> `union-webapp-dev §7` 의 조건을 «아예 안 기댄다» 로 푼다 — 가장 강한 형태의 만족»*
→ **이제 «하나»를 쓴다.** `Marker({ map: panorama })` 다.
  (`getProjection().fromOffsetToCoord`(바닥 탭)는 **여전히 안 쓴다** — 사용자가 요구하지 않았다.)

### ★ 그러나 «금지» 를 어기는 것이 아니다
`union-webapp-dev §7` 은 **금지가 아니라 조건**이다 — ***«없어졌을 때 무엇이 남는가».***
→ **기능 감지 + 조용한 비활성**으로 감싸고, **실패해도 기존 경로가 남게** 한다.
→ 8/28 이 이미 그렇게 짜여 있다(`RallyMap.tsx:1375` 가 **계열 표시**까지 달고 있다 —
  주석 축약 라운드에서 검사자가 **되살린** 바로 그 문장이다).
→ **`FOLLOWUPS #12` 를 «9/4 도 해당» 으로 갱신해야 한다**(현재 «8/28 만» 으로 좁혀 적혀 있다).

## ★★★ 리더가 지목하는 최대 위험 — **화장실 3곳이 전부 `estimated` 다**

`strikeMap.ts` 실측:
| 지점 | 확신도 | 근거 |
|---|---|---|
| `toilet-north` (간이화장실 · 대오 1 서쪽) | **`estimated`** | 원본 픽토그램 환산 · **외부 출처 0**(임시 시설) |
| `toilet-south` (간이화장실 · 대오 3 남쪽 끝 부근) | **`estimated`** | 〃 |
| `toilet-cityhall` (시청역 화장실) | **`estimated`** | OSM 화장실 노드가 **64 m 북쪽** — 동일 시설 여부 **미확인** |

★★ **내가 M-18 에 이렇게 적었다:**
> ⚠ **`estimated` 좌표(대오·무대)를 로드뷰 시작점으로 쓰지 마라 — ±25 m 면 엉뚱한 곳을 비춘다.**

→ **그 금지가 화장실에도 그대로 걸린다.** 오차 예산은 **±25 m**(§53-6)이고,
  **로드뷰는 «그 지점에 서서 보는 화면»** 이라 오차가 **시각적으로 확정처럼 보인다.**
→ ★ **«와드»(파노라마 안 위치 표시)가 그 문제를 «키운다»** — 화면 안에 점을 찍으면
  조합원이 **«저기가 화장실이다»** 로 읽는다. **우리는 ±25 m 안에 있다는 것밖에 모른다.**

⚠ **이것은 «하지 말자» 가 아니다.** 사용자가 요구했고, 8/28 도 `estimated` 좌표(집회 3구역)에
로드뷰를 열었다. **판정은 «어떻게 정직하게 보여 주는가» 다.** → **검증 회부.**

## 함께 판정할 것
1. **팝업이 생기면 §54.13-6 이 죽는다** — *«`※ 지도의 번호를 누르면…` 을 쓰지 마라 — 팝업이 없어 거짓이 된다»*.
   ★ 이제 **팝업이 생기므로 그 문장이 참이 될 수 있다.** 다만 **`※` 는 종류 1 고정**이라 여전히 못 쓴다.
   → **조작을 어떻게 알리는가**(또는 알릴 필요가 없는가)를 판정하라.
2. **화장실 3곳에 pill 이 없다**(§54.5-3 — «화장실 3은 픽토그램만»). **팝업을 열려면 누를 것이 필요하다.**
   → 픽토그램 자체를 누르게 할 것인가, pill 을 줄 것인가. **§54.5-3 이 재판정 대상이다.**
3. **다른 지물에도 팝업을 줄 것인가** — 사용자는 **화장실만** 말했다. **대오·무대·역은 그대로 둔다**(범위 밖).

## M-28. 사용자 답 3건 — 확정 (2026-08-29)

| 질문 | 답 |
|---|---|
| ±25 m 오차를 어떻게 다루나 | **한 줄 붙인다** — 지도의 «점선·옅은 면» 이 하던 일을 로드뷰에서는 **문장이 진다** |
| 무엇을 누르나 | **픽토그램을 누른다** — pill 을 새로 만들지 않는다(§54.5-3 유지 · 겹침 재측정 회피) |
| 범위 | **화장실 3 + 역 2** — 대오·무대는 **제외** |

### ★★ 대오·무대를 뺀 것이 중요하다 — 사용자가 위험을 피했다
대오 하나를 로드뷰로 비추면 **«코스콤은 여기»** 로 읽힌다 —
M-3(«코스콤지부가 어느 대오인지는 추후 안내합니다»)과 **정면 충돌**이고,
§54.16-6 (2)가 «초기 시점을 우리가 고르지 않는다» 로 막은 것과 **같은 기전**이다.
→ **범위를 화장실·역으로 한정한 것이 그 충돌을 원천 차단한다.**

## ★★★ M-29. 두 그룹의 **확신도가 다르다** — 문면·형태가 갈려야 한다

| 그룹 | 좌표 확신도 | 근거 |
|---|---|---|
| **역 2**(광화문역 ⑥·⑤ · 시청역) | ★ **`verified`** | OSM 직접 도달 · 시청역은 원본 픽셀 환산과 **1.2 m 일치** · 광화문역은 **원본 역명판이 직접 가리킨 두 출구** |
| **화장실 3** | **`estimated`**(±25 m) | 원본 픽토그램 환산 · 간이화장실은 **외부 출처 0**(임시 시설) · 시청역 화장실은 **동일 시설 미확인** |

→ ★ **«근사 위치입니다» 한 줄은 화장실에만 붙는다.** 역에 붙이면 **거짓**이다
  (확인된 좌표를 근사라고 말하는 것).
→ ⚠ **반대로 화장실에 안 붙이면 «확정»을 암시한다.**
→ **한 컴포넌트가 두 확신도를 다루게 된다** — 조건 분기를 **`confidence` 데이터 축으로** 하라.
  ⚠ **`id === "toilet-north"` 처럼 id 로 분기하지 마라**(§54.8 이 이미 세운 규율).

## ★ M-30. 광화문역이 **두 점**이라 로드뷰를 «어느 점»에서 여나

§54-2·M-19: 광화문역은 **⑥ `37.569796,126.976615` · ⑤ `37.569823,126.977422` 두 점이 한 항목**이다
(범례 1행 · pill 1개 · 같은 z). **중점을 만들면 도로 노면 한가운데**라 금지(조건 14).
→ **팝업을 어디에 띄우고 로드뷰를 어느 점에서 여는가** — **판정 사항.**
⚠ **두 점 각각에 팝업을 주면 «한 항목» 계약(조건 15)이 화면에서 깨진다.**

## M-31. 죽는 절 — 이름으로 지목한다
| 죽는 절 | 죽는 문장 | 대체 |
|---|---|---|
| **§54.16-6 (1) 표 2행** | *«팝업에서 로드뷰 열기 — 팝업이 없다»* · *«로드뷰 안 라벨 — 안 가져온다»* | **M-27** — 둘 다 «가져온다» |
| **§54.16-6 (1) 말미** | *«9/4 는 미문서 API 를 «하나도» 쓰지 않는다 — 가장 강한 형태의 만족»* | ★ **이제 «하나» 를 쓴다**(`Marker({map:panorama})`). `fromOffsetToCoord` 는 **여전히 안 쓴다** |
| **§54.13-6** | *«`※ 지도의 번호를 누르면…` 을 쓰지 마라 — 팝업이 없어 거짓이 된다»* | **근거 소멸**(팝업이 생긴다). ⚠ **다만 `※` 는 종류 1 고정이라 여전히 못 쓴다** — 조작 안내가 필요하면 **`※` 없이** |
| **§54.5-3** | *«화장실 3은 픽토그램만 — 누를 것이 없다»* | **픽토그램이 누를 것이 된다.** pill 은 **여전히 안 만든다** |

→ ★ **`FOLLOWUPS #12` 를 «9/4 도 해당» 으로 갱신하라** — 현재 «8/28 만» 으로 좁혀 적혀 있다.
  ⚠ 단 **절반만**이다: `Marker({map:panorama})` 는 9/4 도 쓰고, `fromOffsetToCoord` 는 **8/28 만** 쓴다.

## M-32. 디자인 §54.18 · 검증 §58 수령 — 리더 확정 (2026-08-29)

### ★★ 판정 27 — **터치 24×24 승인. «지도 마커 한정» 예외로 명시한다**

**승인 근거는 «44 를 지키면 접근성이 «더» 나빠진다» 이다:**
서쪽 열 마커 최소 세로 간격이 **26.7px**(메인무대 pill ↔ 간이화장실 배지)인데
44×44 히트 박스를 주면 **인접 히트가 서로를 삼켜 «누를 수 없는 지물»이 생긴다.**
좌표를 옮겨 간격을 벌리는 것은 **금지**다(M-19 — `verified` 가 `estimated` 가 된다).

→ **최소 `24 × 24`(WCAG 2.5.8 AA).** `44 × 44`(2.5.5 AAA)는 **«못 만든다»로 명시**한다.
⚠⚠ **이것은 §5 의 «지도 마커 한정» 예외다. 페이지의 다른 곳은 44 그대로**
  (복귀 링크 · CTA · 지도 컨트롤 · 시트 조작 — `min-h-touch` 유지).
⚠ **예외 근거를 소스 주석에 «값으로» 남겨라**(간격 26.7 · pill 22→24 · 배지 24 · 잔여 여유 2.7px).
  안 적으면 다음 사람이 «44 를 안 지켰네» 로 되돌리고 **그 순간 지물 하나가 안 눌린다.**
★ **QA-541(서쪽 열 여유 2.7px)을 반드시 재라.**

### 판정 28 — **범례 행을 두 번째 진입 경로로 만들지 «않는다»**
사용자가 «픽토그램을 누른다»를 골랐고(M-28) **범위를 넘는다.**
⚠ 다만 **§55-7(키보드 사용자가 거리뷰에 도달 못 할 수 있다)이 이번에 «부분 해소»된다** —
  마커가 **버튼**이 되므로 **탭 이동으로 닿는다.** 그 사실을 QA 가 확인하게 하라.
→ **그것으로 충분한지 QA 실측 후 재판정.** 미달이면 그때 범례 경로를 연다.

### ★ 판정 29 — 어포던스 문장은 **사실 주장이다**
*«어포던스를 문장 하나가 진다»* 를 채택하되:
⚠⚠ **그 문장은 «무엇을 누를 수 있는가» 를 «주장»한다.** 대오·무대에 `roadview` 를 붙이는 순간 **거짓이 된다.**
→ **문면과 «그 문장이 참인 조건»을 검증 §58 후속으로 확정하라.**
⚠ **`※` 를 쓸 수 없다**(종류 1 고정).

### ★★ 판정 30 — 검증(§58-2)과 디자인(§54.18-7 #30)이 **갈렸다. 검증을 채택한다**

| | 판정 |
|---|---|
| **검증 §58-2** | 「근사」 한 줄을 **팝업·시트 «둘 다»** |
| 디자인 #30 | **시트에만** — 지도엔 이미 3채널(점선·옅은 면·범례 키)이 있고 로드뷰엔 0채널 |

→ ★ **검증 채택.** 결정 근거는 **«두 화면이 «동시에» 뜨지 않는다»** 이다 —
  누르면 **팝업이 닫히고** 시트가 열린다(8/28 §25.7). 그러므로 **한 화면에 두 번이 아니라
  «연속된 두 화면에 각각 한 번»** 이고, §5.3(정밀도 고지 중복)에 걸리지 않는다.
→ ⚠ **디자이너 근거도 부분적으로 옳다** — 지도에 3채널이 있는 것은 맞다. **다만 팝업은 «지도 위»가 아니라
  «그 지물에 대한 화면»** 이고, 조합원이 **거리뷰를 안 열고 팝업만 보고 닫을 수 있다.** 그때 근사 고지가 0 이 된다.
⚠ **셋째 자리(지도 캔버스 위)에는 넣지 마라** — 그것이 §5.3 위반이 되는 선이다.

### 판정 31 — `FOLLOWUPS #12` 갱신은 **«절반만»** 으로 적는다
| API | 8/28 | 9/4 |
|---|---|---|
| `Marker({ map: panorama })` | 쓴다 · **가드 없음**(`:1799~1801`) | ★ **쓰게 된다 — `try/catch` 필수**(§58-7) |
| `getProjection().fromOffsetToCoord` | 쓴다 · 가드 있음(`:1913`) | ❌ **여전히 안 쓴다** |
⚠ **«9/4 도 해당» 으로 뭉치지 마라** — 다음 사람이 9/4 에서 `fromOffsetToCoord` 를 찾다가
  **없는 것을 «누락» 으로 본다.**
★ **9/4 에 `try/catch` 를 감으면 미해결은 8/28 하나로 남는다** — 확산을 «범위»로 막는 것이다.

### 판정 — 검증 §58 문면 전부 채택
- 화장실 한 줄: **`표시 위치는 약 ±25 m 근사입니다.`** · 역에는 **아무것도 안 붙인다**
- 팝업 본문: **`legend` 파생 · 신규 문자열 0** · 제목 줄 만들지 마라 · `popupNote` 안 쓴다
- 와드: **«지금 연 그 지점» 하나만 · 이름만**(`legend` 의 «— » 앞부분) · `aria-hidden`
- 버튼: **`거리뷰 보기`**(§55-4 통일 · `로드뷰 보기` 복사 금지)
- 광화문역: 팝업 **하나**(pill 앵커) · 로드뷰는 **«누른 그 점»** ⚠ 중점 금지

★ **§58-3 이 내 전제를 정정했다** — *«한쪽만 문장이 있으면 조합원이 차이를 읽는다»* 는 **틀렸다.**
  **«부재»는 신호가 아니다.** 그리고 **정확도를 «주장»하면 그 주장을 우리가 진다**(역 좌표도 ±1~13 m 잔차가 있다).
★ **§58-5 도 내 우려를 정정했다** — 팝업이 `legend` 를 반복하는 것이 아니라
  **«문자열 하나가 두 표면에 나타나는 것»** 이고, **별도 문면을 짓는 쪽이 요구 88 위반**이다.

## M-33. 검증 §59(어포던스 문장) — **전부 채택**

### ★★ 내 질문이 반쯤 어긋나 있었다
내가 남긴 선택지: *«픽토그램·pill 이 «눌리는 것»으로 보이면 문장이 §5.3 에 걸린다»*
→ 검증 정정: ★ **문제는 «누를 수 있는가» 가 아니라 «어느 것이 눌리는가» 다.**

| | |
|---|---|
| 지도 항목 | **13개** |
| `roadview` 대상 | **5개**(화장실 3 · 역 2) |
| ★ **눌러도 아무 일 없는 것** | **8개 — 대오 4 · 무대 4** |

→ **조합원이 가장 먼저 누를 것이 «대오» 다**(자기 자리를 찾으려는 페이지다). **아무 일도 안 일어난다.**
→ 다음 행동은 *«누르는 게 아니구나»* 이고 **화장실·역도 안 눌러 본다** —
  **사용자가 명시적으로 요구한 기능(M-27)이 그렇게 죽는다.**
→ §5.3 **두 방향으로 갈린다**: ① 안 열리는 것을 누른다 ② **열리는 것을 안 누른다.**

### 확정 문면
```
지도의 화장실과 역을 누르면 거리뷰가 열립니다.
```
**자리: 조작 안내 문단 «안» 둘째 문장**(문단을 새로 만들지 않는다 — 지도 위 우리 문단이 3덩어리가 되면
«지도에 닿기 전에 읽어야 할 벽»이 된다).

| 요소 | 근거 |
|---|---|
| **`지도의`** | 빼면 «실제 화장실·역을 누르는 것»으로 읽힐 여지 |
| **`화장실과 역`** | `roadview` 5개를 **정확히** 덮는다 |
| **`누르면`** | 「탭」은 데스크톱에서 · 「클릭」은 터치에서 어긋난다 — **기기 중립** |
| ⚠ **부정문 금지** | `무대·대오는 열리지 않습니다` 를 덧붙이지 마라 — **긍정문이 이미 범위를 닫는다** |
| ⚠ **`거리뷰를 볼 수 있습니다` 금지** | 지도 안에 **`거리뷰` 토글이 따로 있다.** 「누르면 열린다」가 **이 문장이 말하는 «다른 경로»를 특정**한다 |

### ★★ 주석에 넣을 «참인 조건» — **양방향으로 적는다**
> 이 문장은 **사실 주장**이다 — 지금 참인 것은 `roadview` 가 **다섯 개뿐**이기 때문이다.
> ★ **양방향으로 깨진다**: **늘리면** «화장실과 역»이 대상을 **좁혀** 말하게 되고,
>   **줄이면** «없는 것을 누를 수 있다»고 말한다. **같은 무게로 거짓이다.**
> ⚠ 「무대·대오는 안 됩니다」로 때우지 마라 — 부정문이 늘고, **늘린 쪽은 여전히 거짓**이다.

★ **양방향을 «둘 다» 적는 것이 이 주석의 전부다.** 이 프로젝트가 반복한 사고는 «근거가 소멸했는데
판정이 안 고쳐졌다» 인데, 이 문장은 **근거가 «늘어나도» 깨진다** — 한 방향만 적으면 나머지가 통과한다.

### 기계적 방어 — **채택**
`roadview` 인 `id` 집합을 **상수로 못박고 실제 데이터와 다르면 모듈 로드 시점에 죽게** 한다
(`rally.ts:20` 단일 출처 가드와 같은 계보 · D-11 이 9/4 에도 «가드 필수»로 요구했다).
⚠ **문면을 데이터에서 «생성»하지 마라** — 생성된 문면은 **검증을 통과한 적이 없다.**

### ★ 미발견 — **버튼화가 `aria-hidden` 계약을 깬다**(§59-5)
현행 마커 DOM «전체»가 `aria-hidden="true"` 이고 뜻은 범례 13행이 진다(§54.12).
→ **버튼이 되는 5개는 `aria-hidden` 을 벗겨야 하고, 그러면 스크린리더가 마커와 범례를 두 벌로 읽는다.**
→ 8/28 이 같은 문제를 푼 방식을 9/4 어휘로 옮긴다(`RallyMap.tsx:510-511` —
  *«`aria-label` 은 짧게. 길게 쓰면 그룹 순회 때 범례 전문이 6번 반복된다»*).
⚠ **이것이 내가 개발자에게 준 «`sr-only` 에 `legend` 전문» 지시와 충돌한다** — **§59-5 처방이 이긴다.**

## M-34. ★★ 접근성 이름 — **범례 «전문» 유지. 내 정정(M-33)을 다시 뒤집는다**

### 경위 — 판정이 두 번 갔다
1. 내가 개발자에게 *«`legend` 전문을 `sr-only` 에»* 지시
2. 검증 §59-5 가 *«전문이면 마커와 범례를 두 벌로 읽는다 → 짧은 이름»* 으로 정정 → 내가 M-33 에 채택
3. ★ **개발자가 반대 근거를 찾았다** — 그것이 맞다

### 개발자 근거 (`strikeMap.ts:518` 주석)
> ★ 화장실 2곳이 같은 이름(`간이화장실`)이 되지만 **와드는 한 번에 하나**라 충돌하지 않는다.
> ⚠ **그래서 이 값을 «접근성 이름»으로 쓰면 안 된다 — 둘이 구별되지 않는다.**

→ ★★ **짧은 이름을 쓰면 스크린리더 사용자가 «간이화장실» 을 두 번 듣고 «어느 쪽인지 알 방법이 없다».**
  그것은 «반복이 거슬린다» 보다 **무겁다** — **구별 불가는 «못 쓰는 것»** 이고 반복은 «불편»이다.

### §59-5 근거가 왜 여기선 약한가 — 8/28 과 조건이 다르다
| | 8/28 | **9/4** |
|---|---|---|
| 근거 문장 | *«길게 쓰면 그룹 순회 때 **범례 전문이 6번 반복**»* | — |
| 마커 이름 | `{번호} {이름}` — ★ **번호가 구별자였다** | ❌ **번호가 0**(§54.16-12) |
| 로빙 그룹 | **전 마커** | **누를 수 있는 5개만** 버튼 |

→ ★ **8/28 이 짧은 이름을 쓸 수 있었던 것은 «번호» 라는 구별자가 있었기 때문이다.**
  **9/4 는 그 구별자가 없다.** §59-5 가 그 차이를 안 봤다.

### 확정
- **접근성 이름 = `legend` 전문**(현행 구현 유지). **`aria-label` 아님 · `sr-only` 내부 텍스트.**
- **와드 라벨 = `featureShortName`(짧은 이름)** — 와드는 **한 번에 하나**라 구별 문제가 없고,
  `nowrap` 한 줄이라 문장이 화면을 가로지른다.
- ★ **두 값이 다른 것이 «설계»다.** `featureShortName` 주석에 *«접근성 이름으로 쓰지 마라»* 가 이미 있다 — **유지.**

## M-35. 남은 것 — 어포던스 문장이 «아직 없다»
§59 확정 문면 `지도의 화장실과 역을 누르면 거리뷰가 열립니다.` 가 **미구현**이다(개발자가 §59 확정 전에 끝냈다).
→ **지금은 «무엇이 눌리는가» 를 말하는 화면 문장이 0 이다.**
→ **추가 지시로 넣는다.**

## M-36. 개발자 판정·보고 접수
| 항목 | 처분 |
|---|---|
| 광화문역 pill → **`points[0]` = ⑥** | ⭕ **승인.** ★ *««더 가깝다»·«주로 쓴다» 로 고르면 우리에게 없는 근거를 주장하는 것»* 이 정확하다. **«배열 순서» 라는 주장하지 않는 규칙**을 썼고 «순서를 바꾸면 시작점이 바뀐다»를 주석에 남겼다 |
| 마커가 **`<button>` 이 아니라 `role="button"`** | ⭕ **승인.** `<button>` 이면 **Enter 가 클릭을 «또» 만들어 열자마자 닫힌다.** 포인터는 네이버 마커 이벤트가 받아야 **거리뷰 모드에서 «길을 눌러 이동» 이 함께 발동하지 않는다** |
| ⚠ **pill 세로를 10개 «전부» 24 로** | ⭕ 승인(어포던스 축은 문장이 진다 — 크기가 뜻을 지면 안 된다). **★ QA-541 겹침 재측정에 «대오·무대 pill +2px» 을 포함시켜라** |
| 시트가 **열릴 때 포커스**를 받게 함 | ⭕ 승인 — 팝업이 닫히며 포커스가 `body` 로 떨어진다. 닫으면 **연 마커로 복귀**(실측). **스펙 밖이지만 옳다** |
| 탭 정지점 5개 증가(로빙 미도입) | 접수. **§55-7 부분 해소**(Enter 로 팝업 열림 실측). **QA 가 «충분한지» 판정** |

## M-37. ★★★ 접근성 이름 — **«짧은 이름»으로 «잠근다». 이 항목은 더 뒤집지 않는다**

### ⚠⚠ 이 항목이 **네 번** 갔다 — 그 자체가 사고다
1. 리더: «`legend` 전문» → 2. 검증 §59-5: «짧은 이름»(두 벌 읽기) → 3. 리더 M-34: «전문»(구별 불가) →
4. **개발자: «짧은 이름»** + 새 근거

★ **오케스트레이터 규율이 «같은 항목을 세 번 뒤집어 모순된 지시가 갔다»를 사고로 기록했다.**
**이번이 네 번이다.** → **여기서 잠근다. 새 근거가 나와도 «이 절»을 먼저 읽고 그것을 반박한 뒤에 뒤집어라.**

### 확정: **`featureShortName`(짧은 이름). 현행 구현 유지**

**개발자 근거가 내 M-34 를 이긴다:**
> ★ **활성화하면 «한 걸음 뒤»에 구별된다** — 팝업 첫 줄이 `legend` **전문**이고,
> **열릴 때 포커스가 팝업으로 옮겨 가 즉시 낭독**된다.
> → **이름은 «무엇을 누를지», 팝업은 «어느 것인지»를 진다.**

→ ★★ **내 M-34 는 «구별 불가»를 «영구»로 봤는데 틀렸다 — «한 걸음 지연»이다.**
  그리고 그 한 걸음이 **막다른 길이 아니다**(팝업이 반드시 열리고 포커스가 간다 — 실측 확인).
→ ⚠ **이름에 위치를 넣어 구별하면 그것이 §59-5 가 막은 «두 벌로 읽기» 그 자체다** — 개발자 지적이 맞다.

**남는 비용(정직하게 적는다)**: 스크린리더로 **훑기만 하는** 사용자는 두 `간이화장실` 을
**활성화 전에는** 구별하지 못한다. **비용을 알고 택한 것이다.**
★ 그 비용을 갚는 것이 **①팝업 첫 줄 전문 ②순회 순서 = 범례 순서 ③범례 13행이 별도로 있다** 셋이다.

## M-38. 두 문장 사이 — **공백으로 잇는다. 개발자 판정 승인**
> *«새 `<p>` 는 금지됐고 `<br>`·`block` span 도 안 썼다 — 그러면 «안 나뉜 척하는 두 덩어리»가 되어
> 금지의 취지를 형식만 피하는 것»*

⭕ **승인.** 내 예시가 두 줄이었던 것은 **읽기 쉬우라고 그렇게 적은 것**이지 **줄바꿈 지시가 아니었다.**
⚠ **다만 조작 안내 문단이 이제 «3문장 · `font-semibold`» 다.** 지도 «위»에 굵은 글씨가 늘었다.
→ **QA 가 세로와 200% 를 재라**(§54.16-7 예산 약 898px 기준).

## M-39. `ROADVIEW_IDS` 가드 — **`throw` 유지. 개발자 근거 승인**
> *«두 값이 «같은 파일의 정적 데이터»라 불일치가 `next build` 프리렌더에서 반드시 평가된다.
> 이 `throw` 의 실제 효과는 «페이지가 죽는다»가 아니라 «배포되지 않는다» 다»*

⭕ **승인.** `rally.ts:20` 의 `console.error` 와 **거동이 다른 것이 옳다** —
거기는 «옛 날짜를 들고 남아도 안내 자체는 유효»지만, 여기는 **문면이 «거짓이 되는» 것**이라 배포를 막는 편이 맞다.
★ **§54.10(«지도가 죽어도 페이지는 지킨다»)과 충돌하지 않는다** — 그것은 **런타임** 규율이고 이것은 **빌드 타임**이다.
★★ **개발자가 가드를 «양방향으로 실제 발동시켰다»**(늘림·줄임 각각 빌드 실패 확인 후 되돌림) —
  이 프로젝트의 «조건문은 발동 절차와 짝이어야 한다» 규율의 모범 이행이다.

## M-40. ⚠ QA 기대 개수 표를 갱신하라 (개발자 보고)
§54.18-1 (2)가 예고한 *«범례 문면이 라이브 DOM 에서 2회»* 는 **이제 1회**다.
**닫힘 상태 라이브 실측(개발자):**
| 문자열 | 라이브 | 구성 |
|---|---|---|
| `간이화장실 — 대오 1 서쪽입니다`(전문) | **1** | 범례 행뿐 |
| `간이화장실` | **4** | 범례 2행 안 2 + 마커 `sr-only` 2 |
| `시청역 화장실` | **2** | 범례 1 + `sr-only` 1 |
| `광화문역(5호선)` · `시청역(1·2호선)` | **각 3** | 범례 1 + pill 시각 1 + `sr-only` 1 |

⚠ **pill 은 시각 텍스트(`aria-hidden`) + `sr-only` 가 «같은 값»이라 DOM 에 두 번 있다. 합치지 않았다** —
합치면 **pill 만 이름이 `label` 축에서 나오고 나머지 셋은 `legend` 축**이 되어 **다섯의 이름 출처가 둘로 갈린다.**
지금은 **전부 `legend` 파생이라 감사 가능**하다. ⭕ **승인.**

## M-41. ★★★ 접근성 이름 — **«전문»으로 «최종» 확정. M-37 을 정정한다**

### ⚠ 무엇이 일어났나 — 정직하게 적는다
**메시지가 엇갈렸다.** 내가 M-37 로 «짧은 이름»을 «잠그는» 동안, 개발자는
**내 직전 «철회»(M-34 · «전문 유지»)를 반영해 «전문»으로 되돌리고 있었다.**
→ **M-37 의 «잠금»은 개발자가 반대 방향으로 이미 구현한 것을 «모른 채» 쓰였다.**

### 최종: **`sr-only` = `feature.legend`(전문). 현재 코드 그대로.**

**여기서 «또» 뒤집지 않는 근거 셋:**
1. ★ **정보 손실이 없는 쪽이다.** 짧은 이름은 «훑기만 하는» 사용자에게 두 `간이화장실` 이
   **활성화 전까지 구별되지 않는다.** 전문은 **어느 시점에도 빠지는 정보가 없다.**
   «두 벌 읽기»는 **불편**이고 «구별 불가»는 **장벽**이다.
2. ★★ **개발자가 M-34 의 근거(«8/28 은 «번호»가 구별자였다»)를 소스 주석에 «값으로» 적었다** —
   *«8/28 의 «이름을 짧게»를 근거만 보고 옮기지 마라. 그 처방이 기대던 구별자가 9/4 엔 없다(번호 0)»*.
   **다음 사람이 §59-5 만 보고 되돌리는 길이 막혔다.** 그것이 이 판정의 방어선이다.
3. **이 항목이 이미 네 번 갔다.** 다섯 번째는 **어느 쪽이 옳든 그 자체가 더 나쁘다.**

⚠ **§59-5(짧은 이름)는 죽었다. 인용하지 마라.** 근거는 8/28 의 «번호»에 기댔는데 **9/4 엔 번호가 0** 이다.
⚠ **M-37 의 «짧은 이름으로 잠근다»도 죽었다.** 그 절의 «네 번 갔다 · 여기서 잠근다» 부분은 **유효**하다 —
  **잠그는 대상이 «전문» 으로 바뀐 것이다.**
★ **`featureShortName` 은 «와드 하나»에만 쓴다.** 두 값이 갈라져 있는 것이 **판정**이다(합치지 마라).

### ★ 내 보고 하나도 정정한다
내가 *«지금은 «무엇이 눌리는가»를 말하는 화면 문장이 0 이다»*(M-35)라고 적었으나 **틀렸다** —
어포던스 문장과 `ROADVIEW_IDS` 가드는 **직전 라운드(§12)에 이미 들어가 있었다.**
내 grep 시점이 그보다 앞섰다. **M-35 는 죽었다.**
★ 이 세션에서 **«낡은 스냅샷을 근거로 «없다»고 보고한» 네 번째**다(QA 리포트 · 컴파일 CSS · §52 · 이 건).

### QA 기대 개수 — **M-40 표는 죽었다. 회수한다**
| 범례 전문 | 프리렌더 | 라이브 |
|---|---|---|
| `roadview` 5개 문면 | 각 **1** | **각 2**(범례 행 + 마커 `sr-only`) |
| 나머지 8행 | 각 1 | **각 1** |
→ §54.18-1 (2)의 **원래 예고로 복귀**한다.

## M-42. 개발자 산출물 처리 — 승인
`03_developer_impl.md` §12 (1)을 **«철회됨»으로 표기**하고 **§13 을 «자족적 확정본»으로 새로 쓴 것** ⭕ **승인.**
★ 이 프로젝트의 «이력을 따라가게 하면 낡은 절을 인용한다 — 구현자에게는 자족적 확정본 하나만» 규율의 이행이다.

## M-43. QA §54.18 — 판정 (2026-08-29)
**통과 25 · 실패 5 · 관찰 3 · 미측정 3 · 게시 차단 0.**

### F-A — **모순이 아니다. QA 가 M-41 «이전» 지시로 쟀다**
QA 가 «M-37(짧은 이름) ↔ QA-543(전문) ↔ 산출물(전문)» 이 모순이라 보고했다.
→ **M-41 이 이미 닫았다**(내 QA 지시보다 «뒤»에 썼다): **«전문» 최종 · M-37 과 §59-5 는 죽었다 · M-40 표는 회수.**
→ **산출물이 옳다. QA-543 과도 일치한다.** ★ **QA 가 «어느 쪽이 옳은지 정하지 않는다»** 로 넘긴 것이 정확한 처신이다.

### ★★ F-B — **진짜 결함. 고친다.** 그리고 **QA 가 자기 이전 판정을 정정했다**
**글자 크기 200% 에서 상자가 336×420 → 296×370 인데 마커 5개가 «한 픽셀도» 안 움직인다.**
상자를 200px 로 줄이면 **히트 4개가 상자 밖**. 실제 뷰포트 리사이즈는 정상.
- **원인**: `window.dispatchEvent(new Event('resize'))` 를 쏘면 즉시 재중심화된다 →
  **네이버는 `window.resize` 만 듣고 «컨테이너만» 바뀐 것을 모른다.**
- **처방**: RO 콜백에서 `fit()` **앞에** `safely(() => map.refresh())` 또는 `Event.trigger(map,"resize")`.
  ★ **둘 다 공식 API 이고 로드된 `maps.js` 에 실재 확인** — **문서 없는 API 를 늘리지 않는다.**
- ★★ **QA 가 §54 회귀에서 «재적합이 일어난다» 로 통과시킨 것이 틀렸다고 스스로 정정했다** —
  `--strike-label-max` 변화는 **«RO 가 울렸다»의 증거일 뿐**이고 **참값은 «마커가 움직이는가»** 다.
  → **§5.8.4 ③ «프록시로 판정하지 마라» 에 QA 가 걸렸고 스스로 잡았다.** `doc-commit` 대상.

### ★ F-C — **진짜 결함. 고친다**
`StrikeMap.tsx:1305` `new maps.Marker({...})` 가 **effect 본문 · `try/catch` 없음**(정리 쪽만 `safely()`).
**발동 측정**: `maps.Marker` 를 없애고 거리뷰를 열자 **`TypeError` → `main` 0개.**
→ **QA-546(«없어져도 거리뷰 자체는 열리는가»)이 «안 열린다»** — 트리가 통째로 날아간다.
⚠ **와드 쪽 가드가 있어도 이 effect 가 «먼저» 죽어 그 방어가 무의미해진다.**
★ **기전이 §54.10 과 같다**(인증 깨진 뒤 생성이 throw). **`:1305` 를 `try/catch` 로 감싸고 표식만 생략.**

### F-D — z17 히트 교차 7×1px · **디자이너 회부**(경미)
`cityhall-toilet` × `cityhall`. 원인은 **시청역 히트가 pill(화면 20px 고정)** 이고 화장실 배지는 **지리 고정**이라
배율에 따라 관계가 바뀌는 것. **그 자리는 z17 에서 상자 밖이고 z 순서상 pill 이 먹어 «못 누르는 지물»은 안 생긴다.**
→ `labelGap` 20 → 24 로 벌어진다. **디자이너 판정.**

### F-E — 설계 예측 3건 정정 (문서)
① «서쪽 열 여유 2.7px» → 실측 **히트↔히트 1.0px · 히트↔장식 pill 0.0px(정확히 맞닿음)**
② «세로 약 950px» → **910.27px(증분 0)** — ★ **어포던스 문장이 기존 2줄에 «흡수»됐다**(78자 · 줄 상자 2 · 클리핑 0)
③ «범례 문면 라이브 2회» → F-A 와 연동(전문 유지이므로 **2회가 맞다**)

### ★★ O-2 — **M-33 «양방향»에 «세 번째 방향»이 빠졌다.** 검증·디자이너 회부
**401 상태에서 조작 안내 3문장이 남아 전부 거짓이 된다**(어포던스 추가로 2 → 3).
`status==="ready"` 로 숨기면 **CLS 40px**.
→ ★ 내가 M-33 에 «늘리면 / 줄이면» 두 방향을 적었는데 **«지도가 «없을» 때»** 가 빠졌다.
  **어포던스 문장은 «무엇이 눌리는가»를 주장하는데, 누를 지도가 없으면 그 주장이 통째로 거짓이다.**

### O-1 — 히트 포커스 링 둘레의 **9.2%가 3:1 미달**(중앙값 9.79 · 링은 확실히 그려진다)
★ **와드는 `#1a1a1a` 외곽선으로 같은 문제를 풀었는데 링엔 없다.** 디자이너 회부.

### ★ 통과 중 값진 것
- **§55-7 해소** — **키보드만으로 거리뷰까지 도달한다**(마커→Enter→팝업→「거리뷰 보기」→시트 · 포커스도 따라간다).
  → **M-32 판정 28 의 «미달 시 범례 두 번째 경로» 는 발동하지 않는다.**
- **«44 를 못 만든다»가 계산으로 확인됐다** — 44 주면 `cityhall-toilet`×`cityhall` **297px²** ·
  `toilet-north`×메인무대 pill **240px²** 로 삼킨다. **예외 미확산 ⭕**(컨트롤·팝업 버튼·복귀 링크 전부 44).
- **QA-548 을 픽셀로 닫았다** — 흰 면 단독은 **42%가 3:1 미달**인데 **«흰 면 or `#1a1a1a` 외곽선»** 기준으로 **78/78 전 둘레 통과.**

### ★ 계수 사고 — 같은 계보의 «두 번째 층»
컴파일 CSS 에서 `focus-visible:outline-3` 을 그대로 grep 하니 **0** 인데,
실제로는 **`.focus-visible\:outline-3:focus-visible` 로 이스케이프**돼 있었다(참값 3).
→ ★ **내 «`.next/static/css/` 가 아니라 `chunks/*.css`» 에 이은 두 번째 층이다 —
  «경로»를 맞춰도 «표기»가 틀리면 여전히 0 이 나온다.** `doc-commit` 대상.

## M-44. 디자인 §54.19 — **F-D «고치지 않는다» · O-1 «이중 링». 둘 다 승인**

### ★★ F-D — **`labelGap` 20 유지. 고치지 않는다**

**«고칠 대상이 아니다» 가 결론이고 근거가 넷이다:**
1. ★ **z17 «한 배율»의 과도 현상이고 z18 에서 스스로 사라진다.**
   세로 이격이 **21.45 m × 축척** 이라 배율에 «비례» 하는데, z17 에서 임계(24px)에 **1px 모자란다**(중심거리 23px).
   **z18 에서 2배(≈46px)가 되어 넘는다.** → **z16 은 가로가 막고 z18 이상은 세로가 막는다. 남는 것은 z17 하나.**
2. 겹침 **7 px²** 는 배지 히트 **576 px² 의 1.2%** 다. **남은 569 px² 가 24×24 규격을 유지**하고
   **그 자리는 z17 에서 상자 «밖»** 이다 — **«못 누르는 지물»이 안 생긴다.**
3. ⚠⚠ **가로만 고치는 값이라 «다른 축»을 못 본다.** z17 가로 겹침 8.6px 를 없애려면 gap **≥28.6** 이 필요한데
   **24 로는 4.6px 가 남는다.** **한 배율을 사려고 상수를 옮기는데 그 배율조차 다 못 산다.**
4. ⚠ **여유가 «0» 인 자리가 있다**(히트↔장식 pill **0.0px 맞닿음**). `labelGap` 은 동쪽 열 값이지만
   **`FIT_PADDING.right` 안에서 pill 우단이 밀려 초기 `fitBounds` 결과가 바뀌고 → 축척이 바뀌고 →
   서쪽 열 0.0px 자리가 함께 움직인다.** ★ **여유 0 인 자리 «옆»에서는 상수를 만지지 않는다.**

→ **QA-541 은 «관측 항목»으로 남기고 처방하지 마라**(§54.17-3 QA-523 과 같은 처분).

### ★★★ F-D 가 남기는 «규칙» — 승격 승인
> **지도 위 겹침을 판정할 때, 두 요소가 «화면 고정»인지 «지리 고정»인지 «먼저» 가른다.**
> | 조합 | 배율이 바뀌면 | 상수 하나로 푸는가 |
> |---|---|---|
> | 둘 다 **지리 고정** | 관계 유지 | ⭕ 푼다 |
> | 둘 다 **화면 고정**(같은 앵커) | 관계 유지 | ⭕ 푼다 |
> | ★ **섞임**(pill `labelGap` ↔ 지리 고정 기호) | **관계가 바뀐다** | ❌ **못 푼다** |
> → 섞인 겹침은 **값을 조정해 없애려 하지 마라.** ① 어느 배율에서 나타나고 사라지는지 «산출»하고
>   ② «못 누르는 지물»이 생기는지로 «판정»하고 ③ 정말 없애야 하면 **한쪽의 좌표계를 바꾼다.**

★ **이 규칙은 §54.18-1(«pill ↔ 기호»)이 M-30 과 F2-히트를 푼 «대가» 다.**
  **대가를 규칙으로 적어 두면 다음 사람이 그것을 결함으로 오인하지 않는다.** → `doc-commit` 대상.

### ★★ O-1 — **이중 링(흰 + 검정) 채택**

**결정적 수치:**
| 조합 | 비율 |
|---|---|
| `#093389`(현행 링) ↔ `#1a1a1a`(어두운 타일·그늘) | ★ **1.53 — UI 3:1 미달** |
| `#ffffff` ↔ `#1a1a1a` | **17.40** |

★★ **9.2% 미달은 «운 나쁜 타일»이 아니라 «어두운 타일에서는 «반드시» 미달» 이다. 주색으로는 못 고친다.**

**처방:**
```
box-shadow: 0 0 0 2px #ffffff, 0 0 0 4px #1a1a1a;
outline: 2px solid transparent; outline-offset: 1px;   ← 고대비 모드 보험
```
★★★ **핵심 근거: 두 링이 인접하므로 «판정 대상 배경»이 «상대 링» 이 되고 그 대비가 17.40 으로 «고정»된다.**
→ **링이 배경 대비에 의존하지 않는다.** 밝은 하늘이든 어두운 그늘이든 **둘 중 하나는 반드시 보이고 둘의 경계는 언제나 보인다.**
⚠ **F1 처방(안쪽 링)을 못 쓰는 이유**: 마커는 **«안쪽»이 없다**(24×24 이고 그 자체가 내용) · **배경이 우리 것이 아니다.**

### ★ 규칙 승격 승인
> **배경이 «임의»인 표면(지도 타일 · 파노라마 사진) 위의 요소는
> «흰 + `#1a1a1a`» 쌍으로 «자기 경계»를 만든다. 주색에 기대지 마라.**

★ **와드 처방(§54.18-4)과 같은 계보이고 `§0.7`(«배경이 매 프레임 바뀌는 곳에서 테두리 경계는 약해진다»)의
«그럼 어떻게» 에 해당한다.** ⚠ `#1a1a1a` 는 **의미색이 아니라 중립색** — **§2 «의미색 3종 상한»을 늘리지 않는다. 신규 색 0.**
⚠ **«왜 여기만 다른가»를 주석에 남겨라**(§0.8.2 정신) — 페이지 다른 포커스는 `outline-primary`,
  컨트롤 버튼은 **안쪽 흰 링**이다. **셋이 다른 것이 설계다.**

## M-45. ★★★ 검증 §60 — **게시 차단 결함 1건 발견. 대체면이 «안 보인다»**

### 선행 결함 — 실측
```html
<div class="… aspect-[4/5] overflow-hidden">      ← 상자 420px
  <div class="size-full" style="touch-action:none"></div>   ← height:100%
  <div class="rounded-card flex h-full …">…</div>            ← height:100%, position:static
</div>
```
**두 자식이 «둘 다» 정상 흐름 · «둘 다» 높이 100%** → 둘째가 상자 «아래»로 밀리고 `overflow:hidden` 이 자른다.

| 실측 | 값 |
|---|---|
| 대체면 `offsetTop`(상자 기준) | **420.0** ← 상자가 끝나는 바로 그 지점 |
| **대체면 `visibleHeight`** | ★★ **0.0** |
| `box.scrollHeight` | **840**(= 420 × 2) — 넘쳐서 잘린 직접 증거 |

★★ **로딩·실패 «둘 다» 안 보인다.** 401 에서 조합원이 보는 것은 **빈 회색 상자 420px** 이다.
**처방: 대체면에 `absolute inset-0`.** 상자가 `relative isolate` 라 그대로 겹치고 **CLS 0 유지.**

### ⚠⚠ 검증자가 «자기 최종 게이트 문장»을 정정했다
§54 최종 게이트 §G-4 에서 *«실제 대체면은 한 줄이고 위치를 하나도 주장하지 않는다»* 라고 적었는데
**문자열로는 맞지만 «렌더된다»는 함의가 틀렸다.**
→ ★ **소스 계층(③)에서 «문자열만» 확인하고 «배치»를 재지 않았다** —
  **§5.1 이 «배치·가림·표시 여부는 QA 소관»으로 나눈 바로 그 «틈»** 이다. `doc-commit` 대상.

### ★★ O-2 판정 — **«3문장이 거짓»이 아니다. ②는 참이고 «가장 필요하다»**

| 문장 | 401 에서 | |
|---|---|---|
| ① `지도는 손가락 하나로 움직입니다.` | ❌ 거짓 | 지시대상 없음 |
| ② `페이지를 내릴 때는 지도 위나 아래 빈 곳을 쓸어 주세요.` | ⭕ **참 · 가장 필요** | ★ **마운트 `<div style="touch-action:none">` 가 `status` 분기 «밖»**이라 401 에서도 **상자 420px 를 꽉 채운다.** 그 위를 쓸면 **갇힌다.** ②가 그것을 푸는 **유일한 문장** |
| ③ `지도의 화장실과 역을 누르면 거리뷰가 열립니다.` | ❌ 거짓 | 누를 것 없음 |

→ ⚠⚠ **내가 검토한 «(가) 숨긴다»는 명백히 틀렸다** — 거짓 2문장을 지우려다 **참이고 가장 필요한 문장을 함께 지운다.**

### ★ ①③ 이 게시 가능한 이유 — **«거짓인가»가 아니라 «잘못 행동하는가»**
| 문장 | 조합원이 하는 일 | 피해 |
|---|---|---|
| ① | 회색 상자를 쓸어 본다 | **0** |
| ③ | 상자를 눌러 본다 | **0** |
★ **이 프로젝트가 «거짓»으로 막은 것들은 전부 «행동 → 피해»가 있었다** —
QR 링크(믿고 눌러 출석 실패) · 무대 자리(다른 지부 대오에 선다). **여기는 피해가 0 이다.**

#### ⚠ 내 질문 하나가 틀렸다 — *«드물다»로 넘길 수 있는지*
> ★ **«드물다»로 넘기는 것이 아니다. 근거는 «빈도»가 아니라 «피해» 다.**
> **빈도로 판정하면 다음 건에서 틀린다** — 드물어도 피해가 있으면 막아야 하고,
> 잦아도 피해가 없으면 문장을 늘리지 않는다.
> **§54.10 이 401 을 프로덕션에서 재현한 이상 «드물다»는 근거가 될 수 없다.**

### 확정 — **(다) 그대로 둔다. 선행 수정 2건이 조건**
1. **대체면 `absolute inset-0`**(게시 차단)
2. **실패 대체면에 한 줄:**
```
지도를 불러오지 못했습니다.
무대·대오 위치는 아래 목록에 있습니다.
```
⚠ **둘째 줄은 `failed` 에서만**(로딩은 곧 뜬다) ·
⚠⚠ **「범례」라는 낱말을 쓰지 마라 — 화면에 그런 제목이 없다**(`figcaption` 목록에 헤딩이 없다).
  **「아래 목록」이 실제로 보이는 것을 가리킨다.**

## M-46. ★★★ F-B — **내 처방 ①(`map.refresh()`)이 틀렸다. 개발자가 실측으로 기각했다**

| 후보 | 상자 300×375 에서 `map.getSize()` | |
|---|---|---|
| ① `map.refresh()` (**내 처방**) | ★ **336×420 «그대로»** | ❌ 소스가 **투영·타일**을 다시 보는 코드다 |
| ② `Event.trigger(map,"resize")` | 300×375 | ⭕ |
| ③ **`map.setSize(new maps.Size(w,h))`** | 300×375 | ⭕ **채택** |

### ⚠⚠ 내가 «프록시로 판정하지 마라»를 인용한 «그 메시지에서» 그 오류를 저질렀다
내 문장: *«둘 다 «공식 API» 이고 로드된 `maps.js` 에 «실재 확인»됐다»*
→ ★★ **«실재한다»를 «고친다»의 프록시로 쓴 것이다.** `refresh` 는 **실재하지만 다른 일을 한다.**
→ 같은 메시지에서 내가 QA 에게 *«§5.8.4 ③ 프록시로 판정하지 마라»* 를 인용했다. **§5.8.4 ③ 그 자체다.**
→ ★ **`doc-commit` 대상**: **«API 가 존재한다» 는 «그 API 가 이 문제를 고친다» 의 근거가 아니다.**

### ③ 채택 근거 (개발자)
ⓐ ★ **`pano.setSize` 와 «같은 패턴»** 이고 `union-webapp-dev §7` 이 이미 세운 규칙인데
   **지도에만 안 걸려 있었다** — **한 패턴이 둘 되지 않는다**
ⓑ ②는 **«이벤트 이름» 계약**이라 내부 리스너가 사라지면 **조용히** 끊긴다 ·
   ③은 **우리가 «잰 값»을 넘기는 «메서드» 계약**
ⓒ 크기 출처는 **«상자»** 다(마운트는 네이버가 박은 «옛 px»)

**발동 확인**: 336×420 → 296×370 에서 **마커 5개 전부 이동** · 오른쪽 여백 50.8 → **35.8**(전엔 10.8) · **상자 밖 히트 0.**

### ⚠ 새 항목 — 200px 상자에서 `cityhall` 하나가 넘친다(4 → 1)
**재적합 결함이 아니다** — **`fitBounds` 패딩이 «앵커 점»만 보고 «pill 폭»을 모르는** 한계다.
**200px 는 실기기 조건 밖**(360 뷰포트 → 336 · 200% → 296)이고 **296px 에서는 0.**
→ **F-D(`labelGap`)와 같은 축** — **디자이너 몫.** ⚠ 다만 F-D 는 «고치지 않는다» 로 판정됐다(M-44).

## M-47. F-C — 개발자가 **지목 밖 2곳을 함께** 고쳤다. 승인
| 자리 | 처방 | 남는 것 |
|---|---|---|
| 거리뷰 표식 `new maps.Marker`(내가 지목) | `try/catch` → `null` | **거리뷰는 열린다** |
| ★ `build()` 도형·마커 생성 루프 | `try/catch` → `setStatus("failed")` | **대체면 + 범례 13행** |
| ★ `new maps.StreetLayer()` | `try/catch` → `null` | **클릭 이동**(리스너는 `try` 밖) |
**발동 확인 2종**: `maps.Marker` 제거 → `main` **1**(전엔 0) · 시트 열림 · **와드만 0** · 오류 표시 없음 /
`maps.Polygon` 이 던지게 + 재마운트 → `main` **1** · 대체면 + 범례 13행 유지.

## M-48. 대체면 — 개발자가 **독립으로 같은 결함을 찾아 고쳤다**
검증 §60-1 과 **같은 결함**(보이는 높이 **0px**)을 개발자도 프로브로 재고 **`absolute inset-0`** 으로 고쳤다
(8/28 `RallyMap` 이 쓰던 래퍼). 고친 뒤 **420px.**
★ **`loading` 에도 해당** — *「불러오는 중입니다」* 가 **보인 적이 없었다.**

## M-49. 남은 3건
1. **O-1 이중 링**(흰+검정 `box-shadow`) — **미구현**
2. **실패 대체면 한 줄** `무대·대오 위치는 아래 목록에 있습니다.`(`failed` 에서만) — **미구현**
   ⚠ **「범례」 낱말 금지** — 화면에 그런 제목이 없다
3. **조작 안내 «세 번째 방향» 주석** — **미구현**

---

# ★★★ v2 라운드 (2026-09-01) — 자료 전면 교체 · 9/4 까지 사흘

## M-50. 검증 §61 — **반려. 현행 게시본에 거짓 4건. 전부 채택**

| # | 지금 게시 중인 거짓 | 급함 |
|---|---|---|
| A-1 | **지도의 대오 1~4** — 신판에 «개념 자체가 없다» | ★★★ 조합원이 «내 대오»를 찾는데 **화면에 틀린 답이 있다** |
| A-5 | **식순** — 폐회 `14:20` → **`15:15`**(55분) · 순서 변경 · 응원이벤트 신규 | ★★★ **뒤 일정을 잘못 잡는다** |
| A-4 | **`코스콤지부가 어느 대오인지는 추후 안내합니다.`** | ★★★ **답이 있는데 «없다»고 말한다** |
| A-6 | 비고 **`윤석구 금융노조 위원장`·`김동명 한국노총 위원장 외`** | ★★ 원본 확인 — **비고란 20행 전부 공란** |

### ★ 진행 순서 — 검증자 권고 채택
**① 식순·인명(즉시 · 좌표 불필요) → ② 코스콤 한 줄 → ③ 지도 재도출**
⚠ **③ 이 가장 크다** — `strikeMap.ts` 가 **«밴드가 있다»는 전제로 짜인 데이터 모델**이고
  범례·pill·팝업·와드·`ROADVIEW_IDS` 가 전부 거기 매달려 있다. **«문면 교체»로 잡지 마라.**

## ★★★ M-51. 코스콤 위치 — **«사실»과 «좌표»를 갈라라**

| 대상 | 확신도 |
|---|---|
| ★★ **코스콤지부 = 무대 2 구역** | **확인(1급)** — 자료 **3장이 독립 일치** + 사용자 축자 |
| ★ **무대 2 = 코리아나호텔(폴바셋) 앞** | **확인(1급)** — ★ **거리뷰의 `PAUL BASSETT` 간판이 확증** |
| 무대 2 의 **지상 좌표** | **`estimated`** — 아직 도출 안 함 |
| 코스콤 구역의 **도로 «횡단» 위치** | ❌ **도출 불가**(아래) |

★★★ **«코스콤 = 무대 2»는 좌표가 아니라 «사실»이고 1급이다. 좌표가 근사라는 것이 그 사실까지 근사로 만들지 않는다.**
→ **화면에서 두 층을 갈라라**: **문장은 확정형** · **지도 도형은 `estimated` 표시.**

### ⚠⚠ 검증자가 찾은 함정 — 거리뷰 폴리곤의 좌우를 «지상의 좌우»로 읽지 마라
폴리곤이 화면 **오른쪽 차로**에 있지만 **카메라가 어느 차로에 있었는지 모른다.**
→ **원문 축자가 더 강하다**: 「**코리아나호텔(폴바셋) 앞**」·「코리아나 호텔 옆」은 **서측**이다.
★ **§53-4(v1 밴드가 차도에서 20m 벗어남)와 같은 계보 — «그림의 좌우»를 «지상의 좌우»로 읽지 않는다.**

## M-52. ⚠ 좌표를 **하나도** 도출하지 못했다 — 별도 라운드
**v2 지도는 v1 과 축척·범위·기하가 전부 다르다**(종각역~덕수궁까지 훨씬 넓다).
**§53 방법론을 «이방비 판별»부터 처음부터 다시** 돌려야 한다.
⚠⚠ **v1 좌표 재사용 금지** — v1 무대2(37.568206)와 코리아나호텔(37.568231)이 **거의 같은 위도라 될 «것처럼» 보이는데**,
  **v1 무대3(37.567482)과 v2 「서울시의회 본관」은 어긋난다.** ★ **한 점이 맞는다고 나머지가 맞지 않는다.**

## M-53. 확정 문면 (§61-5 전부 채택)
| 자리 | 문면 |
|---|---|
| **코스콤 위치** | **`코스콤지부는 무대 2 구역입니다. 코리아나호텔(폴바셋) 앞입니다.`** ⚠ **사용자 확인 대상** |
| 식순 | **원본 축자 20행** ⚠ **`13:00~13:15`·`13:15~13:30` 만 `~`** — **통일 금지** |
| `11:00-11:30` 칸 | **두 줄**: `사전집회` / `구호제창 / 동영상 상영/경과보고` ⚠ **`상영/경과보고` 에 공백 없음** |
| 비고란 | ★ **비운다.** 열 유지 여부는 **디자이너**(D-29 가 3열→2열 전례) |
| 화장실 마커 | **`임시화장실`** ×2 ⚠ **v1 「시청역 화장실」 마커는 죽는다** — 신판이 「인근화장실」로 뭉쳤다. **되살리면 원문보다 정밀한 주장** |
| 화장실 범례 | **`화장실 : 임시화장실 및 인근화장실(시청역, 스벅 등) 이용`** ⚠ **`스벅` 을 펴지 마라** |
| ~~북단 무대~~ | ⛔ ~~**`메인무대`**(v1 유지 …)~~ **[뒤집힘 2026-09-01 · M-55 ③ 사용자 선택]** → **`무대 1`**(렌더 `무대 1` 3 · `메인무대` **0**). ★ **이 표는 제목이 「확정 문면」이라 다음 사람이 확정 문면을 찾을 때 여는 자리다** — 그 행을 인용하면 **게시 중인 문면을 되돌린다** |

### 미게시
`6,000명`·`3,000명`·`4,000명`·`천막 N` · `2행`·`둘째 줄` · **`촬영 2026년 04월`**(원본 UI 값 — 네이버가 갱신한다) ·
**좌측 세로 라벨 20개**(⚠ **문서가 «천막»이라 말하지 않고 개수도 헤더와 안 맞는다** — v1 전례를 옮기는 것이
바로 §5.8.2 가 막은 «구판에 기댄 판정»이다)

## M-54. 금지 목록 개정 — **기대가 0 이 아닌 행이 7개. 일괄 판정 금지**
`대오` **18 → 1**(식순 `대오정비` 만) · `추후 안내` **2 → 1** · `간이화장실` **2 → 0** · `임시화장실` **0 → ≥2** ·
`시청역 화장실` **2 → 0** · `무대 2` **2 → ≥4** · `코리아나` **0 → ≥1** ·
★ **`14:20` 1 → 0**(회귀 감시 최우선) · `15:15` **0 → ≥1** · `윤석구`·`김동명`·`한국노총` **각 1 → 0**

## M-55. 사용자 답 3건 (2026-09-01)

### ① 코스콤 문면 — **확정. 검증 권고안 그대로**
```
코스콤지부는 무대 2 구역입니다. 코리아나호텔(폴바셋) 앞입니다.
```
★ **자리는 §53-9 (5) 그대로** — 지도 `figure` 안 · 캔버스 밖. **그 자리에서 «문면만» 바뀐다.**
  (M-3 가 *«위치가 정해지면 이 한 줄이 그 자리에서 바뀐다 — 새 자리를 만들지 않는다»* 로 미리 정해 뒀다.)
⚠ **`추후 안내` 기대가 2 → 1 로 바뀐다**(QR 한 줄만). **금지 목록을 함께 고쳐라**(§5.9.3).

### ② 좌측 세로 칸 20개 — **«지부 천막»이 맞다**(사용자 확인 · 1급)
→ **§61-5 (6)의 «게시 불가»가 풀렸다.** ⚠ **다만 «게시한다»가 자동으로 따라오지 않는다:**
- **위치를 모른다** — 사용자가 «위치도 알려주겠다»를 고르지 않았다. **코스콤 천막이 현장 어디인지 우리는 모른다.**
- ⚠ **개수 불일치는 그대로다**(무대 2 옆 11칸 ↔ 헤더 «천막 7»). **그 수치를 인용하지 마라.**
→ ★ **§5.3 판정**: *«코스콤 천막이 있다»만 알고 «어디인지» 모르면 조합원이 다르게 행동하는가?*
  → **검증 회부.** ⚠ **v1 의 «지부천막에서 참석명단 작성» 같은 8/28 문장을 옮기지 마라** — 9/4 근거 0.

### ③ ★★ 북단 무대 — **`무대 1`. 검증 §61-3(«메인무대»)을 뒤집는다**
**사용자 선택: `무대 1`.**
→ ⚠⚠ **§61-3 의 «메인무대» 판정은 죽었다. 인용하지 마라.**
**뒤집는 근거**: 내가 물은 것이 ***«현장 표지판은 어느 쪽입니까?»*** 였고 사용자가 **`무대 1`** 을 골랐다.
→ ★ **현장 표지판이 조합원이 실제로 보는 것이다.** 검증자 근거 3개(v1 연속성 · «넷이 대등» 함의 · 배치도가
  정보가 많다)는 **전부 «우리 화면 안»의 논리**인데, **현장에서 다르게 쓰이면 그것이 이긴다.**
→ **`메인무대` 를 «무대 1」로 교체한다.** 금지 목록: **`메인무대` 0 · `무대 1` ≥1**(§61-8 의 그 행이 뒤집힌다).
⚠ **배치도 헤더는 여전히 「메인무대」다** — **우리는 «지도» 쪽 낱말을 쓴다.** 그 판단 근거를 주석에 남겨라.

## M-56. 검증 §62 — **v2 좌표 도출. 조건부 승인. 전부 채택**

### ★★★ v2 는 «등척» 이다 — v1 결론을 옮겼으면 틀렸다
| 파일 | 아이콘 실측 `sx/sy` | 판정 |
|---|---|---|
| v1 `IMG_6833.JPG` | **1.5632 ± 0.0059** | 이방(등척을 96.3σ 기각) |
| ★ **v2 `총파업_지부위치.png`** | **1.0027 ± 0.0026**(n=50) | ★ **등척 — 1.0σ** |
★★ **«v1 이 이방이었다»를 옮기는 것은 «v1 이 등척이라 가정»하는 것과 «똑같은» 오류다.**
  v1 은 A4 에 붙이며 늘어난 캡처였고 **v2 는 원본 해상도 그대로**(1300×1179)다.

### ★★ 이방 모델을 «독립 관측량»으로 잘랐다 — v1 과 «같은 구조»
| 모델 | RMS | 판정 |
|---|---|---|
| **등척·무회전(3)** | **13.3 m** | ⭕ 채택 (`s = 0.89897 m/px`) |
| 축정렬 이방(4) | 11.3 m | ❌ **기각** — 요구 `sx/sy 1.0432` 가 아이콘 실측 **1.0027±0.0026** 과 **15.6σ** 어긋난다 |
★ **잔차가 2 m 좋아지는 것은 «모델이 참»이라는 증거가 아니다.** 여분 자유도가 GCP 노이즈를 먹었다.

### 확정 좌표 (전부 `estimated` · 오차 상한 **±30 m**)
| 대상 | 좌표 |
|---|---|
| 무대 1 | `37.569655, 126.977183` |
| 무대 2 | `37.568681, 126.977188` |
| 무대 3 | `37.567606, 126.977188` |
| 무대 4 | `37.566340, 126.977137` |
| ★★ **코스콤지부 구역** | **`37.568220, 126.977135`** · 사각형 **36 × 40 m** |
| 임시화장실(북) | `37.569130, 126.976362` |
| 임시화장실(남) | `37.566844, 126.976623` |

★★ **교차 검산**: 코스콤 `37.568220` ↔ **OSM 코리아나호텔 `37.568231` — 위도 차 1.2 m.**
  **환산과 원문 축자(「코리아나호텔(폴바셋) 앞」)가 서로를 확증한다.**
⚠ v1 무대2(37.568206)와 14 m 차이지만 **재사용이 아니라 독립 도출**이다 — §61-6 #3 함정을 피했다.
⚠ **오차가 v1(±25)보다 크다** — 축척이 **0.899 m/px**(v1 0.375 의 **2.4배**)로 거칠다.

### 게시 조건
- ⚠ **무대는 «점»이 아니다.** 원본에 도형이 없고 **라벨 상자 중심은 우리 가정**이다.
  **도트 금지 · 원(반경 30 m).** ★ **v1 보다 약하다** — 띠가 없어 «대오 북단» 같은 보조 근거조차 없다.
- ⚠ **코스콤 구역의 «도로 횡단» 위치를 문장으로 말하지 마라.** 사각형 그대로 그린다.
- ⚠ **외부 지물 이름은 「코리아나호텔(폴바셋)」 하나만** — **원문 축자**라 허용된다.
  **「서울시의회 본관」·「광화문빌딩」 금지**(바탕 지도 라벨이다).

## M-57. 범례 **9행**(v1 13행 → 9행) · `roadview` 대상 5개
```
1  무대 1 — 광화문역 남쪽, 가장 북쪽 무대입니다
2  ★ 무대 2 — 코스콤지부가 있는 무대입니다
3  ★ 코스콤지부 구역 — 무대 2 남쪽, 코리아나호텔(폴바셋) 앞입니다
4  무대 3 — 무대 2 남쪽입니다
5  무대 4 — 시청역 북쪽, 가장 남쪽 무대입니다
6  임시화장실 — 무대 1 과 무대 2 사이 서쪽입니다
7  임시화장실 — 무대 3 과 무대 4 사이 서쪽입니다
8  광화문역(5호선) — 무대 1 북쪽입니다
9  시청역(1·2호선) — 무대 4 남쪽입니다
키  점선 = 근사 · 옅은 면 = 범위 근사
```
★ **«양 끝이 역으로 닫힌 사슬»** 형태가 v2 에서도 성립한다. **실측 검산 통과**(코스콤 = 무대2 남 51 m 등).

**`roadview` 5개**: **코스콤지부 구역** · 임시화장실 2 · 광화문역 · 시청역
⚠ **무대 4개는 넣지 마라** — 원본에 도형이 없어 **빈 도로를 가리킨다.**
★★ **어포던스 문장을 «먼저» 고치고 `ROADVIEW_IDS` 를 갱신하라**(§59-4 «늘리면 깨진다»의 실제 발동):
```
지도의 코스콤지부 구역·화장실·역을 누르면 거리뷰가 열립니다.
```
⚠ **`지도의 표식을 누르면…` 으로 뭉뚱그리지 마라** — 무대 4개는 안 열린다. **그 순간 거짓이다.**

## M-58. ★ 코스콤 «천막» — **미게시**(§62-7)
사용자가 «지부 천막이 맞다»를 확인했으나(1급) **위치를 알려 주지 않았다.**
→ *«천막이 있다»만 알고 «어디인지» 모르면* **행동으로 옮길 수 없다** — 3,000명 구역에서 «어딘가 있다»는 안내가 아니다.
→ ⚠⚠ **그리고 «있다»만 말하면 위험이 «생긴다»** — 8/28 참석자가 **「참석명단 작성(지부천막)」**(8/28 문장)을 유추한다.
  **9/4 근거 0** 이고 **이번엔 막을 문장이 없다**(위치를 모르니 «방법은 추후» 같은 조건절도 못 단다).
→ **위치가 확인되면 다시 판정한다.**

## M-59. 못 한 것 3건 — 접수
1. ⚠ **광화문역을 «역 노드»로 쓸지 «출구»로 쓸지** — 역 노드는 **무대 1 에서 북쪽 218 m**(v1 은 출구 기준 42 m).
   **범례 8행 「무대 1 북쪽입니다」는 참이지만 «218 m»를 감춘다.** → **디자이너·QA 회부.**
2. 광화문역 출구 배지 번호 ↔ OSM 매칭 불일치 — GCP 에서 뺐다.
3. **거리뷰 시작점 좌표** — ★ **필요 없다**(마커를 누르면 그 좌표에서 열린다).

## M-60. 금지 목록 (§61-8 → §62-8 로 갱신)
**`메인무대` 0**(§61-3 뒤집힘) · **`무대 1` ≥2** · `대오` **1** · `추후 안내` **1** ·
`임시화장실` **≥2** · `간이화장실`·`시청역 화장실` **각 0** · **`코스콤지부 구역` ≥2** ·
`코리아나호텔(폴바셋)` **≥1** · ⛔ ~~★ `천막` 0~~ **[회수 2026-09-01 · B-8]**

⛔ **위 `천막` 0 을 인용하지 마라** — 바로 아래 **M-61** 이 그것을 죽였다(사용자 축자).
★ **기대값은 «회차»마다 다르다**: M-60 회차 **0** → §68 회차 **1** → **v3 회차 4**(`KOSCOM_LOCATION_NOTE` 1 + v3 표 2 + V-b 문장 1).
⚠ **«어느 회차 기준인가»를 안 적은 기대값은 다음 게이트를 거짓 실패로 만든다.** 정본은 `V3-PLAN.md` §3.

## M-61. ★★ 사용자 확인 — **«천막도 코리아나호텔 앞»** (2026-09-01)

### §62-7(«천막 미게시»)의 근거가 소멸했다
§62-7 이 미게시로 판정한 근거는 ***«위치를 알려 주지 않았다 → 행동으로 옮길 수 없다»*** 였다.
→ ★ **위치가 나왔다. 그 근거는 죽었다.**

### ★ 그리고 «원문이 이미 그렇게 말하고 있었다»
지도 범례 상자 축자: **`천막 및 집회: 코리아나호텔(폴바셋) 앞`**
→ §61-5 (5)가 *«머리(「천막 및 집회:」)를 옮기지 마라 — «천막»이 무엇인지 안 밝혔다»* 로 잘랐는데,
  **사용자 확인(«좌측 세로 칸 = 지부 천막» · «천막도 코리아나호텔 앞»)이 그 두 구멍을 «둘 다» 메웠다.**
→ ⚠ **§61-5 (5)의 «장소 부분만 게시» 판정은 죽었다.**

### 확정 — **코스콤 한 줄에 «합친다». 새 마커·새 자리를 만들지 마라**
```
코스콤지부는 무대 2 구역입니다. 천막과 집회 모두 코리아나호텔(폴바셋) 앞입니다.
```
**근거:**
1. ★ **좌표가 «같다»** — 별도 마커를 만들면 **같은 자리에 두 도형**이 겹치고, 조합원은 «둘이 다른 곳인가»로 읽는다.
2. ★★ **원문 축자에 기댄다** — 「천막 및 집회」가 **한 줄에 함께** 적힌 것이 원문이다. **우리가 나눈 것이 아니다.**
3. **§5.3 통과** — *«천막이 «따로» 있나»* 라는 물음을 닫는다. 안 닫으면 조합원이 **현장에서 천막을 따로 찾는다.**

⚠⚠ **여전히 «천막에서 무엇을 하는지»는 말하지 마라** — **9/4 근거 0** 이다.
  ★ **8/28 의 「참석명단 작성(지부천막)」·「물품 수령」을 옮기면 거짓이다.**
  §62-7 이 지목한 «유추 위험»은 **위치를 밝혀도 사라지지 않는다** — **위험이 «이동»했을 뿐이다**
  («어딘지 모른다» → «거기서 무엇을 하는지 모른다»). **그 자리를 «채우지» 마라.**

### 금지 목록 개정
**`천막` 0 → 1**(위 문장) · ⚠ **`참석명단`·`물품 수령`·`명단 작성` 은 «각 0» 유지**

## M-62. 검증 §63 — 역 좌표·sr-only 문면. **전부 채택**

### ★ 판정 1 — **(가) 역 노드.** 내 lean((나)/(다))이 기각됐다
| 안 | 기각 사유 |
|---|---|
| **(나) 두 점** | ★★ **v2 원본에 근거가 «0»** 이다. M-19 의 근거는 **v1 렌더 특징**(자주색 역명판에서 줄기 2개가 ⑥·⑤ 배지에 꽂힌다 · 실측 x 385~394·571~583)인데 **v2 는 ⑤ 원형 배지 + 텍스트뿐이고 줄기가 없다.** 옮기면 §5.8.2 위반 |
| **(다) 출구 한 점** | **어느 출구인지 모른다**(§62-6 #2 — 배지 `1` 이 OSM 광화문 1번과 어긋난다). ⚠ ★ **«가장 가까워 보이는 것»을 고르는 것이 이 프로젝트가 «날조»로 부르는 것**이다 |

**확정 — ⚠ 두 역의 «등급이 다르다». 같게 적지 마라:**
| 역 | 좌표 | 확신도 |
|---|---|---|
| **시청역(1·2호선)** | `37.565480, 126.977114` | **`verified`** — OSM + v1 원본 픽셀 환산 **1.2 m 일치** |
| **광화문역(5호선)** | `37.571616, 126.976903` | ⚠ **`calculated`** — OSM 단독 · **v2 원본과 대조 못 했다** |

### ★★ 판정 1-보 — **`fitBounds` 에 광화문역을 «넣는다». 내 우려는 실측으로 맞았지만 결론이 반대다**
| bounds | 세로 범위 |
|---|---|
| 무대 4 + 코스콤 + 화장실 2 | 368 m |
| + 시청역 | 464 m (**+26%**) |
| **+ 광화문역** | **682 m** (**+85%**) |

→ **내 우려(«가장 중요한 것이 작아진다»)는 «참»이다. 그런데 빼면 «더 나쁘다»:**
★★ **범례 8행(`광화문역(5호선) — 무대 1 북쪽입니다`)이 «지도에 없는 것»을 말하게 된다.**
★★★ **결정적 근거 — §53-15 가 «거리 문구»를 금지한 이유가 «지도가 원근을 보여 준다» 였다.**
  **지도가 안 보여 주면 그 금지의 전제가 깨지고**, 그러면 조합원이
  ***«광화문역에서 내릴까 시청역에서 내릴까»***(**§5.3 행동이 갈리는 판단**)를 할 수 없다.
⚠ **축척 감소의 대가는 «확대»가 진다**(M-14 로 확대가 열려 있다 · §54.16-2 와 같은 논리).
⚠⚠ **둘을 «따로» 결정하지 마라** — 빼면 범례 8행을 함께 고쳐야 하고, 고칠 문면이 마땅치 않으면
  **항목을 통째로 빼야 하는데 그것이 §5.3 위반**이다.
★ **초기 화면에서 라벨이 겹치면 `labelGap` 으로 풀어라. 항목을 접지 마라**(§54.16-3 그대로).

### 판정 2 — `sr-only`·대체면: **«뭉뚱그린다»**
★ **답은 «나열»이 아니라 «뭉뚱그리는 낱말»이다** — 무엇이 있는지는 **범례 9행이 전부 말한다.**
```
① MAP_SR_INTRO
아래는 세종대로 집회 장소를 표시한 지도입니다. 같은 내용을 지도 아래 범례에서 텍스트로 제공합니다.

② StrikeMapFallback 둘째 줄 (failed 에서만)
집회 장소는 아래 목록에 있습니다.
```
| 요소 | 근거 |
|---|---|
| **`세종대로`** | **원문 축자**(「장소 : 세종대로 …」) + **헤딩 `세종대로 안내지도` 와 같은 낱말** |
| **`집회 장소`** | 9개 항목을 **하나도 빠뜨리지 않고** 덮는다. ★ **나열하면 반드시 하나가 빠진다** |
⚠ **①의 둘째 문장을 지우지 마라** — **§55-5 가 잡은 함정의 정정본**이다(«지도 아래 범례»는 v2 에서도 참).
⚠ **②에 「범례」를 쓰지 마라** — **화면에 그 제목이 없다**(§60-4). **「아래 목록」** 유지.
⚠ **②를 「코스콤지부 구역을 포함한…」으로 늘리지 마라** — 실패 화면의 목적은 **«아래로 가라» 하나**다.

### 판정 3 — **`±25 m` → `±30 m` 정당. 확인**
v2 오차 상한이 ±30 m 다(잔차 13.3 · 축척 0.899 m/px = v1 의 **2.4배 거칢**).
**`±25 m` 를 남기면 «정밀도 과장»이고 그것이 거짓이다.**
⚠ **금지 목록**: `±25 m` **0** · `±30 m` **≥1** — **회귀 감시 대상.**

## M-63. 검증 §64 — **④ 채택**(내 절충안) · 낱말 충돌 **그대로**

### ★★★ §63-2 가 «뒤집혔다» — 그러나 **당시 근거는 오보였다**

⛔⛔ **[회수 2026-09-01 · 리더] 아래 인용문은 «죽었다». ④의 근거로 인용하지 마라.**
> ~~→ z15 로 떨어지면 지도는 «원근»을 보여 주는 대신 **«아무것도 못 누르게»** 만든다
> (무대2 pill ↔ 화장실북 배지 **13.2 px** · 24 px 미달).~~
> ~~→ 원근 정보의 «있으면 좋음» < 조작 가능성의 «상실». **저울이 뒤집힌다.**~~

**개발자 오보였고 개발자가 스스로 정정했다**(`M-66-2` · `M-67`): `13.2 px` 는 **세로 성분만**이고
그 쌍은 **가로로 떨어져 겹치지 않으며**, 애초에 **무대 pill 은 히트가 아니라 WCAG 2.5.8 「대상 쌍」이 아니다.**
**참값(전폭 실측 · QA §67-2): 히트↔히트 2차원 최소 67.54~67.98 px — z15·z16 «둘 다» 통과.**

★ **④ 판정 자체는 살아 있다. 근거가 다르다 — «판독»이다:**
> z16 에서 코스콤지부 구역 사각형이 **19×21 px** 인데 z15 면 **9.5×10.6 px** 로 반토막 난다.
> **그 사각형이 이 지도에서 유일하게 「조합원이 서는 자리」다. 나머지 8행은 전부 참조물이다.**
> **맥락 지물 하나를 첫 화면에 담으려고 「행동 대상」을 반으로 줄이지 않는다.**

⚠⚠ **「조작 가능성의 «상실»」을 판독 축으로 옮겨 살리지 마라 — 거기서는 «거짓»이다**(검증 §69-3).
**68 px 이면 z15 에서도 누를 수 있다.** 옮기면 **회수한 오보가 문장만 갈아입고 부활한다.**
★ 이 자리가 위험한 이유: **M-63 은 「④를 왜 골랐나」를 찾을 때 «가장 먼저 여는» 자리다.**
⚠ **§63-2 의 «넣는다»는 죽었다. 인용하지 마라.**

### 확정 — **④ `fitBounds` 대상에서만 뺀다**
```
fitBounds 대상 = 무대 4 + 코스콤지부 구역 + 임시화장실 2 + 시청역   (남북 464 m)
표시(마커·범례) = 위 + 광화문역                                    (범례 9행 «불변»)
```
| 검사 | |
|---|---|
| 범례 8행이 참인가 | ⭕ **참 — 지도에 «있다».** §63-2 가 걱정한 것이 지켜진다 |
| §0.4 콘텐츠 은폐인가 | ❌ **아니다.** §0.4 가 막는 것은 **탭·아코디언 «뒤»에 숨기는 것**이다. 지도는 본래 **움직이는 매체**이고 **M-14 로 드래그가 열려 있으며 조작 안내 문장이 그것을 말한다** |
| §5.3 | ⭕ **통과.** «어느 역에서 내릴까»는 **범례 8행 + 원문 축자 「세종대로 (광화문역, 시청역)」** 이 이미 답한다. **초기 화면의 «정확한 원근»은 «있으면 좋은» 것이지 «없으면 잘못 가는» 것이 아니다** |
| 시청역은 왜 넣나 | **거리가 다르다** — 96 m(+26% · **464 m**) vs 218 m(+85% · 682 m). ★ **대칭은 목적이 아니다.** 464 < 494(①안)이라 **여유가 ①보다 크다** |

### ★★★ ④의 결정적 이점 — **예보가 틀려도 손해가 없다**
개발자 예보는 **브라우저 실측이 아니다**(정수 줌·상자·`FIT_PADDING` 계산값 · §5.7).
→ ★ **④는 bounds 를 «좁히는» 방향이라 축척이 같거나 «올라간다». 예보가 틀렸어도 (가)보다 나빠질 수 없다.**
→ **QA 실측을 기다리지 말고 진행한다. 실측은 «게시 조건»으로 남긴다.**

⚠ **`FIT_PADDING` 을 함께 줄여 «맞추려» 하지 마라** — 건드리면 **초기 화면 라벨 여유가
  §54.16-3(서쪽 27 px)에서 다시 계산돼야 한다.**

### ①②③ 기각 근거 (요약)
- **①** 출구 — ★ **근거를 세울 수 없다.** «두 점»은 v1 렌더 특징 · «⑥ 하나»는 배지↔OSM 매칭 실패.
  ⚠ **«가장 가까워 보이는 것»을 고르는 것이 §12-4 가 «날조»로 부른 것이다**
- **②** 종횡비 — §54.16-2 가 **이미 한 번 뒤집은 값**이고 «빈 곳 44 px» 판정선이 매달려 있다. **사흘 남기고 그 사슬을 흔들지 마라**
- **③** 미표시 — 범례 **사슬의 북쪽 끝이 끊긴다** · **조합원이 광화문역에서 온다**

## M-64. 낱말 충돌 — **그대로 둔다.** 내 lean 이 맞았다

★ **M-1 의 위험과 «다른 것»이다**: M-1 이 막은 것은 **«9/4 페이지가 8/28 의 구획 «체계»를 빌려 쓰는 것»** 이었다.
지금은 **각 페이지가 «자기 집회의 자기 자리»를 「코스콤지부 구역」이라 부른다** — 뜻이 **같다.**
**낱말 충돌이 아니라 «정상적 재사용»이다.**

**맥락이 갈리는 근거 3중**: ① 그 문장 «안»에 **「여의도공원」** ② **§56-1 실측** — 8/28 `past` 프리렌더에서
`RALLY_PAST_NOTE` 가 `<h1>` 바로 아래 **본문 1회 실재**(프레임이 **페이지 최상단에서 이미 갈린다**)
③ **9/4 에 「여의도」·「공원 화장실」 렌더 0** — **두 낱말이 한 화면에 없으면 충돌이 성립하지 않는다.**

⚠⚠ **고치지 마라** — 그 문장은 **8/28 검증 25회차 확정 문안**이고 주석이 *«문구는 카드에 있던 그대로다.
다시 쓰지 마라»* 로 못박고 있다(요구 186·187). **뒤집을 새 사실이 없다.**

## M-65. 게시 조건 3건 (§64-3)
| # | |
|---|---|
| 59 | **`fitBounds` 에서 광화문역만 뺀다.** 마커·범례 9행 그대로. ⚠ **`FIT_PADDING` 손대지 마라** |
| 60 | ~~**QA 실측**: `320·360·390·412` 줌 레벨 + **무대2 pill ↔ 화장실북 배지 ≥ 24 px**~~ → **[부분 해소 2026-09-01 · M-66-3]** 360 실측 **68.0 px**. ⚠ **조건 문면이 틀렸었다 — 무대 2 에 pill 이 없다.** 남은 폭 3개는 QA |
| 61 | ~~**초기 화면 밖 광화문역이 «드래그로 실제로 닿는지» 실측**~~ → **[해소 2026-09-01 · M-66-4]** 드래그 120 px 로 닿는다. ⚠ **그러나 「키보드」는 안 닿았다 — M-66-5** |

## M-66. ★ 실측 라운드 — **「잴 수 없다」가 거짓이었다.** 게시 조건 60·61 해소 + 새 결함 1건

**2026-09-01 · 리더 실측**(Playwright MCP · `localhost:3000` 개발 서버)

### M-66-0. ⛔ **401 전제가 죽었다** — 인용 금지 목록

`.env.local` 의 `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID` 는 **10자 실키(`x79smqla3u`)이고 localhost 가 등록돼 있다.**
네이버 타일 **16장 실렌더** · 대체면 미출현. 개발자 보고 *「이 환경 키가 `x` 라 401 대체면만 나온다」* 는 **오보**다
(env 를 안 읽은 실행을 본 것으로 보인다 — **`x` 로 시작하는 키를 키 이름으로 읽었을 가능성**).

⛔ **죽은 전제 — 이 문장에 기대어 내린 판단은 전부 다시 연다:**
- *「브라우저 실측 0 · 겹침·터치·200% 는 전부 계산 예보」*
- *「사흘 남기고 그 사슬을 흔들지 마라」* 의 **근거 절반**(= 재계산해도 «확인할 방법이 없다»). ★ **금지는 유지하되(M-66-6) 근거가 바뀌었다.**

### M-66-1. 측정 환경표 (§5.5)
`360 × 640` · **`innerWidth − clientWidth = 0`(스크롤바 없음)** · `dpr 1` · `visibilityState: visible`
루트 `font-size 12px` · `documentElement.scrollWidth 360`(**가로 스크롤 0**) · 지도 상자 `336 × 420`

### M-66-2. 실측값 — 개발자 정정이 **확증됐다**
| 항목 | 개발자 예보 | **실측** | |
|---|---|---|---|
| 초기 줌 | z16 | **z16** — 1.892 m/px (무대1↔무대4 **369.0 m / 195 px**) | ✓ |
| 히트 대상 | 무대 pill 은 히트 아님 | **히트 5개** ⭕. ~~무대 1~4 는 pill 자체가 없다~~ ⛔ **거짓 — M-67** | 앞 절반만 ✓ |
| 히트↔히트 2차원 최소 | 68.2 px | **68.0 px** (`koscom-area` ↔ `toilet-north` · dx 61 · dy 30) | ✓ 오차 0.2 |
| 코스콤지부 구역 사각형 | — | **19 × 21 px** (SVG rect 25×27 은 스트로크 포함). **z15 면 9.5 × 10.6** | ④ 근거 확증 |

⛔⛔ **이 문단은 «인용 금지» — M-67 이 철회했다. 내가 틀렸다.**
> ~~폐기: 개발자의 「남는 임계 z16 2.4 px 세로」. 축도 값도 틀렸다 — 무대 2 에 pill 이 있다고 가정해
> `26.4 − 24` 로 세운 값인데 그 pill 은 없다. 참값: `toilet-north` 배지 ↔ 무대 2 «원» 2차원 최소 3.0 px~~

**참값은 M-67 에 있다: `toilet-north` 배지 ↔ 무대 2 «pill» 2차원 최소 2.0 px(세로).**
결론만 유지 — **겹침 0 · 시각 사안 · 규격 아님**(v1 「여유 2.7 px」와 같은 계열).

### M-66-3. 게시 조건 **60 해소** [해소 2026-09-01]
`360` 에서 히트↔히트 **68.0 px ≥ 24 px**. ⚠ **조건 문면 자체가 틀렸었다** — *「무대2 pill ↔ 화장실북 배지」*
라고 적혀 있는데 **무대 2 에는 pill 이 없다.** 그 쌍은 **히트↔장식**이라 2.5.8 대상이 아니다.
★ **남은 폭(320·390·412) 은 QA 로 넘긴다** — 해소된 것은 **「360 에서 규격 미달이 아니다」** 까지다.

### M-66-4. 게시 조건 **61 해소** [해소 2026-09-01] — **단, 절반만 참이었다**
지도 중앙에서 **아래로 120 px 드래그** → 광화문역이 상자 안에 **완전히** 들어왔다(`gwNowInsideBox: true`).
**포인터 사용자는 닿는다.** → §0.4 「은폐 아님」의 조건 ② 성립.
⚠ **그러나 조건을 「드래그」로만 적은 것이 M-66-5 를 놓치게 했다.**

### M-66-5. ★★★ 새 결함 — **④가 만든 WCAG 2.4.7 (AA) 위반**
```
광화문역 히트 rect  y 796.2 – 820.2      (role="button" tabindex="0")
지도 상자 top       y 852.2
→ 상자보다 32 px «위». overflow:hidden 에 통째로 잘린다.
```
- **탭이 도달한다**(`activeElement` 확인). 포커스 링은 **클립 밖**에 그려져 **보이지 않는다.**
- 컨테이너 `scrollTop` **0 → 0**(`containerScrolled: false`). **요소가 스크롤 원점 「위」라 음수 scrollTop 이 필요**해
  영영 안 들어온다. `scrollHeight 548 / clientHeight 420` 이라 **「스크롤 가능」해 보이는 것이 함정**이다.
- 브라우저는 대신 **페이지**를 스크롤했다(`scrollY 0 → 440`). ⚠ **「페이지가 움직였으니 됐다」가 이 결함을 숨기는 방식이다.**
- **처방**: 히트가 **포커스를 받을 때** 마커 rect 가 상자 안에 완전히 없으면 `panTo`. **이미 안이면 아무것도 하지 마라.**
- ⛔ `aria-hidden`·`tabindex="-1"` 로 **숨겨서 고치지 마라** — 키보드 사용자만 광화문역 거리뷰를 잃고,
  **역 둘 중 하나만 거리뷰가 열리면 「그쪽으로 오라」** 가 된다(M-63 에서 이미 기각한 형태).

★ **이 결함이 M-63 ①②③ 기각 검토에 없었던 이유**: 셋을 **「초기 화면에 보이는가」** 축으로만 갈랐고
**「보이지 않는데 조작 가능한가」** 를 안 물었다. **「안 보인다」와 「없다」는 다르다** — 후자만 안전하다.

### M-66-6. 종횡비 — **금지 유지. 근거를 다시 쓴다**(판정 기록 규율)
```
지도 상자 높이 ≥ 453 px 이면 z16 에서 9개 전부 초기 화면에 담긴다.
  남북 682.4 m / 1.8934 = 360.4 px  +  FIT_PADDING(top 48 + bottom 44) = 452.4
현재 336 × 420 → 33 px 부족.
```
⛔ **이번에 고치지 마라.** ★ **종전 근거**(*「고쳐도 확인할 방법이 없다」*)는 **M-66-0 으로 죽었다.**
**남은 근거는 하나뿐이고 그만큼 약하다**: 축척 → §54.16-3 서쪽 여유 27 px → 팝업 배치가 **한 줄로 매달려 있어**
**4개 폭 × 확대 배율까지 재측정**이 필요하다. **9/4 까지 사흘.** → **후속 #30.**

## M-67. ⛔ **리더 자기 정정** — M-66-2 의 「무대 pill 이 없다」가 거짓이었다

**2026-09-01 · 리더**

### M-67-1. 무엇이 틀렸나
지도 상자 안 라벨을 **`span` 태그로만** 훑었다. **무대 1~4 pill 은 `<div>` 다.** 그래서 「없다」가 나왔다.
★★ **개발자에게 「세는 방법이 질문에 안 맞는다」(한 축 성분으로 2차원 판정)고 지적하면서 리더가 같은 계보의 오류를 냈다** —
이번 형태는 **「태그를 가정하고 셌다」** 다. **이 프로젝트 「세는 방법」 계보에 하나 더 얹는다.**

### M-67-2. 참값 (태그 무가정 · 상자 안 후손 전수 · 자기 직접 텍스트 노드 기준 · `360×640`)
| 라벨 | 태그 | rect |
|---|---|---|
| 무대 1 | **div** | x 102.3–147.0 · y 937.2–961.2 |
| **무대 2** | **div** | x 100.7–147.0 · y **994.2**–1018.2 |
| 무대 3 | div | x 100.4–147.0 · y 1058.2–1082.2 |
| 무대 4 | div | x 98.3–145.0 · y 1132.2–1156.2 |
| `toilet-north`(히트) | span | x 141–165 · y 968.2–**992.2** |

**히트 ↔ 장식 2차원 최소 = `toilet-north` 배지 ↔ 무대 2 pill = 2.0 px**(가로 겹침 · **세로** 2.0). 차점 무대 1 pill **7.0 px**.
→ **개발자의 「2.4 px 세로」는 축이 맞았고 값은 0.4 px 차이**(모델 vs 실측). **그의 기하 모델은 신뢰할 만하다** —
광화문역 pill 이 점보다 **38 px 위**라는 것도 리더 실측(37.6)과 맞았다.

### M-67-3. 살아남은 것 — **결론은 안 바뀐다**
- 무대 pill 은 `roadview` 가 없어 **히트가 아니다** → **WCAG 2.5.8 대상 쌍이 아니다.** 그 2.0 px 은 **시각이지 규격이 아니다.**
- **히트 5개 · 히트↔히트 2차원 최소 68.0 px** — 변동 없음.
- **M-66-5(WCAG 2.4.7)·M-66-3·M-66-4·M-66-6 은 이 오류와 무관하다.**

### M-67-4. `placement: "top"`(광화문역 pill) — **금지 유지. 근거를 «새로» 적는다**
개발자 실측: pill 이 점보다 38 px 위라 **`320·360·390·412` 에서 pill 이 «완전히» 밖**이다(412 는 4.2 px 모자람).
★ **즉 「초기 화면 밖」은 예외가 아니라 휴대폰의 기본 상태다.**
개발자 지적대로 **이 값의 종전 근거는 두 겹 다 죽었다**(「두 점 위」→ 점이 하나 · 「최북단」→ bounds 밖).

⛔ **그래도 지금 바꾸지 않는다. 새 근거:**
> `placement` 를 바꾸면 **충돌 지형이 통째로 바뀐다.** 히트↔장식 최악이 **2.0 px** 로 여유가 거의 없고,
> `"bottom"` 은 광화문역 pill 을 **무대 1 쪽으로** 내린다. 결과는 **`320·360·390·412·768·1280` × 기본·200%**
> 를 다시 재야 확정된다. **9/4 까지 사흘.**

⚠ **「근거가 죽었으니 바꾸자」가 아니라 「바꿀 시간이 없다」가 지금의 근거다 — 둘은 다르다.**
→ **후속 #30 에 합류**(상자 높이 33 px 과 같은 사슬). 9/4 이후 **한 번에** 다시 잰다.

### M-67-5. §0.4(콘텐츠 은폐) 재확인 — **통과. 단 「경로」가 하나 준다**
가려진 것은 **정보가 아니라 「거리뷰 진입점」** 이다. 정보는 **범례 8행**이 텍스트로 말한다.
남는 경로: **포인터 = 드래그·축소**(M-66-4 실측 · `MAP_GESTURE_NOTE` 안내) · **키보드 = 없었다 → M-66-5 `panTo` 가 만든다.**

## M-68. 「세는 방법」 계보 — **이 라운드에만 3건.** 그리고 새 축 하나

**2026-09-01 · 개발자 자수 1건 + 리더 자수 1건**

### M-68-1. ★ 새 변종 — **「무엇이 움직였나」를 물을 때 「무엇을 기준으로」를 안 정했다**
개발자가 `panTo` 가드를 확인하며 **「지도 이동 400 px」** 을 얻어 **「가드가 깨졌다」를 보고할 뻔했다.**
원인: **지도 이동을 「뷰포트 절대좌표」로 쟀다.** 직전 포커스가 **페이지를 488 px 스크롤**시켜
**상자 자체가 움직여 있었고**, 잰 것은 **지도 이동이 아니라 페이지 스크롤**이었다.
**참값은 「상자 기준 상대좌표」** — 상자도 함께 움직이므로 차분에서 소거된다 → **0.0 px.**

★ **`union-qa-testing §5.8.4 ③(프록시 금지)`의 새 변종**이다. 거기는 «행 높이로 줄바꿈을 추정»이고
여기는 **«스크롤하는 문서 안에서, 자기도 스크롤되는 상자 «안» 요소의 이동을 절대좌표로 잼»** 이다.
⚠ **이 페이지에서는 특히 잘 걸린다 — 포커스가 «페이지를» 스크롤시키는 것이 M-66-5 의 핵심 관측이었다.**
**같은 현상이 한 번은 「단서」였고 한 번은 「오염원」이었다.**

### M-68-2. 「세는 방법이 질문에 안 맞는」 계보 — **한 라운드에 3건**
| # | 누가 | 무엇을 셌나 | 왜 틀렸나 |
|---|---|---|---|
| 1 | 개발자 | 「한 축 성분」으로 2차원 간격 | 세로 13.2 px 만 보고 **가로 4.7 px 를 안 봤다** → 「24 px 미달」 오보 |
| 2 | 리더 | **`[data-strike-hit]`** 로 라벨 계수 | 그 쿼리는 **히트만** 센다 — 장식은 안 잡힌다 |
| 3 | 리더 | **`span` 태그로만** 상자 안 라벨 훑기 | **무대 pill 은 `<div>` 다** → 「무대 pill 이 없다」 거짓 판정(M-67) |
| **4** | **QA** | **여러 전이를 «한 차분»에 뭉쳤다**(누적 ↔ 단계) | `panTo` 가드 검증에서 **「가드가 228 px 깨졌다」**를 낼 뻔했다. 원인은 **`cityhall` 로 가는 탭 경로가 `gwanghwamun` 을 «반드시» 통과**하는 **경로 교락**이다 — tab 7 이 의도된 panTo 이고 tab 8 은 그 팬 때문에 `cityhall` 이 **진짜로** 밖이 돼서 옮긴 것. **탭 «한 칸마다» 차분**을 내서야 갈렸다 |

| **5** | **리더** | **«어느 파일을 세는가»** — 회수 잔존을 세면서 `03_developer_impl.md`·`04_qa_report.md`·`05_verifier_final.md` 를 **범위에서 뺐다**(내 계수 3·5 ↔ 검증 참값 9·17. **판정은 같았다 — 26건 전부 정당**) | 리더 |

★ **5번이 말하는 것**: *「회수가 됐는가」를 물을 때 **산출물 문서를 빼면 안 된다** — 회수를 «기록한» 자리와
«주장하는» 자리는 **같은 문자열로 나타난다.*** 범위를 좁히면 **«주장하는» 자리를 못 본다.**

| **6** | 리더·개발자 | ★ **입력** — «내가 누른 것이 그것이 맞나» | 고정 헤더 68px 가 클릭을 훔쳤다(M-73-2) |

⛔ **[정정 2026-09-01] 리더가 여기에 «일곱째 축: 시점»을 만들었다가 되돌렸다.**
**축을 늘리면 다음 사람이 «나는 «대상»은 봤으니 안전하다»로 읽고 같은 함정에 걸린다.**
★ **개발자 진단이 맞다: «시점»은 «기전»이고 «원인»은 «대상»이다.** → **2번 축의 «하위 형태»로 접었다.**

### 2번 축(대상)의 ★ 하위 형태 — **«같은 대상의 «다른 계산량»을 쟀다»**

앞선 대상 오류는 **«어느 «물건»인가»** 였다(장식↔히트 · `span`↔`div`).
이것은 **«같은 물건의 «어느 값»인가»** 다 — 그래서 **더 안 보인다.**

| | |
|---|---|
| 질문 | **`sizes` 가 평가하는 값**은 무엇인가 |
| 리더가 잰 것 | `getBoundingClientRect().width` = **레이아웃 폭**(루트 18px 에서 306 → 279) |
| 왜 안 닿나 | ★ **`sizes` 는 프리로드 스캐너가 «CSS 적용 전»에 평가**한다 → `rem` 은 **초기 16px** |
| 결과 | `4.5rem` = 54px 이 **아니라 72px** · 두 조건에서 **슬롯보다 4.8~6.0% 작은 이미지**(QA §75) |

## ★★★ 그런데 이 사례가 «앞의 것들과 다른 점»은 남는다 — 측정이 «있었는데도» 틀렸다

앞의 사례들은 전부 **«안 쟀거나 엉뚱한 것을 쟀다»** 였다. 이번은 다르다:

> **둘 다 «측정한 것»은 맞았는데 «그 측정이 이 결정에 닿는가»를 아무도 안 물었다.**(개발자 관찰)

- 개발자가 **구조 논증**을 냈다(«`rem` 이 패딩 사슬을 그대로 쓴다») — **CSS 상 참이다**
- 리더가 **레이아웃 실측**으로 보강했다(루트 18px → 슬롯 306 → 279) — **관측으로 참이다**
- ★ **그런데 `sizes` 는 그 어느 시점에도 살지 않는다** — **프리로드 시점에 얼어 있다**

⚠⚠ **리더가 «말로 다투지 않고 쟀다»고 선언했고, 개발자가 그 실측을 근거로 주석까지 강화했다.**
**측정이 판정을 «틀린 쪽으로» 밀었다.** 이 저장소에서 처음이다.

> **규칙**: 값이 **CSS 밖에서 평가되는 자리**(`sizes` · `media` 속성 · 프리로드 힌트)를 판정할 때는
> **«내 측정이 그 자리와 같은 시점에 사는가»** 를 먼저 묻는다.
> ⛔ **레이아웃 관측을 프리로드 판정에 옮기지 마라.**
> ★ 그리고 **«같은 값의 두 표기»가 «평가 시점»에서 갈릴 수 있다**(`4.5rem` ↔ `54px` — CSS 상 동일, `sizes` 에서 상이).

★★ **여섯 축이 «서로 다르고, 아무도 남의 함정에 걸리지 않았다 — 각자 «자기» 축에서 걸렸다»**(개발자 관찰).
⚠ 그래서 **한 사람이 앞 사고를 알아도 다음 사고를 못 막는다.** 축 이름을 목록으로 유지하는 이유다:
**기준계 · 대상 · 태그 · 구간.**
★ QA 는 **개발자가 경고한 함정(기준계 = 뷰포트 절대좌표)을 실제로 피했는데** 그렇게 고친 것만으로는 부족했다 —
**상대좌표로 바꿔도 «구간»이 틀리면 같은 거짓 실패가 난다.**

★★ **2·3 은 「같은 한 쌍」을 재면서 연달아 났다.** 그리고 **리더는 그 직전에 개발자에게 1번을 지적하고 있었다.**
→ **소스 주석에는 「누가 틀렸나」가 아니라 「셈법 표」로 적었다**(개발자 판단, 채택). 다음 사람에게 필요한 것은
**누가 틀렸는지가 아니라 「이 지도에서 무엇을 세면 무엇이 빠지는가」** 다.

⚠ **계수를 적지 않는다** — `union-qa-testing §5.7` 이 「여섯 번째」라고 적었다가 같은 라운드에 2건이
세지 않고 추가돼 숫자가 낡았다. **목록으로만 유지한다.**

### M-68-3. 이 회차 최종 해시
`src/lib/strikeMap.ts` = **`f3e4d48f…`** (QA 가 관측한 `ab161af4…` 이후 **주석만** 변경).
⚠ QA 는 이 값을 기준선으로 다시 잡는다(§5.7 빌드 동결).

## M-69. §68 — 「기대 `천막` 0」이 낡았다 [해소 2026-09-01]

**리더가 diff 검토 중 발견.** `page.tsx` 의 「의도적으로 **빠져 있는 것** — 채우지 마라」 목록에
`천막` 이 *「여전히 한 줄도 없다 · **위치를 알려 주지 않았다** · 기대 `천막` **0**」* 로 적혀 있었다.

**셋 다 사실과 어긋났다**: 사용자가 축자로 **「천막도 코리아나호텔 앞」** 을 줬고,
`KOSCOM_LOCATION_NOTE` 가 그것을 말하며, **렌더 실측 `천막` 1** 이다.

★★ **이번 라운드에 「새로 쓴」 주석이다** — 사용자가 위치를 알려 준 **뒤에** 쓰였다.
**근거가 죽은 게 아니라 「죽은 근거를 새로 옮겨 적었다」.** ⚠ 이 형태를 따로 세어 둔다 —
지금까지의 실패는 «낡은 것을 안 고쳤다» 였는데 이것은 **«낡은 것을 새 문서에 복사했다»** 다.

⚠ **위험의 방향**: 기대 0 인 문자열을 **사용자 지정 문면이 포함**한다 → 다음 사람이 그 목록으로 점검하면
**「기대 0 인데 1이 나왔다」로 사용자 축자 문장을 지운다.** `추후 안내` 사고(§5.9)의 재판이고,
**대상이 사용자 지정 문면이라 한 걸음 더 나쁘다.**

**처분**(개발자 반영 · 주석만): `천막` 을 「빠져 있는 것」에서 **빼고** 「코스콤지부 위치」에 붙여
**세 처분 표**로 갈랐다 — **위치 = 생겼다 / 천막 = 「위치가 확인돼」 생겼다 / 대오 = 사라졌다.**
기대를 **계층과 함께** 적었다(`천막` **렌더 1**(`KOSCOM_LOCATION_NOTE`) · `참석명단` 0 · **`지부천막` 0 — 한 덩어리로 읽지 마라**).
`M-58 · §62-7` 인용은 **남기고** *「이 근거는 사용자 확인으로 소멸했다」* 를 명시.

★ **주석만 바뀌었음을 「값으로」 보였다**: 프리렌더 **가시 마크업 sha256 `7fe7130407ee…`(17,911자)가 §68 전후 완전 일치**
(`<script>` 제거 후 — Next 는 RSC 페이로드에 시간에 따라 변하는 수치를 넣는다).

## M-70. 확신도 등급 → 시각 상태 매핑 — **자족적 확정본** (소스가 인용할 «실재하는» 자리)

⚠⚠ **이 절은 「없던 자리를 만드는 것」이다.** 소스 `CONFIDENCE_VISUAL` 이 **`§65-3` 을 8회 인용**하는데
**그 번호는 어느 문서에도 없다**(검증 §68-⑤(1) 실측). **판정이 메시지 이력에만 있었다** —
`union-page-orchestrator` 「판정 기록 규율」이 막으려던 바로 그 상태다. **리더가 만든 것이다.**
→ **소스는 이제부터 `M-70` 을 인용한다.**

### M-70-1. 판정
**9/4 는 확신도 «세 등급»을 쓰고 시각 상태는 «둘»이다. 셋째 시각 상태를 만들지 않는다. 강등도 하지 않는다.**

| 등급 | 지물 | 시각 상태 |
|---|---|---|
| `verified` | 시청역 | 실선 · 「근사」 줄 **없음** |
| **`calculated`** | **광화문역** | **실선 · 「근사」 줄 없음**(= `verified` 와 «같게» 그린다) |
| `estimated` | 무대 1~4 · 코스콤지부 구역 · 임시화장실 2 | 점선(`shortdot`) · 옅은 면 · **`APPROX_NOTE` 「±30 m」** |

### M-70-2. 근거 — **두 축이 다르다**
| 축 | 무엇을 말하나 | 조합원에게 |
|---|---|---|
| 확신도 등급 | **우리가 출처를 몇 개 봤나** | **우리 작업 사정** — `union-fact-check §5.3` 이 **쓰지 말라고 한 것** |
| 점선·「근사」 | **지물이 실제로 거기 있나** | **행동이 갈린다** |

판정 기준 그대로 — *「이 표시가 없으면 조합원이 다르게 행동하는가」*: **아니다.**
조합원은 **우리 핀으로 지하철역을 찾지 않는다**(역 표지로 찾는다). 핀의 역할은 「무대 1 북쪽」이라는
**방향 기준**이고 수십 m 오차로 방향이 바뀌지 않는다.
★ **임시화장실은 정반대다** — 그 자리에 가서 **못 찾을 수 있다.** 그래서 거기 점선이 정당하다.

### M-70-3. `estimated` 강등이 «틀린 처방»인 이유 (개발자 논거 · 채택)
`±30 m` 는 **픽셀 → 좌표 환산의 오차 예산**인데 **광화문역 좌표는 환산 산물이 아니다**(OSM 노드).
강등하면 **없는 오차 예산을 주장**하게 된다 — **거짓이 된다.**

### M-70-4. 범례 키가 안전한 이유
화면의 키는 **「점선 = 근사」** 뿐이다. **실선을 정의하지 않는다** → **실선이 「정확」을 주장하지 않는다.**

### M-70-5. 코드가 지켜야 할 «형태»
- 등급 → 시각 상태를 **3항 전부 명시한 매핑**으로 쓴다. ⛔ **`=== "estimated"` 식의 «우연한 2분기» 금지.**
- `calculated` 행 옆에 **한 줄**: *「누락이 아니라 판정(M-70). 확신도는 출처 이력이고 시각 상태는 지물 존재다 — 다른 축이다.」*
- 등급을 추가하면 **타입 오류로 막힌다**(개발자 `TS2741` 실측 확인).

## M-71. ⚠ 「v1 인용 금지」의 **범위** — 문자 그대로 적용하면 출처가 사라진다

검증 §68 이 잡았다. **v2 자료는 이미지 4장이고 「2. 개요」·「3. 집결시간」을 담고 있지 않다.**
→ **「구판」의 범위는 «그 4장이 대체한 것»까지다.**

> **v1 전사본은 «식순·지도 부분만» 죽었다. 개요·집결시간은 살아 있다.**

⚠ 그 파일 **6행의 「식순 20행 전부 일치」는 «구판 식순»에 대한 확인**이라 **인용하면 거짓이 된다.**

## M-72. ★ 규율 후보 — **「한 글자도 바꾸지 마라」는 지시도 «원문»이 아니라 «지시 문면»일 수 있다**

**개발자 제기 · 리더 채택.** `doc-commit` 에서 `union-fact-check` 로 올린다.

### 장면
식순 2행이 원본에서 **두 줄**(1줄 `사전집회` / 2줄 `구호제창 / 동영상 상영/경과보고`)인데
렌더는 **한 줄 3항 나열**이었다. **오염원은 우리 표기다** — `M-53`·`§61-5` 가 *「**두 줄**: `사전집회` / `구호제창 / …`」*
로 적을 때의 그 `/` 는 **「1줄 / 2줄」을 가르는 메타 표기**였는데,
**세 단계를 거치며 원문의 일부로 굳었고**, 리더가 최종 게이트 지시 `V-2` 에서
**「한 글자도 바뀌면 실패」인 대조 «참값»으로 제시**했다.

★★ **지시대로 대조했으면 게이트가 「통과」로 닫혔다.** 검증자가 **원본 이미지를 직접 열어서만** 갈렸다.

### 규칙 (초안)
> **축자 대조의 「참값」은 지시 문면이 아니라 원본이다.**
> 「한 글자도 바꾸지 마라」가 붙은 문자열이라도 **최종 게이트에서는 원본을 다시 연다.**
> ⚠ 특히 **우리가 구조를 서술하려고 넣은 기호**(`/` `·` `→` 줄바꿈 표기)는
> **전달 단계를 지날수록 내용으로 굳는다.**

★ **`union-fact-check §5.8.1`(「도착지를 직접 본다」)의 «문면 판»** 이다 — 그 절은 **링크·QR** 을 다루고
**문면 쪽 대응물이 없다.** ⚠ 두 절을 한 덩어리로 쓰지 마라: 하나는 **«도착지»**, 하나는 **«원본»** 이다.

### 함께 드러난 것 — **귀속 오류가 이 라운드에 24곳**
| 낸 사람 | 무엇 | 처리 |
|---|---|---|
| 리더 | **메시지 번호(`§65`~`§68`)를 문서 절처럼** 소스에 박히게 했다 — **16곳** | `M-66`·`M-67`·**`M-70`(신설)** 로 재지정 |
| 개발자 | **`§63-2 ④`** 로 6곳 인용 — ④를 채택한 절은 **`M-63`** 이고 **`§63-2` 는 그것이 «개정한» 죽은 절** | `M-63 ④` 로 정정 + 「§63-2 를 ④ 근거로 인용 금지」 명기 |
| 개발자 | 「(QA 실측)」 귀속 2곳 — 실제로는 **리더 실측** | 정정 |

⚠ **기계적 치환이 답이 아니었다** — `§66-4`(WCAG)↔`§66-5`(드래그)가 **서로 뒤바뀌어** 있어
순차 치환이면 **그럴듯하게 틀린** 자리를 가리킨다. 개발자가 **단일 패스 정규식 + 치환 전 문맥 전수 확인**으로 처리했다.

## M-73. QA 방법 규율 후보 2건 (2026-09-01 · QA 자기 기록)

`doc-commit` 에서 `union-qa-testing` 으로 올린다.

### M-73-1. ★ 「전부 실패면 방법을 의심하라」는 **부분 실패를 안 잡는다**
> *전부 실패였으면 방법을 먼저 의심했을 텐데, **일부가 성공하니 「지점에 따라 다르다」는
> 그럴듯한 설명이 먼저 떠올랐다.***  — QA §69-8

★ **§67-6 이 세운 규율의 구멍을 QA 가 스스로 찾았다.** **「절반만 실패」가 「전부 실패」보다 위험하다** —
성공한 절반이 **알리바이**가 된다. → 규칙: **성공률이 0 도 100 도 아니면, 「대상의 차이」를 설명하기 «전»에
「내가 만든 상태가 매번 같았는가」를 먼저 확인한다.**

### M-73-2. **고정 헤더가 클릭을 훔친다** — 이 저장소 전 페이지 공통
★★ **여섯째 축 = 「내가 누른 것이 그것이 맞나」**(개발자 명명). ⚠ **앞의 다섯(기준계·대상·태그·구간·파일 범위)은
전부 «재는 쪽»인데 이것만 «입력 쪽»이다** — 측정은 옳았고 **내가 만든 입력이 내가 믿은 입력과 달랐다.**
**축을 헷갈리지 않으려면 이 대비를 먼저 읽어라.**
QA 가 거리뷰 지점을 누르는데 **클릭이 고정 헤더(높이 68 px)의 로고 링크에 맞아 페이지가 `/` 로 이동**했다.
⚠ **`elementFromPoint` 가 안심시켰는데 그 값은 「스크롤 전」에 읽은 것**이라 클릭 시점의 참값이 아니었다
(§67-6 과 **같은 「언제 쟀나」** 오류). → 규칙: **좌표 클릭 전에 「그 좌표를 지금 무엇이 덮고 있는가」를
«클릭 직전에» 읽는다.** 이 저장소는 **모든 페이지에 그 헤더가 있다.**

## M-74. ★★ 「근거가 죽었다」에는 **두 종류**가 있다 — 낡은 것 / **원래부터 틀렸던 것**

**개발자 관찰 · 리더 채택.** `doc-commit` 에서 `union-page-orchestrator` 「판정 기록 규율」로 올린다.

이 라운드에 «근거가 안 고쳐진» 건이 일곱이었다. **여섯은 «맞았다가 낡은» 것**이고
(`±25 m` · 범례 `13행` · 「천막 위치 미확인」 · `13.2 px` · `§63-2` · 「401 이라 못 잰다」),
**하나는 «원래부터 틀렸던» 것**이다 — `closeStreetView` 의 *「길만 남으면 눌러도 열 것이 없다」*.
**모드가 켜진 채 시트만 닫히면 길 클릭이 `else` 분기로 가 시트를 «연다».** 처음부터 거짓이었다.

★★ **이 구분이 실무를 가른다**: 우리가 쓰는 점검은 전부 **«무엇이 바뀌었나»** 를 본다
(자료 교체 → 역방향 검증 · 판정 뒤집힘 → 죽은 절 지목 · 값 변경 → 파생값 회수).
**«원래부터 틀린» 근거는 아무것도 바뀌지 않아서 그 그물에 안 걸린다.**

> **규칙**: 어떤 판정의 근거를 인용할 때는 **「그 뒤에 바뀐 것이 있나」와 함께 「그 문장이 지금 코드에서 참인가」를 «따로» 확인한다.**
> ⚠ 낡음 점검만으로는 이 형태를 못 잡는다.

★ 함께: **거짓 문장 옆에 참 문장을 나란히 두지 마라** — 다음 사람이 «어느 쪽을 믿을지» 판정해야 한다.
**죽은 절을 이름으로 지목해 교체**한다(행 번호 병기를 금지한 것과 같은 이유). 그 때문에 「한 줄」 지시가 3줄이 된 것은 **정당하다.**

### M-74-2. 처방 — **표식이냐 교체냐를 가르는 것은 「낡음」이 아니라 「인용 가능성」이다**

**개발자가 O7 사유를 고치며 스스로 발견했다** — 「죽은 절 표식」 규율을 **과적용**하고 있었다는 것을.
`closeStreetView` 의 거짓 사유에 취소선 + 지목을 달아 3줄로 만들었다가, **교체·1줄로 되돌렸다.**

| 대상 | 처방 | 왜 |
|---|---|---|
| **판정 기록·산출물**의 죽은 절 | **원문 남기고 표식** | **다른 문서가 이미 인용 중**이다. 지우면 인용이 허공을 가리키고, 경위 추적도 끊긴다 |
| **코드 주석 안의 내부 사유** | ★ **교체(삭제)** | **인용 대상이 아니다.** 교체하면 가리킬 것이 남지 않는다 |

★★ **판별자는 「낡았나 / 원래 틀렸나」가 아니라 「이 문장이 «인용되고 있나」」다.**
두 축은 대개 같이 가지만(뒤집힌 판정은 기록이라 인용되고, 코드 주석은 아니다) **같은 것이 아니다** —
**산출물에 적힌 「원래부터 틀린」 근거라면 표식이 맞다.** 이 라운드의 `13.2 px`(§64-1·M-63)이 그랬다.

⚠ **그리고 거짓 문장을 파일에 남기는 것 자체가 비용이다** — **표식이 있어도 훑는 사람은 본문을 읽는다.**
**남길 이유가 「인용 차단」 하나뿐**이면, 인용될 일이 없는 자리에서는 **남기지 않는 쪽이 낫다.**
