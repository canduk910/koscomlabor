# CI — 프론트(Next.js) + 백엔드(server/) 검증
# main push 시 성공하면 deploy.yml(workflow_run)이 웹 배포를 이어간다.
name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  web:
    name: web (typecheck·lint·build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci
      # 라우트 타입(LayoutProps/PageProps)은 Next가 생성한다 — tsc 전에 생성해야
      # 로컬(.next 존재)과 CI(클린 체크아웃)의 검사 결과가 일치한다.
      - run: npx next typegen
      - run: npx tsc --noEmit
      - run: npm run lint
      - name: Build (NEXT_PUBLIC_API_BASE_URL 은 CI 더미 값 — 실배포 값은 서버 compose 가 주입)
        run: npm run build
        env:
          NEXT_PUBLIC_API_BASE_URL: https://ci-dummy.invalid

  server:
    name: server (typecheck·build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: server/package-lock.json
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
