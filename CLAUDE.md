# 코스콤지부 노동조합 공식페이지

## 하네스: 노조 공식페이지 제작·운영

**목표:** 조합원에게 정밀하게 검증된 정보를 시각적으로 명쾌하게 전달하는 공식 웹페이지를 만들고 운영한다. 검증되지 않은 정보는 게시하지 않는다.

**트리거:** 노조 페이지 관련 작업(콘텐츠 게시, 페이지/기능 추가, 디자인 변경, 버그 수정, 이전 결과 수정·재실행) 요청 시 `union-page-orchestrator` 스킬을 사용하라. 단순 질문·조회는 직접 응답 가능.

**문서 최신화:** 작업이 끝난 뒤 문서·기록을 지금 사실에 맞추는 것은 `doc-commit` 스킬이 담당한다. 이 프로젝트는 **문서가 다음 세션의 유일한 기억**이라, 새로 정해진 규칙·뒤집힌 판정·미해결 항목이 **어느 문서에 앉을지**를 그 스킬이 가린다.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-16 | 초기 구성 (에이전트 4종 + 스킬 5종) | 전체 | - |
| 2026-08-16 | backend-developer 에이전트 + union-backend-dev 스킬 추가, 오케스트레이터에 백엔드 경로 반영 | agents/backend-developer.md, skills/union-backend-dev, skills/union-page-orchestrator | NCP 백엔드(방명록 서버) 로드맵 확정, 프론트와 전문성 축 분리 |
| 2026-08-16 | 모던 스타일 기준(§0) 추가 — Pretendard 필수, 라운드·히어로 패널 언어 | skills/union-design-system | 사용자 피드백: "디자인 구식, 폰트 촌스러움" |
| 2026-08-17 | 프로덕션 데이터 변경 시 WHERE 절 필수 규칙 추가 | skills/union-backend-dev | 배포 중 스모크 정리에 조건 없는 DELETE 사용 발생(실손실 0, 재발 방지) |
| 2026-08-17 | 위 WHERE 절 규칙에 "휘발성 인증 상태 테이블" 승인된 예외 명문화 | skills/union-backend-dev §3 | 비밀번호 변경 시 전 기기 로그아웃은 전체 삭제가 곧 요구사항이라 규칙과 충돌. 개발·QA 양쪽이 판정을 요청해 예외 조건(전용 함수·근거 주석·호출처 1곳·콘텐츠 테이블 제외)을 규칙에 흡수 |
| 2026-08-21 | **★ 문안 절제 절 신설(§5.3)** — 우리가 만든 문장을 최소로 / 시각 자료가 말하는 것을 문장으로 반복하지 않는다 / `※` 단서는 개수를 센다 / 우리 작업 사정을 쓰지 않는다 / 판정 기준 *"이 문장이 없으면 조합원이 다르게 행동하는가"* | skills/union-fact-check §5.3 | **사용자가 두 번 같은 지적**을 했다 — *"애매한 문구는 혼란을 준다"*, *"내용 삭제하고 그냥 그림만 넣어도 되겠다. 쓸데없는 추가문구는 삭제해줘."* 검증을 거칠수록 문안이 길어졌다(근거·정밀도 고지·오독 예방 단서). **하나하나는 정당해 보이는데 합치면 조합원이 읽지 않는 벽**이 된다. 블록 2 는 6줄 중 **4줄이 우리 파생 문장**이었고, 주최측 배치도 원본이 들어오자 그 존재 이유가 사라졌다. ⚠ 함께 명시: 줄이면 안 되는 4가지(원문 확정도·행동이 갈리는 사실·시각 자료에 없는 정보·**접근성 대체 경로**) |
| 2026-08-21 | **최종 게이트 대조 방법 명문화(§5.1·§5.2)** — 3계층(프리렌더 본문 / **속성 텍스트** / **소스 문자열 집합 차분**), `innerText` 는 문안 게이트에 쓰지 않는다, **`grep -c` 금지(`grep -o \| wc -l`)** | skills/union-fact-check §5.1~5.2 | 태그 제거 정규식이 **`aria-label`·`title`·`alt` 를 통째로 버려** 문안 게이트가 시각 텍스트만 보고 있었다(`<dialog aria-label>` 이 차분에 나올 수 없었다). 그리고 **리더가 `grep -c` 결과 `1` 을 근거로 잘못된 지적**을 냈다 — 미니파이 산출물은 한 줄이라 무엇을 세든 1이다(참값 2). 기능 플래그 뒤 문자열이 **"신규 저작"으로 오인**되는 것도 소스 집합 차분으로만 갈린다 |
| 2026-08-21 | **§5.5 3개 항목 추가** — 환경표에 정적자산 해시·브라우저 라우트 잔존 / **런타임 위치 요소는 표본 집합으로** / **조건문은 발동 절차와 짝이어야 한다**. **후속 목록 갱신 규율** 추가 | skills/union-qa-testing §5.5, skills/union-page-orchestrator | 프리즈 파기에서 `.next/static` 이 `server.js` 보다 **먼저** 갈린 구간이 실재했다(구 서버+신 자산은 `BUILD_ID` 대조로 안 잡힌다). `내 위치` 겹침을 **모킹 좌표 1점**으로 재고 "겹침 0"으로 적을 뻔했고, 그 위에 세운 종결 조건(*"실교차 관측 시 재개"*)은 **발동 경로가 없어 통과 사유로만 기능**했다. 그리고 `FOLLOWUPS #10` 의 원인 2건이 이전 라운드에 이미 해소됐는데 항목이 안 고쳐져 **리더가 낡은 자료를 설계 판정에 넘겼다** |
| 2026-08-21 | **판정 기록 규율 절 신설** — 판정은 산출물에 두고 메시지는 그것을 가리킨다 / 뒤집을 때 죽은 절을 이름으로 지목 / 대체 표식 / 회수는 파생값까지 / 같은 숫자가 다른 계약이면 계약으로 부른다 | skills/union-page-orchestrator | **리더(나)가 같은 항목을 세 번 뒤집어** 개발자에게 모순된 지시가 갔다. 판정이 메시지 이력에만 있어서 디자이너 스펙과 엇갈렸다. 함께 흡수: 지표 철회 시 **파생 수치까지 회수**(이번 실행에서 4회 발생 — 검증 2·QA 2), `8px`이 세 계약을 가리켜 **설계 반송 사유가 경고로 흡수될 뻔한 건** |
| 2026-08-21 | **실측 규율 절(§5.5) 신설** — 측정 환경표(`innerWidth − clientWidth` 필수), 종횡비 검산의 한계, 빌드 동결, 양방향 의심, 최악 조건 판정선, 사실 주장 문구 검증 | skills/union-qa-testing §5.5 | 지도 점유율 측정에서 **개발자와 QA가 같은 정의로 다른 값**(81.5% vs 85.4%)을 냈다. 원인은 데스크톱 컨텍스트의 스크롤바 15px 이 콘텐츠 폭을 313 으로 줄인 것. 그 환경에서는 참 최악값(85.4%)이 **나올 수 없어** 최악 지점을 잰 적이 없었다. ★ **종횡비 `CSS-IMPOSSIBLE` 규칙(25회차)으로는 이 오염을 못 잡는다** — 폭이 줄면 높이도 비례해 줄어 비율은 그대로다. 빌드 교체 사고 3회·축척 왕복 3회의 교훈이 워크스페이스 문서에만 있어 다음 작업으로 넘어가지 않던 것도 함께 흡수 |
| 2026-08-17 | 모던 스타일 기준 v2 → **v3 전면 개편** (§0.1~0.5): toss.im 계열 지향, 모던함을 만드는 6가지 수단, 노조 페이지가 양보하지 않는 선, **콘텐츠 은폐 금지 패턴표**, 이미지·썸네일 규칙 | skills/union-design-system §0 | 사용자가 toss.im 을 이상향으로 지정하고 디자인 전격 교체를 요청. 동시에 이번 세션에서 **탭 기본값 때문에 사이트의 유일한 콘텐츠가 hidden 뒤에 가려지는 실제 사고**가 발생 → 모던 UI 패턴(탭·아코디언·캐러셀·등장 애니메이션)의 은폐 위험을 스킬 차원 규칙으로 승격. 저대비 회색이 "세련미"로 도입되는 것을 명시 차단 |
| 2026-08-25 | **`doc-commit` 스킬 신설** — 작업 후 문서 최신화·커밋 절차 | skills/doc-commit, CLAUDE.md(트리거 절) | 이 프로젝트의 사고 3건이 *"적혀 있었지만 **적힌 자리가 틀렸다**"* 였다: **FOLLOWUPS #10 의 원인 2건이 해소됐는데 항목이 안 고쳐져 리더가 낡은 자료를 설계 판정에 넘겼고**, 같은 항목을 **세 번 뒤집어** 모순된 지시가 갔으며(판정이 메시지 이력에만 있었다), **빌드 교체 3회·축척 왕복 3회의 교훈이 워크스페이스 문서에 갇혀** 다음 작업으로 넘어가지 않았다. 그래서 스킬의 절반을 «무엇을 쓸까» 가 아니라 **«어디에 쓸까»(배치 판정표)** 로 짰다. 함께 명문화: **소스 주석에 있는 것을 문서로 복사하지 마라**(이 프로젝트 주석은 두꺼운 것이 설계다 — 두 벌이 되면 코드 고치는 사람은 주석만 고친다) · **최신화의 절반은 추가가 아니라 회수**(해소 항목은 지우지 말고 `[해소 날짜]` 표기, 낡은 근거는 제거) · 문서 커밋은 **코드와 분리** |
| 2026-08-25 | **§5.4~5.7 신설** — 링크·QR **도착지 확인** / 콘텐츠 교체 시 **파생값 전수 검색** / **같은 사실을 두 확정도로 말하지 않기** / 원문 오타 예외 | skills/union-fact-check §5.4~§5.7 | **지시대로 썼으면 거짓을 게시할 뻔했다.** 사용자가 *"QR로 찍어서 들어가는 페이지와 동일하다는 설명을 붙여줘"* 라고 했는데, 게시 중이던 안내 이미지 속 QR(빨간 동그라미까지 쳐진)을 디코딩해 따라가 보니 `m.site.naver.com/1tgqt → qrpage.kr/pr/` **웹 제작 업체 회사소개 홈페이지**였다 — 출석 페이지가 아니었다. **지시가 곧 사실은 아니다.** 함께 흡수: 이미지 교체분에서 **출석 시각이 바뀐 것을 사용자가 말해 주지 않았고**(1차 `~19:30`→`~19:20`·2차 `20:00~`→`20:10~`) 그 값이 **본문 문장에도 있어** 육안 대조로 찾아냈다(안 찾았으면 한 페이지가 두 시각을 말했다) · 본문·지도가 3구역을 **확정**으로 말하는데 원문 그대로 옮긴 표 한 칸에 `예정` 이 들어가 **같은 사실이 두 확정도**가 됐다(조합원은 낮은 쪽을 믿는다) |
| 2026-08-25 | **§7 신설** — 문서에 없는 서드파티 API 사용 조건 + **위젯이 점유한 마운트 노드의 크기 감시는 부모에서** | skills/union-webapp-dev §7 | 로드뷰 두 기능(바닥 탭 이동·로드뷰 안 라벨)이 **공식 문서에 없는 API**(`getProjection().fromOffsetToCoord`, `Marker({map: panorama})`)에 기댄다. 금지가 아니라 **«없어졌을 때 무엇이 남는가»** 가 조건이라는 것을 규칙으로 세웠다. 함께: `ResizeObserver` 를 **마운트 요소**에 걸었더니 **영영 울리지 않았다** — 네이버가 그 노드에 인라인 px 를 박아 크기가 우리 CSS 가 아니라 위젯 소관이었다. **부모를 감시해야** 한다. 이는 드래그 기능이 만든 것이 아니라 **회전·주소창 접힘에도 있던 잠복 결함**이다 |
| 2026-08-25 | **§5.6 신설** — 측정 전 `visibilityState`·rAF 발화 확인 필수 + **열이 늘면 확대 배율 재측정** | skills/union-qa-testing §5.6 | **없는 결함을 있다고 판정할 뻔했다.** *"`ResizeObserver → setSize` 가 한 번도 호출되지 않는다"* 는 측정이 나왔는데, 대조로 붙인 자체 옵저버도 안 울려 **코드가 아니라 환경**임이 드러났다 — 탭이 `visibilityState: "hidden"` 이라 rAF·ResizeObserver 가 아예 안 돌았다. 또: 순서 표를 2열→3열로 바꾸고 **기본 배율 360·390·1280 넘침 0** 만 확인해 배포했는데, 200% 로 재니 **전체 203px 중 표 기여 134px** 이었다(`display:none` 토글로 기여분을 갈랐다). **열이 늘면 배율 축을 다시 재라** |
| 2026-08-25 | **§0.6·§0.7 신설** — 이미지 교체 시 **판독 대상(QR)부터 지키기**·캐시 끊기 / **필 버튼=페이지 안 조작, 카드=외부 이동** | skills/union-design-system §0.6~§0.7 | 출석 안내 원본이 **3.9MB PNG** 였다(집회 현장 모바일 데이터로 받는 이미지다). PNG 로는 1920 폭에서도 2.1MB 라 **1600 폭 JPEG q92 = 361KB** 로 바꾸고 **QR 이 그 상태에서도 디코딩되는지 확인**했다 — 육안이 아니라 디코딩이다. 파일명도 바꿔 캐시를 끊었다(같은 이름으로 덮으면 **시각이 바뀌었는데 옛 이미지가 계속 보인다**). 함께: 사용자가 *"버튼을 달아도 좋다"* 고 했지만 필 버튼은 **페이지 안 조작의 형태**라 카드를 유지하고 **면을 채워 무게만 올렸다**(형태=어디로, 무게=얼마나 — 다른 축) |
| 2026-08-26 | **§5.7 신설** — 측정값은 **«값 + 방법 + 기준»이 한 벌** / **빌드 완료 ≠ 배포 완료**(확인 3종) / **높이 판정선 `360×640`** | skills/union-qa-testing §5.7 | 한 라운드에 **측정 신뢰가 세 번 깨졌다.** ① 디자이너와 QA 가 **`min-content` 라는 같은 이름으로 다른 양**을 쟀다(`width:0` 의 `scrollWidth` ↔ `width:min-content` 의 rect) — **`break-keep` 단독 열에서는 두 값이 같아 아무도 눈치채지 못했고 `+break-words` 열에서만 갈렸다.** ② 사전 실측표에 **어느 슬롯 기준인지 안 적혀** 개발자가 **카드 밖 자리에 카드 안 수치를 인용**했다(이 페이지는 같은 뷰포트에서 슬롯이 201·281 두 가지다). ★ **§5.5 와 결이 다르다** — §5.5 는 «같은 정의로 다른 값»(스크롤바 오염)인데 이번은 **«다른 정의를 같은 이름으로»** 다. ③ **리더가 자기가 경고하던 함정에 걸렸다**: 앞 서버를 안 죽이고 재기동해 **`EADDRINUSE` 로 죽었는데 종료코드가 `0`** 이라 실패 신호가 없었고, `curl` 은 **낡은 서버가 응답해 성공**했다. 증상은 **CSS 하나만 404**(JS 9개는 200)였고 **`BUILD_ID` 대조로는 안 잡힌다** — 서버가 **자기 옛 값을 들고 있어 자기 기준으로는 일관적**이다. **낡은 «파일»이 아니라 낡은 «프로세스»**. 함께: 이 프로젝트 QA 관행에 **뷰포트 폭만 있고 높이가 없었다** |
| 2026-08-26 | **§5.8 신설** — 도착지 «내용 없음»은 **원본 HTML** 을 본 뒤에 / 교체본은 **«그 자료에 기댄 판정»을 역방향으로** 열거 / 금지어는 **«기대 개수»로** 판정 | skills/union-fact-check §5.8 | **셋 다 정반대 결론이 날 뻔했다.** ① 구글 폼 도착지에서 **리더와 검증자가 둘 다 «설명문 없음»으로 답했다** — `WebFetch` 마크다운 변환이 폼 설명을 통째로 버렸고, 원본 HTML 의 `FB_PUBLIC_LOAD_DATA_` 에 **식대·교통비 5만원과 우리 사이트 역링크(출처 확증)** 가 있었다(§5.1 «속성 텍스트»와 같은 계보 — 도구마다 조용히 버리는 계층이 있다). ② 사용자가 자료를 교체하며 **무엇이 바뀌었는지 말해 주지 않았다.** §5.5(값 전수 검색)의 **반대 방향**이 필요했다 — «우리가 그 자료에 기대어 만든 문장»을 먼저 열거해야 했고, 그렇게 걸러진 6건 중 **둘은 게시되면 거짓**이었다. 하나는 검증자가 **«필수 게시 조건»으로 올린 문장**이었는데 신판에서 그 자리는 **다른 지부**였다 — **조합원을 다른 지부 대오에 세울 뻔했다.** ★ **«극값에 기댄 판정»과 «중간값에 기댄 판정»은 강도가 다르다.** ③ 리더가 만든 «사라져야 하는 것» 목록에 **정당한 출현 5건이 섞여** 있었다 — 그대로 갔으면 **거짓 실패 5건이 나고 개발자가 정상 코드를 되돌렸다** |
| 2026-08-26 | **§0.8 신설** — 텍스트 확대 처방은 **«문자 종류»가 가른다**(한글 → `break-words` / 라틴·도메인 → `break-all`) + 자리별 글자 수 판정선 | skills/union-design-system §0.8 | 한 라운드에 **같은 결함을 네 번**(캡션·본문·도메인 줄·조건 문장) 만났다. 같은 «공백 없는 덩어리»인데 **처방이 반대**였다 — `keep-all` 은 **CJK 규칙이라 라틴 낱말을 안 끊는다.** ★ 그리고 **`break-keep` 이 «없었으면» 안 넘쳤을 줄**이 있었다(낱말이 하나뿐인 캡션은 막아 줄 «단어 중간 끊김» 대상이 없는데 통짜 덩어리만 만든다) — **처방이 문제를 만든 경우다.** 눈대중이 **두 방향으로 틀렸다**: **괄호에는 줄바꿈 기회가 있고**(`작성(지부천막)과` 242px 인데 문장 min-content 165px) **가운뎃점에는 없다**(`식대·교통비` = `식대교통비` + 점). ★ **한국어 종결어미가 이미 판정선에 걸린다**(`지급됩니다.` 6자) → 본문 문장은 사실상 전부 `break-words` 가 필요하다 |
| 2026-08-26 | **§8 신설** — 링크의 **접근성 이름은 내부 텍스트가 진다**. `aria-label`·`title` 로 만들지 마라 + 랜드마크 이름 규칙 | skills/union-webapp-dev §8 | **혼동을 막으려고 넣은 문장이 그 위험에 가장 취약한 사용자에게만 없어질 뻔했다.** 외부 폼 배너에 «외부 이동 3중 병행»을 지키려고 `aria-label` 을 붙이려 했는데, **`aria-label` 은 내용을 «대체»한다** — 그 카드의 **게시 조건 경고**(*"당일 QR 출석체크는 따로 해야 합니다."*)가 **링크 낭독에서 사라진다.** 링크 목록·탭 이동으로 훑는 스크린리더 사용자는 경고를 못 듣고 누른다. ⚠ `title` 은 **기전이 다르다**(이름은 못 덮고 설명이 된다) — **두 금지를 한 덩어리로 적으면** 나중에 누가 *"`title` 은 이름을 안 덮는데?"* 로 **금지 자체를 뒤집는 근거**로 쓴다. 함께: 사이트 전수 실측으로 **«화면에 헤딩이 있으면 `aria-labelledby`, 없으면 `aria-label`»**(11:5)이라는 **이미 서 있던 규칙**이 드러났다 |
| 2026-08-26 | **판정 기록 규율에 하위 절 2개** — 금지를 유지해도 **근거가 바뀌면 근거를 다시 쓴다** / 근거 하나가 죽으면 **남은 근거의 강도를 다시 적는다** + **구현 착수 전 «자족적 확정본»** | skills/union-page-orchestrator | 이 프로젝트가 반복한 *"근거가 소멸했는데 판정이 안 고쳐졌다"* 의 **거울상**이 났다 — 어떤 세부를 «미확인이라 못 쓴다»로 금지했는데 **사용자 확인으로 뜻이 밝혀졌다.** 금지 유지는 맞았지만(§5.3 기준에 안 걸린다) **근거가 통째로 바뀌었고**, 안 고치면 다음 사람이 *"이제 확인됐으니 써도 되겠다"* 로 뒤집는다. 함께: 판정이 **§40 → §41 → §42 → §43 → §44 → §47 → §48 로 7회 갱신**되고 **죽은 절이 14건**이 되자, **«이력을 따라가게 하면 낡은 절을 인용한다»** 가 실증됐다 — 구현자에게는 **자족적 확정본 하나**만 넘긴다 |
| 2026-08-27 | **§5.7 개정** — «대조는 «두 출처 사이»에 한다, «두 시각 사이»가 아니다» 절 추가. 빌드 동결의 참 기준 3종 명시 | skills/union-qa-testing §5.7 | **§5.7 이 세운 «확인 3종»의 ②를 QA 가 잘못 쓸 뻔했고, 스스로 잡았다.** 측정 전후 프리렌더 sha256 이 `ac8a87da…` → `bdc7ec6b…` 로 갈려 **«빌드 동결 위반»으로 회차를 버릴 뻔했다.** 원인을 가르니 **전체 바이트는 74,400 으로 동일**했고 **`<script>` 제거 후 가시 마크업 해시는 완전 일치**(29,941자)했다 — 차이는 **RSC flight 페이로드 안 숫자 77자**뿐이었다(`,4608]}` → `,4256]}`). **Next 가 그 페이로드에 시간에 따라 변하는 캐시 수치를 넣는다.** ★ **틀린 것은 «두 출처를 맞대라»가 아니라 «같은 해시가 시간이 지나도 유지된다»는 암묵 가정**이다. 이로써 이 프로젝트에서 **«세는 방법이 질문에 안 맞는» 사례가 여섯 번째**가 됐고(`grep -c` · `min-content` 정의 · 슬롯 기준 · RSC 계수 · 텍스트 좌표 슬라이싱 · 이 건) **그중 둘이 RSC 페이로드**라 «Next 산출물을 셀 때는 `<script>` 를 먼저 지운다»를 기본값으로 세웠다 |
| 2026-08-27 | **§5.8 신설** — «높이»를 잴 때의 함정 셋: ① 스크롤바 오염이 **높이로 나타난다** ② **인라인 세로 패딩은 레이아웃을 밀지 않는다** ③ 실기기 가용 높이는 **`svh`·`lvh` 대조로 «못 잼»을 판별한다** | skills/union-qa-testing §5.8, _workspace/FOLLOWUPS.md #19·#20 | **§5.7 이 높이 판정선(`360×640`)을 세운 바로 다음 실측에서 셋에 전부 걸렸다.** ① 같은 항목이 두 번 다르게 나왔다(**548.3 vs 598.3 — 50px**). 원인은 헤더의 `flex-wrap` 이었다 — 한 줄 유지에 `clientWidth ≥ 351.2px` 이 필요한데 **스크롤바 15px 이 345 로 깎아 두 줄이 됐고 그 아래 모든 것이 50px 내려갔다.** §5.5 는 스크롤바를 «폭» 오염으로만 적었는데 **폭 감소가 레이아웃 분기를 넘기면 높이가 계단식으로 바뀐다.** 그 오염값으로 낸 판정(«today 여유 14.7px»)이 **참값(64.7px)의 4분의 1** 이었고 **이미 커밋·푸시된 뒤에 QA 가 스스로 잡았다.** ② 예측과 실측이 **두 회차 연속 정확히 3px** 갈렸다 — 상태 배지 `<span>` 이 `display:inline` 이라 **세로 패딩이 줄 상자를 안 늘리는데** 개산이 패딩을 포함해 세고 있었다. **같은 폭으로 반복해 갈리면 우연이 아니라 계통 차이다.** ③ `clientHeight`·`innerHeight`·`visualViewport.height` 가 전부 640 을 내서 «쟀다»고 믿을 뻔했는데, **`100svh` 와 `100lvh` 가 같다는 것**이 «이 환경에는 툴바 구분 자체가 없다»를 드러냈다. **못 재는 것을 «통과»로 적지 않는다.** 함께: 이 실측이 **`#20` 첫 화면 «50px 절벽»** 을 드러냈다 — 360px 기기가 절벽에서 **8.8px 위**이고 **방아쇠가 우리 문면(지부명·글자크기 컨트롤 폭)** 이라 «문면이 한 글자만 늘어도 절벽이 이동한다» |
| 2026-08-27 | **§5.8.4 신설** — 측정 «위생»: ① **내가 남긴 것을 지우고 «값으로» 확인**(인라인 스타일·주입 style·프로브·**`localStorage` 등 영속 상태**) ② **미디어 쿼리는 «뷰포트» 기준**이라 요소 폭 변경으로는 못 잰다 ③ **프록시로 판정하지 말고 참값을 재라** | skills/union-qa-testing §5.8.4, _workspace/FOLLOWUPS.md #20 해소 | **헤더 «50px 절벽»(`#20`)을 고치면서 리더가 한 라운드에 셋 다 걸렸다.** ① 절벽을 이분 탐색하며 `documentElement.style.width` 를 320px 로 둔 채 정리하지 않았고 **다음 측정이 700px 뷰포트에서 그 320px 를 재서** «헤더가 접혔다» 는 **거짓 결과**를 냈다. ② `sm:` 반응형 복원을 `style.width = '700px'` 로 재니 «복원 안 됨»이 나왔다 — **미디어 쿼리는 뷰포트를 보므로 요소 폭은 무관**하다(실제 리사이즈하니 정상). ③ «줄바꿈 = 행 높이 > 60px» 휴리스틱을 썼는데 **폭·확대에 따라 행 높이가 68/77/118/144.8/195.5/227.5 로 널뛰어** 프록시가 깨졌다 — 참값은 **두 자식의 `getBoundingClientRect().top` 비교**다. ★ QA 가 범위를 넓혔다: 슬라이더를 130% 까지 올려 재는 동안 그 값이 **`localStorage` 에 저장돼** 정리하지 않았으면 **이후 모든 페이지 측정이 130% 로 오염**될 뻔했다. §5.6 이 «측정 «전» 환경이 **잴 수 있는** 상태인가» 라면 이것은 «환경이 **깨끗한** 상태인가» 로 **같은 계열의 다른 축**이다 |
| 2026-08-27 | **§5.7 재개정** — 해시 대조의 **오탐 모드가 둘**임을 표로 못박음(① RSC 캐시 수치 표류 ② **라이트백 중 읽기**) + *«같은 시각에, 안정될 때까지 2~3회»* | skills/union-qa-testing §5.7 | 같은 날 아침에 ①을 잡아 §5.7 을 개정했는데 **오후에 ②가 나왔다.** 측정 후 확인에서 디스크 프리렌더 sha256 이 **`e3b0c44298fc1c14…`** 로 떠 **파일이 사라진 줄 알았는데 74,419바이트로 멀쩡했다** — Next 응답 캐시 **라이트백 순간에 읽은 것**이다(3회 재측정하니 서버↔디스크가 3회 모두 동일). ★ **`e3b0c44…` 는 «빈 문자열»의 sha256** 이라 **«파일이 없다»가 아니라 «빈 것을 읽었다»를 먼저 의심**하면 5초에 갈린다. 두 모드가 **증상이 정반대**다(①은 «달라진다» · ②는 «비어 있다») — 한 문장으로 뭉치면 둘 중 하나를 놓친다 |
| 2026-08-27 | **§0.8 보강** — 텍스트 확대 처방의 **세 번째 문자 종류 «숫자·괄호·기호»** + «`break-keep` 을 빼서 고치려 하지 마라» | skills/union-design-system §0.8, _workspace/FOLLOWUPS.md #9·#21 해소 | QA 가 헤더 회귀를 돌리며 **홈 200% 가로 스크롤 61px · 임단협 34px** 을 찾았다(둘 다 헤더 무관·기존). 범인이 **`8/28(금) 18:30 결의대회 참석안내`** 와 **`96.05%`** 인데 **둘 다 `break-keep` 이 이미 걸려 있어 «그것이 원인»으로 의심됐다.** ★ **빼도(=`normal`) `scrollWidth` 334 그대로**였다 — **숫자·괄호·기호는 «끊을 자리가 자체적으로 없다».** `break-words` 를 얹으니 334 → 75 · 초과 0. §0.8 이 «한글 → `break-words` / 라틴 → `break-all`» 둘만 적어 두었는데 **셋째가 나왔고, 이것은 «어느 처방이 듣나»가 아니라 «어느 처방을 의심하면 안 되나»** 를 말한다. 함께 드러난 것: **«200% 가로 스크롤 0» 규율이 `/rally-2026-08-28` 에만 적용돼 있었다**(#10·#11 이 거기서 나왔다) — **상세 페이지는 지금도 통과한 적이 없다.** 그리고 **탭 스트립이 범인으로 오인될 뻔했다**(버튼이 뷰포트를 넘지만 부모가 `overflow-x:auto` 라 문서를 안 민다) — **«요소가 뷰포트를 넘는다» ≠ «문서가 스크롤한다»** |
| 2026-08-27 | **커밋 본문 검산 규율** — `doc-commit` §6 에 «커밋 «전»에 `git diff --cached --name-only` 로 본문이 주장하는 파일이 실제로 스테이징됐는지 확인» 추가 | skills/doc-commit §6 | **커밋 `1021fff` 의 본문이 «§0.8 보강»을 주장했는데 그 파일이 커밋에 없었다.** 파이썬 편집 스크립트가 **앵커 불일치로 `AssertionError` 를 내고 죽었는데**, 뒤이은 `git add -A && git commit` 이 **별도 명령이라 그대로 실행**됐다. **문서에 «있다»고 적힌 것이 실제로는 없는** 상태가 푸시까지 갔다 — 이 프로젝트가 반복한 *"적혀 있었지만 적힌 자리가 틀렸다"* 의 **가장 나쁜 형태**(적힌 자리가 틀린 게 아니라 **아예 없다**)다. ⚠ 스크립트 실패를 **커밋 실패로 전파**시키려면 `&&` 로 잇거나 `set -e` 를 쓴다 |
| 2026-08-29 | **§0.8.1·§0.8.2 신설** — «문자 종류» 위에 **«자리»** 라는 축이 하나 더 있다 / 한 컴포넌트 안에서 처방이 갈리면 «왜 여기만» 을 주석에 남긴다 | skills/union-design-system §0.8.1~§0.8.2 | **이 사이트가 다섯 번 쓴 처방이 처음으로 안 들었다.** `세종대로 (광화문역, 시청역)` 이 **블록에서는 `break-words` 로 146 → 31px** 로 접히는데 **`grid-cols-[auto_1fr]` 의 `1fr` 트랙은 146 을 그대로 잡아** 200% 에서 문서를 **9px** 밀었다. 원인은 **`overflow-wrap` 이 «넘칠 때 끊는» 규칙이지 «min-content 기여값을 줄이는» 규칙이 아니라는 것** — 트랙·표 열·flex 아이템은 **그 기여값으로 자기 폭을 정한다.** ★ 문자 종류 판정(§0.8)은 **맞았고 처방도 이미 걸려 있었다.** 틀린 것은 **«어디에 놓였는가»** 였다. 함께: 같은 컴포넌트에서 **제목에는 `text-balance` 가 필요한데 부제에 붙이면 더 나쁘다**(가운뎃점이 줄머리로 가고 괄호쌍이 갈린다) — **처방이 반대인 형제**가 생기면 안 붙인 쪽에 사유를 남긴다. ⚠ 검사자가 초안의 «같은 **시각 위계**의 형제» 를 잡았다 — **그 절을 만든 장면이 제목↔부제(위계가 다른 형제)라 규칙이 자기 장면을 배제**하고 있었다 |
| 2026-08-29 | **§5.9 신설(3절)** — 판정선은 «자리» 를 가린다 / **전이가 걸린 속성은 duration+delay 를 넘겨 읽는다** / **런타임 주입 프로토타입으로 판정선을 확정하지 마라**. 측정 환경표에 **«루트 폰트 크기»** 항목 추가. **계보를 «숫자» 가 아니라 «목록» 으로 전환** | skills/union-qa-testing §5.5·§5.9 | 셋 다 **«세는 방법이 질문에 안 맞는»** 계보다. ① `:focus-visible` 아웃라인을 `focus()` 직후 **rAF 2회(≈32ms)** 에 읽어 **`rgb(91,119,176)`** 이 나왔다 — **Tailwind v4 `transition-colors` 가 `outline-color` 를 포함**해 **전이 중간값**이었다(400ms 후 참값 흰색). 그대로 적었으면 **«아웃라인 대비 미달» 거짓 실패**. ② 세로 예산 예측 **173.6** vs 산출물 **188.44** — **행 간격 유틸리티가 프로토타입에서 생성되지 않아 `row-gap:0` 으로 그려졌다**(사용처가 신설 페이지 3곳뿐이라 컴파일 CSS 에 없었다). ★★ **같은 함정을 그 라운드에서 이미 만났고 «가로는 인라인으로 우회했는데 세로는 안 했다»** — 같은 원인, 다른 축. ⚠ 검사자가 **초안이 규칙을 «세로» 로 한정한 것**을 잡았다 — **장면이 «가로만 막은 것이 실패다» 라고 적어 놓고 규칙이 그 반대로 좁아져** 있었다. ③ **계수를 버렸다**: §5.7 이 «여섯 번째» 라고 적었는데 **같은 라운드에 `union-fact-check` 가 같은 계보 2건을 세지 않고 추가**했다 — **숫자는 적은 순간부터 낡는다** |
| 2026-08-29 | **§5.9 신설(3절)** — **금지어는 «렌더되는 문자열» 에서 센다** / `<title>` 은 «속성» 이 아니라 «요소 텍스트» / **금지 목록도 «근거가 죽는다»** | skills/union-fact-check §5.9 | **리더가 거짓 실패를 10건 넘게 낼 뻔했다.** 신설 페이지에 금지 문자열을 **«소스 전체 grep»** 으로 돌렸더니 `손피켓`·`상품권`·`위치서비스`·`임금피크제`·`QrAttendanceCard` 가 전부 잡혔는데 **하나도 빠짐없이 «쓰지 마라» 를 기록한 주석**이었다 — **이 프로젝트는 주석에 금지어를 적는 것이 설계다**(레포 실측 10개 파일). 그대로 넘겼으면 **개발자가 정상 코드를 되돌렸다.** ★ §5.1 이 3계층을 규정했으나 **«어느 계층이 어느 질문에 답하는가» 가 없었다** — «소스 문자열 집합 차분» 은 **«신규 저작 판별»용이지 «금지어 판별»용이 아니다.** 함께: **`<wbr/>` 이 19개** 있어 태그를 안 지우면 `10:30-11:00` 이 참값과 다른 계층에서 나온다 · **`추후 안내` 를 기대 0 으로 올려 뒀는데 나중에 확정된 문면이 그 문자열을 포함**해(근거가 3회 뒤집혔다) **정상 문면이 실패로 올라갈 뻔했다** |
| 2026-08-29 | **판정 기록 규율에 하위 절 2개** — **«못 고치는 것» 을 «고쳤다» 고 적지 마라**(그리고 «못 고친다» 만 적어도 안 된다) / **원문과 화면이 달라지는 지점은 «순서·열·묶음» 세 축으로 센다** | skills/union-page-orchestrator | ① 리더가 게시 조건을 걸었다 — *"대상 명칭이 시각과 «같은 줄»에서 갈리게 하라."* 디자이너가 실측해 **조판 수단이 없음**을 보였다(필요 슬롯 **303.8px** vs 200% 모바일 **201px** · 그 슬롯은 뷰포트 463px 부터 — **선언 뷰포트 360·390·412 전부 갈린다**). *"막을 수 없는 것을 «막았다»고 적지 않았습니다"* 로 되돌아왔다. ★ 이것은 **«못 재는 것을 통과로 적지 않는다»(`union-qa-testing §5.8.3`)의 거울상**이다. ★★ **그런데 «못 고친다» 만 적어도 안 된다** — 두 오독이 각각 난다(«조판 불가» 만 → *«그럼 실패네»* 로 되돌린다 / «목적 달성» 만 → *«그럼 같은 줄로도 되겠네»* 로 문면을 만진다). **두 문장을 한 벌로** 적는다. ② 리더가 최종 게이트에 «원문 구조 변경» 을 **2건**(행 순서·블록 순서) 올렸는데 **검증자가 3건이라고 정정**했다 — 빠진 것은 **표가 3열 → 2열이 되어 「비고」 열이 사라진 것**이다. **리더가 «순서» 만 세고 «열» 을 안 셌다.** 문자열이 그대로여도 **순서·열·묶음은 원문 구조 변경**이고 **`grep` 은 그것을 세지 않는다** |
