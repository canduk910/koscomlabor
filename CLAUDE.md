# 코스콤지부 노동조합 공식페이지

## 역할 분담 — **먼저 읽어라**

| | 누가 |
|---|---|
| **내용이 맞는가** (사실·시각·장소·문면) | ★★ **사용자다. 우리가 아니다.** |
| **그것을 정확히 구현했는가** | 우리 |
| **화면에서 깨지지 않는가** | 우리 |

⛔ **내용 검증을 하지 마라.** 사용자가 준 문면·시각·사실을 **그대로** 옮긴다.
- 출처를 따지지 않는다 · 사실 여부를 판정하지 않는다 · 「게시 조건」을 만들지 않는다
- **사용자 지정 문면은 한 글자도 바꾸지 않는다**(띄어쓰기·가운뎃점·구분자 포함)
- 자료가 서로 어긋나 보이면 **판정하지 말고 사용자에게 «묻는다»**. 우리가 고르지 않는다

★ **불필요한 문장을 만들지 마라.** 사용자가 준 것 외에 우리가 짓는 문장은 최소로 둔다.
사용자가 세 번 지적했다 — *「애매한 문구는 혼란을 준다」 · 「쓸데없는 추가문구는 삭제해줘」*.

## 작업 방식

- **단계별로 하나씩 개발해 배포한다.** 묶어서 한 번에 올리지 않는다
- 배포 후 **프로덕션에서 값으로 확인**하고 보고한다
- 커밋·푸시는 **사용자가 지시할 때** 한다

## 무엇을 테스트하나 — **화면이 깨지는가만 본다**

`union-qa-testing` 스킬이 방법을 담는다. 요지:
- **빌드·타입·린트**
- **브라우저 실측** — 200% 텍스트 확대에서 가로 스크롤 0 · 터치 대상 24px · 포커스가 보이는가 · 링크가 404 가 아닌가
- ⚠ **측정 전에 「잴 수 있는 상태인가」를 확인**한다(뷰포트·스크롤바·루트 폰트 크기를 함께 적는다)
- ⛔ **문면이 맞는지는 테스트 항목이 아니다**

## 주석

**「하지 마라 + 왜」만 남긴다.** 사용자가 과한 주석을 지적했고 저장소 주석을 48% 걷어낸 적이 있다.
★ 되돌림을 막는 한 줄은 값을 낸다 — **분량이 아니라 형태다.**

## 스택

Next.js 16 · Tailwind v4 · TypeScript · `output: "standalone"` · main 푸시 시 GitHub Actions → NCP 배포.
⚠ **루트 폰트가 12px 이다**(`globals.css` `html{font-size:75%}`). `rem` 개산에서 16px 을 가정하면 틀린다.
