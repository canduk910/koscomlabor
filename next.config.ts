import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Docker 배포용: 서버 런타임을 .next/standalone 으로 self-contained 출력
  output: "standalone",

  /*
   * ★★ **`expireTime` 을 `revalidate` 와 같은 값으로 맞춘다** (2026-09-07 · 실측 확정).
   *
   * 기본값이 **1년**이다. 그러면 ISR 항목이 `revalidate` 60초를 넘겨도 «만료»는 아니라서
   * 서버가 **낡은 응답을 먼저 확정해 내보내고** 뒤에서 재생성한다(stale-while-revalidate).
   * 그래서 «한산했다가 들어온 첫 방문자»는 값이 몇이든 반드시 낡은 응답을 받았다:
   *   · 관리자가 «공개» 를 켠 직후 → 조합원이 **404** (프로덕션에서 실제로 발생)
   * 60 으로 맞추면 두 시각이 겹쳐 그 구간이 사라진다
   * (`incremental-cache/index.js` 가 `isStale = -1` 을 세우고, 그때만 `response-cache` 가
   *  조기 반환을 건너뛰고 새 렌더를 기다린다).
   *
   * ⛔ **지우지 마라.** 지우면 그 404 가 그대로 돌아온다.
   * ⛔ **늘리지 마라.** 늘린 초만큼이 그대로 «낡은 값을 내보내는 창»이다.
   * ⛔ **라우트로 옮길 수 없다** — Next 16 세그먼트 설정에 `expire` 가 없다(전역 옵션뿐).
   * ⚠ 적용 대상은 `revalidate` 를 **이미 가진** 라우트뿐이다. 완전 정적 라우트(`/admin` 등)는
   *   영향받지 않는다(빌드표 Expire 열로 확인할 것).
   * ⚠ 대가: 만료 후 재생성이 **조합원 요청 위에서** 돈다. 그래서 API 조회에 타임아웃이
   *   필요하다 — `src/lib/api/*.ts` 의 `AbortSignal.timeout` 주석을 함께 읽어라.
   *   ⚠ 그 재생성을 기다리는 것은 «첫 한 명»이 아니라 **그동안 도착한 요청 전원**이다.
   * ⚠ 프리페치도 만료 항목은 재생성시킨다 — 메인의 「전체보기」 링크 때문에 아무도 안 눌러도
   *   `/pledges` 가 60초마다 1회 재생성된다(종전 0회). 동시 요청은 1회로 합쳐진다.
   */
  expireTime: 60,

  images: {
    /*
     * ★ **`quality` 허용 목록**(2026-08-26 추가 · 디자이너 실측으로 발견).
     *
     * Next **16** 부터 `images.qualities` 기본값이 **`[75]`** 다. 목록에 없는 값을 요청하면
     * 옵티마이저가 **HTTP 400** 을 내고, `next/image` 는 조용히 기본값으로 떨어진다.
     * 그래서 `rally-2026-08-28/page.tsx` 의 배치도가 **`quality={90}` 을 선언했는데
     * 실제 `srcset` 8개 후보가 전부 `q=75` 로 나가고 있었다**(실측).
     *
     * ⚠ **이번 이미지 교체 작업이 만든 결함이 아니다** — 종전 `rally-layout.png` 도 같은 상태였다.
     * 선언과 실제가 어긋난 채 두면 다음 사람이 *"우리는 q=90 으로 낸다"* 를 근거로 삼는다.
     *
     * **90 이 필요한 이유**: 배치도는 **이미지 안에 읽어야 할 문자가 있다**(구역 라벨·무대 이름).
     * 검증 §44 조건 7 이 «quality 90 이상» 을 게시 조건으로 건다.
     *
     * ⚠ **허용 목록이라 75 를 빼면 안 된다** — `quality` 를 지정하지 않은 나머지 이미지가 전부 400 이 된다.
     * ⚠ 값을 늘릴 때마다 변형이 그만큼 더 생성·캐시된다. **필요한 값만 넣어라.**
     */
    qualities: [75, 90],
  },
};

export default nextConfig;
